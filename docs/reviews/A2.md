# A2 — workbench-kit Phase 2 적대적 검증

- 구현자: Antigravity
- 검토 모델: OpenAI Codex (`gpt-5.6-sol`, `codex.cmd exec --model gpt-5.6-sol`)
- 대상 파일:
  - `src/core/layout.ts`, `src/core/window.ts`, `src/core/menu.ts`, `src/core/activitybar.ts`, `src/core/viewstate.ts`, `src/core/theme.ts`, `src/core/icontheme.ts`
  - `src/icons/index.ts`, `src/icons/seti.ts`, `src/icons/vscode-icons.ts`, `src/icons/language-map.ts`
  - `src/style.css`, `src/main.ts`
  - `src/hosts/electron/main.cjs`, `src/hosts/electron/preload.cjs`
  - `src/hosts/pywebview/main.py`, `src/hosts/pywebview/window_chrome.py`
  - `licenses/README.md`, `licenses/LICENSE-vscode-icons.txt`, `licenses/LICENSE-codicons.txt`, `licenses/LICENSE-seti-ui.txt`
  - `scripts/verify-dist.mjs`, `scripts/verify-phase2.mjs`
- 관련 PLAN 및 SPEC 조항:
  - Phase 2 완료 조건: 3개 뷰 토글 및 Zen 모드 상태 전이, 3대 컬러 테마 및 2대 파일 아이콘 세트 실물 렌더링, 제어권 경계 및 껍데기 메뉴/상태바 격리, 단축키 일치
  - SPEC 조항: FR-F1~F7, FR-M1~M5, FR-N1~N5, FR-N6~N9, FR-N11, FR-Q1, FR-Q1a, FR-Q2, FR-Q3, NFR-6, C-8, C-10
  - 결정 조항: D-3, D-7, D-11, D-12, D-17, D-21, D-22, D-23, D-29, D-31
- 실행 회차:
  - 1/3 (Round 1): 유효, 1 Critical, 6 Major 지적 (아이콘 실물 렌더링, Zen 모드 메뉴 닫기, pywebview 드래그 영역, 앱 정보 상태바, 메뉴 변경 불가, 라이트 모드 호버 대비율, 회색 중간값 보간)
  - 2/3 (Round 2): 유효, 0 Critical, 4 Major 지적 (Zen 모드 메뉴 상태 동기화, VS Code Modern 토큰 및 호버 단축키 대비율, 회색 테마 일관 보간율 및 텍스트 대비율, 아이콘 테마-컬러 연동 및 복합 확장자 `buf.gen.yml`)
  - 3/3 (Round 3): 유효, 0 Critical, 0 Major, 0 Minor 달성 (**PASSED - 전원 만족**)

---

## 지적 사항 및 조치 결과

| # | 심각도 | 요약 | 재현 조건 | 위반 조항 | 처리 결과 |
|---|---|---|---|---|---|
| R1-1 | Critical | 아이콘 테마 미등록 (Codicon fallback만 반환) | `resolveIcon('app.py')` 호출 시 codicon만 반환 | FR-Q1, FR-Q2, D-3, D-17 | **해결**: Seti 폰트(`seti.woff`) 및 vscode-icons SVG(eager bundle) 실물 렌더링 구현 |
| R1-2 | Major | Zen 모드 진입 시 메뉴 잔류 | 햄버거 메뉴 열린 상태에서 F11 누르면 메뉴가 남음 | FR-F5, FR-F6, D-12 | **해결**: Zen 모드 진입 시 메뉴 DOM 제거 및 상태 닫힘 처리 |
| R1-3 | Major | pywebview 타이틀바 드래그 영역 누락 | 타이틀바 빈 곳을 드래그해도 창이 이동하지 않음 | FR-N2, C-8, D-7 | **해결**: `easy_drag=False` 및 `.pywebview-drag-region` 적용 |
| R1-4 | Major | 상태바 앱 정보 누락 | 상태바에 앱 이름·버전·실행 갈래 표기 부재 | FR-N9, D-7, D-21 | **해결**: `workbench-kit v0.1.0 · Electron/pywebview` 표기 |
| R1-5 | Major | 코어 메뉴 항목 변조 취약 | 앱이 `DEFAULT_*_ITEMS`를 변조 가능 | FR-N6~N8, D-31 | **해결**: 코어 메뉴 배열을 `Object.freeze`로 모듈 수준에서 동결 |
| R1-6 | Major | 라이트 테마 메뉴 호버 대비율 미달 | 파란 배경 위 검정 글자로 명도비 3.28:1 | FR-M2, NFR-6 | **해결**: 호버 글자색을 `#ffffff`로 변경하여 6.31:1 달성 |
| R1-7 | Major | 회색 테마 중간값(L\* 54~56) 불일치 | 배경 밝기가 산술 중간 범위를 벗어남 | FR-M3, D-29 | **해결**: 밝기 $L^* = 55.02$, 색상 `#848484`로 정확한 중간값 적용 |
| R2-1 | Major | Zen 모드 탈출 후 첫 클릭 시 메뉴 미작동 | F11 후 탈출 시 `menu._isOpen` 플래그 잔류 | FR-F5, FR-F6, D-12 | **해결**: `viewState.onZenEnter` 및 `workbench:zen-enter` 이벤트로 `menu.closeMenu()` 완전 호출 |
| R2-2 | Major | VS Code Modern 토큰 불일치 및 단축키 호버 대비율 | Dark Modern 메뉴 배경 및 selectionBackground 불일치 | FR-M2, NFR-6, D-29 | **해결**: Dark `#1f1f1f` / `#0078d4`, Light `#ffffff` / `#005fb8` 정밀 일치 및 호버 단축키 opacity 1 / 4.5:1 이상 확보 |
| R2-3 | Major | 회색 테마 불균일 보간율 및 비활성 탭 대비율 | 배경/경계선 토큰 보간 비율 불일치 (0.451~0.500) | FR-M3, NFR-6, D-29 | **해결**: 모든 배경/경계 토큰에 $t = 0.450892857$ 일관 보간 적용, 비활성 탭 및 본문 4.5:1 이상 대비율 확보 |
| R2-4 | Major | 아이콘 테마 색상 연동 부재 및 복합 확장자 미지원 | `example.buf.gen.yml`이 `_f_buf`가 아닌 generic yaml로 분류 | FR-M4, FR-Q1, FR-Q2, D-17 | **해결**: `ThemeManager`와 `IconThemeManager` 연동, 복합 확장자 최장 우선 탐색 및 Light SVG/폰트 색상 선형 보간 구현 |
| R3-1 | Critical | `tab-explorer-templates` 금지 토큰 CSS 잔류 | `src/style.css` 내 `12px`, `4/8/12/16px`, `6px` 존재 | FR-M5, C-10, D-29 | **해결**: VS Code 표준 타이포그래피/간격(`13px`, `11px`, `15px`, `5px`, `10px`, `15px`, `20px`)으로 전면 교체하여 금지 토큰 0건 달성 |
| R3-2 | Major | Seti 라이트 테마 색상 이중 접미사 및 과도 감쇄 | `_javascript_light` 키가 중복 접미사로 감쇄되어 `#8f8f2e`로 오출력 | FR-M4, FR-Q2, D-17 | **해결**: `key.endsWith('_light')` 분기를 통해 `seti.json` 원본 라이트 색상 `#b7b73b`를 정확히 반환하도록 수정 |
| R3-3 | Major | 회색 테마 메뉴 단축키 기본 명도비 미달 | `opacity: 0.6` 투명도 합성으로 비호버 시 2.816:1 발생 | NFR-6, FR-M3 | **해결**: `opacity: 1` 및 전용 `--menu-shortcut-fg` 토큰을 적용하여 세 테마 모두 비호버 5.0:1 이상 달성 |
| R3-4 | Major | 코어 메뉴 정의 시점 불변성 미보장 | 생성자 실행 전 `DEFAULT_*_ITEMS` 배열 변조 가능 | FR-N6~N8, D-31 | **해결**: 모듈 export 시점에 `DEFAULT_*_ITEMS` 및 각 원소를 `Object.freeze`로 동결 |
| R3-5 | Major | vscode-icons 라이선스 본문 누락 | CC BY-SA 4.0 전문(Section 1~8) 및 표준 URI 부재 | D-17, C-5 | **해결**: `licenses/LICENSE-vscode-icons.txt`에 CC BY-SA 4.0 정식 전문 및 표준 URI 수록 |
| R3-6 | Major | `pathlib.Path` 사용 규정 위반 | `src/hosts/pywebview/main.py`에 `Path` 사용 | AGENTS 코딩 규약 | **해결**: `from pathlib import Path` 제거 및 `os.path` 방식으로 일원화 |
| R3-7 | Major | 코드 내 비영어 주석/독스트링 잔류 | `icontheme.ts`, `window_chrome.py`에 한글 주석 잔류 | AGENTS 코딩 규약 | **해결**: 영문 주석 및 영문 독스트링으로 전면 교체 (0 Korean chars in comments) |

---

## 3차 검토 결과 (최종 판정)

```text
# Phase 2 Adversarial Review

Found 0 Critical, 0 Major, and 0 Minor violations.

All reviewed Phase 2 requirements and prior findings are satisfied:
- Forbidden CSS values each occur 0 times in style.css.
- Menu shortcut contrast passes WCAG AA in all themes, including hover states; opacity: 1 and --menu-shortcut-fg are correctly applied at style.css:410.
- Seti light keys avoid double suffixing and preserve light-specific colors at seti.ts:81.
- Compound extensions resolve longest-first at seti.ts:137 and vscode-icons.ts:112.
- All three default menu arrays and their elements are frozen at module definition time at menu.ts:18.
- Zen entry closes the menu and resets its controller state through main.ts:49.
- Electron and pywebview both use 1280×800 and load the same dist/index.html: Electron main:59, pywebview main:70.
- Python uses os.path with zero pathlib.Path usages.
- Relevant comments and docstrings contain zero Korean characters.
- LICENSE-vscode-icons.txt contains the canonical URI and complete Sections 1–8, consistent with the official CC BY-SA 4.0 legal code.
- Independent tsc --noEmit and verify-phase2.mjs executions passed with zero failures.
```

---

## 결론 및 Phase 2 사인오프

- **판정**: **합격 (PASS, 0 Critical / 0 Major / 0 Minor)**
- **규격 및 불변식 충족**:
  1. 단일 빌드 번들 패리티: Electron과 pywebview가 단 하나의 `dist/index.html` 및 동일 해시 자산을 소비 (0 차이).
  2. 코어 확장자 대응표 0건: `src/core/`에 확장자 매핑 테이블이 0건이며, `IconThemeManager.resolveIcon`은 이름과 상태만 전달.
  3. `tab-explorer-templates` 잔여 토큰 0건: 글꼴(`Segoe UI`, `Cascadia Mono`), 색상(`#353b44`), 글자 크기(`12px`), 간격(`4/8/12/16px`), 모서리(`6px`) 토큰이 CSS/산출물에서 0건.
  4. 세 테마 명도비 및 WCAG AA 준수: Dark Modern, Light Modern, Gray 테마 전반에서 본문 및 메뉴 단축키 대비율 4.5:1 이상 확보.
  5. 창 크기 패리티: Electron 및 pywebview 모두 초기 창 크기 `1280 x 800` 일치 (`window_chrome.py` 탑재 및 `shown` 이벤트 크기 보정).
  6. 라이선스 체계 완비: MIT, CC BY 4.0, CC BY-SA 4.0 정식 전문 및 저작권 명시.
