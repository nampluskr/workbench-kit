> 버전: v0.2 · 작성일: 2026-09-10 · 상태: 초안 (사람 확인 대기)
>
> Phase 인덱스다. 상세 설계·task 분해·코드는 담지 않는다(각각 `DECISIONS.md`·
> `backlog.json`·구현). 요구 ID는 `SPEC.md`를 가리킨다.

# PLAN — workbench-kit v0.2

## 이 순서에 걸린 세 가지 규칙

**Phase를 작게 쪼갠다(D-13 · `NFR-4`).** v0.1은 133건을 7 Phase로 나눠 Phase당
19건이었고, 그 크기 때문에 Phase 7과 마감 작업 **둘 다 반대 벤더 검토 3회 한도를
소진한 채 미해결 지적을 남기고 닫혔다.** 이번에는 69건을 7 Phase로 나눠 Phase당
평균 10건이다. 한 Phase가 3회 안에 미해결 Critical 0건으로 닫히지 않으면 **그것
자체가 그 Phase가 컸다는 뜻**이므로 `PROGRESS.md`에 기록한다.

**Phase 1이 먼저인 것은 의존 때문이다.** 탭에 "임시냐 확정이냐"라는 상태가 생겨야
Phase 2가 그 위에 표시를 얹을 수 있다. 반대 순서는 성립하지 않는다.

**모든 Phase의 완료 조건은 두 갈래에서 각각 판정한다**(`SPEC.md` 0절 공통 판정 규칙).
Phase마다 반복해 적지 않고, Phase 7이 `NFR-1`로 전건을 대조한다. 그리고 모든 Phase의
판정은 **화면에서 한다**(`NFR-3`) — 요소가 있는지로 끝나는 판정을 쓰지 않는다.

---

## Phase 1 — 임시 자리와 확정

**목적** — 훑어보기와 붙잡아 두기를 가른다. 이번 버전의 토대다.

**대응 요구 ID** — `FR-P1` ~ `FR-P8` · `FR-P10` · `FR-T1` ~ `FR-T3` (12건)

**완료 조건**
- 한 번 클릭하면 임시 자리에 열리고, 다른 것을 한 번 클릭하면 **같은 자리가 대체된다.**
  파일과 폴더가 같은 규칙을 쓴다. 여러 개를 차례로 클릭해도 한 칸의 임시 자리는
  언제나 최대 1개다.
- 확정하는 네 경로가 각각 동작한다 — 파일 더블클릭 · 임시 자리 제목 더블클릭 ·
  트리에서 `Enter` · (파일과 폴더 모두).
- **트리에서 폴더를 더블클릭하면 확정하지 않고 펼쳐진다.** 그때 열려 있던 임시 자리는
  임시인 채로 남는다.
- 트리에서 `→`는 폴더를 펼치고 파일에는 아무 일도 하지 않는다. `Enter`는 아직 열려
  있지 않은 것을 **바로 확정된 자리로 연다.**
- 확정된 자리가 있는 상태에서 다른 것을 한 번 클릭하면 자리 수가 늘고, 그중 정확히
  1개가 임시다.

---

## Phase 2 — 선택과 포커스 표시

**목적** — 무엇을 골랐는지와 어디에 있는지를 따로 보이게 한다.

**대응 요구 ID** — `FR-P9` · `FR-F1` ~ `FR-F11` (12건)

**완료 조건**
- 임시 자리와 확정된 자리를 **보기만 해도 구분**할 수 있다. 세 색 테마 모두에서
  성립한다.
- `Tab` · `Shift+Tab`이 보이는 트리와 각 칸을 양방향으로 순환하고, `Ctrl+Tab`은
  현재 칸 안에서만 자리를 전환한다. 숨겨진 영역은 순환에서 빠진다.
- **영역만 옮겨 다니는 조작이 트리 선택과 각 칸의 활성 자리를 바꾸지 않는다.**
  탐색기 빈 영역을 클릭해도 선택이 유지되고 새 자리가 열리지 않는다.
- 선택 배경은 포커스가 떠나도 남고, 포커스 표시만 현재 영역으로 옮겨 간다.
- 칸이 여럿이면 **모든 칸의 활성 자리 표시가 유지**되고 포커스 표시는 정확히 1개다.
- 트리 커서와 활성 자리가 서로를 강제로 따라가지 않는다. 커서가 화면 밖으로 나가면
  다시 보이도록 스크롤된다.

---

## Phase 3 — 창 껍데기 배치

**목적** — 전역 상태는 창 제어 옆으로, 세로 띠는 영역 토글만, 프로그램 정보는
상단 바 한 줄로 모은다.

**대응 요구 ID** — `FR-C1` ~ `FR-C11` (11건)

**완료 조건**
- 상단 바 오른쪽이 왼쪽부터 **Zen · 색 테마 · 최소화 · 최대화/복원 · 닫기** 순서다.
  Zen 아이콘은 활성 여부를, 테마 아이콘은 현재 테마를 나타내며, `F11`과 메뉴로 바꾼
  상태도 같은 아이콘에 반영된다.
- 세로 띠에 Zen · 색 테마 · 분할 아이콘이 **각각 0개**다. 칸 머리와 메뉴의 분할
  경로는 그대로 동작한다.
- 세로 띠의 **맨 위가 상단 바 토글, 맨 아래가 하단 바 토글**이고, 각각 해당 영역만
  감추고 되돌린다.
- 상단 바 왼쪽 햄버거 바로 오른쪽에 프로그램 정보가 한 줄로 좌측 정렬된다. 날짜가
  마지막 코드 수정일이고, 갈래 표기가 두 갈래에서 각각 정확하다.
- 하단 바 오른쪽에 껍데기가 놓은 프로그램·버전·갈래 정보가 **0개**다.

---

## Phase 4 — 메뉴와 칸 수명

**목적** — 메뉴를 목표 구조로 다시 세우고, 칸을 닫는 길을 연다.

**대응 요구 ID** — `FR-M1` ~ `FR-M12` · `FR-P11` · `FR-P12` (14건)

**완료 조건**
- `File` · `View` · `Help`의 항목 목록과 순서, 구분선 위치가 `SPEC.md` `FR-M1` ·
  `FR-M2` · `FR-M3` · `FR-M6`과 정확히 같고, 그 밖의 껍데기 항목이 0개다.
- 단축키가 정의된 다섯 항목에만 오른쪽 정렬 표기가 있고, **표기한 키를 직접 눌렀을
  때 메뉴를 누른 것과 같은 결과**가 난다.
- `Recent Folders`가 저장된 경로를 모두 보이고, 비면 `(Empty)`가 보이며, 행마다
  삭제가 동작한다. `Color Theme` 셋과 `Icon Theme` 둘이 목표 표기·순서다.
- 하위 메뉴 표시가 chevron 아이콘이고 텍스트 `▶`와 ASCII `>`가 0건이다.
- **세 닫기 명령이 각각 다르게 동작한다** — 활성 자리 하나 · 그 칸 전체 · 열린 자리
  전부. 세로 띠와 우클릭에는 칸 닫기 항목이 계속 0개다.
- 오른쪽·아래쪽 분할로 만든 새 칸이 `Untitled` 임시 자리를 갖고 생기고, **별도로
  자리를 추가하지 않고 그 자리만 닫으면 칸도 함께 닫힌다.**

---

## Phase 5 — 탐색기 구조

**목적** — 제목·액션·폭·들여쓰기를 요청한 모양으로 맞춘다.

**대응 요구 ID** — `FR-X1` ~ `FR-X9` (9건)

**완료 조건**
- 탐색기 제목이 폴더를 열기 전후 모두 `EXPLORER`이고 루트 폴더명이 제목에 0건이다.
- 뷰 제목줄에 **New File · New Folder · Refresh · Collapse All** 넷이 이 순서로
  보이고, 프리셋 정보가 0개다.
- New File · New Folder가 **이름을 받는 입력 행**을 띄우고, `Enter`로 앱 호출이
  1건 생기며 **껍데기 쪽 생성 동작이 0회**다. `Escape`면 입력 행이 사라지고 앱 호출이
  0건이다.
- 경계를 끌어 폭이 연속으로 바뀌고, 최소 폭에서 제목과 네 액션이 잘리지 않으며,
  다시 열면 초기 폭으로 시작한다.
- 두 단계 이상 중첩된 폴더에서 **하위 항목이 상위보다 들여쓰기**되고, 세로 정렬선이
  각 계층의 들여쓰기 위치에 있다. 탐색기 왼쪽 경계에 붙은 선이 0개다.

---

## Phase 6 — 테마 · 아이콘 · 치수

**목적** — 테마 색이 화면 끝까지 닿게 하고, 영역 두께를 하나로 맞춘다.

**대응 요구 ID** — `FR-X10` ~ `FR-X14` · `FR-D1` ~ `FR-D3` (8건)

**완료 조건**
- 색 테마를 바꾸면 뷰 액션 · chevron · 폴더 · 파일 아이콘의 색이 모두 따라 바뀌고,
  세 테마 각각에서 배경과 대비를 갖는다. **앱을 다시 열지 않아도 즉시 반영된다.**
- 트리 스크롤바의 트랙과 손잡이에 테마 색이 적용되고, 손잡이의 기본·hover·active가
  서로 구분된다. 가로 스크롤바도 같다.
- 두 아이콘 테마 각각에서 대표 파일명 · 단일 확장자 · 복합 확장자의 아이콘이
  `_clones/tab-explorer-templates`의 것과 같고, 열린 폴더와 닫힌 폴더가 다르다.
- 상단 바 높이 · 하단 바 높이 · 세로 띠 너비 · 탐색기 제목줄 높이 · 탭 줄 높이가
  **모두 30 CSS px**이고, 최대화·복원 · 테마 변경 · 시스템 배율 변경 후에도 그대로다.
- 이 치수가 VS Code와 다르다는 것이 기준 문서에 **의도된 차이로 기록**되어 있다.

---

## Phase 7 — 표시 언어와 전건 대조

**목적** — 제품이 내보내는 글자를 한 언어로 맞추고, 두 갈래에서 전건을 대조해 이번
버전을 닫는다.

**대응 요구 ID** — `FR-L1` ~ `FR-L3` · `NFR-1` ~ `NFR-8`

**완료 조건**
- 메뉴 · 우클릭 · 뷰 액션 · 다이얼로그 · 상태 문구 · 빈 상태 안내 · 도움말 풍선을
  모두 열어 수집한 제품 텍스트에 **한글이 0건**이다. 같은 기능의 이름이 두 갈래에서
  같고, 실제 파일·폴더 이름은 그대로 보인다.
- **`SPEC.md` 1절 69건을 Electron과 pywebview에서 각각 확인해 결과가 다른 항목이
  0건**이다.
- **v0.1 회귀 대조** — `SPEC.md` 0.1절에서 대체한 10건 말고는 v0.1의 검사가 이번
  산출물에서 그대로 통과한다. 통과하지 못하는 항목이 있으면 그것이 대체 목록에
  있는지로 회귀와 의도된 변경을 가른다.
- 기준 문서의 divergence 표시가 실제 동작과 일치한다 — 이번에 **없어진 차이**
  (임시 자리 없음)와 **새로 생긴 차이**(공통 치수 · 새 탭 버튼 위치)가 모두 반영되어
  있다.
- 1절에 나오는 모든 명령에 마우스 없이 실행하는 경로가 1개 이상 있고, 그 키가 예약
  키 목록 문서에 있다.
- **A7 이월분 — 이름 기반 정적 검사 보강.** 라이브러리를 직접 구현하지 않았는지
  확인하는 검사가 지정한 파일·클래스 이름 밖에서 우회 가능했던 것을 좁힌다. 이번
  버전에서 다루는 A7·A8 잔여 위험은 **이것 하나뿐**이다(사람 결정, 2026-09-10).

---

## 적대적 검증

절차는 `docs/ADVERSARIAL-REVIEW.md`에 있다. 여기에는 이 프로젝트에서만 정해지는 두
가지만 적는다.

**필수 통과 Phase — 1 · 4 · 7** (사람 결정, 2026-09-10).

미해결 Critical이 있으면 다음 Phase로 넘어가지 않는다. 이 셋을 고른 이유는 1이
나머지가 얹히는 토대이고, 4가 v0.1의 칸 수명 규칙을 뒤집어 회귀 위험이 가장 크며,
7이 두 갈래 전건 대조이기 때문이다.

**Phase별 공격 초점**

| Phase | 검토자에게 무엇을 깨라고 하는가 |
| --- | --- |
| 1 | 임시 자리가 둘 이상 생기거나, 확정한 것이 대체되거나, 폴더 더블클릭이 확정으로 새는 조작 순서를 찾아라 |
| 2 | 영역을 옮기는 것만으로 선택이나 활성 자리가 바뀌는 경로, 포커스 표시가 둘 이상이거나 0개가 되는 상태를 찾아라 |
| 3 | 아이콘의 상태 표시가 실제 상태와 어긋나는 경로(키·메뉴·아이콘이 서로 다른 것을 보는 경우)를 찾아라 |
| 4 | 세 닫기 명령이 서로의 결과를 침범하는 경우, 칸이 사라지거나 남는 규칙이 v0.1과 달라진 경우를 찾아라 |
| 5 | 껍데기가 파일을 만들거나 리소스 종류를 알게 되는 경로, 최소 폭에서 액션이 잘리는 조건을 찾아라 |
| 6 | 테마 조합 중 아이콘이나 스크롤바가 배경에 묻히는 것, 배율이나 최대화에서 치수가 흔들리는 조건을 찾아라 |
| 7 | 판정이 화면을 보지 않고 통과하는 검사, divergence 표시와 실제 동작이 어긋나는 항목, 대체 목록에 없는데 v0.1 검사가 깨진 항목을 찾아라 |

**리팩토링이 섞이는 Phase**는 공격 초점을 **동작 동등성**으로 바꿔 준다
(`ADVERSARIAL-REVIEW.md` 3절). 이번 버전은 v0.1의 결정을 뒤집는 변경이 많아, 어디까지가
의도된 변경이고 어디부터가 회귀인지가 매번 쟁점이 된다. **`SPEC.md` 0.1절의 대체
목록이 그 경계**이므로 검토자에게 함께 준다.
