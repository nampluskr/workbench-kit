# A30 — workbench-kit v0.4 Phase 1 적대적 검증

- 구현자: Codex
- 세션 내 리뷰어: `.claude/agents/reviewer.md` 기준 검토 완료
- 반대 벤더 검토 모델: Claude opus-5.5
- 대상 파일: `src/core/markdown/index.ts` (1회차에는 `src/main.ts`, `src/style.css`, `scripts/verify-phase3.mjs`도 요청)
- 관련 PLAN 조항: Phase 1 렌더링 파이프라인
- 실행 회차: 3/3

| 회차 | 일시 (KST) | 소요 | 턴 수 | 결과 |
| --- | --- | --- | --- | --- |
| 1 | 2026-09-25 | 40초 | 제한 초과 | `max_turns`로 유효한 검토 결과 없음 |
| 2 | 2026-09-25 | 43초 | 2 | 유효. Major 1건, Minor 4건 |
| 3 | 2026-09-25 | 29초 | 2 | 유효. Major 해소 확인, 미해결 Critical 없음 |

## 지적

| # | 심각도 | 요약 | 재현 조건 | 위반 조항 | 처리 |
| --- | --- | --- | --- | --- | --- |
| 1 | Major | 원문 HTML의 form·area와 중간 클릭 링크가 앱 창을 다른 페이지로 이동시킬 수 있음 | `form action`, `map/area href`, 링크 중간 클릭 | Phase 1 임의 마크다운 스크립트 미실행 | form·입력·map·area 태그 금지, `auxclick`·`submit` 기본 동작 차단. 3회차에서 해소 확인 |
| 2 | Minor | DOM이 없는 환경에서 정화가 작동하지 않을 수 있음 | Node 환경에서 `renderMarkdown` 직접 호출 | Phase 1 정화 | `DOMPurify.isSupported` 검사 후 오류를 던지도록 수정 |
| 3 | Minor | 원문 스타일이 앱 전체를 가리거나 재설정할 수 있음 | `<style>` 또는 `style` 속성 | Phase 1 안전한 렌더링 | 두 입력을 금지. Phase 3 수식 출력의 스타일 필요성은 별도 검토 |
| 4 | Minor | `id`·`name`으로 DOM 전역 이름을 오염시킬 수 있음 | 사용자 HTML의 `id`·`name` | Phase 1 안전한 렌더링 | `SANITIZE_NAMED_PROPS` 적용 |
| 5 | Minor | 겹친 새로 고침 응답이 오래된 내용을 다시 쓸 수 있음 | 빠르게 연속 호출한 두 읽기가 역순 완료 | Phase 1 rendered 내용 | 세대 번호로 이전 응답 폐기 |

## 유효하지 않은 지적과 반박 근거

- 없음. 2회차의 지적을 모두 반영했다.

## 미해결 · 남은 위험

- 3회차는 링크 드래그 앤 드롭에 의한 이동 가능성을 관찰했다. 드롭 경로는 이 Phase의 완료 조건에 포함되지 않으며 반대 벤더도 script 실행 finding으로 분류하지 않았다. 링크 열기 규칙을 구현하는 Phase 4에서 함께 확인한다.
- `npm test`의 기존 v0.1 Phase 4 단언 7건 실패가 재현됐다. v0.3 `PROGRESS.md`에 기록된 기존 Phase 4/5/7 실패 계열이다. v0.4 Phase 1의 전용 Electron·pywebview 검사와 `typecheck`·`build`·`verify:phase3`·`verify:dist`는 통과했다.
