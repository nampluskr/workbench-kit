> 버전: v0.2 · 작성일: 2026-09-10 · 상태: 초안 (사람 확인 대기)
>
> 이번 버전에 적용할 설계 결정과 그 근거를 기록한다. 변하지 않는 의도는 `INTENT.md`에,
> 이번 버전에 무엇을 왜 하는지는 `BRIEF.md`에 있다. 결정은 INTENT에 어긋나면 안 된다.
>
> 번호는 버전 안에서만 유일하다. v0.1의 `D-n`과는 다른 번호 체계이므로, v0.1의 결정을
> 가리킬 때는 `v0.1 D-n`으로 적는다.

# DECISIONS — workbench-kit v0.2

이번 버전의 결정은 대부분 **v0.1에서 정해 둔 것을 뒤집는다.** 뒤집는 결정에는 v0.1의
어느 결정을 왜 뒤집는지를 함께 적는다. 그러지 않으면 다음 버전에서 "왜 이렇게 됐나"를
물을 때 v0.1 문서와 충돌하는 것처럼 보인다.

---

## D-1. 고른 것을 임시로 열고, 확정하는 동작이 있을 때만 남긴다

**선택**

탐색기에서 무언가를 한 번 고르면 **임시 자리**에 연다. 그 상태에서 다른 것을 고르면
새 자리를 만들지 않고 같은 임시 자리를 대체한다. **확정하는 동작**을 했을 때만 그것이
자리를 차지하고 남으며, 그다음에 고른 것은 다시 새 임시 자리에 열린다. 임시인지
확정인지는 보기만 해도 구분된다.

이는 v0.1 D-5의 "탐색기 선택은 탭을 늘리지 않고 현재 탭에서 연다"와 그것을 받은 D-10을
**뒤집는 결정**이다. `docs/vscode-comparison.md`에 의도된 차이로 기록해 둔 항목이기도
하므로, 그 기록도 함께 고친다.

**근거**

찾는 것을 고를 때 사람은 여러 개를 열어보고 그중 하나만 남긴다. v0.1 방식은 자리를
하나만 쓰는 대신 **"이건 남겨두고 다음 걸 보자"가 구조적으로 불가능하다.** 반대로
고를 때마다 자리를 만들면 탭 줄이 금방 지저분해진다. 임시와 확정을 가르면 훑어보는
동안에는 자리를 하나만 쓰고, 붙잡기로 한 순간에만 자리를 차지한다 — 두 요구를 동시에
만족하는 방식은 이것뿐이다.

v0.1 D-5가 이 방식을 고른 이유는 "탭은 사용자가 추가할 때만 늘어난다"였다. 그 판단은
**화면 없이 문서에서** 내려졌고, 실제로 써 보자마자 뒤집혔다(`BRIEF.md` 3절).
덧붙이면 임시 자리는 **사용자가 추가한 탭이 아니다.** D-5의 취지("사용자가 만들지 않은
탭이 쌓이지 않는다")는 임시 자리를 하나만 쓰는 규칙으로 오히려 더 잘 지켜진다 —
뒤집는 것은 취지가 아니라 "탭을 하나만 쓴다"는 수단이다.

**배제한 대안**

- **v0.1 방식 유지(활성 탭 내용 교체).** 붙잡아 두는 일이 안 된다. 이번 버전을 여는
  이유 자체이므로 배제한다.
- **고를 때마다 새 자리를 만든다.** 훑어보기만 해도 자리가 쌓인다. 정리하는 일이
  사용자에게 새 부담으로 넘어가므로 배제한다.
- **자동으로 오래된 자리를 닫는다.** 사용자가 남겨 둔 것이 예고 없이 사라진다.
  "붙잡아 둔다"는 요구와 정면으로 어긋나므로 배제한다.

---

## D-2. 폴더의 더블클릭은 확정이 아니라 펼치기이며, 폴더에는 확정 경로를 따로 둔다

**선택**

파일과 폴더는 **같은 임시/확정 규칙**을 쓴다(D-1). 다만 확정하는 동작이 대상에 따라
갈린다.

- **파일**: 탐색기에서 더블클릭하면 확정한다.
- **폴더**: 탐색기에서 더블클릭하면 **확정하지 않고 그 폴더를 펼친다.**

폴더를 확정하는 길은 따로 둔다 — 탐색기에서 고른 상태로 확정 키를 누르거나, 임시로
열린 그 자리의 제목을 직접 더블클릭한다. 이 두 경로는 파일에도 똑같이 동작한다.

**근거**

트리에서 폴더를 더블클릭하는 것은 **이미 "펼친다"는 뜻으로 쓰이고 있고**, 그건 트리를
쓰는 사람의 몸에 밴 동작이다. 여기에 "확정"을 겹쳐 실으면 둘 중 하나를 잃거나, 같은
조작이 상황에 따라 다르게 동작해 예측이 안 된다.

그래서 **대상(파일/폴더)이 아니라 조작한 자리(트리/제목)로 가른다.** 트리에서의
더블클릭만 대상에 따라 갈리고, 제목 더블클릭과 확정 키는 대상과 무관하게 항상 확정이다.
사용자가 외울 것은 "트리에서 폴더를 두 번 누르면 펼쳐진다" 하나뿐이다.

**배제한 대안**

- **폴더도 더블클릭 = 확정.** 트리에서 폴더를 펼치는 기본 동작을 잃는다. 트리의
  기본기를 UI 일관성과 바꾸는 셈이라 배제한다.
- **폴더는 임시/확정 규칙에서 제외한다(항상 확정으로 연다).** INTENT 3절이 파일 축과
  폴더 축에 **같은 탭 규칙**을 요구한다. 축마다 규칙이 갈리면 껍데기가 리소스 종류를
  알게 되는 방향이므로 배제한다.
- **폴더 확정을 아예 없앤다(폴더는 항상 임시).** 폴더를 탭에 붙잡아 두는 것은 폴더
  축 앱(파일 커맨터 · 멀티터미널)의 기본 사용 형태다. 배제한다.

---

## D-3. 전역 화면 상태는 창 제어 옆으로 모으고, 세로 띠에는 레이아웃 토글만 남긴다

**선택**

화면 전체의 상태를 바꾸는 것(색 테마 전환 · Zen)은 **창 제어 버튼 옆**으로 옮긴다.
편집 영역을 나누는 것(좌우·상하 분할)은 세로 띠에서 **없애고 메뉴에 둔다.**

세로 띠에는 영역을 보이고 감추는 토글만 남기며, **화면의 위아래 방향과 자리를
맞춘다** — 상단 바 토글이 맨 위, 하단 바 토글이 맨 아래.

이는 v0.1 D-11(세로 띠는 자주 쓰는 명령을 담는 자리)과 D-13(분할 방향마다 세로 띠에
아이콘을 둔다)이 정한 세로 띠 구성을 **뒤집는 결정**이다. D-12(탐색기 접기와 Zen)와
D-14(상단 바·하단 바를 각각 감춘다)가 정한 **동작 자체는 그대로 두고, 부르는 자리만
옮긴다.**

**근거**

지금 세로 띠에는 성격이 다른 셋이 섞여 있다 — 영역 토글, 편집 영역 분할, 전역 상태
전환. 섞여 있으면 무엇이 어디 있는지 매번 찾아야 한다. **성격으로 갈라 각각 제자리에
둔다.**

- 전역 상태는 창 전체에 걸리므로 **창 제어 옆**이 맞다.
- 분할은 편집 영역에만 걸리고 **이미 각 칸의 머리에 같은 버튼이 있다.** 세로 띠의
  것은 중복이므로 없애고, 마우스 없이 부를 길만 메뉴로 남긴다.
- 영역 토글만 남으면 세로 띠의 역할이 "영역을 보이고 감춘다" 하나로 정리된다. 그때
  위아래 방향을 화면과 맞추면 아이콘을 읽지 않아도 손이 간다.

**배제한 대안**

- **현행 유지.** 성격이 다른 셋이 섞인 채로 남는다. 이번 버전을 여는 이유 중 하나이므로
  배제한다.
- **전부 메뉴로만 옮기고 세로 띠를 없앤다.** 자주 쓰는 영역 토글이 두 번 클릭으로
  멀어진다. 세로 띠의 존재 이유가 사라지므로 배제한다.
- **분할을 세로 띠에 남긴다.** 칸 머리의 같은 버튼과 중복이다. 최소 구현 원칙
  (INTENT 3절)에 어긋나므로 배제한다.

---

## D-4. 프로그램 정보는 상단 바 왼쪽 한 줄로 모으고, 하단 바 오른쪽은 앱의 자리로 비운다

**선택**

프로그램 이름 · 버전 · 마지막 코드 수정일 · 어느 갈래로 떠 있는지를 **상단 바 왼쪽에
한 줄로** 모은다. 하단 바 오른쪽에서는 이 정보를 **없애고**, 그 자리는 앱이 올리는
것만 쓴다.

이는 v0.1 D-23이 정한 "하단 바는 경로와 앱 정보" 구성 중 앱 정보 자리를 **없애는
결정**이다. D-21(실패도 진행도 하단 바 한 줄로만 알린다)과 D-32(앱이 하단 바에 올린다)는
그대로 유지한다 — 없애는 것은 껍데기가 제 정보를 놓던 자리뿐이다.

**근거**

같은 정보가 상단 바와 하단 바에 나뉘어 있었다. **한 정보는 한 자리에 있어야 한다.**

상단 바로 모으는 쪽을 고른 이유는 둘이다. 하나는 이 정보가 **창에 관한 것**이라
창 제목 자리가 자연스럽다는 것이고, 다른 하나는 **하단 바 오른쪽을 앱에게 온전히
넘기기 위해서**다. 껍데기가 하단 바 한쪽을 계속 차지하고 있으면, 그 위에 올릴 앱이
쓸 수 있는 자리가 껍데기 사정에 따라 좁아진다(INTENT 2절 "갈아끼우는 자리").

날짜를 **마지막 코드 수정일**로 두는 것은, 이 껍데기가 복사해서 쓰는 템플릿이기
때문이다(INTENT 4절). 복사본을 받은 사람이 "언제 것을 받았나"를 화면에서 바로 안다.

**배제한 대안**

- **하단 바로 모은다.** 앱에게 넘길 자리를 껍데기가 계속 차지한다. 배제한다.
- **현행처럼 나눠 둔다.** 같은 정보가 두 곳에 있다. 배제한다.
- **날짜를 빌드 시각으로 둔다.** 코드가 안 바뀌어도 빌드할 때마다 달라져서, 받은
  사람이 "무엇을 받았나"를 알 수 없다. 배제한다.

---

## D-5. 칸을 닫는 명령을 메뉴에 둔다

**선택**

활성 탭이 속한 **칸 전체를 닫는 명령**을 메뉴에 둔다. 탭 하나를 닫는 것, 칸을 닫는
것, 열린 탭을 모두 닫는 것 셋을 각각 구분해 제공한다.

이는 v0.1 D-26의 "칸은 명시적으로 닫지 않는다"를 **뒤집는 결정**이다. v0.1은 세로 띠·
메뉴·우클릭 세 자리 모두에서 이 명령이 0개인 것을 완료 조건으로 두고 있었다.

**근거**

v0.1 D-26이 이 명령을 막은 이유는 "칸은 마지막 탭이 닫히면 저절로 사라지므로 별도 명령이
필요 없다"였다. 실제로 써 보니 그 전제가 맞지 않았다 — **탭이 여러 개 열린 칸을
없애려면 탭을 하나씩 닫아야 한다.** 칸을 정리하려는 사람에게 탭 개수만큼의 조작을
요구하는 셈이다.

"저절로 사라진다"는 규칙 자체는 유지한다. 이번에 더하는 것은 **그 상태에 한 번에
도달하는 길**이지, 칸 수명 규칙을 바꾸는 것이 아니다.

**배제한 대안**

- **v0.1 유지.** 위의 이유로 배제한다.
- **칸 머리에 닫기 버튼을 단다.** 칸 머리에는 이미 분할과 새 탭 버튼이 있어 좁고,
  탭의 닫기 버튼과 눈으로 헷갈린다. 칸을 통째로 없애는 것은 되돌리기 어려운 조작이라
  손이 미끄러지기 쉬운 자리에 두지 않는다. 배제한다.
- **칸 합치기도 함께 넣는다.** 요청에 없고, 지금 불편한 것은 "없애는 길"이다. 이번
  범위 밖으로 둔다.

---

## D-6. 탐색기 뷰 제목줄에 만들기 액션을 노출하되, 만드는 일은 여전히 앱이 한다

**선택**

탐색기 뷰 제목줄의 껍데기 액션을 **넷**으로 늘린다 — 새 파일 · 새 폴더 · 새로 고침 ·
모두 접기. 지금 거기 있는 프리셋 정보 표시는 **메뉴로 옮긴다.**

이는 v0.1 D-30의 "뷰 제목줄의 껍데기 액션은 정확히 둘"을 **뒤집는 결정**이다.

다만 v0.1 D-30의 다른 절반 — **껍데기는 파일을 만들지 않는다** — 은 그대로 지킨다.
껍데기가 주는 것은 이름을 받는 입력 장치까지이고, 실제로 만드는 것은 앱이다.

**근거**

v0.1에서 새 파일·새 폴더를 부를 수 있는 자리는 **기본으로 꺼져 있는 우클릭 메뉴**뿐이었다.
껍데기가 그 장치를 갖고 있는데 켜지 않으면 아무도 못 쓴다. 파일 커맨터처럼 만들기가
기본 동작인 앱에게는 사실상 없는 기능이었다.

프리셋 정보를 메뉴로 옮기는 것은, 그것이 **자주 쓰는 것이 아니라 확인용**이기
때문이다. 뷰 제목줄은 좁고, 그 자리는 자주 쓰는 것에 준다.

"만드는 일은 앱이 한다"를 유지하는 이유는 이것이 껍데기가 리소스 종류를 모르게 하는
INTENT 3절의 핵심이기 때문이다. 액션을 노출하는 것과 껍데기가 파일 시스템을 아는
것은 별개다.

**배제한 대안**

- **v0.1 유지(우클릭에만 둔다).** 기본이 꺼짐이라 사실상 없는 기능이다. 배제한다.
- **껍데기가 직접 만든다.** INTENT 3절과 v0.1 D-30을 정면으로 어긴다. 배제한다.
- **프리셋 정보를 뷰 제목줄에 남긴다.** 좁은 자리를 확인용이 차지한다. 배제한다.

---

## D-7. 주요 영역의 두께를 하나의 값으로 맞추고, VS Code와의 차이를 기록한다

**선택**

상단 바 · 하단 바 · 세로 띠 · 탐색기 제목줄 · 탭 줄의 두께를 **하나의 공통 값**으로
맞춘다. 창을 최대화하거나 테마를 바꾸거나 시스템 배율이 바뀌어도 이 값은 유지된다.

이 값은 VS Code의 대응 값과 다르다. 따라서 **의도된 차이로 기록한다.**

**근거**

지금은 영역마다 두께가 제각각이라(상단 30 · 하단 22 · 세로 띠 50 · 제목줄과 탭 줄 35)
화면이 정렬되어 보이지 않는다. VS Code는 각 영역이 오랜 시간에 걸쳐 따로 조정된
결과라 값이 다른 것이고, 이 껍데기는 그 역사를 물려받을 이유가 없다.

INTENT 6절은 VS Code를 기준으로 삼되 **"다르게 둘 때는 그 차이가 의도된 것임을
판단할 수 있어야 한다"**고 요구한다. 그래서 값을 맞추는 것과 동시에 그것이 의도된
차이임을 기준 문서에 남긴다. v0.1에서 divergence 기록을 소홀히 했다가 반대 벤더
검토에 잡힌 전례가 있다(`docs/reviews/A7.md`).

**배제한 대안**

- **VS Code 값을 그대로 쓴다.** 정렬되지 않은 화면이 그대로 남는다. 배제한다.
- **밀도 설정을 두어 사용자가 고르게 한다.** 기능을 늘리는 방향이고, 지금 필요한 것은
  "하나로 맞춘다"이다. INTENT 3절 제1원칙에 어긋나므로 배제한다.

---

## D-8. 탐색기 폭은 드래그로 조절하되 저장하지 않는다

**선택**

탐색기와 편집 영역 사이 경계를 **드래그해 폭을 바꿀 수 있게** 한다. 줄일 수 있는
한계는 **제목과 네 개의 액션이 잘리지 않는 폭**이다. 조절한 폭은 **저장하지 않으며**,
앱을 다시 열면 언제나 정해진 초기 폭으로 시작한다.

**근거**

폭이 고정이면 긴 경로를 볼 때 잘리고, 넓은 화면에서는 낭비된다. 조절은 필요하다.

저장하지 않는 쪽을 고른 것은 **되살릴 상태를 늘리지 않기 위해서**다. v0.1 D-27은
다시 열 때 살리는 것을 마지막 루트 폴더 하나로 못 박았고, 그 결정의 취지는 "복원할
것이 많아질수록 무엇이 살아나고 무엇이 안 살아나는지가 흐려진다"였다. 폭은 언제든
다시 끌면 되는 것이라, 그 취지를 깨면서까지 저장할 값이 아니다.

줄이는 한계를 액션 개수로 잡는 것은, 액션이 잘려 안 보이는 상태를 **사용자가 만들 수
없게** 하기 위해서다.

**배제한 대안**

- **고정 폭 유지.** 위의 이유로 배제한다.
- **조절한 폭을 저장한다.** v0.1 D-27의 복원 범위를 넓힌다. 이번 버전의 문제와 무관한
  상태를 늘리므로 배제한다.
- **최소 폭을 두지 않는다(0까지 줄인다).** 접기 기능이 이미 따로 있다. 접힌 것도
  아니고 안 보이지도 않는 어중간한 상태가 생기므로 배제한다.

---

## D-9. 제품이 내보내는 글자는 영어로 통일하고, 언어를 바꾸는 장치는 두지 않는다

**선택**

메뉴 · 버튼 · 안내 · 상태 문구 · 도움말 풍선 등 **제품이 표시하는 모든 글자를
영어**로 맞춘다. 실제 파일·폴더 이름과 사용자 데이터는 그대로 둔다.

**언어를 바꾸는 장치는 만들지 않는다.** v0.1이 국제화를 범위 밖으로 둔 것은 그대로
유지한다.

**근거**

지금은 한국어와 영어가 섞여 있어 같은 기능이 자리에 따라 다른 말로 불린다. 어느
쪽으로든 하나여야 한다.

영어를 고른 이유는 이 껍데기가 **VS Code를 기준으로 삼기 때문**이다(INTENT 6절).
기준 문서와 대조할 때, 참조 구현과 대조할 때, 반입한 라이브러리가 내보내는 문구와
나란히 놓일 때 — 셋 다 영어여야 대조가 성립한다.

**배제한 대안**

- **한국어로 통일한다.** 위의 대조가 매번 번역을 끼고 이뤄진다. 반입 라이브러리의
  문구는 어차피 영어라 섞임이 완전히 사라지지도 않는다. 배제한다.
- **언어 교체 장치를 넣는다.** 지금 필요한 것은 "섞이지 않는 것"이지 "고를 수 있는
  것"이 아니다. INTENT 3절 제1원칙에 어긋나므로 배제한다.

---

## D-10. 무엇을 골랐는지와 어디에 있는지를 따로 표시한다

**선택**

**고른 것**의 표시와 **지금 키보드가 있는 곳**의 표시를 분리한다. 고른 것은 다른
영역으로 옮겨 가도 표시가 유지되고, 지금 있는 곳만 따로 드러낸다.

칸이 여럿이면 **각 칸의 활성 탭 표시는 모두 유지**하고, 지금 작업 중인 칸의 활성
탭에만 있는 곳 표시를 더한다.

영역만 옮겨 다니는 조작은 **무엇을 골랐는지를 바꾸지 않는다.** 탐색기에서 고른 것과
탭에서 보고 있는 것은 서로를 강제로 따라가지 않는다.

**근거**

"무엇을 골랐나"와 "어디를 조작하고 있나"는 다른 정보인데, 하나로 표시하면 영역을
옮기는 순간 고른 것이 사라진 것처럼 보인다. 칸이 여럿일 때 특히 그렇다 — 세 칸 중
어디를 조작 중인지 알 수 없으면 키를 누르기 전에 매번 확인해야 한다.

둘을 강제로 묶지 않는 것도 같은 이유다. 탭을 바꿨다고 탐색기의 선택이 따라 움직이면,
사용자가 표시해 둔 자리를 제품이 마음대로 옮기는 셈이 된다.

**배제한 대안**

- **하나로 표시한다(현행).** 영역을 옮기면 고른 것이 사라진 것처럼 보인다. 배제한다.
- **탐색기 선택과 활성 탭을 항상 같이 움직인다.** 사용자가 잡아 둔 자리를 제품이
  옮긴다. 훑어보기(D-1)와도 어긋나므로 배제한다.

---

## D-11. 테마 색은 아이콘과 스크롤바까지 닿게 하고, 파일 아이콘은 참조 구현의 자산·매핑을 그대로 쓴다

**선택**

색 테마를 바꾸면 탐색기의 **모든 아이콘과 스크롤바까지** 색이 따라 바뀐다. 테마를
바꾼 즉시 반영되며, 앱을 다시 열 필요가 없다.

파일·폴더 아이콘의 **자산과 매핑은 참조 구현(`_clones/tab-explorer-templates`)의
것을 그대로 쓴다.** 아이콘 테마는 둘이고, 이름은 그 출처를 드러내는 표기로 맞춘다.

**색 테마와 아이콘 테마는 서로 독립이다.** 색 테마는 잘 보이는지를, 아이콘 테마는
어떤 그림이냐를 정한다.

**근거**

색 테마가 배경만 바꾸고 그 위의 아이콘과 스크롤바를 그대로 두면, 밝은 배경에서
아이콘이 안 보이는 조합이 생긴다. 테마를 고르는 목적 자체가 무너진다.

아이콘 매핑을 직접 만들지 않는 것은 v0.1 D-3(아이콘을 직접 그리지 않는다)의 연장이다.
**어떤 확장자에 어떤 그림을 붙이느냐는 방대한 표**이고, 그것을 직접 유지하는 것은
껍데기가 리소스 종류를 아는 방향이기도 하다(INTENT 3절). 이미 검증된 참조 구현의
결과를 그대로 쓴다.

**배제한 대안**

- **아이콘 색을 테마와 무관하게 고정한다.** 안 보이는 조합이 생긴다. 배제한다.
- **확장자-아이콘 매핑을 직접 만든다.** 방대한 표를 떠안고, 껍데기가 리소스 종류를
  알게 된다. 배제한다.
- **아이콘 테마를 하나로 줄인다.** 요청이 둘이고, 둘은 성격이 다르다(글꼴 glyph 대
  파일 종류별 그림). 배제한다.

---

## D-12. 판정은 "있다"가 아니라 "보인다 · 눌린다 · 바뀐다"로 쓴다

**선택**

이번 버전에 새로 쓰는 검사는 요소가 **존재하는지로 판정하지 않는다.** 화면에 실제로
그려지는지(크기 · 색 · 가려짐), 실제 조작이 닿는지, 그 결과로 화면이 바뀌는지로 쓴다.
내부 함수를 직접 불러 상태를 만들지 않는다.

**근거**

v0.1에서 자동 검사가 전건 통과하는데 화면에서는 안 되는 상황이 **세 번** 있었다
(`BRIEF.md` 3절). 세 번 다 "요소가 DOM에 있다"까지만 확인하고 있었다.

- 구분선이 0 크기로 그려지고 있었다 — 질의에는 잡히니 통과했다.
- 드래그 표시가 아예 안 그려졌다 — 드롭 자체는 동작하니 통과했다.
- 창을 안 그리는 상태로 검사를 돌리고 있었다 — 라이브러리가 화면 갱신 주기에 맞춰
  입력을 확정하기 때문에 눌러도 아무 일이 없었는데, 검사는 내부 함수를 직접 불러
  통과했다.

셋 다 **반대 벤더 검토에서 잡혔고, 사람이 써 보기 전에는 아무도 몰랐다.** 이번 버전의
변경은 대부분 "화면에 어떻게 보이느냐"라서 같은 함정이 그대로 반복된다.

**배제한 대안**

- **현행 방식 유지.** 위의 세 사례가 그대로 재발한다. 배제한다.
- **화면을 그림으로 찍어 비교한다.** 테마 세 가지 · 아이콘 테마 두 가지 · 두 갈래의
  조합마다 기준 그림이 필요하고, 글꼴이나 배율이 조금만 달라도 깨진다. 유지 비용이
  얻는 것보다 크므로 배제한다.

---

## D-13. 검증 단위를 작게 쪼갠다

**선택**

반대 벤더 검토에 한 번에 넣는 덩어리를 작게 잡는다. **한 검토 대상이 한 번에 다룰 수
있는 크기**로 Phase를 나누고, 여러 결정이 얽힌 변경은 얽힌 채로 넣지 않는다.

**근거**

v0.1에서 Phase 7과 마감 작업 **둘 다 3회 한도를 소진한 채 미해결 지적을 남기고
닫혔다.** 한 번에 넣은 덩어리가 컸기 때문이다. 덩어리가 크면 한 라운드에서 나오는
지적도 많고, 고치면 새 지적이 그 위에 붙어 3회로 수렴하지 않는다.

한도는 규칙이고(`docs/ADVERSARIAL-REVIEW.md` 4절) 이번에 바꿀 것이 아니다. 바꿀 수
있는 것은 **한 번에 무엇을 넣느냐**다.

**배제한 대안**

- **한도를 늘린다.** 규칙을 바꾸는 일이고, 근본 원인(덩어리가 크다)은 그대로다.
  배제한다.
- **현행대로 두고 넘치면 다음 버전으로 넘긴다.** v0.1이 그렇게 했고, 그 결과가 지금
  v0.2로 넘어온 잔여 위험이다. 같은 것을 반복하므로 배제한다.
