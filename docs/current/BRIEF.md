> 버전: v0.4 · 작성일: 2026-09-25 · 상태: 초안 (사람 확인 대기)
>
> 변하지 않는 의도는 `INTENT.md`에, 이번 버전의 설계 결정은 `DECISIONS.md`에 있다.
> 출발점은 `docs/refs/next-version.md`의 `N-2`다.

# BRIEF — workbench-kit v0.4

## 1. 배경

- `INTENT.md` 7절은 "마크다운 뷰어"를 이 껍데기의 세 검증 앱 중 하나로 명시한다.
- 지금은 `.md`를 열면 원문(monaco)만 보인다. 마크다운을 HTML로 렌더링하는 코드가
  저장소에 없다.
- v0.3 때 요청했으나 v0.3 범위(Folder Tabs·에디터 전환 모드)와 무관해 v0.4로
  넘겼다(`docs/refs/next-version.md` N-2).
- `INTENT.md`가 2026-09-25에 바뀌었다 — workbench-kit은 복사해 쓰는 껍데기가 아니라
  필수·공통 기능을 구현해 둔 stand-alone 베이스 앱이고, 마크다운 렌더링이 그 필수
  기능이다(1·3·4·7절).

## 2. 이번 버전에서 풀고 싶은 문제

- Activity Bar 토글을 켜면 탐색기에서 연 `.md`가 렌더링된 화면(Rendered)으로 보인다.
- 렌더링에 테이블과 수식이 포함된다.
- 임의 마크다운을 열어도 스크립트가 실행되지 않는다.

## 3. 이전 버전에서 아쉬웠던 것

- 반대 벤더 재검증을 받지 못하고 닫은 것이 2건 있다 — `A19`(v0.3 Phase 4) 4회차 미실행,
  `A29`(탐색기 삭제 기능) 마지막 Critical 수정의 재검증 미실행. 둘 다 회귀 테스트로만
  확인했다.
- 계획 외 개선이 20건 이상 붙었다(backlog 28건 외).
- 탐색기 삭제가 **비활성** Folder Tab이 가리키는 경로를 지워도 그 탭이 즉시 갱신되지
  않는다. 다음에 그 탭을 열 때 D-6 오류 탭으로만 드러난다.

## 4. 하지 않을 것

- `N-1` — 앱 보기 안에서 쓸 편집기 위젯 대여
- 3절의 v0.3 아쉬웠던 것 3건의 해소
- Mermaid 등 다이어그램 렌더링
- editor와 rendered 사이의 스크롤 동기화
- `.md` 외 형식의 렌더링
- rendered 화면에서의 직접 편집
- 마크다운 렌더링 외 기능을 공통부(`src/core`)로 옮기는 것
- 원격(`http`·`https`) 이미지 표시 (`DECISIONS.md` D-7)
- 저장 전 타이핑·외부 편집기 변경의 rendered 반영 (D-9)

## 5. 완료 조건

1. 토글 아이콘이 꺼짐=사각형 테두리, 켜짐=사각형 채움 + 글리프 반전으로 보인다.
   재시작 뒤에도 상태가 유지되고, 처음 실행 시 꺼짐이다.
2. 토글이 켜지면 `.md`는 트리 클릭·`Enter`·`F3`으로 rendered, `F4`로 editor가 열린다.
   토글이 꺼지면 v0.3과 동작이 같다.
3. rendered 화면에 테이블·수식(`$…$`, `$$…$$`)·코드 블록 문법 강조가 표시되고,
   해당 요소의 실측 크기·색이 0이 아니다(KaTeX CSS·글꼴이 실제로 적용됨).
4. 상대 경로 로컬 이미지가 표시된다.
5. 링크: 상대 경로 → 앱 안에서 열림, `http(s)` → 외부 브라우저, `#…` → 문서 안 스크롤.
6. 같은 파일을 editor에서 저장하면 rendered 탭이 다시 그려진다.
7. `<script>`·`onerror` 등이 든 `.md`를 열어도 스크립트가 실행되지 않는다.
8. 1~7이 Electron·pywebview 두 갈래 모두에서 성립한다.
9. `npm run typecheck`·`npm run build`·기존 `verify:*`가 통과한다(회귀 없음). 공통부
   정적 검사(`scripts/verify-phase3.mjs`)는 마크다운 렌더링 모듈만 허용 목록으로 빼고,
   그 밖의 `src/core` 파일에는 기존 단언이 그대로 적용된다.
