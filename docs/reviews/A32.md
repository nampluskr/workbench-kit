# A32 — workbench-kit v0.4 Phase 3 적대적 검증

- 구현자: Codex
- 세션 내 리뷰어: `.claude/agents/reviewer.md` 기준 검토 완료
- 반대 벤더 검토 모델: Claude opus-5.5
- 대상: `src/core/markdown/index.ts`, `src/style.css`, Phase 3 두 호스트 검사
- 관련 PLAN 조항: Phase 3 수식과 코드 강조
- 실행 회차: 3/3

| 회차 | 일시 | 소요 | 턴 수 | 결과 |
| --- | --- | --- | --- | --- |
| 1 | 2026-09-25 | 10초 | 제한 초과 | `max_turns`로 유효 결과 없음 |
| 2 | 2026-09-25 | 93초 | 제한 초과 | `max_turns`로 유효 결과 없음 |
| 3 | 2026-09-25 | 60초 | 2 | 유효. 원격 자원 자동 요청 가능성 1건과 경미한 보강 사항 지적 |

## 지적

| # | 심각도 | 요약 | 처리 |
| --- | --- | --- | --- |
| 1 | Major 조건부 | `file:` 호스트에서 `//host` 이미지·미디어 주소가 네트워크 요청을 유발할 수 있음 | Phase 3에서 이미지·미디어·SVG·MathML 태그 자체를 금지. 공격 입력을 두 호스트에 추가해 부재 확인. Phase 4에서 안전한 이미지 경로로 교체 예정 |
| 2 | Minor | KaTeX 결과를 DOM에 합친 후 문자열로 다시 파싱함 | 현재 KaTeX `trust: false`와 별도 DOMPurify 정화 후 삽입. 재현 가능한 공격은 제시되지 않았으며 Phase 4에서도 안전 경계를 유지할 것 |
| 3 | Minor | CSP가 KaTeX 인라인 스타일과 글꼴을 막을 가능성 | 두 호스트에서 KaTeX 글꼴 `loaded`, 번들 스타일시트 등록, 실제 수식의 KaTeX 글꼴·크기 확인 |
| 4 | Minor, 세션 내 리뷰어 | 코드 토큰 색과 KaTeX CSS 실측 판정이 약함 | 두 호스트 검사를 본문 글자색 대비와 수식의 계산된 글꼴로 강화 |

## 검증 결과

- Electron·pywebview에서 인라인·블록 수식 크기, KaTeX 글꼴 로드와 계산된 스타일, 세 테마 코드 토큰색, 악성 HTML·수식·코드 입력 미실행 통과.
- `npm run typecheck`, `npm run build`, `verify:phase3`, `verify:dist`, `verify:open-modes`, `verify:text-open` 통과. 미해결 Critical 없음.
- 전체 `npm test`는 이전 Phase부터 기록된 v0.1 Phase 4 계열 실패로 별도 처리한다.
