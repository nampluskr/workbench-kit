Reading additional input from stdin...
OpenAI Codex v0.153.4
--------
workdir: D:\projects\workbench-kit
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 01a083a1-6bed-70c0-b0ad-94e2c38ad05d
--------
user
You are an adversarial reviewer for Phase 5 (확장점 — extension points) of workbench-kit. Your job is to break this code, not to confirm it works.
codex
Phase 5의 요구사항과 구현을 먼저 대조한 뒤, 정상 경로보다 실패 경로·경계 조건·확장 계약 위반을 중심으로 검토하겠습니다. 재현 가능한 문제만 근거와 함께 보고하겠습니다.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'C:\\Users\\nampl\\.codex\\RTK.md'; rg --files -g 'AGENTS.md' -g 'TODO.md' -g 'SESSION_NOTES.md' -g '*phase*' -g '"'!node_modules'"' -g '"'!vendor'"'; git status --short --branch" in D:\projects\workbench-kit
 succeeded in 413ms:
# RTK - Rust Token Killer (Codex CLI)

**Usage**: Token-optimized CLI proxy for shell commands.

## Rule

Always prefix shell commands with `rtk`.

Examples:

```bash
rtk git status
rtk cargo test
rtk npm run build
rtk pytest -q
```

## Meta Commands

```bash
rtk gain            # Token savings analytics
rtk gain --history  # Recent command savings history
rtk proxy <cmd>     # Run raw command without filtering
```

## Verification

```bash
rtk --version
rtk gain
which rtk
```

docs\reviews\phase5_round1_prompt.txt
docs\reviews\phase5_round1_out.txt
docs\reviews\phase4_round4_prompt.txt
docs\reviews\phase4_round4_out.txt
docs\reviews\phase4_round3_prompt.txt
docs\reviews\phase4_round3_out.txt
docs\reviews\phase4_round2_prompt.txt
docs\reviews\phase4_round2_out.txt
docs\reviews\phase4_round1_prompt.txt
docs\reviews\phase4_round1_out.txt
scripts\verify-phase5.mjs
scripts\verify-phase4.mjs
scripts\verify-phase3.mjs
scripts\verify-phase2.mjs
scripts\phase5-suite.js
scripts\phase5-electron-runner.cjs
scripts\phase4-suite.js
scripts\phase4-electron-runner.cjs
warning: unable to access 'C:\Users\nampl/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\nampl/.config/git/ignore': Permission denied
## main...origin/main
 M CLAUDE.md
 M package.json
 M src/core/editor.ts
 M src/core/layout.ts
 M src/core/menu.ts
 M src/hosts/pywebview/main.py
 M src/main.ts
 M src/style.css
?? docs/reserved-keys.md
?? docs/reviews/phase5_round1_out.txt
?? docs/reviews/phase5_round1_prompt.txt
?? scripts/phase5-electron-runner.cjs
?? scripts/phase5-suite.js
?? scripts/verify-phase5.mjs
?? src/core/contextmenu.ts
?? src/presets/
?? src/registry/

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk Get-Content -Raw 'docs\\reviews\\phase5_round1_prompt.txt'; rtk Get-Content -Raw 'docs\\reviews\\phase5_round1_out.txt'; rtk rg -n \"Phase 5|확장점|extension|registry|preset|contextmenu\" README.md docs src package.json scripts -g '"'!docs/reviews/phase5_round1_out.txt'"'" in D:\projects\workbench-kit
 succeeded in 693ms:
rtk: Failed to resolve 'Get-Content' via PATH, falling back to direct exec: Binary 'Get-Content' not found on PATH
[rtk: program not found]
rtk: Failed to resolve 'Get-Content' via PATH, falling back to direct exec: Binary 'Get-Content' not found on PATH
[rtk: program not found]
146 matches in 35 files:

docs\current\PLAN.md:73:Phase 5(`FR-I9` ~ `FR-I11`)이고, 여기서는 껍데기 항목만 센다.
docs\current\PLAN.md:126:## Phase 5 — 갈아끼우는 자리와 앱 표면
docs\current\PLAN.md:212:**Phase 1 · Phase 4 · Phase 5 · Phase 7.** 이 넷은 미해결 Critical이 남아 있으면...
docs\current\PLAN.md:218:시험 통과와 실제 동작이 가장 쉽게 갈리는 자리이며, Phase 5는 이 프로젝트의 존재...
docs\current\backlog.json:32:"확장점",
docs\current\backlog.json:460:"category": "확장점",
docs\current\backlog.json:476:"category": "확장점",
docs\current\backlog.json:492:"category": "확장점",
docs\current\backlog.json:508:"category": "확장점",
docs\current\backlog.json:525:"category": "확장점",
docs\current\backlog.json:541:"category": "확장점",
docs\current\backlog.json:558:"category": "확장점",
docs\current\backlog.json:574:"category": "확장점",
docs\current\backlog.json:787:"title": "Phase 5 적대적 검증",
docs\reviews\A2.md:60:- Compound extensions resolve longest-first at seti.ts:137 and vscode-icons.t...
docs\reviews\A3.md:64:[PASS] Common core extension table count: 0
docs\reviews\A4.md:84:[PASS] src/core/editor.ts contains zero extension mapping tables (NFR-1, INTE...
docs\reviews\codex_round1.txt:10:- **Reproduction conditions:** Resolve `index.py`, `README.md`, and `package....
docs\reviews\codex_round1.txt:71:- **Core purity:** No extension-to-icon mapping table appears in `src/core`.
docs\reviews\codex_round2.txt:30:These values can be compared directly with Microsoft?셲 [Light Modern](https:/...
docs\reviews\codex_round2.txt:70:### 4. File-icon color-theme behavior and compound-extension resolution remai...
docs\reviews\codex_round2.txt:74:Additionally, vscode-icons only tests the final two extension components: [vs...
docs\reviews\codex_round2.txt:90:- No extension-to-icon table exists under `src/core`.
docs\reviews\codex_round2.txt:94:- Core menu arrays are frozen and View/Help have no app-extension API.
docs\reviews\codex_round2_prompt.txt:30:- Zero extension-to-icon mapping tables in src/core/
docs\reviews\codex_round2_prompt.txt:33:1. Seti and vscode-icons real artwork resolution by filename/extension
docs\reviews\codex_round3.txt:10:- Compound extensions resolve longest-first at [seti.ts:137](/D:/projects/wor...
docs\reviews\codex_round3_prompt.txt:40:7. Compound extensions: Multi-part compound extensions (e.g. `example.buf.gen...
docs\reviews\phase4_round1_out.txt:194:- No filesystem imports, filesystem bridge calls, or extension mapping tables...
docs\reviews\phase4_round1_prompt.txt:47:- Zero extension mapping tables in src/core/editor.ts (NFR-1, INTENT 3).
docs\reviews\phase4_round3_prompt.txt:23:* FR-C9 & D-22: Dispatched `contextmenu` event on tab title and asserted stri...
docs\reviews\phase5_round1_prompt.txt:1:You are an adversarial reviewer for Phase 5 (확장점 — extension points) of workb...
docs\reviews\phase5_round1_prompt.txt:7:- src/core/contextmenu.ts
docs\reviews\phase5_round1_prompt.txt:8:- src/registry/kind-registry.ts
docs\reviews\phase5_round1_prompt.txt:9:- src/presets/file-preset.ts
docs\reviews\phase5_round1_prompt.txt:10:- src/presets/folder-preset.ts
docs\reviews\phase5_round1_prompt.txt:20:Attack these specific points (translated from PLAN.md's Phase 5 attack-focus ...
docs\reviews\phase5_round1_prompt.txt:22:that knows about files, folders, terminals, or file extensions; any bypass th...
docs\reviews\phase5_round1_prompt.txt:24:extension (File menu items, view-titlebar actions) can remove or reorder the ...
docs\reviews\phase5_round1_prompt.txt:29:Validate against these PLAN.md clauses (read docs/current/PLAN.md's "## Phase 5"
docs\reviews\phase5_round1_prompt.txt:36:so file-preset.ts/folder-preset.ts must stay placeholder stubs, not real file...
scripts\phase4-suite.js:504:tabEl.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable...
scripts\phase5-electron-runner.cjs:59:console.log('[electron] Phase 5 Results: ' + JSON.stringify(testResult));
scripts\phase5-electron-runner.cjs:60:assert(testResult && testResult.success === true, 'All Phase 5 Electron in-br...
scripts\phase5-suite.js:2:* Host-neutral Phase 5 Acceptance Test Suite for workbench-kit.
scripts\phase5-suite.js:62:'A test-registered "widget" kind opens and renders via the registry with zero...
scripts\phase5-suite.js:66:// 2. File preset & folder preset (FR-I2, FR-I3, WK-026)
scripts\phase5-suite.js:68:editor.openItem('/preset/file-a.txt', 'file-a.txt', { meta: { kind: 'file' } });
scripts\phase5-suite.js:73:fileEl !== null && fileEl.textContent === 'File preset view: /preset/file-a.t...
scripts\phase5-suite.js:74:'File preset renders its target as the active tab content (FR-I2)'
scripts\phase5-suite.js:77:editor.openItem('/preset/folder-a', 'folder-a', { meta: { kind: 'folder' } });
scripts\phase5-suite.js:82:folderEl !== null && folderEl.textContent === 'Folder preset view: /preset/fo...
scripts\phase5-suite.js:83:'Folder preset renders its target as the active tab content (FR-I3)'
scripts\phase5-suite.js:201:row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: ...
scripts\phase5-suite.js:216:row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: ...
scripts\phase5-suite.js:237:row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: ...
scripts\phase5-suite.js:293:fileGroup.items[coreFileItems.length + 1].id === 'app:file:preset-info',
scripts\phase5-suite.js:327:record('RUNNER_ERR', false, 'Unhandled error in Phase 5 suite: ' + (err && er...
scripts\prompt-review-p2.txt:34:- File group: exactly 5 items (폴더 열기(Ctrl+O), 최근 폴더, 탭 닫기(Ctrl+W), 폴더 닫기, 끝내기...
scripts\prompt-review-p2.txt:55:- Common core contains zero extension-to-icon mapping tables (FR-Q3).
scripts\verify-dist.mjs:208:assert(!/(extensions?|languageId|fileExtensions)\s*[:=]\s*\{/i.test(code), `s...
scripts\verify-dist.mjs:210:console.log('[PASS] Common core contains zero extension-to-icon mapping table...
scripts\verify-phase2.mjs:125:// Test compound extension lookup in data
scripts\verify-phase2.mjs:132:'vscode-icons resolver iterates compound extensions from longest to shortest'
scripts\verify-phase2.mjs:141:'seti resolver iterates compound extensions from longest to shortest'
scripts\verify-phase3.mjs:49:assert(!content.includes('extensionMap') && !content.includes('fileExtensions...
scripts\verify-phase4.mjs:46:assert(!editorTs.includes('extensionMap') && !editorTs.includes('fileExtensio...
scripts\verify-phase5.mjs:10:console.log('=== workbench-kit: Phase 5 (Extension Points) Quality Verificati...
scripts\verify-phase5.mjs:34:const coreFiles = ['editor.ts', 'contextmenu.ts', 'layout.ts', 'menu.ts', 'si...
scripts\verify-phase5.mjs:38:const kindVocabCheckFiles = ['editor.ts', 'contextmenu.ts', 'layout.ts', 'sid...
scripts\verify-phase5.mjs:49:// 2. App-facing surface: presets/registry never manipulate tabs/panes
scripts\verify-phase5.mjs:51://    and registry.register are used.
scripts\verify-phase5.mjs:54:const appLayerFiles = ['src/presets/file-preset.ts', 'src/presets/folder-pres...
scripts\verify-phase5.mjs:62:// The preset helpers must type against the narrow AppEditorSurface, not the
scripts\verify-phase5.mjs:65:for (const rel of ['src/presets/file-preset.ts', 'src/presets/folder-preset.t...
scripts\verify-phase5.mjs:119:const match = runnerOutput.match(/\[electron\] Phase 5 Results:\s*(\{.*\})/);
scripts\verify-phase5.mjs:123:assert(runnerOutput.includes('All Phase 5 Electron in-browser runtime asserti...
scripts\verify-phase5.mjs:160:const pyMatch = pyOutput.match(/\[pywebview\] Phase 5 Results:\s*(\{.*\})/);
scripts\verify-phase5.mjs:173:// Phase 5 Required Assertion Manifest (Immutable Contract)
scripts\verify-phase5.mjs:249:assert(divergences === 0, `Zero divergence between Electron and pywebview acr...
scripts\verify-phase5.mjs:259:console.log('SUCCESS: All Phase 5 quality checks passed (0 failures).');
src\core\editor.ts:31:* The entire surface the shell exposes to app code (presets, registry
src\core\icontheme.ts:80:* Shell is oblivious to file extensions (FR-Q3, INTENT 3).
src\core\icontheme.ts:82:* Common core maintains zero extension-to-icon mapping tables.
src\hosts\pywebview\main.py:233:sys.stdout.write(f"[pywebview] Phase 5 Results: {res}\n")
src\hosts\pywebview\main.py:238:sys.stderr.write("[pywebview] Error: Phase 5 test timed out waiting for resul...
src\hosts\pywebview\main.py:243:sys.stderr.write(f"[pywebview] Error executing Phase 5 test: {e}\n")
src\icons\assets\vscode-icons\file_type_registry.svg:1:<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><title>file_type_...
src\icons\data\vscode-icons.json:3330:"_f_registry": {
src\icons\data\vscode-icons.json:3331:"iconPath": "../../icons/file_type_registry.svg"
src\icons\data\vscode-icons.json:4375:".ebextensions": "_fd_elasticbeanstalk",
src\icons\data\vscode-icons.json:4565:"extension": "_fd_plugin",
src\icons\data\vscode-icons.json:4566:".extension": "_fd_plugin",
src\icons\data\vscode-icons.json:4567:"extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:4568:".extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:4867:".ebextensions": "_fd_elasticbeanstalk_open",
src\icons\data\vscode-icons.json:5057:"extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5058:".extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5059:"extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5060:".extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5610:"reg": "_f_registry",
src\icons\data\vscode-icons.json:6501:"extensions.xml": "_f_maven",
src\icons\data\vscode-icons.json:8125:".ebextensions": "_fd_elasticbeanstalk",
src\icons\data\vscode-icons.json:8315:"extension": "_fd_plugin",
src\icons\data\vscode-icons.json:8316:".extension": "_fd_plugin",
src\icons\data\vscode-icons.json:8317:"extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:8318:".extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:8617:".ebextensions": "_fd_elasticbeanstalk_open",
src\icons\data\vscode-icons.json:8807:"extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:8808:".extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:8809:"extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:8810:".extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:9360:"reg": "_f_registry",
  +1 more in src\icons\data\vscode-icons.json
src\icons\language-map.ts:2:// Maps common file extensions to language IDs for Seti and vscode-icons reso...
src\icons\seti.ts:137:// Check compound extensions from longest to shortest (e.g. buf.gen.yml, gen....
src\icons\vscode-icons.ts:112:// Check compound extensions from longest to shortest (e.g. buf.gen.yml -> ge...
src\main.ts:13:import { ContextMenuController, ContextMenuItem } from './core/contextmenu';
src\main.ts:14:import { ResourceKindRegistry } from './registry/kind-registry';
src\main.ts:15:import { registerFilePreset, FILE_KIND } from './presets/file-preset';
src\main.ts:16:import { registerFolderPreset, FOLDER_KIND } from './presets/folder-preset';
src\main.ts:58:// Resource kind registration slot + minimal example presets (FR-I1, FR-I2, F...
src\main.ts:145:this.layout.sidebarContent.addEventListener('contextmenu', (e) => {
src\main.ts:153:this.layout.editorContainer.addEventListener('contextmenu', (e) => {
src\main.ts:174:// Minimal example wiring proving the app-facing extension slots (WK-048, WK-...
src\main.ts:179:id: 'app:file:preset-info',
src\main.ts:188:id: 'app:sidebar:preset-info',
src\main.ts:198:const presetInfoEl = document.createElement('span');
src\main.ts:199:presetInfoEl.className = 'statusbar-app-item';
src\main.ts:200:presetInfoEl.textContent = 'Presets: file, folder';
src\main.ts:201:this.layout.statusbarAppItems.appendChild(presetInfoEl);
src\presets\file-preset.ts:1:import type { ResourceKindRegistry } from '../registry/kind-registry';
src\presets\file-preset.ts:4:// NOT part of the common core. This preset is a minimal example of a
src\presets\file-preset.ts:11:export function registerFilePreset(registry: ResourceKindRegistry): void {
src\presets\file-preset.ts:12:registry.register(FILE_KIND, (targetId) => {
src\presets\file-preset.ts:14:element.className = 'preset-file-view';
src\presets\file-preset.ts:15:element.textContent = `File preset view: ${targetId}`;
src\presets\folder-preset.ts:1:import type { ResourceKindRegistry } from '../registry/kind-registry';
src\presets\folder-preset.ts:10:export function registerFolderPreset(registry: ResourceKindRegistry): void {
src\presets\folder-preset.ts:11:registry.register(FOLDER_KIND, (targetId) => {
src\presets\folder-preset.ts:13:element.className = 'preset-folder-view';
src\presets\folder-preset.ts:14:element.textContent = `Folder preset view: ${targetId}`;
src\registry\kind-registry.ts:41:const registry = this;
src\registry\kind-registry.ts:42:return () => new KindDispatchRenderer(registry);
src\registry\kind-registry.ts:51:constructor(private registry: ResourceKindRegistry) {
src\registry\kind-registry.ts:81:const factory = this.registry.getFactory(kind);

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk powershell -NoProfile -Command \"Get-Content -Raw 'docs\\reviews\\phase5_round1_prompt.txt'; Get-Content -Raw 'docs\\reviews\\phase5_round1_out.txt'\"; rtk powershell -NoProfile -Command \""'$p=Get-Content '"'docs\\current\\PLAN.md'; "'$p[120..175]"; rtk powershell -NoProfile -Command "Get-Content -Raw '"'src\\registry\\kind-registry.ts'; Get-Content -Raw 'src\\core\\contextmenu.ts'; Get-Content -Raw 'src\\core\\editor.ts'\"" in D:\projects\workbench-kit
 succeeded in 1022ms:
You are an adversarial reviewer for Phase 5 (?뺤옣????extension points) of workbench-kit. Your job is to break this code, not to confirm it works.

Review ONLY these product and test source files (all under the repo root):
- src/core/editor.ts
- src/core/layout.ts
- src/core/menu.ts
- src/core/contextmenu.ts
- src/registry/kind-registry.ts
- src/presets/file-preset.ts
- src/presets/folder-preset.ts
- src/main.ts
- src/hosts/pywebview/main.py
- src/style.css
- docs/reserved-keys.md
- scripts/phase5-suite.js
- scripts/phase5-electron-runner.cjs
- scripts/verify-phase5.mjs
- package.json

Attack these specific points (translated from PLAN.md's Phase 5 attack-focus row):
Break the "zero core diff" claim ??find any code in the common core (src/core/*.ts)
that knows about files, folders, terminals, or file extensions; any bypass through the
app-facing surface that lets app code create or close a tab/pane; any way an app
extension (File menu items, view-titlebar actions) can remove or reorder the shell's
own items, or reach the View/Help menu groups; and any key that leaks outside the
documented reserved-key list (docs/reserved-keys.md) ??either a reserved key that
silently reaches the app, or a non-reserved key that the shell secretly intercepts.

Validate against these PLAN.md clauses (read docs/current/PLAN.md's "## Phase 5"
section and its completion conditions verbatim):
- FR-I1 through FR-I11, FR-N10, FR-N12, FR-G5, FR-G6, FR-R1, FR-R1a, NFR-1, NFR-7
  (see docs/current/SPEC.md for each ID's exact requirement/acceptance wording)
- D-4, D-5, D-9, D-11, D-19, D-22, D-23, D-30, D-31 (see docs/current/DECISIONS.md)
- INTENT.md section 3 ("the shell does not know resource kinds") and section 7
  (this version builds only the slots these apps plug into, not the apps themselves ??  so file-preset.ts/folder-preset.ts must stay placeholder stubs, not real file I/O)

Do not inspect Git status, branches, remotes, or commit history, and do not use shell
tools beyond reading files. For each finding, report severity (Critical/Major/Minor),
exact reproduction conditions (file:line where relevant), and the violated PLAN/SPEC/
DECISIONS clause. Order findings by severity. Do not modify any file.

Reading additional input from stdin...
OpenAI Codex v0.153.4
--------
workdir: D:\projects\workbench-kit
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 01a083a1-6bed-70c0-b0ad-94e2c38ad05d
--------
user
You are an adversarial reviewer for Phase 5 (?뺤옣????extension points) of workbench-kit. Your job is to break this code, not to confirm it works.
codex
Phase 5???붽뎄?ы빆怨?援ы쁽??癒쇱? ?議고븳 ?? ?뺤긽 寃쎈줈蹂대떎 ?ㅽ뙣 寃쎈줈쨌寃쎄퀎 議곌굔쨌?뺤옣 怨꾩빟 ?꾨컲??以묒떖?쇰줈 寃?좏븯寃좎뒿?덈떎. ?ы쁽 媛?ν븳 臾몄젣留?洹쇨굅? ?④퍡 蹂닿퀬?섍쿋?듬땲??
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 'C:\\Users\\nampl\\.codex\\RTK.md'; rg --files -g 'AGENTS.md' -g 'TODO.md' -g 'SESSION_NOTES.md' -g '*phase*' -g '"'!node_modules'"' -g '"'!vendor'"'; git status --short --branch" in D:\projects\workbench-kit
 succeeded in 413ms:
# RTK - Rust Token Killer (Codex CLI)

**Usage**: Token-optimized CLI proxy for shell commands.

## Rule

Always prefix shell commands with `rtk`.

Examples:

```bash
rtk git status
rtk cargo test
rtk npm run build
rtk pytest -q
```

## Meta Commands

```bash
rtk gain            # Token savings analytics
rtk gain --history  # Recent command savings history
rtk proxy <cmd>     # Run raw command without filtering
```

## Verification

```bash
rtk --version
rtk gain
which rtk
```

docs\reviews\phase5_round1_prompt.txt
docs\reviews\phase5_round1_out.txt
docs\reviews\phase4_round4_prompt.txt
docs\reviews\phase4_round4_out.txt
docs\reviews\phase4_round3_prompt.txt
docs\reviews\phase4_round3_out.txt
docs\reviews\phase4_round2_prompt.txt
docs\reviews\phase4_round2_out.txt
docs\reviews\phase4_round1_prompt.txt
docs\reviews\phase4_round1_out.txt
scripts\verify-phase5.mjs
scripts\verify-phase4.mjs
scripts\verify-phase3.mjs
scripts\verify-phase2.mjs
scripts\phase5-suite.js
scripts\phase5-electron-runner.cjs
scripts\phase4-suite.js
scripts\phase4-electron-runner.cjs
warning: unable to access 'C:\Users\nampl/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\nampl/.config/git/ignore': Permission denied
## main...origin/main
 M CLAUDE.md
 M package.json
 M src/core/editor.ts
 M src/core/layout.ts
 M src/core/menu.ts
 M src/hosts/pywebview/main.py
 M src/main.ts
 M src/style.css
?? docs/reserved-keys.md
?? docs/reviews/phase5_round1_out.txt
?? docs/reviews/phase5_round1_prompt.txt
?? scripts/phase5-electron-runner.cjs
?? scripts/phase5-suite.js
?? scripts/verify-phase5.mjs
?? src/core/contextmenu.ts
?? src/presets/
?? src/registry/

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk Get-Content -Raw 'docs\\reviews\\phase5_round1_prompt.txt'; rtk Get-Content -Raw 'docs\\reviews\\phase5_round1_out.txt'; rtk rg -n \"Phase 5|?뺤옣??extension|registry|preset|contextmenu\" README.md docs src package.json scripts -g '"'!docs/reviews/phase5_round1_out.txt'"'" in D:\projects\workbench-kit
 succeeded in 693ms:
rtk: Failed to resolve 'Get-Content' via PATH, falling back to direct exec: Binary 'Get-Content' not found on PATH
[rtk: program not found]
rtk: Failed to resolve 'Get-Content' via PATH, falling back to direct exec: Binary 'Get-Content' not found on PATH
[rtk: program not found]
146 matches in 35 files:

docs\current\PLAN.md:73:Phase 5(`FR-I9` ~ `FR-I11`)?닿퀬, ?ш린?쒕뒗 猿띾뜲湲???ぉ留??쇰떎.
docs\current\PLAN.md:126:## Phase 5 ??媛덉븘?쇱슦???먮━? ???쒕㈃
docs\current\PLAN.md:212:**Phase 1 쨌 Phase 4 쨌 Phase 5 쨌 Phase 7.** ???룹? 誘명빐寃?Critical???⑥븘 ?덉쑝硫?..
docs\current\PLAN.md:218:?쒗뿕 ?듦낵? ?ㅼ젣 ?숈옉??媛???쎄쾶 媛덈━???먮━?대ŉ, Phase 5?????꾨줈?앺듃??議댁옱...
docs\current\backlog.json:32:"?뺤옣??,
docs\current\backlog.json:460:"category": "?뺤옣??,
docs\current\backlog.json:476:"category": "?뺤옣??,
docs\current\backlog.json:492:"category": "?뺤옣??,
docs\current\backlog.json:508:"category": "?뺤옣??,
docs\current\backlog.json:525:"category": "?뺤옣??,
docs\current\backlog.json:541:"category": "?뺤옣??,
docs\current\backlog.json:558:"category": "?뺤옣??,
docs\current\backlog.json:574:"category": "?뺤옣??,
docs\current\backlog.json:787:"title": "Phase 5 ?곷???寃利?,
docs\reviews\A2.md:60:- Compound extensions resolve longest-first at seti.ts:137 and vscode-icons.t...
docs\reviews\A3.md:64:[PASS] Common core extension table count: 0
docs\reviews\A4.md:84:[PASS] src/core/editor.ts contains zero extension mapping tables (NFR-1, INTE...
docs\reviews\codex_round1.txt:10:- **Reproduction conditions:** Resolve `index.py`, `README.md`, and `package....
docs\reviews\codex_round1.txt:71:- **Core purity:** No extension-to-icon mapping table appears in `src/core`.
docs\reviews\codex_round2.txt:30:These values can be compared directly with Microsoft???[Light Modern](https:/...
docs\reviews\codex_round2.txt:70:### 4. File-icon color-theme behavior and compound-extension resolution remai...
docs\reviews\codex_round2.txt:74:Additionally, vscode-icons only tests the final two extension components: [vs...
docs\reviews\codex_round2.txt:90:- No extension-to-icon table exists under `src/core`.
docs\reviews\codex_round2.txt:94:- Core menu arrays are frozen and View/Help have no app-extension API.
docs\reviews\codex_round2_prompt.txt:30:- Zero extension-to-icon mapping tables in src/core/
docs\reviews\codex_round2_prompt.txt:33:1. Seti and vscode-icons real artwork resolution by filename/extension
docs\reviews\codex_round3.txt:10:- Compound extensions resolve longest-first at [seti.ts:137](/D:/projects/wor...
docs\reviews\codex_round3_prompt.txt:40:7. Compound extensions: Multi-part compound extensions (e.g. `example.buf.gen...
docs\reviews\phase4_round1_out.txt:194:- No filesystem imports, filesystem bridge calls, or extension mapping tables...
docs\reviews\phase4_round1_prompt.txt:47:- Zero extension mapping tables in src/core/editor.ts (NFR-1, INTENT 3).
docs\reviews\phase4_round3_prompt.txt:23:* FR-C9 & D-22: Dispatched `contextmenu` event on tab title and asserted stri...
docs\reviews\phase5_round1_prompt.txt:1:You are an adversarial reviewer for Phase 5 (?뺤옣????extension points) of workb...
docs\reviews\phase5_round1_prompt.txt:7:- src/core/contextmenu.ts
docs\reviews\phase5_round1_prompt.txt:8:- src/registry/kind-registry.ts
docs\reviews\phase5_round1_prompt.txt:9:- src/presets/file-preset.ts
docs\reviews\phase5_round1_prompt.txt:10:- src/presets/folder-preset.ts
docs\reviews\phase5_round1_prompt.txt:20:Attack these specific points (translated from PLAN.md's Phase 5 attack-focus ...
docs\reviews\phase5_round1_prompt.txt:22:that knows about files, folders, terminals, or file extensions; any bypass th...
docs\reviews\phase5_round1_prompt.txt:24:extension (File menu items, view-titlebar actions) can remove or reorder the ...
docs\reviews\phase5_round1_prompt.txt:29:Validate against these PLAN.md clauses (read docs/current/PLAN.md's "## Phase 5"
docs\reviews\phase5_round1_prompt.txt:36:so file-preset.ts/folder-preset.ts must stay placeholder stubs, not real file...
scripts\phase4-suite.js:504:tabEl.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable...
scripts\phase5-electron-runner.cjs:59:console.log('[electron] Phase 5 Results: ' + JSON.stringify(testResult));
scripts\phase5-electron-runner.cjs:60:assert(testResult && testResult.success === true, 'All Phase 5 Electron in-br...
scripts\phase5-suite.js:2:* Host-neutral Phase 5 Acceptance Test Suite for workbench-kit.
scripts\phase5-suite.js:62:'A test-registered "widget" kind opens and renders via the registry with zero...
scripts\phase5-suite.js:66:// 2. File preset & folder preset (FR-I2, FR-I3, WK-026)
scripts\phase5-suite.js:68:editor.openItem('/preset/file-a.txt', 'file-a.txt', { meta: { kind: 'file' } });
scripts\phase5-suite.js:73:fileEl !== null && fileEl.textContent === 'File preset view: /preset/file-a.t...
scripts\phase5-suite.js:74:'File preset renders its target as the active tab content (FR-I2)'
scripts\phase5-suite.js:77:editor.openItem('/preset/folder-a', 'folder-a', { meta: { kind: 'folder' } });
scripts\phase5-suite.js:82:folderEl !== null && folderEl.textContent === 'Folder preset view: /preset/fo...
scripts\phase5-suite.js:83:'Folder preset renders its target as the active tab content (FR-I3)'
scripts\phase5-suite.js:201:row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: ...
scripts\phase5-suite.js:216:row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: ...
scripts\phase5-suite.js:237:row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: ...
scripts\phase5-suite.js:293:fileGroup.items[coreFileItems.length + 1].id === 'app:file:preset-info',
scripts\phase5-suite.js:327:record('RUNNER_ERR', false, 'Unhandled error in Phase 5 suite: ' + (err && er...
scripts\prompt-review-p2.txt:34:- File group: exactly 5 items (?대뜑 ?닿린(Ctrl+O), 理쒓렐 ?대뜑, ???リ린(Ctrl+W), ?대뜑 ?リ린, ?앸궡湲?..
scripts\prompt-review-p2.txt:55:- Common core contains zero extension-to-icon mapping tables (FR-Q3).
scripts\verify-dist.mjs:208:assert(!/(extensions?|languageId|fileExtensions)\s*[:=]\s*\{/i.test(code), `s...
scripts\verify-dist.mjs:210:console.log('[PASS] Common core contains zero extension-to-icon mapping table...
scripts\verify-phase2.mjs:125:// Test compound extension lookup in data
scripts\verify-phase2.mjs:132:'vscode-icons resolver iterates compound extensions from longest to shortest'
scripts\verify-phase2.mjs:141:'seti resolver iterates compound extensions from longest to shortest'
scripts\verify-phase3.mjs:49:assert(!content.includes('extensionMap') && !content.includes('fileExtensions...
scripts\verify-phase4.mjs:46:assert(!editorTs.includes('extensionMap') && !editorTs.includes('fileExtensio...
scripts\verify-phase5.mjs:10:console.log('=== workbench-kit: Phase 5 (Extension Points) Quality Verificati...
scripts\verify-phase5.mjs:34:const coreFiles = ['editor.ts', 'contextmenu.ts', 'layout.ts', 'menu.ts', 'si...
scripts\verify-phase5.mjs:38:const kindVocabCheckFiles = ['editor.ts', 'contextmenu.ts', 'layout.ts', 'sid...
scripts\verify-phase5.mjs:49:// 2. App-facing surface: presets/registry never manipulate tabs/panes
scripts\verify-phase5.mjs:51://    and registry.register are used.
scripts\verify-phase5.mjs:54:const appLayerFiles = ['src/presets/file-preset.ts', 'src/presets/folder-pres...
scripts\verify-phase5.mjs:62:// The preset helpers must type against the narrow AppEditorSurface, not the
scripts\verify-phase5.mjs:65:for (const rel of ['src/presets/file-preset.ts', 'src/presets/folder-preset.t...
scripts\verify-phase5.mjs:119:const match = runnerOutput.match(/\[electron\] Phase 5 Results:\s*(\{.*\})/);
scripts\verify-phase5.mjs:123:assert(runnerOutput.includes('All Phase 5 Electron in-browser runtime asserti...
scripts\verify-phase5.mjs:160:const pyMatch = pyOutput.match(/\[pywebview\] Phase 5 Results:\s*(\{.*\})/);
scripts\verify-phase5.mjs:173:// Phase 5 Required Assertion Manifest (Immutable Contract)
scripts\verify-phase5.mjs:249:assert(divergences === 0, `Zero divergence between Electron and pywebview acr...
scripts\verify-phase5.mjs:259:console.log('SUCCESS: All Phase 5 quality checks passed (0 failures).');
src\core\editor.ts:31:* The entire surface the shell exposes to app code (presets, registry
src\core\icontheme.ts:80:* Shell is oblivious to file extensions (FR-Q3, INTENT 3).
src\core\icontheme.ts:82:* Common core maintains zero extension-to-icon mapping tables.
src\hosts\pywebview\main.py:233:sys.stdout.write(f"[pywebview] Phase 5 Results: {res}\n")
src\hosts\pywebview\main.py:238:sys.stderr.write("[pywebview] Error: Phase 5 test timed out waiting for resul...
src\hosts\pywebview\main.py:243:sys.stderr.write(f"[pywebview] Error executing Phase 5 test: {e}\n")
src\icons\assets\vscode-icons\file_type_registry.svg:1:<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><title>file_type_...
src\icons\data\vscode-icons.json:3330:"_f_registry": {
src\icons\data\vscode-icons.json:3331:"iconPath": "../../icons/file_type_registry.svg"
src\icons\data\vscode-icons.json:4375:".ebextensions": "_fd_elasticbeanstalk",
src\icons\data\vscode-icons.json:4565:"extension": "_fd_plugin",
src\icons\data\vscode-icons.json:4566:".extension": "_fd_plugin",
src\icons\data\vscode-icons.json:4567:"extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:4568:".extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:4867:".ebextensions": "_fd_elasticbeanstalk_open",
src\icons\data\vscode-icons.json:5057:"extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5058:".extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5059:"extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5060:".extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:5610:"reg": "_f_registry",
src\icons\data\vscode-icons.json:6501:"extensions.xml": "_f_maven",
src\icons\data\vscode-icons.json:8125:".ebextensions": "_fd_elasticbeanstalk",
src\icons\data\vscode-icons.json:8315:"extension": "_fd_plugin",
src\icons\data\vscode-icons.json:8316:".extension": "_fd_plugin",
src\icons\data\vscode-icons.json:8317:"extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:8318:".extensions": "_fd_plugin",
src\icons\data\vscode-icons.json:8617:".ebextensions": "_fd_elasticbeanstalk_open",
src\icons\data\vscode-icons.json:8807:"extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:8808:".extension": "_fd_plugin_open",
src\icons\data\vscode-icons.json:8809:"extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:8810:".extensions": "_fd_plugin_open",
src\icons\data\vscode-icons.json:9360:"reg": "_f_registry",
  +1 more in src\icons\data\vscode-icons.json
src\icons\language-map.ts:2:// Maps common file extensions to language IDs for Seti and vscode-icons reso...
src\icons\seti.ts:137:// Check compound extensions from longest to shortest (e.g. buf.gen.yml, gen....
src\icons\vscode-icons.ts:112:// Check compound extensions from longest to shortest (e.g. buf.gen.yml -> ge...
src\main.ts:13:import { ContextMenuController, ContextMenuItem } from './core/contextmenu';
src\main.ts:14:import { ResourceKindRegistry } from './registry/kind-registry';
src\main.ts:15:import { registerFilePreset, FILE_KIND } from './presets/file-preset';
src\main.ts:16:import { registerFolderPreset, FOLDER_KIND } from './presets/folder-preset';
src\main.ts:58:// Resource kind registration slot + minimal example presets (FR-I1, FR-I2, F...
src\main.ts:145:this.layout.sidebarContent.addEventListener('contextmenu', (e) => {
src\main.ts:153:this.layout.editorContainer.addEventListener('contextmenu', (e) => {
src\main.ts:174:// Minimal example wiring proving the app-facing extension slots (WK-048, WK-...
src\main.ts:179:id: 'app:file:preset-info',
src\main.ts:188:id: 'app:sidebar:preset-info',
src\main.ts:198:const presetInfoEl = document.createElement('span');
src\main.ts:199:presetInfoEl.className = 'statusbar-app-item';
src\main.ts:200:presetInfoEl.textContent = 'Presets: file, folder';
src\main.ts:201:this.layout.statusbarAppItems.appendChild(presetInfoEl);
src\presets\file-preset.ts:1:import type { ResourceKindRegistry } from '../registry/kind-registry';
src\presets\file-preset.ts:4:// NOT part of the common core. This preset is a minimal example of a
src\presets\file-preset.ts:11:export function registerFilePreset(registry: ResourceKindRegistry): void {
src\presets\file-preset.ts:12:registry.register(FILE_KIND, (targetId) => {
src\presets\file-preset.ts:14:element.className = 'preset-file-view';
src\presets\file-preset.ts:15:element.textContent = `File preset view: ${targetId}`;
src\presets\folder-preset.ts:1:import type { ResourceKindRegistry } from '../registry/kind-registry';
src\presets\folder-preset.ts:10:export function registerFolderPreset(registry: ResourceKindRegistry): void {
src\presets\folder-preset.ts:11:registry.register(FOLDER_KIND, (targetId) => {
src\presets\folder-preset.ts:13:element.className = 'preset-folder-view';
src\presets\folder-preset.ts:14:element.textContent = `Folder preset view: ${targetId}`;
src\registry\kind-registry.ts:41:const registry = this;
src\registry\kind-registry.ts:42:return () => new KindDispatchRenderer(registry);
src\registry\kind-registry.ts:51:constructor(private registry: ResourceKindRegistry) {
src\registry\kind-registry.ts:81:const factory = this.registry.getFactory(kind);

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk powershell -NoProfile -Command \"Get-Content -Raw 'docs\\reviews\\phase5_round1_prompt.txt'; Get-Content -Raw 'docs\\reviews\\phase5_round1_out.txt'\"; rtk powershell -NoProfile -Command \""'$p=Get-Content '"'docs\\current\\PLAN.md'; "'$p[120..175]"; rtk powershell -NoProfile -Command "Get-Content -Raw '"'src\\registry\\kind-registry.ts'; Get-Content -Raw 'src\\core\\contextmenu.ts'; Get-Content -Raw 'src\\core\\editor.ts'\"" in D:\projects\workbench-kit

At line:1 char:39
+ =Get-Content 'docs\current\PLAN.md'; [120..175]
+                                       ~
Missing type name after '['.
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : MissingTypename
 
import type { IContentRenderer, GroupPanelPartInitParameters } from 'dockview-core';
import type { EditorComponentFactory } from '../core/editor';

// NOT part of the common core (src/core/**). This module is free to know
// about resource kinds; the shell it plugs into never imports it (FR-I1,
// NFR-1, INTENT 3, D-4).

export type KindRendererFactory = (targetId: string) => {
  element: HTMLElement;
  dispose?: () => void;
};

/**
 * Resource kind registration slot (WK-025, FR-I1). Registering a new kind
 * here never touches src/core/*.ts: the shell only ever calls
 * `createComponentFactory()` once, and every future kind is added by calling
 * `register()` again.
 */
export class ResourceKindRegistry {
  private factories = new Map<string, KindRendererFactory>();

  public register(kind: string, factory: KindRendererFactory): void {
    this.factories.set(kind, factory);
  }

  public has(kind: string): boolean {
    return this.factories.has(kind);
  }

  public getFactory(kind: string): KindRendererFactory | undefined {
    return this.factories.get(kind);
  }

  /**
   * Returns a single EditorComponentFactory suitable for
   * `EditorController.setComponentFactory()`. The returned renderer defers
   * kind dispatch to `init()`, since dockview only supplies panel params at
   * that point, not at component-creation time.
   */
  public createComponentFactory(): EditorComponentFactory {
    const registry = this;
    return () => new KindDispatchRenderer(registry);
  }
}

class KindDispatchRenderer implements IContentRenderer {
  public readonly element: HTMLElement;
  private inner: { element: HTMLElement; dispose?: () => void } | null = null;
  private params: { kind?: string; targetId?: string } = {};

  constructor(private registry: ResourceKindRegistry) {
    this.element = document.createElement('div');
    // Reuses the shell's editor-panel-content contract (D-20, FR-C2): an
    // unresolved/empty panel renders 0 children and 0 text, same as before
    // any kind was registered.
    this.element.className = 'editor-panel-content';
  }

  public init(parameters: GroupPanelPartInitParameters): void {
    this.params = (parameters.params || {}) as { kind?: string; targetId?: string };
    this.renderForCurrentParams();
  }

  public update(event: { params?: Record<string, unknown> }): void {
    if (event.params) {
      this.params = { ...this.params, ...event.params };
      this.renderForCurrentParams();
    }
  }

  private renderForCurrentParams(): void {
    this.inner?.dispose?.();
    this.inner = null;
    this.element.innerHTML = '';

    const { kind, targetId } = this.params;
    if (!kind || !targetId) {
      return;
    }

    const factory = this.registry.getFactory(kind);
    if (!factory) {
      return;
    }

    this.inner = factory(targetId);
    this.element.appendChild(this.inner.element);
  }

  public dispose(): void {
    this.inner?.dispose?.();
    this.inner = null;
    this.element.innerHTML = '';
  }
}

export interface ContextMenuItem {
  id: string;
  label: string;
  action: () => void;
}

/**
 * Generic right-click menu device (D-22). The shell only draws, positions,
 * and navigates the popup; it never decides what items appear. Default is
 * disabled, and showing zero items renders zero DOM elements (FR-G5).
 */
export class ContextMenuController {
  private enabled = false;
  private menuEl: HTMLElement | null = null;
  private currentItems: ContextMenuItem[] = [];
  private focusedIndex = -1;

  constructor(private rootContainer: HTMLElement) {
    document.addEventListener('mousedown', (e) => {
      if (this.menuEl && !this.menuEl.contains(e.target as Node)) {
        this.hide();
      }
    });
    document.addEventListener('keydown', (e) => this.handleKeydown(e));
    if (typeof window !== 'undefined') {
      window.addEventListener('blur', () => this.hide());
    }
  }

  public setEnabled(enabled: boolean): void {
    this.enabled = enabled;
    if (!enabled) {
      this.hide();
    }
  }

  public isEnabled(): boolean {
    return this.enabled;
  }

  public isOpen(): boolean {
    return this.menuEl !== null;
  }

  /**
   * Shows the menu at (x, y). No-op when disabled (D-22) or when the caller
   * supplies zero items, so a shell with no app-defined items draws nothing.
   */
  public show(x: number, y: number, items: ContextMenuItem[]): void {
    this.hide();
    if (!this.enabled || items.length === 0) {
      return;
    }

    this.currentItems = items;
    this.focusedIndex = 0;

    const menuEl = document.createElement('div');
    menuEl.className = 'workbench-context-menu';
    menuEl.style.left = `${x}px`;
    menuEl.style.top = `${y}px`;

    items.forEach((item, index) => {
      const row = document.createElement('div');
      row.className = 'context-menu-item-row' + (index === 0 ? ' focused' : '');
      row.dataset.itemId = item.id;
      row.textContent = item.label;
      row.addEventListener('click', (e) => {
        e.stopPropagation();
        item.action();
        this.hide();
      });
      menuEl.appendChild(row);
    });

    this.rootContainer.appendChild(menuEl);
    this.menuEl = menuEl;
  }

  public hide(): void {
    if (this.menuEl && this.menuEl.parentNode) {
      this.menuEl.parentNode.removeChild(this.menuEl);
    }
    this.menuEl = null;
    this.currentItems = [];
    this.focusedIndex = -1;
  }

  private handleKeydown(e: KeyboardEvent): void {
    if (!this.menuEl) return;

    if (e.key === 'Escape') {
      e.preventDefault();
      this.hide();
      return;
    }
    if (e.key === 'ArrowDown') {
      e.preventDefault();
      this.moveFocus(1);
      return;
    }
    if (e.key === 'ArrowUp') {
      e.preventDefault();
      this.moveFocus(-1);
      return;
    }
    if (e.key === 'Enter') {
      e.preventDefault();
      const item = this.currentItems[this.focusedIndex];
      if (item) {
        item.action();
        this.hide();
      }
    }
  }

  private moveFocus(delta: number): void {
    if (!this.menuEl || this.currentItems.length === 0) return;
    const rows = Array.from(this.menuEl.querySelectorAll('.context-menu-item-row'));
    rows[this.focusedIndex]?.classList.remove('focused');
    this.focusedIndex = (this.focusedIndex + delta + this.currentItems.length) % this.currentItems.length;
    rows[this.focusedIndex]?.classList.add('focused');
  }
}

import {
  createDockview,
  DockviewApi,
  DockviewGroupPanel,
  IDockviewPanel,
  IContentRenderer,
  IHeaderActionsRenderer,
  IGroupHeaderProps,
  GroupPanelPartInitParameters,
  Direction,
  DockviewPanelRenderer,
} from 'dockview-core';

export interface EditorOpenOptions {
  renderer?: DockviewPanelRenderer;
  isUserCreatedEmptyTab?: boolean;
  /**
   * Opaque metadata the caller attaches to a panel (FR-I1, D-4, NFR-1).
   * The shell stores and forwards this bag without inspecting its keys or
   * values, so registering a new resource kind never requires a core change.
   */
  meta?: Record<string, unknown>;
}

export interface PanePlacement {
  groupId: string;
  targets: (string | null)[];
}

/**
 * The entire surface the shell exposes to app code (presets, registry
 * helpers) that opens or inspects panes (FR-I4, FR-I5, FR-I8, C-9). It
 * deliberately excludes every method that creates, closes, or moves a tab
 * or pane (addNewTab, closePanel, splitGroup, ...) ??those stay shell-only.
 * `EditorController` satisfies this structurally; no wrapper is needed.
 */
export interface AppEditorSurface {
  openItem(
    targetId: string,
    title?: string,
    options?: EditorOpenOptions,
    targetGroup?: DockviewGroupPanel
  ): IDockviewPanel;
  openBeside(targetId: string, title?: string, options?: EditorOpenOptions): IDockviewPanel;
  queryPanePlacement(panelId?: string): { own: PanePlacement; beside: PanePlacement | null } | undefined;
}

export interface PanelContentState {
  targetId: string | null;
  isUserCreatedEmptyTab: boolean;
  creationCount: number;
  disposalCount: number;
  instanceStamp: string;
  isDirty: boolean;
}

/**
 * Custom content renderer for editor panels.
 * Tracks lifecycle creation and disposal counts (FR-E1 ~ FR-E5).
 */
export class EditorContentRenderer implements IContentRenderer {
  public readonly element: HTMLElement;
  public creationCount = 0;
  public disposalCount = 0;
  public instanceStamp: string;
  public params: any = {};
  public isShown = false;
  private timer: any = null;
  private timerTicks = 0;
  public domStateValue = '';

  constructor() {
    this.creationCount++;
    this.instanceStamp = 'stamp-' + Math.random().toString(36).slice(2, 10);
    this.element = document.createElement('div');
    this.element.className = 'editor-panel-content';

    // Live continuous state tracking (FR-E1 ~ FR-E5, D-24)
    this.timer = setInterval(() => {
      this.timerTicks++;
    }, 50);
  }

  public getTimerTicks(): number {
    return this.timerTicks;
  }

  public init(parameters: GroupPanelPartInitParameters): void {
    this.params = parameters.params || {};
    this.updateContent();
  }

  public onShow(): void {
    this.isShown = true;
  }

  public onHide(): void {
    this.isShown = false;
  }

  public updateContent(): void {
    this.element.setAttribute('data-instance-stamp', this.instanceStamp);
    if (this.params.targetId) {
      this.element.setAttribute('data-target-id', this.params.targetId);
      this.element.textContent = `Content of ${this.params.targetId}`;
      if (this.domStateValue) {
        const stateEl = document.createElement('span');
        stateEl.className = 'dom-state-tag';
        stateEl.textContent = this.domStateValue;
        this.element.appendChild(stateEl);
      }
    } else {
      // Empty tab: strictly 0 child elements and 0 text (FR-C2)
      this.element.innerHTML = '';
      this.element.removeAttribute('data-target-id');
    }
  }

  public setDomState(val: string): void {
    this.domStateValue = val;
    this.updateContent();
  }

  public update(event: any): void {
    if (event.params) {
      this.params = { ...this.params, ...event.params };
      this.updateContent();
    }
  }

  public dispose(): void {
    if (this.timer) {
      clearInterval(this.timer);
      this.timer = null;
    }
    this.disposalCount++;
    this.element.remove();
  }
}

export type EditorComponentFactory = (options: { id: string; name: string }) => IContentRenderer;

/**
 * Group header action buttons for each tab bar (FR-C1, FR-D1, FR-D2).
 * Renders in exact required order (D-13, D-15, FR-C1):
 * 1. 'Split Right' button (Split Horizontal)
 * 2. 'Split Down' button (Split Vertical)
 * 3. '+' button (New Tab) - located at the RIGHT END!
 */
export class EditorHeaderActionsRenderer implements IHeaderActionsRenderer {
  public readonly element: HTMLElement;
  private group: DockviewGroupPanel | null = null;

  constructor(private editorController: EditorController) {
    this.element = document.createElement('div');
    this.element.className = 'editor-group-header-actions';
  }

  public init(params: IGroupHeaderProps): void {
    this.group = params.group as DockviewGroupPanel;
    this.render();
  }

  private render(): void {
    this.element.innerHTML = `
      <button class="editor-action-btn tab-action-split-right" title="Split Right (Ctrl+\\)" aria-label="Split Right">
        <i class="codicon codicon-split-horizontal"></i>
      </button>
      <button class="editor-action-btn tab-action-split-down" title="Split Down" aria-label="Split Down">
        <i class="codicon codicon-split-vertical"></i>
      </button>
      <button class="editor-action-btn tab-action-new" title="New Tab" aria-label="New Tab">
        <i class="codicon codicon-plus"></i>
      </button>
    `;

    const splitRightBtn = this.element.querySelector('.tab-action-split-right') as HTMLButtonElement | null;
    const splitDownBtn = this.element.querySelector('.tab-action-split-down') as HTMLButtonElement | null;
    const newBtn = this.element.querySelector('.tab-action-new') as HTMLButtonElement | null;

    splitRightBtn?.addEventListener('click', (e) => {
      e.stopPropagation();
      if (this.group) {
        this.editorController.splitGroup(this.group, 'right');
      }
    });

    splitDownBtn?.addEventListener('click', (e) => {
      e.stopPropagation();
      if (this.group) {
        this.editorController.splitGroup(this.group, 'below');
      }
    });

    newBtn?.addEventListener('click', (e) => {
      e.stopPropagation();
      if (this.group) {
        this.editorController.addNewTab(this.group);
      }
    });
  }

  public dispose(): void {
    this.element.remove();
  }
}

/**
 * Controller for editor area, managing dockview-core layout, tabs, panes, and lifespan.
 */
export class EditorController {
  private api: DockviewApi;
  private panelCounter = 0;
  private contentRenderers = new Map<string, IContentRenderer>();
  private activePanelChangeListeners: ((panel: IDockviewPanel | undefined) => void)[] = [];
  private layoutChangeListeners: (() => void)[] = [];
  private customComponentFactory: EditorComponentFactory | null = null;
  public readonly panelLifecycleStats = new Map<string, {
    creationCount: number;
    disposalCount: number;
    instanceStamps: string[];
  }>();

  constructor(private container: HTMLElement) {
    this.api = createDockview(this.container, {
      theme: {
        name: 'custom',
        className: 'workbench-dockview-theme',
      },
      disableFloatingGroups: true,
      noPanelsOverlay: 'emptyGroup',
      createComponent: (options) => {
        let stats = this.panelLifecycleStats.get(options.id);
        if (!stats) {
          stats = { creationCount: 0, disposalCount: 0, instanceStamps: [] };
          if (this.panelLifecycleStats.size >= 100) {
            const oldestKey = this.panelLifecycleStats.keys().next().value;
            if (oldestKey) {
              this.panelLifecycleStats.delete(oldestKey);
            }
          }
          this.panelLifecycleStats.set(options.id, stats);
        }
        stats.creationCount++;

        let renderer: IContentRenderer;
        if (this.customComponentFactory) {
          renderer = this.customComponentFactory(options);
          if (typeof (renderer as any).init !== 'function') {
            (renderer as any).init = () => {};
          }
          if (typeof (renderer as any).update !== 'function') {
            (renderer as any).update = () => {};
          }
        } else {
          renderer = new EditorContentRenderer();
        }

        const stamp = (renderer as any).instanceStamp || ('stamp-' + Math.random().toString(36).slice(2, 10));
        stats.instanceStamps.push(stamp);
        this.contentRenderers.set(options.id, renderer);

        const originalDispose = renderer.dispose ? renderer.dispose.bind(renderer) : null;
        renderer.dispose = () => {
          stats!.disposalCount++;
          try {
            if (originalDispose) {
              originalDispose();
            }
          } finally {
            this.contentRenderers.delete(options.id);
          }
        };

        return renderer;
      },
      createRightHeaderActionComponent: () => {
        return new EditorHeaderActionsRenderer(this);
      },
    });

    // Ensure initial state has strictly 1 group and 0 panels (FR-K2, D-20, D-26)
    if (this.api.size === 0) {
      this.api.addGroup();
    }

    this.api.onDidActivePanelChange((event) => {
      this.activePanelChangeListeners.forEach((cb) => cb(event.panel));
    });

    this.api.onDidLayoutChange(() => {
      this.layoutChangeListeners.forEach((cb) => cb());
    });
  }

  public setComponentFactory(factory: EditorComponentFactory | null): void {
    this.customComponentFactory = factory;
  }

  public getLifecycleStats(panelId: string) {
    return this.panelLifecycleStats.get(panelId) || {
      creationCount: 0,
      disposalCount: 0,
      instanceStamps: [],
    };
  }

  public clearLifecycleStats(): void {
    this.panelLifecycleStats.clear();
  }

  public getApi(): DockviewApi {
    return this.api;
  }

  /**
   * Clears all panels and groups, ensuring exactly 1 empty group remains (FR-K2, D-20, D-26).
   */
  public clear(): void {
    this.api.clear();
    this.contentRenderers.clear();
    this.panelLifecycleStats.clear();
    if (this.api.groups.length === 0) {
      this.api.addGroup();
    }
    const rect = this.container.getBoundingClientRect();
    if (rect.width > 0 && rect.height > 0) {
      this.api.layout(rect.width, rect.height);
    }
  }

  public getGroups(): DockviewGroupPanel[] {
    return this.api.groups;
  }

  public getGroupCount(): number {
    return this.api.size;
  }

  public getPanels(): IDockviewPanel[] {
    return this.api.panels;
  }

  public getPanelCount(): number {
    return this.api.totalPanels;
  }

  public getActiveGroup(): DockviewGroupPanel | undefined {
    return this.api.activeGroup || this.api.groups[0];
  }

  public getActivePanel(): IDockviewPanel | undefined {
    return this.api.activePanel;
  }

  public getContentRenderer(panelId: string): IContentRenderer | undefined {
    return this.contentRenderers.get(panelId);
  }

  /**
   * Adds a new blank tab to the specified group (or active group) (FR-C1).
   * Tab is appended at the right end and activated.
   */
  public addNewTab(targetGroup?: DockviewGroupPanel): IDockviewPanel {
    const group = targetGroup || this.getActiveGroup() || this.api.groups[0];
    const id = `tab-${++this.panelCounter}`;

    const panel = this.api.addPanel({
      id,
      component: 'editor-panel',
      title: 'Untitled',
      position: {
        referenceGroup: group,
        direction: 'within',
      },
      params: {
        isUserCreatedEmptyTab: true,
        targetId: null,
      },
    });

    panel.api.setActive();
    return panel;
  }

  public setActiveGroup(group: DockviewGroupPanel): void {
    if ((this.api as any).component?.doSetGroupAndPanelActive) {
      (this.api as any).component.doSetGroupAndPanelActive(group);
    }
  }

  /**
   * Opens an item in accordance with D-5 and FR-B1 ~ FR-B4.
   * - If active tab is user-created empty tab: opens in that tab (FR-B3).
   * - If item already open elsewhere: relocates to existing tab without creating new tab (FR-B2).
   * - Otherwise: replaces content of active tab without increasing tab count (FR-B1).
   * - If active group is empty pane: opens as first tab in that pane (FR-J8).
   */
  public openItem(
    targetId: string,
    title?: string,
    options?: EditorOpenOptions,
    targetGroup?: DockviewGroupPanel
  ): IDockviewPanel {
    const displayTitle = title || targetId.split(/[\/\\]/).pop() || targetId;
    const requestedRenderer = options?.renderer || 'onlyWhenVisible';
    const meta = options?.meta || {};
    const group = targetGroup || this.getActiveGroup() || this.api.groups[0];
    const activePanel = group?.activePanel;

    // Rule FR-B3: If active tab in target group is a user-created empty tab, open in it even if duplicate
    if (activePanel && activePanel.params?.isUserCreatedEmptyTab && !activePanel.params?.targetId) {
      activePanel.setTitle(displayTitle);
      activePanel.api.setRenderer(requestedRenderer);
      activePanel.update({
        params: {
          ...meta,
          targetId,
          isUserCreatedEmptyTab: false,
        },
      });
      activePanel.api.setActive();
      return activePanel;
    }

    // Rule FR-B2: Target is already open in any panel across workbench -> jump to it (D-5)
    const existingPanel = this.api.panels.find((p) => p.params?.targetId === targetId);
    if (existingPanel) {
      existingPanel.api.setActive();
      return existingPanel;
    }

    // Rule FR-B1: Target not open anywhere; update active tab in place without increasing tab count
    if (activePanel) {
      activePanel.setTitle(displayTitle);
      activePanel.api.setRenderer(requestedRenderer);
      activePanel.update({
        params: {
          ...meta,
          targetId,
          isUserCreatedEmptyTab: false,
        },
      });
      activePanel.api.setActive();
      return activePanel;
    }

    // Rule FR-J8: If group has 0 panels (empty pane), open first tab in it
    const id = `tab-${++this.panelCounter}`;
    const panel = this.api.addPanel({
      id,
      component: 'editor-panel',
      title: displayTitle,
      renderer: requestedRenderer,
      position: {
        referenceGroup: group,
        direction: 'within',
      },
      params: {
        ...meta,
        targetId,
        isUserCreatedEmptyTab: false,
      },
    });

    panel.api.setActive();
    return panel;
  }

  /**
   * Reports the caller's own group and the targets currently open in the
   * spatially adjacent group (FR-I5, D-19). The shell only reports pane
   * placement; it never performs the copy/move itself (INTENT 3).
   */
  public queryPanePlacement(panelId?: string): { own: PanePlacement; beside: PanePlacement | null } | undefined {
    const panel = panelId ? this.api.getPanel(panelId) : this.getActivePanel();
    const group = panel?.group || this.getActiveGroup();
    if (!group) return undefined;

    const own: PanePlacement = {
      groupId: group.id,
      targets: group.panels.map((p) => (p.params?.targetId as string | undefined) ?? null),
    };

    const besideGroup = this.findBesideGroup(group);
    const beside: PanePlacement | null = besideGroup
      ? {
          groupId: besideGroup.id,
          targets: besideGroup.panels.map((p) => (p.params?.targetId as string | undefined) ?? null),
        }
      : null;

    return { own, beside };
  }

  /**
   * Spatially locates the most appropriate beside group (to the right, or left if right is absent).
   * In 2D layouts, prioritizes groups that share vertical coordinate alignment (FR-A14, D-19).
   */
  public findBesideGroup(group: DockviewGroupPanel): DockviewGroupPanel | undefined {
    const from = group.element.getBoundingClientRect();
    const candidates = this.api.groups.filter(
      (g) => g !== group && g.api.location.type === 'grid'
    );

    // 1. Look for candidate groups strictly to the right
    const rightCandidates = candidates.filter((g) => {
      const rect = g.element.getBoundingClientRect();
      const dx = rect.left + rect.width / 2 - (from.left + from.width / 2);
      const dy = rect.top + rect.height / 2 - (from.top + from.height / 2);
      return dx > 0 && Math.abs(dx) >= Math.abs(dy) - 10;
    });

    if (rightCandidates.length > 0) {
      rightCandidates.sort((a, b) => {
        const ra = a.element.getBoundingClientRect();
        const rb = b.element.getBoundingClientRect();
        const dxa = ra.left - from.left;
        const dxb = rb.left - from.left;
        if (Math.abs(dxa - dxb) > 20) {
          return dxa - dxb;
        }
        return Math.abs(ra.top - from.top) - Math.abs(rb.top - from.top);
      });
      return rightCandidates[0];
    }

    const dvRight = this.api.adjacentGroupInDirection(group, 'right');
    if (dvRight) {
      return dvRight as DockviewGroupPanel;
    }

    // 2. Fallback to left neighbor
    const leftCandidates = candidates.filter((g) => {
      const rect = g.element.getBoundingClientRect();
      const dx = rect.left + rect.width / 2 - (from.left + from.width / 2);
      const dy = rect.top + rect.height / 2 - (from.top + from.height / 2);
      return dx < 0 && Math.abs(dx) >= Math.abs(dy) - 10;
    });

    if (leftCandidates.length > 0) {
      leftCandidates.sort((a, b) => {
        const ra = a.element.getBoundingClientRect();
        const rb = b.element.getBoundingClientRect();
        const dxa = Math.abs(ra.left - from.left);
        const dxb = Math.abs(rb.left - from.left);
        if (Math.abs(dxa - dxb) > 20) {
          return dxa - dxb;
        }
        return Math.abs(ra.top - from.top) - Math.abs(rb.top - from.top);
      });
      return leftCandidates[0];
    }

    const dvLeft = this.api.adjacentGroupInDirection(group, 'left');
    if (dvLeft) {
      return dvLeft as DockviewGroupPanel;
    }

    return undefined;
  }

  /**
   * Opens an item in the beside pane (FR-A14, Ctrl+Enter, FR-I4).
   * - First checks workbench-wide duplicate rule (FR-B2, D-5).
   * - Spatially locates adjacent group (FR-A14, D-19, FR-D6).
   * - If no horizontal neighbor exists: splits right and opens in the new pane.
   * - If adjacent group exists: opens in that beside group.
   */
  public openBeside(
    targetId: string,
    title?: string,
    options?: EditorOpenOptions
  ): IDockviewPanel {
    // Rule FR-B2: If target is already open anywhere across workbench, jump to it (FR-B2, D-5)
    const existingPanel = this.api.panels.find((p) => p.params?.targetId === targetId);
    if (existingPanel) {
      existingPanel.api.setActive();
      return existingPanel;
    }

    const activeGroup = this.getActiveGroup() || this.api.groups[0];

    // Spatially locate adjacent beside group (FR-A14, D-19)
    const besideGroup = this.findBesideGroup(activeGroup);

    if (!besideGroup) {
      // No horizontal neighbor exists: split right
      const beside = this.splitGroup(activeGroup, 'right');
      return this.openItem(targetId, title, options, beside);
    }

    // Spatial beside group exists: open in it
    this.setActiveGroup(besideGroup);
    return this.openItem(targetId, title, options, besideGroup);
  }

  /**
   * Splits a group in the specified direction (FR-D1, FR-D2).
   */
  public splitGroup(group: DockviewGroupPanel, direction: 'right' | 'below'): DockviewGroupPanel {
    const dir: Direction = direction === 'right' ? 'right' : 'below';
    return this.api.addGroup({
      referenceGroup: group,
      direction: dir,
    });
  }

  /**
   * Splits active group in the specified direction (FR-D3).
   */
  public splitActiveGroup(direction: 'right' | 'below'): DockviewGroupPanel | undefined {
    const group = this.getActiveGroup();
    if (!group) return undefined;
    return this.splitGroup(group, direction);
  }

  /**
   * Closes the active tab in the active group (FR-N6b, Ctrl+W, FR-C3).
   * When the last tab of a group closes:
   * - If other groups exist, that group disappears (FR-C4, FR-J1).
   * - If it is the last group, it remains as an empty pane (FR-C4, FR-J7).
   */
  public closeActiveTab(): void {
    const activeGroup = this.getActiveGroup();
    const activePanel = activeGroup?.activePanel;
    if (activePanel) {
      this.closePanel(activePanel);
    }
  }

  /**
   * Closes a specific panel and disposes its view (FR-E3).
   * Preserves empty group if it is the last panel in the workbench (FR-C4, FR-J7).
   */
  public closePanel(panel: IDockviewPanel): void {
    const isLastPanelInWorkbench = this.api.totalPanels === 1 && this.api.groups.length === 1;
    if (isLastPanelInWorkbench) {
      (this.api as any).component.removePanel(panel, { removeEmptyGroup: false });
    } else {
      (this.api as any).component.removePanel(panel, { removeEmptyGroup: true });
    }
    if (this.api.groups.length === 0) {
      this.api.addGroup();
    }
  }

  /**
   * Closes all tabs in the specified group (or active group) (FR-J2).
   * If other groups exist, that group disappears (FR-J1).
   * If it is the last group, it remains as an empty pane (FR-J2, FR-J7).
   * If already an empty pane, changes nothing (FR-J4).
   */
  public closeAllTabsInGroup(targetGroup?: DockviewGroupPanel): void {
    const group = targetGroup || this.getActiveGroup();
    if (!group) return;

    const panels = [...group.panels];
    if (panels.length === 0) {
      // Empty group: already empty, nothing changes (FR-J4)
      return;
    }

    const isLastGroupInWorkbench = this.api.groups.length === 1;
    for (let i = 0; i < panels.length; i++) {
      const panel = panels[i];
      const isLastInThisOperation = isLastGroupInWorkbench && i === panels.length - 1;
      (this.api as any).component.removePanel(panel, {
        removeEmptyGroup: !isLastInThisOperation,
      });
    }
    if (this.api.groups.length === 0) {
      this.api.addGroup();
    }
  }

  /**
   * Sets tab dirty indicator (?? (FR-L1).
   */
  public setTabDirty(panelId: string, dirty: boolean): void {
    const panel = this.api.getPanel(panelId);
    if (!panel) return;

    const baseTitle = (panel.title || '').replace(/^??s*/, '');
    const newTitle = dirty ? `??${baseTitle}` : baseTitle;
    panel.setTitle(newTitle);
    panel.update({ params: { isDirty: dirty } });
  }

  /**
   * Subscribes to active panel changes.
   */
  public onActivePanelChange(callback: (panel: IDockviewPanel | undefined) => void): () => void {
    this.activePanelChangeListeners.push(callback);
    return () => {
      this.activePanelChangeListeners = this.activePanelChangeListeners.filter((cb) => cb !== callback);
    };
  }

  /**
   * Subscribes to layout mutations and changes.
   */
  public onLayoutChange(callback: () => void): () => void {
    this.layoutChangeListeners.push(callback);
    return () => {
      this.layoutChangeListeners = this.layoutChangeListeners.filter((cb) => cb !== callback);
    };
  }
}


codex
현재 검토 범위와 공격 기준을 확인했습니다. 특히 정적 검증 스크립트가 금지 문자열만 탐지하고 실제 권한 경계를 보장하지 못하는지, 런타임 테스트가 동일 객체의 가변 참조를 통해 우회될 수 있는지를 집중적으로 추적하고 있습니다.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk cmd /c type docs\\current\\PLAN.md & rtk cmd /c type docs\\current\\SPEC.md & rtk cmd /c type docs\\current\\DECISIONS.md & rtk cmd /c type docs\\current\\INTENT.md" in D:\projects\workbench-kit
 exited 1 in 134ms:
At line:2 char:38
+ rtk cmd /c type docs\current\PLAN.md & rtk cmd /c type docs\current\S ...
+                                      ~
The ampersand (&) character is not allowed. The & operator is reserved for future use; wrap an ampersand in double quot
ation marks ("&") to pass it as part of a string.
At line:2 char:77
+ ... docs\current\PLAN.md & rtk cmd /c type docs\current\SPEC.md & rtk cmd ...
+                                                                 ~
The ampersand (&) character is not allowed. The & operator is reserved for future use; wrap an ampersand in double quot
ation marks ("&") to pass it as part of a string.
At line:2 char:121
+ ... current\SPEC.md & rtk cmd /c type docs\current\DECISIONS.md & rtk cmd ...
+                                                                 ~
The ampersand (&) character is not allowed. The & operator is reserved for future use; wrap an ampersand in double quot
ation marks ("&") to pass it as part of a string.
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : AmpersandNotAllowed
 

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk cmd /c type docs\\current\\PLAN.md; rtk cmd /c type docs\\current\\SPEC.md; rtk cmd /c type docs\\current\\DECISIONS.md; rtk cmd /c type docs\\current\\INTENT.md" in D:\projects\workbench-kit
 succeeded in 414ms:
> 버전: v0.1 · 작성일: 2026-09-08
>
> Phase 인덱스다. 상세 설계·task 분해·코드는 담지 않는다(각각 `DECISIONS.md`·
> `backlog.json`·구현). 요구 ID는 `SPEC.md`를 가리킨다.

# PLAN — workbench-kit

## 이 순서에 걸린 두 가지 규칙

**두 갈래는 갈라 붙이지 않는다(D-6).** Electron을 먼저 만들고 pywebview를 나중에
붙이는 Phase는 없다. Phase 1이 두 갈래를 동시에 세우고, Phase 2 ~ 6은 갈래를 모르는
웹 코드만 늘린다. 산출물이 한 벌이므로(D-25) 갈래 차이가 들어올 자리가 구조적으로
없다. 갈래마다 다른 코드가 실제로 있는 곳은 Phase 1(빌드·창 띄우기)과 Phase 2의
프레임리스 창 조작뿐이다.

**모든 Phase의 완료 조건은 두 갈래에서 각각 판정한다**(`SPEC.md` 1절 공통 판정 규칙).
Phase마다 반복해 적지 않고, Phase 7이 `FR-H1`으로 133건 전건을 대조한다.

**반입은 그 기능 Phase 안에서 한다.** "라이브러리 붙이기" Phase를 따로 두지 않는다 —
반입이 곧 그 Phase의 구현이기 때문이다(D-2). codicons·seti·vscode-icons는 Phase 2,
`dockview-core`는 Phase 4, monaco는 Phase 6이다. 라이선스 본문은 반입한 Phase에서
함께 넣고, Phase 7이 반입 라이브러리 다섯을 한 번에 센다.

---

## Phase 1 — 두 갈래 뼈대와 한 벌 빌드

**목적** — Vite 산출물 한 벌을 Electron과 pywebview가 똑같이 읽는 상태를 먼저 세운다.

**대응 요구 ID** — `FR-H2` · `NFR-2`

**완료 조건**
- 두 갈래가 각각 창을 띄우고, 그 창이 같은 산출물을 읽는다.
- 갈래별로 갈라진 빌드 설정이 0개다.
- 두 갈래가 읽는 산출물의 파일 목록과 각 파일의 해시가 같다.

**실행 환경 최소 버전 (C-11)**
- Node: v22.22.3
- Python: 3.11.8
- Electron: v44.2.0
- pywebview: 6.2.1

---

## Phase 2 — 공통 UI 껍데기: 다섯 조각 · 명령 체계 · 시각 체계

**목적** — 프레임리스 창의 다섯 조각을 세우고, 메뉴·세로 띠·접기·Zen과 세 색 테마·
아이콘 반입까지 껍데기의 겉을 한 번에 완성한다.

**대응 요구 ID** — `FR-F1` ~ `FR-F7` · `FR-M1` ~ `FR-M5` · `FR-N1` ~ `FR-N5` ·
`FR-N6` ~ `FR-N9` · `FR-N11` · `FR-Q1` · `FR-Q1a` · `FR-Q2` · `FR-Q3` · `NFR-6`

**완료 조건**
- 상단 바에 햄버거·제목·창 버튼 셋이 있고, 창 끌기·크기 조절·최소화·최대화·닫기가
  동작한다. 하단 바에 경로·메시지 자리·앱 정보 세 자리가 있다.
- 메뉴 세 묶음에서 **껍데기가 넣은 항목**의 목록과 순서가 `SPEC.md` `FR-N6` ~ `FR-N8`과
  정확히 같고(File 다섯 · View 열 · Help 하나), "우클릭 메뉴 사용"의 초기 상태가
  꺼짐이다. 세로 띠 아이콘의 목록과 순서가 `FR-N11`과 같고,
  칸 삭제·칸 합치기 아이콘이 0개다.
- 탐색기 접기·상단 바 감추기·하단 바 감추기가 서로 영향 없이 동작하고, Zen이 `F11`로
  켜지고 `Escape`로 풀리며 창 크기가 변하지 않는다.
- 색 테마가 흰색 → 회색 → 검정으로 순환하고, 색 토큰이 VS Code Light Modern /
  Dark Modern의 대응 값과 다른 것이 0건이다. 회색 배경의 밝기가 두 값의 산술 중간이다.
- `tab-explorer-templates`의 글꼴·간격·색 토큰 값이 산출물에서 검색되는 건수가 0이다.
- 아이콘 테마가 seti·vscode-icons 둘이고 View 메뉴로 바뀐다. 색 테마 3 × 아이콘
  테마 2 여섯 조합 각각에서 깨지는 화면이 0건이다.
- 공통 코어에 확장자→아이콘 대응표가 0건이다.

**메모** — View 메뉴에 들어가는 "좌우 스플릿"·"활성 칸 탭 모두 닫기"는 이 Phase에서
**항목이 목록에 있다**까지만 판정한다. 그 동작은 Phase 4(`FR-D3` · `FR-J2`)에 있다.
File의 "폴더 열기"·"폴더 닫기"도 같다 — 항목은 여기, 동작은 Phase 3(`FR-A1` ·
`FR-N6c`). "탭 닫기"는 동작이 Phase 4(`FR-N6b`)다. 앱이 항목을 더하는 표면 자체는
Phase 5(`FR-I9` ~ `FR-I11`)이고, 여기서는 껍데기 항목만 센다.

---

## Phase 3 — 탐색기 트리

**목적** — VS Code와 같은 외형·키보드 조작을 가진 단일 루트 트리를 만든다.

**대응 요구 ID** — `FR-A1` ~ `FR-A5` · `FR-A7` ~ `FR-A13` · `FR-A15` ~ `FR-A24` ·
`FR-G1` · `FR-N6c`

**완료 조건**
- 트리 조작 15건(`FR-A2` ~ `A5`, `A7` ~ `A13`, `A16` ~ `A19`)이 각 항목에 적힌 대로
  나온다. 여기에 `FR-A11`의 `Ctrl+클릭`·`Shift+클릭` 다중 선택이 포함된다.
- 폴더 열기로 루트가 바뀌고 이전 루트를 가리키는 노드가 0개다. 트리 안에서 루트를
  바꾸는 수단이 0개다.
- 폴더를 열지 않은 상태에서 트리 자리에 자식 요소와 텍스트가 0개이고, 클릭해도
  상태가 바뀌지 않는다. File의 "폴더 닫기"가 트리를 이 상태로 되돌리고, 그때
  칸 수와 탭 수가 변하지 않는다.
- 뷰 제목줄이 있고 껍데기 액션이 정확히 둘(모두 접기 · 새로 고침)이다. 모두 접기가
  `Ctrl+←`와 같은 결과를 내고, 새로 고침이 펼침 상태와 선택을 유지한 채 트리를
  다시 읽는다. 껍데기가 무엇을 읽는지 아는 코드가 0건이다.
- 새 파일·새 폴더의 **인라인 이름 입력 장치**가 동작한다 — `Enter`로 앱에 이름을
  넘긴 기록이 1건이고 껍데기 쪽 생성 동작이 0회, `Escape`로 입력 행이 사라지고
  앱 호출이 0건이다. 앱이 꽂지 않으면 그 아이콘이 0개다.

**완료 상태** — 2026-09-08 Phase 3 작업(`WK-014` ~ `WK-018`, `WK-046` ~ `WK-047`) 및 적대적 검증(A3) 완료. 지적 7건 전건 해결(0 Critical / 0 Major / 0 Minor), dist 해시 패리티 0 차이 확인.

---

## Phase 4 — 칸 · 탭 · 보기 수명과 열기 규칙

**목적** — `dockview-core`를 반입해 칸·탭·수명을 세우고, 트리에서 고른 것이 어디에
열리는지를 정한다.

**대응 요구 ID** — `FR-A6` · `FR-A14` · `FR-B1` ~ `FR-B4` · `FR-C1` ~ `FR-C9` ·
`FR-D1` ~ `FR-D8` · `FR-E1` ~ `FR-E5` · `FR-J1` ~ `FR-J8` · `FR-N6b`

**완료 조건**
- 위 37건이 각 항목에 적힌 대로 나온다. File의 "탭 닫기"가 `Ctrl+W`와 같은 결과를
  낸다(`FR-N6b`).
- 탭을 갔다 돌아오거나 다른 칸으로 옮겨도 보기 인스턴스의 생성 횟수가 1 · 폐기가
  0이고, 탭을 닫는 순간 폐기가 1이 된다. (D-24가 8.2.0으로 한 확인 시험을 이
  Phase에서 재현한다.)
- 탐색기에서 고를 때 탭 개수가 늘어나는 경우가 0건이다.
- 마지막 칸의 마지막 탭을 닫아도 칸 수가 1 · 탭 수 0으로 남고, 편집기 영역이
  사라지지 않는다.
- "칸 닫기/삭제" 항목이 세로 띠·메뉴·우클릭 세 자리 모두에서 0개다.

**완료 상태** — 2026-09-09 Phase 4 작업(`WK-019` ~ `WK-024`, `WK-043`) 및 적대적 검증(A4) 완료. 79개 단언 전건 통과, Electron·pywebview 간 divergence 0개, 적대적 검증 0 Critical / 1 Major 기록 완료 / 0 Minor 확인.

---

## Phase 5 — 갈아끼우는 자리와 앱 표면

**목적** — 앱이 공통 코어를 고치지 않고 꽂힐 자리와, 껍데기가 앱에게 여는 표면을
정한다.

**대응 요구 ID** — `FR-G5` · `FR-G6` · `FR-I1` ~ `FR-I11` · `FR-N10` · `FR-N12` ·
`FR-R1` · `FR-R1a` · `NFR-1` · `NFR-7`

**완료 조건**
- 시험이 등록한 임의의 리소스 종류 하나가 **공통 코어 diff 0줄**로 탭에 열린다.
  파일 프리셋·폴더 프리셋도 각각 diff 0줄로 성립한다.
- 공통 코어에서 파일·폴더·터미널·확장자를 아는 코드가 0건이다.
- 껍데기가 먼저 가져가는 키의 목록 문서가 1개 있고, 그 목록에 없는 키는 탭 안의
  보기가 받는다. 트리 포커스에서 트리 키를 눌렀을 때 앱 쪽 수신 기록이 0건이다.
  같은 문서에 `Ctrl+S` · `F2` · `Delete` · `Ctrl+Z`/`Ctrl+Y` · `Ctrl+C`/`Ctrl+V`가
  **가져가지 않는다**로 적혀 있고, 탭 포커스에서 그 여섯의 껍데기 쪽 처리 기록이 0건이다.
- 앱에 노출된 표면에 탭·칸을 만들거나 닫거나 옮기는 수단이 0개다. "옆 칸에 열기"와
  "칸 배치 묻기"만 열려 있다.
- 앱이 하단 바 항목과 세로 띠 항목을 diff 0줄로 바꾼다. 바꾼 뒤에도 View 메뉴의
  열 항목이 그대로다.
- 앱이 File 묶음과 탐색기 뷰 제목줄에 diff 0줄로 항목을 더한다. 더한 뒤에도 File의
  껍데기 항목 다섯과 뷰 제목줄의 껍데기 액션 둘이 목록·순서 그대로이고, 앱 항목은
  전부 가름선 아래(뷰 제목줄은 껍데기 액션 왼쪽)에 있다. 앱이 껍데기 항목을 빼거나
  순서를 바꾸는 수단이 0개이고, View·Help를 건드리는 수단도 0개다.
- 우클릭 메뉴가 기본 꺼짐이고, 앱이 항목을 정의하고 켰을 때 껍데기가 더한 항목이
  0개다.
- 메뉴·세로 띠·탭 줄의 모든 명령에 마우스 없이 실행하는 경로가 1개 이상 있다.

---

## Phase 6 — 편집 · 저장 확인 · 알림 · 다시 열기

**목적** — monaco를 반입해 텍스트·코드 보기를 세우고, 저장 확인·하단 바 알림·
다시 열 때의 상태까지 남은 사용자 동작을 끝낸다.

**대응 요구 ID** — `FR-G2` ~ `FR-G4` · `FR-K1` ~ `FR-K3` · `FR-L1` ~ `FR-L7` ·
`FR-N6a` · `FR-N10a` · `FR-N10b` · `FR-P1` ~ `FR-P7`

**완료 조건**
- 켜기로 한 다섯(문법 색칠·줄 번호·찾기바꾸기·되돌리기/다시하기·읽기 전용)이
  동작하고, 끄기로 한 넷(자동완성·언어 서비스·미니맵·다중 커서)이 각각을 부르는
  조작에도 나타나지 않는다. 앱 쪽 의존성에 monaco가 0건이다.
- 고친 탭에 ● 표시가 붙고, 닫으려 하면 [저장]·[저장 안 함]·[취소] 세 버튼의 확인
  창이 뜬다. 세 버튼이 각각 정해진 결과를 낸다. 앱을 끄려 할 때도 같은 창이 뜬다.
  안 고친 탭은 확인 창 0개로 닫힌다.
- 오류와 진행이 하단 바 한 줄로만 뜨고, 그때 새로 떠 있는 알림 상자가 0개다.
- 앱이 같은 자리에 오류·안내 한 줄과 진행을 올린다. 앱이 준 문구를 껍데기가 고친
  글자가 0개이고, 껍데기와 앱이 이어서 올리면 가장 최근 것 하나만 보이며, 진행은
  앱이 끄기 전에 껍데기가 임의로 끄는 경우가 0건이다.
- 다시 켜면 마지막에 연 루트 폴더만 살아나고, 칸 수 1 · 탭 수 0이며, 펼쳐진 노드가
  루트 하나뿐이다. 최근 폴더에서 고르면 폴더 열기와 같은 결과가 난다.

---

## Phase 7 — 라이선스 · 기준 문서 · 전건 대조

**목적** — 반입물의 라이선스와 기준 문서를 갖추고, 133건을 두 갈래에서 대조해 이번
버전을 닫는다.

**대응 요구 ID** — `FR-H1` · `FR-Q4` · `FR-Q5` · `FR-R2` · `FR-R3` · `NFR-3` ·
`NFR-4` · `NFR-5`

**완료 조건**
- dockview-core · monaco-editor · @vscode/codicons · seti-ui · vscode-icons 다섯의
  라이선스 본문이 한 폴더에 모두 있고, 어느 것이 어디에 쓰이는지 적은 목록이 1개
  있다. codicons·vscode-icons의 저작자 표시가 Help 메뉴의 정보에 있다. 복사한
  사람이 따로 챙길 것이 0건이다.
- 탐색기·탭·분할 영역·선택과 포커스·키보드/마우스 조작 다섯에 대해 VS Code와
  대조 가능한 기준 문서가 있고, 그 문서에 divergence로 기록된 항목이 정확히
  1개(Zen 모드)다.
- 검사 목록 문서에서 구현 세부 검사와 사용자 행동 검사가 갈려 있고, 모든 검사가
  둘 중 하나에 속한다.
- 칸·탭·보기 수명(dockview) · 편집 화면(monaco) · 아이콘(codicons·seti·vscode-icons)의
  자체 구현 코드가 0건이다.
- **`SPEC.md` 1절 133건을 Electron과 pywebview에서 각각 확인해, 결과가 다른 항목이
  0개다.** 사용자 행동 검사가 전건 통과한다.

---

## 적대적 검증

절차·프롬프트·기록 양식은 `project-workflow/docs/ADVERSARIAL-REVIEW.md`에 있다.
여기엔 이 프로젝트에서만 정해지는 두 가지만 적는다.

### 필수 통과 Phase

**Phase 1 · Phase 4 · Phase 5 · Phase 7.** 이 넷은 미해결 Critical이 남아 있으면
다음 Phase로 넘어가지 않는다. 나머지(Phase 2 · 3 · 6)도 검증은 하되 진행을 막지
않으며, 미해결 지적은 기록만 남기고 Phase 7의 전건 대조에서 다시 걸린다.

넷을 고른 이유는 각각 다르다 — Phase 1은 되돌리는 비용이 가장 크고(여기가 어긋나면
이후 전 Phase의 "두 갈래에서 각각 판정"이 성립하지 않는다), Phase 4는 수명 조건이
시험 통과와 실제 동작이 가장 쉽게 갈리는 자리이며, Phase 5는 이 프로젝트의 존재
이유인 "코어를 고치지 않고 꽂힌다"가 판정되는 곳이고, Phase 7은 버전 마감 게이트다.

### Phase별 공격 초점

| Phase | 검토자에게 무엇을 깨라고 하나 |
| --- | --- |
| 1 | 두 갈래가 같은 산출물을 읽는다는 주장을 깨라 — 해시 대조를 비껴가는 갈래별 분기, 런타임에 주입되는 코드, 빌드 밖에서 갈라지는 설정 |
| 2 | 목록·순서·초기 상태가 `SPEC`과 어긋나는 자리, 그리고 색 테마 3 × 아이콘 테마 2 여섯 조합과 접기·Zen이 겹칠 때 깨지는 화면 |
| 3 | 트리 조작의 경계 — 폴더를 열지 않은 빈 상태, 루트를 바꾼 직후, 다중 선택과 키보드 포커스가 엇갈리는 순간 |
| 4 | 생성 1 · 폐기 0이 실제 인스턴스가 아니라 stub·재생성으로 충족된 것은 아닌지, 탭 이동·마지막 탭 닫기·칸 경계에서 수명 카운트가 어긋나는 경로 |
| 5 | **공통 코어 diff 0줄** 주장을 깨라 — 코어가 파일·폴더·터미널·확장자를 아는 코드, 앱 표면으로 칸·탭을 만들거나 닫는 우회로, 메뉴·뷰 제목줄 확장으로 껍데기 항목을 빼거나 View·Help에 닿는 길, 키 가로채기 목록 밖으로 새는 키 |
| 6 | 고친 내용이 확인 창 없이 사라지는 경로, 끄기로 한 넷(자동완성·언어 서비스·미니맵·다중 커서)을 되살리는 조작, 다시 열 때 살아나면 안 되는 상태 |
| 7 | 전건 대조 자체를 깨라 — 두 갈래에서 결과가 같다는 판정이 실제 실행이 아니라 자기 채점으로 만들어진 항목, 검사 목록이 덮지 못하는 요구 ID |

리팩토링 Phase가 나중에 끼어들면 그 공격 초점은 항상 **동작 동등성**이다
(`ADVERSARIAL-REVIEW.md` 3절). 이번 버전의 Phase 1 ~ 7에는 리팩토링 Phase가 없다.
> 버전: v0.1 · 작성일: 2026-09-08
>
> 이번 버전이 만족해야 할 것을 담는다. 구현 방법·Phase 순서·설계 결정은 담지 않는다
> (그것은 `PLAN.md`·`DECISIONS.md`에 있다). 변하지 않는 의도는 `INTENT.md`에 있다.
>
> **1절의 뼈대는 `refs/scenarios.md`를 옮긴 것이다.** 항목과 결과를 바꾸지 않았다.
> 시나리오에 행이 없고 결정 본문에서 결과를 옮긴 항목은 `†`로 표시했다. 각 행의
> `출처`가 그 결과를 낳은 결정(D-n)이다.

# SPEC — workbench-kit

## 1. 기능 요구사항

모두 **133건**이다. `refs/scenarios.md`의 87개 항목이 FR 번호를 달고 그대로 옮겨
왔고(시나리오의 절 기호 A ~ M을 FR 번호에 살려 역참조가 끊기지 않게 했다), 여기에
시나리오에는 없지만 결정이 이미 결과를 정해 둔 46건을 `†`로 표시해 더했다.
그중 10건(FR-A20 ~ A24 · FR-N6b · FR-N6c · FR-I9 ~ I11)은 D-9와
`refs/vscode-reuse-plan.md`가 "둔다"고 한 탐색기 뷰 제목줄이 시나리오로 내려오지
않은 것을 D-30 · D-31로 메운 것이다. 다시 3건(FR-N10a · FR-N10b · FR-R1a)은
D-21과 D-19가 뜻만 밝히고 앱이 부를 자리를 정하지 않은 것을 D-32와 D-19 보강으로
메운 것이다.

**공통 판정 규칙** — 아래 모든 FR은 **Electron 갈래와 pywebview 갈래에서 각각**
판정하며, 두 갈래의 결과가 다른 항목이 하나도 없어야 한다(FR-H1). 판정 방법에
"두 갈래에서"를 매 행마다 반복해 적지 않는다.

### 1.1 탐색기 트리 (FR-A)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-A1 | 폴더 열기(`Ctrl+O`)로 폴더를 고른다 | 트리 맨 위가 그 폴더가 되고 자식이 보인다. 이전 루트는 사라진다 | 폴더 P를 연 뒤 폴더 Q를 연다. 트리의 최상위 노드가 Q 하나이고, P를 가리키는 노드가 0개다 | D-9 · D-16 |
| FR-A2 | `↑` `↓` | 포커스가 한 항목씩 위아래로 간다 | 보이는 항목 n개에서 `↓`를 k번 눌러 포커스가 k칸 아래 항목이고, `↑` k번으로 되돌아온다 | D-9 |
| FR-A3 | 접힌 폴더에서 `→` | 펼쳐진다. 이미 펼쳐져 있으면 첫 자식으로 포커스가 간다 | 접힌 폴더에 포커스를 두고 `→` → 펼침 상태이고 포커스는 그대로. 한 번 더 `→` → 포커스가 첫 자식 | D-9 |
| FR-A4 | 펼친 폴더에서 `←` | 접힌다. 이미 접혀 있으면 부모로 포커스가 간다 | 펼친 폴더에서 `←` → 접힘. 한 번 더 `←` → 포커스가 부모 노드 | D-9 |
| FR-A5 | `Space` | 펼침/접힘이 뒤집힌다 | 폴더에 포커스를 두고 `Space` 두 번 → 펼침 상태가 원래대로 돌아온다 | D-9 |
| FR-A6 | `Enter` | 그 항목이 열린다 (FR-B1과 같은 결과) | `Enter` 후 결과가 FR-B1의 판정과 일치한다 | D-9 · D-5 |
| FR-A7 | `Ctrl+←` | 전부 접힌다 | 여러 폴더를 펼친 뒤 `Ctrl+←` → 펼쳐진 노드가 0개 | D-9 |
| FR-A8 | `Home` / `End` | 처음 / 끝 항목으로 간다 | 중간 항목에 포커스를 두고 `Home` → 첫 항목, `End` → 마지막 보이는 항목 | D-9 |
| FR-A9 | `Shift+↑` `Shift+↓` | 선택 범위가 그 방향으로 넓어진다 | 한 항목 선택 후 `Shift+↓` k번 → 선택 항목 수가 k+1이고 연속 범위다 | D-9 |
| FR-A10 | `Ctrl+A` | 보이는 항목이 전부 선택된다 | `Ctrl+A` 후 선택 항목 수 = 보이는 항목 수 | D-9 |
| FR-A11 | `Ctrl+클릭` / `Shift+클릭` | 하나 더 고르기 / 범위로 고르기 | `Ctrl+클릭`으로 떨어진 두 항목이 선택된다. `Shift+클릭`으로 두 항목 사이 전부가 선택된다 | D-9 |
| FR-A12 | `Esc` | 선택이 풀린다 | 여러 개 선택 후 `Esc` → 선택 항목 수 0 | D-9 |
| FR-A13 | `F3` | 트리 안에서 찾기가 열린다 | `F3` → 찾기 입력 자리가 나타나고, 글자를 넣으면 일치 항목으로 포커스가 간다 | D-9 |
| FR-A14 | `Ctrl+Enter` | 옆 칸에서 열린다. 옆 칸이 없으면 오른쪽에 칸이 만들어진다 | 칸 1개 상태에서 `Ctrl+Enter` → 칸 2개가 되고 오른쪽 칸에서 열린다. 칸 2개 상태에서는 칸 수가 그대로이고 옆 칸에서 열린다 | D-9 · D-19 |
| FR-A15 | 루트를 트리 안에서 바꾸려 한다 | 그런 수단이 없다. 루트는 폴더 열기로만 바뀐다 | 트리 안에 상위로 올라가는 항목·버튼·키가 0개다. 트리 조작만으로는 루트가 바뀌지 않는다 | D-16 |
| FR-A16 † | `PageUp` / `PageDown` | 한 화면씩 포커스가 움직인다 | 화면에 h개가 보일 때 `PageDown` → 포커스가 h칸 아래(끝에 닿으면 마지막 항목)로 간다. `PageUp`은 그 반대 | D-9 |
| FR-A17 † | `Ctrl+↑` / `Ctrl+↓` | 포커스는 그대로 두고 스크롤만 움직인다 | 누른 뒤 스크롤 위치가 바뀌고 포커스 항목은 이전과 같다 | D-9 |
| FR-A18 † | `Ctrl+Shift+Enter` | 지금 항목의 선택이 뒤집힌다 | 선택되지 않은 항목에서 누르면 선택 수 +1, 한 번 더 누르면 −1 | D-9 |
| FR-A19 † | `Ctrl+Alt+F` | 트리 안에서 찾기가 열린다 (FR-A13과 같은 결과) | `Ctrl+Alt+F` 후 결과가 FR-A13의 판정과 일치한다 | D-9 |
| FR-A20 † | 탐색기 위쪽을 본다 | 뷰 제목줄이 있고, 오른쪽에 껍데기 액션 둘(모두 접기 · 새로 고침)이 있다 | 제목줄이 1개 있고, 껍데기가 넣은 액션이 정확히 그 둘이다. 앱이 더한 것이 없으면 그 밖의 아이콘이 0개다 | D-9 · D-30 |
| FR-A21 † | 뷰 제목줄의 **모두 접기** | 전부 접힌다 (FR-A7과 같은 결과) | 여러 폴더를 펼친 뒤 누르면 펼쳐진 노드가 0개다. `Ctrl+←`와 결과가 같다 | D-9 · D-30 |
| FR-A22 † | 뷰 제목줄의 **새로 고침** | 트리가 다시 읽힌다. 펼침 상태와 선택은 유지된다 | 껍데기 밖에서 파일을 하나 만든 뒤 누르면 그 행이 나타나고, 펼쳐져 있던 노드와 선택 항목이 누르기 전과 같다. 껍데기는 트리 공급자를 부르기만 하고 무엇을 읽는지 모른다 | D-30 |
| FR-A23 † | 앱이 꽂은 **새 파일 · 새 폴더** | 트리에 인라인 이름 입력 행이 생기고, `Enter`로 확정 · `Escape`로 취소된다. 실제로 만드는 것은 앱이다 | 누르면 입력 행이 1개 생긴다. `Enter` → 껍데기가 앱에게 이름을 넘긴 기록이 1건이고 껍데기 쪽 생성 동작이 0회다. `Escape` → 입력 행이 사라지고 앱 호출이 0건이다 | D-30 · D-22 |
| FR-A24 † | 앱이 아무것도 꽂지 않은 상태에서 뷰 제목줄을 본다 | 껍데기 액션 둘만 있다. 새 파일·새 폴더 아이콘은 없다 | 껍데기만 있는 상태에서 제목줄의 아이콘이 정확히 2개다 | D-30 |

† 표시는 `refs/scenarios.md`에 행이 없고 **결정 본문에서 결과를 그대로 옮긴** 항목이다.
아래 FR-N ~ FR-R 전체가 같다.

### 1.2 무엇을 고르면 어디에 열리나 (FR-B)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-B1 | 트리에서 항목을 고른다 | 활성 칸의 활성 탭 내용이 그 항목으로 바뀐다. **탭 개수는 변하지 않는다** | 고르기 전후의 탭 개수가 같고, 활성 탭의 대상이 고른 항목이다 | D-5 |
| FR-B2 | 이미 다른 탭에 열려 있는 항목을 고른다 | 그 탭으로 이동한다. 새 탭이 생기지 않고, 방금까지 보던 탭의 내용도 바뀌지 않는다 | 탭 개수가 그대로이고, 활성 탭이 그 항목을 가진 기존 탭이며, 직전 활성 탭의 대상이 그대로다 | D-5 |
| FR-B3 | 새 탭을 추가한 뒤 그 탭에서, 이미 열려 있는 항목을 고른다 | 이동하지 않고 **그 탭에 새로 열린다.** 같은 대상을 가진 탭이 둘이 된다 | `+`로 탭 추가 후 기존 대상 X를 고르면 활성 탭이 방금 추가한 탭 그대로이고, 대상 X를 가진 탭이 2개다 | D-5 |
| FR-B4 | 폴더를 고른다 | 파일과 같은 규칙이 적용된다 (FR-B1 ~ FR-B3) | 폴더 대상으로 FR-B1·B2·B3 판정을 그대로 반복해 같은 결과가 난다 | D-5 |

### 1.3 탭 (FR-C)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-C1 | 탭 줄 오른쪽 끝의 `+` | 그 칸 탭 줄의 **오른쪽 끝**에 빈 탭이 생기고 활성이 된다 | 탭 n개에서 `+` → 탭 n+1개, 새 탭의 위치가 마지막이고 활성 탭이다 | D-15 · D-20 |
| FR-C2 | 빈 탭을 본다 | 아무것도 그려지지 않는다. 안내 문구도 버튼도 없다 | 빈 탭의 내용 영역에 자식 요소와 텍스트가 0개다 | D-20 |
| FR-C3 | 탭의 닫기 버튼 | 그 탭이 사라지고 이웃 탭이 활성이 된다 | 탭 3개 중 가운데를 닫으면 탭 2개가 되고 활성 탭이 이웃 탭이다 | D-10 |
| FR-C4 | 한 칸의 **마지막** 탭을 닫는다 | 그 칸이 사라지고, 남은 칸들이 그 자리를 나눠 갖는다. **단 마지막 한 칸이면 빈 면으로 남는다** | 칸 2개에서 한쪽의 마지막 탭을 닫으면 칸 1개가 되고 남은 칸이 전체를 차지한다. 칸 1개에서 마지막 탭을 닫으면 칸 수 1 · 탭 수 0인 빈 면이다 | D-26 |
| FR-C5 | 탭을 같은 줄에서 끈다 | 순서가 바뀐다. 각 탭의 내용은 그대로다 | 탭 순서가 바뀌고, 각 탭의 대상과 보기 인스턴스 도장이 이동 전과 같다 | D-10 |
| FR-C6 | 탭을 다른 칸으로 끈다 | 그 칸으로 옮겨진다. 보기는 다시 만들어지지 않는다 (FR-E2) | 탭이 목표 칸에 있고, 인스턴스 도장이 같으며 생성 횟수가 늘지 않았다 | D-10 · D-24 |
| FR-C7 | 탭을 편집기 **가장자리로** 끈다 | 오른쪽 가장자리면 오른쪽에, 아래쪽 가장자리면 아래쪽에 새 칸이 생기고 그 탭이 거기로 간다 | 오른쪽 가장자리에 떨구면 칸이 1 늘고 새 칸이 오른쪽에 있다. 아래쪽도 같은 방식으로 아래에 생긴다 | D-13 |
| FR-C8 | 탭을 창 밖으로 끈다 | 아무 일도 일어나지 않는다. 떼어내기·새 창은 없다 | 창 밖에 떨궈도 창 개수가 1 그대로이고 탭의 소속 칸과 순서가 변하지 않는다 | D-13 |
| FR-C9 | 탭 제목에서 오른쪽 버튼 | 기본 상태에서는 아무 일도 없다 (FR-G5) | 껍데기만 있는 상태에서 탭 제목 우클릭 → 메뉴 요소가 0개다 | D-22 |

### 1.4 칸 (FR-D)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-D1 | 탭 줄 오른쪽 끝의 **좌우 스플릿** 아이콘 | 지금 칸의 **오른쪽에** 새 칸이 생긴다 | 칸 수가 1 늘고, 새 칸이 원래 칸의 오른쪽에 위치한다 | D-13 |
| FR-D2 | 탭 줄 오른쪽 끝의 **상하 스플릿** 아이콘 | 지금 칸의 **아래쪽에** 새 칸이 생긴다 | 칸 수가 1 늘고, 새 칸이 원래 칸의 아래쪽에 위치한다 | D-13 |
| FR-D3 | 세로 띠 또는 View 메뉴의 좌우/상하 스플릿 | FR-D1 · FR-D2와 같은 결과 (활성 칸 기준) | 세 경로(탭 줄·세로 띠·View 메뉴) 각각으로 실행해 결과 배치가 서로 같다 | D-11 · D-13 · D-23 |
| FR-D4 | 칸 사이 경계를 끈다 | 맞닿은 칸들의 크기가 바뀐다 | 경계를 d픽셀 끌면 맞닿은 두 칸의 크기가 그만큼 서로 반대로 바뀐다 | D-8 · D-24 |
| FR-D5 | 칸을 끌어 옮긴다 | 격자 안에서 자리가 바뀐다. 각 칸의 내용은 그대로다 | 옮긴 뒤 칸의 위치가 바뀌고, 각 칸의 탭 목록과 보기 인스턴스 도장이 그대로다 | D-8 · D-24 |
| FR-D6 | 오른쪽·아래로 여러 번 나눈다 | 2차원 격자가 된다. 배치·크기·재배치는 dockview가 관리한다 | 오른쪽 1회 + 아래쪽 1회 후 칸 3개가 가로·세로를 모두 쓴 배치로 놓인다 | D-8 · D-24 |
| FR-D7 | 칸을 계속 만든다 | 개수 제한 없이 만들어진다 | 스플릿을 8회 반복해 칸 9개가 되고, 어느 단계에서도 거부나 오류가 없다 | D-8 |
| FR-D8 | 칸을 명시적으로 닫으려 한다 | 그런 수단이 없다. 탭을 모두 닫으면 칸이 사라진다 | 세로 띠·View 메뉴·우클릭 어디에도 "칸 닫기/삭제" 항목이 0개다 | D-26 |

### 1.5 보기 수명 (FR-E)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-E1 | 탭 A에서 B로 갔다가 A로 돌아온다 | A의 보기 인스턴스가 **그대로**다. 다시 만들어지지 않는다 | A의 인스턴스 도장이 같고, 생성 횟수 1 · 폐기 0이다 | D-24 |
| FR-E2 | 탭을 다른 칸으로 옮긴다 | 인스턴스가 그대로다 | 옮긴 뒤 인스턴스 도장이 같고 생성 횟수 1 · 폐기 0이다 | D-24 |
| FR-E3 | 탭을 닫는다 | **그때** 폐기된다. 그 전에는 폐기되지 않는다 | FR-E1·E2를 거치는 동안 폐기 0이고, 닫는 순간 폐기 1이 된다 | D-24 |
| FR-E4 | "항상 그려 두기"로 올린 탭을 안 보이게 둔다 | 화면 요소가 떼어지지 않는다 | 그 탭을 비활성으로 둔 상태에서 보기의 DOM 요소가 문서에 여전히 붙어 있다 | D-24 |
| FR-E5 | 기본 설정인 탭을 안 보이게 둔다 | 화면 요소는 떼어지되 인스턴스는 살아 있다 | 비활성 상태에서 DOM 요소가 떨어져 있고, 인스턴스 도장과 타이머는 계속 살아 있다 | D-24 |

### 1.6 화면 접기와 Zen (FR-F)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-F1 | 탐색기 접기 | 탐색기가 사라지고 탭 영역이 그만큼 넓어진다. **세로 띠는 남는다** | 접은 뒤 탐색기가 보이지 않고, 탭 영역 폭이 탐색기 폭만큼 늘며, 세로 띠는 보인다 | D-12 |
| FR-F2 | 상단 바 감추기 | 상단 바가 사라진다. 창 버튼도 함께 사라지지만, 세로 띠 아이콘으로 되돌릴 수 있다 | 감춘 뒤 상단 바와 창 버튼이 보이지 않고, 세로 띠의 해당 아이콘을 누르면 되돌아온다 | D-14 |
| FR-F3 | 하단 바 감추기 | 하단 바가 사라진다 | 감춘 뒤 하단 바가 보이지 않고, 같은 아이콘으로 되돌아온다 | D-14 |
| FR-F4 | 상단·하단을 각각 감춘다 | 서로 영향을 주지 않는다 | 상단만 감춘 상태에서 하단이 그대로이고, 그 반대도 같다 | D-14 |
| FR-F5 | Zen 모드(`F11`) | 상단 바·세로 띠·탐색기·경계·하단 바가 모두 사라지고 **칸(편집기) 영역만** 남는다. **창 크기는 그대로다** | `F11` 후 다섯 조각 중 칸 영역만 보이고, 창의 바깥 크기와 위치가 이전과 같다 | D-12 |
| FR-F6 | Zen 중 `Escape` | Zen이 풀리고 원래대로 돌아온다 | `Escape` 후 감춰졌던 조각들이 Zen 진입 전 상태 그대로 보인다 | D-12 |
| FR-F7 | 세로 띠를 감추려 한다 | 그런 수단이 없다. Zen 말고는 사라지지 않는다 | 세로 띠와 View 메뉴에 "세로 띠 감추기" 항목이 0개다 | D-14 |

### 1.7 빈 상태와 실패 (FR-G)

| FR | 상황 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-G1 | 폴더를 아직 열지 않았다 | 트리 자리가 빈 면이다. 안내도 버튼도 없고 아무 동작도 일어나지 않는다 | 트리 영역에 자식 요소와 텍스트가 0개이고, 클릭해도 상태가 바뀌지 않는다 | D-20 |
| FR-G2 | 읽을 수 없는 폴더를 연다 | 하단 바에 오류가 한 줄 뜬다. **떠오르는 알림 상자는 없다** | 하단 바 메시지 자리에 오류 문구가 1줄 있고, 그 밖에 새로 뜬 떠 있는 요소가 0개다 | D-21 |
| FR-G3 | 열려 있던 파일이 사라진 뒤 그 탭으로 간다 | 하단 바에 오류가 한 줄 뜬다 | 파일을 지운 뒤 그 탭으로 전환하면 하단 바에 오류가 1줄 나타난다 | D-21 |
| FR-G4 | 오래 걸리는 작업이 진행 중이다 | 하단 바에 진행 표시가 뜬다 | 진행 중에 하단 바에 진행 표시가 있고, 끝나면 사라진다 | D-21 |
| FR-G5 | 오른쪽 버튼을 누른다 (기본 상태) | 아무 일도 일어나지 않는다 | 트리와 탭 어디에서 우클릭해도 메뉴 요소가 0개다 | D-22 |
| FR-G6 | 앱이 항목을 정의하고 View에서 우클릭 메뉴를 켠다 | 그 항목들이 뜬다. 껍데기가 넣은 항목은 없다 | 켠 뒤 뜬 메뉴의 항목 목록이 앱이 정의한 목록과 정확히 같다(껍데기가 더한 항목 0개) | D-22 · D-23 |

### 1.8 두 갈래 (FR-H)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-H1 | FR-A ~ FR-G 전부를 Electron과 pywebview에서 각각 한다 | 같은 조작에 같은 결과가 나온다. 한쪽에만 있는 동작 차이가 없다 | 두 갈래에서 각각 판정한 결과표를 대조해 결과가 다른 항목이 0개다 | D-6 · D-25 |
| FR-H2 | 두 갈래가 읽는 산출물을 비교한다 | 같은 한 벌이다 | 두 갈래가 읽는 웹 산출물의 파일 목록과 각 파일의 해시가 같다 | D-25 |

### 1.9 갈아끼우기 (FR-I)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-I1 | 시험이 임의의 리소스 종류를 하나 등록한다 | 공통 코어 **0줄** 수정으로 그것이 탭에서 열린다 | 등록 코드를 공통 코어 밖에만 두고, 공통 코어 파일의 diff가 0줄인 상태로 그 종류가 탭에 열린다 | D-4 · D-19 |
| FR-I2 | 파일 프리셋을 쓴다 | 트리에 파일이 보이고, 고르면 그 파일을 대상으로 하는 보기가 탭에 열린다 | 공통 코어 diff 0줄로, 트리에 파일 행이 있고 고른 파일이 활성 탭의 대상이 된다 | D-5 · D-19 |
| FR-I3 | 폴더 프리셋을 쓴다 | 트리에 폴더가 보이고, 고르면 그 폴더를 대상으로 하는 보기가 탭에 열린다 | 공통 코어 diff 0줄로, 트리에 폴더 행이 있고 고른 폴더가 활성 탭의 대상이 된다 | D-5 · D-19 |
| FR-I4 | 앱이 "옆 칸에 열기"를 부른다 | FR-A14와 같은 결과가 난다 | 앱 코드에서 그 명령을 부른 결과가 FR-A14의 판정과 일치한다 | D-19 |
| FR-I5 | 앱이 칸 배치를 묻는다 | 자기가 있는 칸과 옆 칸에 무엇이 열려 있는지를 받는다 | 칸 2개에 서로 다른 대상을 열어 두고 물으면, 자기 칸 식별자와 옆 칸의 대상이 실제와 일치해 돌아온다 | D-19 |
| FR-I6 | 포커스가 탭 안일 때 껍데기가 안 쓰는 키를 누른다 | 그 키가 앱에 전달된다 | 껍데기 예약 키 목록에 없는 키(`F3`·`F4`·`F5`·`F6`·`Space` 등)를 누르면 탭의 보기가 그 키를 받는다 | D-19 |
| FR-I7 | 포커스가 트리일 때 트리 키를 누른다 | 껍데기가 처리한다. 앱에 가지 않는다 | 트리 포커스에서 트리 키를 누르면 트리 동작이 일어나고 앱 쪽 수신 기록이 0건이다 | D-9 · D-19 |
| FR-I8 | 앱이 탭이나 칸을 직접 건드리려 한다 | 그런 수단이 없다. 껍데기가 여는 것(FR-I4 · FR-I5)으로만 한다 | 앱에 노출된 표면에 탭·칸을 만들거나 닫거나 옮기는 수단이 0개다 | D-19 |
| FR-I9 † | 앱이 File 묶음에 항목을 더한다 | 껍데기 항목 다섯 아래 가름선 다음에 붙는다. 껍데기 항목은 목록·순서 그대로다 | 공통 코어 diff 0줄로 항목이 붙고, 그 뒤에도 FR-N6의 껍데기 항목 다섯이 목록·순서 그대로다. 앱이 껍데기 항목을 빼거나 순서를 바꾸는 수단이 0개다 | D-31 |
| FR-I10 † | 앱이 탐색기 뷰 제목줄에 액션을 더한다 | 껍데기 액션 둘(모두 접기·새로 고침) 왼쪽에 붙는다 | 공통 코어 diff 0줄로 액션이 붙고, 그 뒤에도 FR-A20의 껍데기 액션 둘이 그대로 오른쪽에 있다 | D-30 · D-31 |
| FR-I11 † | 앱이 View 또는 Help 묶음에 항목을 더하려 한다 | 그런 수단이 없다 | 앱에 노출된 표면에 View·Help 묶음을 건드리는 수단이 0개다 | D-31 |

### 1.10 칸이 사라지는 방식과 마지막 칸 (FR-J)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-J1 | 칸의 탭을 하나씩(`×` · `Ctrl+W`) 마지막까지 닫는다 | 그 칸이 사라지고 남은 칸이 자리를 나눠 갖는다 | 칸 2개 중 한쪽의 탭을 전부 닫으면 칸 1개가 되고 남은 칸이 전체를 차지한다. `×`와 `Ctrl+W` 둘 다로 확인한다 | D-26 |
| FR-J2 | View 메뉴의 "활성 칸 탭 모두 닫기" | 그 칸의 탭이 전부 닫힌다 → FR-J1과 같이 칸이 사라진다 (마지막 칸이면 빈 칸으로 남는다) | 실행 후 그 칸의 탭 수가 0이 되고, 칸이 여럿이면 칸 수가 1 줄며, 마지막 칸이면 칸 수 1 · 탭 수 0으로 남는다 | D-23 · D-26 |
| FR-J3 | "활성 칸 탭 모두 닫기"를 탭 줄·세로 띠에서 찾는다 | 없다. **메뉴에만 있다** | 탭 줄과 세로 띠에 해당 항목이 0개이고, View 메뉴에 1개 있다 | D-26 |
| FR-J4 | 빈 칸(탭 0개)에서 "활성 칸 탭 모두 닫기"를 누른다 | 이미 빈 칸이라 바뀌는 것이 없다. 항목은 메뉴에 그대로 있다 | 실행 전후 칸 수와 탭 수가 같고, 메뉴에 그 항목이 여전히 있다 | D-26 |
| FR-J5 | 사라지는 칸의 탭 | 전부 함께 닫힌다. 다른 칸으로 옮겨지거나 나눠 붙지 않는다. 보기는 그때 폐기된다 (FR-E3) | 칸이 사라진 뒤 남은 칸들의 탭 수 합이 사라진 칸의 탭 수만큼 줄고, 그 탭들의 보기 폐기 횟수가 각각 1이다 | D-24 · D-26 |
| FR-J6 | "칸 닫기/삭제" 전용 명령을 찾는다 | 없다. 세로 띠·메뉴·우클릭 어디에도 항목이 없다 | 세 자리 전부에서 해당 항목이 0개다 | D-26 |
| FR-J7 | 마지막 칸의 마지막 탭까지 닫는다 | 그 칸이 탭 없는 빈 면으로 남는다. 편집기 영역이 통째로 없어지지 않는다 | 닫은 뒤 칸 수 1 · 탭 수 0이고, 칸 영역이 화면에 자리를 차지하고 있다 | D-20 · D-26 |
| FR-J8 | 빈 칸에서 다시 시작한다 | 탐색기에서 파일 고르기(FR-B1) · 세로 띠 스플릿 아이콘 · View 메뉴 중 하나로 | 빈 칸 상태에서 세 경로 각각을 실행해 모두 탭 또는 칸이 생긴다 | D-5 · D-26 |

### 1.11 다시 열 때 (FR-K)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-K1 | 폴더를 열고 탭을 몇 개 연 뒤 앱을 끈다. 다시 켠다 | **마지막에 연 루트 폴더만** 탐색기에 나타난다 | 다시 켠 뒤 트리 루트가 마지막에 연 폴더와 같다 | D-27 |
| FR-K2 | 같은 상황에서 오른쪽을 본다 | 빈 칸 하나가 있다. 이전 칸 배치·탭·활성 탭은 되살아나지 않는다 | 다시 켠 뒤 칸 수 1 · 탭 수 0이다 | D-26 · D-27 |
| FR-K3 | 같은 상황에서 트리를 본다 | 펼침 상태는 되살아나지 않는다. 루트만 열려 있다 | 다시 켠 뒤 펼쳐진 노드가 루트 하나뿐이다 | D-27 |

### 1.12 저장하지 않은 채 닫기 (FR-L)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-L1 | 고친 탭의 제목을 본다 | 고쳐짐 표시(●)가 붙어 있다 | 앱이 "고쳐졌다" 상태를 올리면 그 탭 제목에 ● 표시가 나타나고, "저장됐다"로 바꾸면 사라진다 | D-18 · D-28 |
| FR-L2 | 고친 탭을 닫으려 한다 | 확인 창이 뜬다. 버튼은 [저장] · [저장 안 함] · [취소] 셋 | 닫기를 시도하면 확인 창이 1개 뜨고 버튼이 정확히 그 셋이다 | D-28 |
| FR-L3 | [저장] | 저장한 뒤 닫힌다 | 앱의 저장 호출이 1회 일어나고 그 탭이 닫힌다 | D-28 |
| FR-L4 | [저장 안 함] | 고친 것을 버리고 닫힌다 | 저장 호출이 0회이고 그 탭이 닫힌다 | D-28 |
| FR-L5 | [취소] | 닫히지 않는다. 고친 내용도 그대로다 | 탭이 그대로 있고 ● 표시가 유지된다 | D-28 |
| FR-L6 | 고친 탭이 있는 채로 앱을 끄려 한다 | 같은 확인 창이 뜬다 | 끄기를 시도하면 FR-L2와 같은 창이 뜨고, [취소]를 누르면 앱이 꺼지지 않는다 | D-28 |
| FR-L7 | 안 고친 탭을 닫는다 | 아무것도 묻지 않고 닫힌다 | 확인 창이 0개 뜨고 탭이 바로 닫힌다 | D-28 |

### 1.13 테마 (FR-M)

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-M1 | 세로 띠 아래쪽 또는 View 메뉴의 테마 바꾸기 | 흰색 → 회색 → 검정을 순환한다 | 세 번 눌러 테마가 흰색→회색→검정→흰색 순으로 돌아온다. 두 경로(세로 띠·View 메뉴) 모두에서 같다 | D-11 · D-23 · D-29 |
| FR-M2 | 흰색·검정 테마를 본다 | 색이 VS Code Light Modern / Dark Modern과 같다 | 색 토큰 값을 두 테마의 대응 값과 대조해 다른 값이 0개다 | D-29 |
| FR-M3 | 회색 테마를 본다 | 배경이 흰색·검정 테마 배경의 산술 중간(`#808080` 계열)이고, 색상·채도는 VS Code 팔레트 계열이다 | 배경 토큰의 밝기가 흰색·검정 대응 값의 산술 중간이다(L\* ≈ 54~56) | D-29 |
| FR-M4 | 어느 테마에서든 아이콘을 본다 | codicons·파일 종류 아이콘은 색만 바뀐다 | 세 테마에서 아이콘의 모양(글리프·경로)이 같고 색만 다르다 | D-3 · D-17 |
| FR-M5 | `tab-explorer-templates`의 글꼴·간격·색이 쓰였는지 본다 | 하나도 안 쓰였다 | 산출물에서 그 프로젝트의 글꼴·간격·색 토큰 값이 검색되는 건수가 0이다 | D-29 |

### 1.14 화면 뼈대 · 메뉴 · 하단 바 · 세로 띠 (FR-N) †

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-N1 | 상단 바를 본다 | 왼쪽에 메뉴를 여는 햄버거 버튼(`≡`), 가운데에 창 제목, 오른쪽에 최소화·최대화·닫기가 있다 | 세 자리에 각각 해당 요소가 1개씩 있다 | D-7 |
| FR-N2 | 상단 바의 빈 곳을 끈다 | 창이 따라 움직인다 | 끈 거리만큼 창의 화면상 위치가 바뀐다 | D-7 |
| FR-N3 | 창 가장자리를 끈다 | 창 크기가 바뀐다 | 네 변과 네 모서리 각각에서 끌면 창 크기가 그 방향으로 바뀐다 | D-7 |
| FR-N4 | 창 버튼 셋을 각각 누른다 | 최소화 · 최대화(다시 누르면 복원) · 닫기가 각각 일어난다 | 세 버튼의 결과가 각각 최소화·최대화/복원·종료다 | D-7 |
| FR-N5 | 햄버거 버튼을 누른다 | File · View · Help 세 묶음이 펼쳐진다. 가로로 늘어선 메뉴 줄은 어디에도 없다 | 펼친 메뉴의 최상위 묶음이 정확히 셋이고, 상시 노출된 가로 메뉴 줄이 0개다 | D-7 · D-23 |
| FR-N6 | File 묶음을 연다 | 껍데기 항목 다섯이 있다 — 폴더 열기(`Ctrl+O`) · 최근 폴더 · 탭 닫기(`Ctrl+W`) · 폴더 닫기 · 끝내기. 앱이 더한 항목은 가름선 아래에 붙는다 | **껍데기가 넣은 항목**의 목록과 순서가 그 다섯과 정확히 같다. 앱이 더한 항목은 따로 세며, 전부 가름선 아래에 있다 | D-16 · D-23 · D-31 |
| FR-N6a | 최근 폴더에서 한 항목을 고른다 | 폴더 열기와 같은 결과가 난다 — 그 폴더가 새 루트가 되고 이전 루트는 사라진다 | 고른 뒤 결과가 FR-A1의 판정과 일치한다 | D-16 |
| FR-N6b † | File의 **탭 닫기** | 활성 탭이 닫힌다 (`Ctrl+W`와 같은 결과) | 실행 후 결과가 FR-J1의 판정과 일치한다 | D-26 · D-31 |
| FR-N6c † | File의 **폴더 닫기** | 루트가 비워지고 트리가 폴더를 열지 않은 빈 상태로 돌아간다. 탭과 칸은 그대로다 | 실행 후 트리가 FR-G1의 판정과 일치하고, 칸 수와 탭 수가 실행 전과 같다 | D-20 · D-31 |
| FR-N7 | View 묶음을 연다 | 탐색기 접기/펴기 · 상단 바 감추기/보이기 · 하단 바 감추기/보이기 · Zen 모드 · 테마 바꾸기 · **아이콘 테마 바꾸기** · 좌우 스플릿 · 상하 스플릿 · 활성 칸 탭 모두 닫기 · 우클릭 메뉴 사용(기본 꺼짐) 열 항목이 있다. **앱은 View에 항목을 더할 수 없다** | 항목 목록이 그 열과 정확히 같고, "우클릭 메뉴 사용"의 초기 상태가 꺼짐이다 | D-17 · D-22 · D-23 · D-31 |
| FR-N8 | Help 묶음을 연다 | 정보 한 항목이 있다 | 항목이 1개이고 "정보"다 | D-23 |
| FR-N9 | 하단 바를 본다 | 왼쪽에 지금 탭이 가리키는 대상의 경로, 오른쪽에 앱 이름·버전·실행 갈래가 있다. 그 사이가 오류·진행 메시지 자리다(FR-G2 ~ G4) | 세 자리의 내용이 각각 실제 대상 경로, 앱 이름·버전·갈래 이름과 일치한다 | D-21 · D-23 |
| FR-N10 | 앱이 하단 바에 자기 항목을 하나 붙인다 | 껍데기 항목과 함께 보인다 | 공통 코어 diff 0줄로 앱 항목이 하단 바에 나타나고, 껍데기의 세 자리가 그대로 남는다 | D-23 |
| FR-N10a † | 앱이 하단 바에 오류·안내 한 줄을 올린다 | 껍데기 오류(FR-G2 ~ G3)와 **같은 자리에 같은 모양으로** 뜬다. 떠오르는 알림 상자는 없다 | 메시지 자리에 앱이 준 문구가 1줄 그대로(껍데기가 고친 글자 0개) 나타나고, 그때 새로 떠 있는 요소가 0개다. 껍데기가 올린 것과 앱이 올린 것을 이어서 올리면 **가장 최근 것 하나만** 보인다 | D-21 · D-32 |
| FR-N10b † | 앱이 진행 표시를 켜고 끈다 | 껍데기 진행(FR-G4)과 같은 자리에 같은 모양으로 뜨고, 앱이 끄면 사라진다 | 켜면 진행 표시가 나타나고, 끄기 전에는 껍데기가 임의로 끄지 않으며(일정 시간 뒤 사라지는 경우가 0건), 끄면 사라진다 | D-21 · D-32 |
| FR-N11 | 세로 띠를 본다 | 위에서부터 탐색기 접기/펴기 · 상단 바 감추기/보이기 · 하단 바 감추기/보이기 · 좌우 스플릿 · 상하 스플릿 · Zen 모드가 있고, 아래쪽에 테마 바꾸기가 있다. **칸 삭제·칸 합치기 아이콘은 없다** | 아이콘 목록과 순서가 그와 같고, 칸 삭제·칸 합치기 항목이 0개다 | D-11 · D-12 · D-13 · D-14 |
| FR-N12 | 앱이 세로 띠 항목을 바꾼다 | 공통 코어를 고치지 않고 바뀐다. 메뉴(View)의 대응 항목은 그대로 남는다 | 공통 코어 diff 0줄로 띠의 항목이 바뀌고, 그 뒤에도 FR-N7의 View 항목 열이 그대로다 | D-11 · D-23 |

### 1.15 텍스트·코드 보기 (FR-P) †

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-P1 | 텍스트·코드 대상을 탭에서 연다 | 문법에 따른 색칠과 줄 번호가 보인다 | 색칠된 토큰이 1개 이상이고 줄 번호 열이 보인다 | D-18 |
| FR-P2 | 찾기·바꾸기를 쓴다 | 동작한다 | 찾기로 일치 위치가 잡히고, 바꾸기로 내용이 바뀐다 | D-18 |
| FR-P3 | 되돌리기 / 다시하기를 쓴다 | 동작한다 | 고친 뒤 되돌리기로 이전 내용이 되고, 다시하기로 되돌아온다 | D-18 |
| FR-P4 | 읽기 전용으로 연다 | 내용을 고칠 수 없다 | 입력해도 내용이 바뀌지 않는다 | D-18 |
| FR-P5 | 자동완성 · 언어 서비스 · 미니맵 · 다중 커서를 찾는다 | 넷 다 꺼져 있다 | 각각을 부르는 조작을 해도 나타나지 않는다(제안 목록 0건, 오류 표시 0건, 미니맵 요소 0개, 커서 1개) | D-18 |
| FR-P6 | 앱이 monaco를 따로 가져오는지 본다 | 껍데기가 기본으로 안고 있어 앱이 가져오지 않는다 | 앱 쪽 의존성에 monaco가 0건이고, 앱 코드 수정 0줄로 FR-P1이 성립한다 | D-18 |
| FR-P7 | 앱이 "고쳐졌다 / 저장됐다" 상태를 올린다 | 껍데기는 탭에 표시만 그린다. 무엇을 어떻게 저장할지는 앱이 한다 | 상태를 올리면 FR-L1의 표시가 바뀌고, 껍데기 쪽 저장 동작은 0회다 | D-18 |

### 1.16 아이콘과 반입물 (FR-Q) †

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-Q1 | 파일 종류 아이콘 테마를 본다 | 기본(**seti**)과 컬러(**vscode-icons**) 둘이다. 직접 그린 세트는 없다 | 고를 수 있는 아이콘 테마가 그 둘이고, 껍데기가 직접 그린 아이콘 자산이 0건이다 | D-3 · D-17 |
| FR-Q1a | View 메뉴의 "아이콘 테마 바꾸기"를 누른다 | 기본(seti)과 컬러(vscode-icons)가 번갈아 바뀐다. 고르는 자리는 **View 메뉴 하나**다 | 눌러서 아이콘 테마가 바뀌고, 두 번 누르면 처음으로 돌아온다. 세로 띠에는 이 항목이 0개다 | D-17 · D-23 |
| FR-Q2 | 색 테마와 아이콘 테마를 함께 바꾼다 | 두 축은 서로 독립이다 | 색 테마 3 × 아이콘 테마 2 = 여섯 조합 모두에서 화면이 성립한다 | D-17 |
| FR-Q3 | 껍데기가 확장자를 아는지 본다 | 모른다. 트리 행의 이름과 펼침 여부만 넘기고, 무엇을 어떻게 그릴지 서술한 값 하나를 돌려받는다 | 공통 코어에 확장자→아이콘 대응표가 0건이다 | D-17 · INTENT 3 |
| FR-Q4 | 템플릿을 복사해 배포한다 | 반입물의 라이선스 본문과 저작자 표시가 템플릿 안에 이미 들어 있다. 복사한 사람이 따로 챙길 것이 없다 | dockview-core(MIT) · monaco-editor(MIT) · @vscode/codicons(CC BY 4.0 + MIT) · seti-ui(MIT) · vscode-icons(MIT + CC BY-SA)의 본문 파일이 한 폴더에 모두 있고, 어느 것이 어디에 쓰이는지 적은 목록이 1개 있다 | D-3 · D-17 · BRIEF 4 |
| FR-Q5 | Help 메뉴의 정보를 연다 | 저작자 표시가 필요한 반입물(codicons · vscode-icons)의 표시가 거기 있다 | 정보 화면에 두 세트의 저작자 표시가 각각 있다 | D-23 · BRIEF 4 · `refs/licenses.md` 2 |

### 1.17 문서로 남아야 하는 것 (FR-R) †

| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
| --- | --- | --- | --- | --- |
| FR-R1 | 껍데기가 먼저 가져가는 키가 무엇인지 찾는다 | 목록이 문서로 있다. 그 목록에 없는 키는 앱에 간다(FR-I6) | 예약 키 목록 문서가 1개 있고, 목록에 없는 키를 눌렀을 때 FR-I6이 성립한다 | D-19 |
| FR-R1a † | 껍데기가 **가져가지 않는** 흔한 키가 무엇인지 찾는다 | 같은 문서에 적혀 있다 — `Ctrl+S` · `F2` · `Delete` · `Ctrl+Z` / `Ctrl+Y` · `Ctrl+C` / `Ctrl+V`는 앱 몫이다 | 그 여섯이 목록에 "가져가지 않는다"로 적혀 있고, 탭 포커스에서 각각을 눌렀을 때 껍데기 쪽 처리 기록이 0건이며 탭의 보기가 받는다 | D-18 · D-19 · D-22 · D-30 |
| FR-R2 | VS Code와 이 템플릿의 동작을 대조한다 | 탐색기 · 탭 · 분할 영역 · 선택과 포커스 · 키보드/마우스 조작 다섯에 대해 대조할 수 있는 기준 문서가 있고, 다르게 둔 동작은 그 차이가 의도임이 적혀 있다 | 다섯 항목이 모두 그 문서에 있고, 다르게 둔 동작마다 의도임을 밝힌 줄이 1개씩 있다 | INTENT 6 · BRIEF 4 |
| FR-R3 | 검사가 무엇을 확인하는지 본다 | 구현 세부를 확인하는 검사와 사용자 행동을 확인하는 검사가 문서에서 구분되어 있다 | 검사 목록 문서에서 두 종류가 갈려 있고, 모든 검사가 둘 중 하나에 속한다 | BRIEF 4 |

---

## 2. 비기능 요구사항

| NFR | 내용 | 판정 방법 | 출처 |
| --- | --- | --- | --- |
| NFR-1 | 리소스 종류를 아는 코드가 공통 코어에 없다 | 공통 코어에서 파일·폴더·터미널·확장자를 아는 코드가 0건이다 | INTENT 3 · D-4 · D-17 |
| NFR-2 | 두 갈래의 동등성이 확인이 아니라 **구조**로 보장된다 | 갈래별로 갈라진 빌드 설정이 0개이고, 두 갈래가 읽는 산출물이 한 벌이다(FR-H2) | D-25 |
| NFR-3 | VS Code와 의도적으로 다르게 둔 동작은 Zen 모드 하나뿐이다 | 기준 문서(FR-R2)에 divergence로 기록된 항목이 정확히 1개(Zen 모드)다 | D-8 · D-12 · D-13 · D-26 |
| NFR-4 | 이미 검증된 것을 다시 만들지 않는다 | 칸·탭·보기 수명(dockview), 편집 화면(monaco), 아이콘(codicons·seti·vscode-icons)의 자체 구현 코드가 0건이다 | D-2 · D-3 · D-17 · D-18 · D-24 |
| NFR-5 | 검사 통과만으로 기능 연결이 검증됐다고 보지 않는다 | 1절 133건을 두 갈래에서 각각 사용자 행동으로 확인해 전건 통과한다. 구현 세부 검사는 이 전건에 포함되지 않는다(FR-R3) | BRIEF 2 · BRIEF 4 |
| NFR-6 | 세 색 테마 × 두 아이콘 테마 여섯 조합에서 화면이 성립한다 | 여섯 조합 각각에서 겹치거나 읽히지 않는 화면이 0건이다 | BRIEF 3 · D-29 |
| NFR-7 | 공통 UI의 모든 명령을 **키보드만으로** 부를 수 있다 | 메뉴(FR-N5 ~ N8) · 세로 띠(FR-N11) · 탭 줄의 명령 각각에 마우스 없이 실행하는 경로가 1개 이상 있다. 그 경로에 쓰이는 키는 FR-R1의 예약 키 목록 문서에 적혀 있다 | D-9 · D-19 · 이번 세션 확정 |

## 3. 제약사항

| # | 내용 | 출처 |
| --- | --- | --- |
| C-1 | 빌드를 전제한다. **Vite**로 웹 자산을 한 벌 빌드하고, 두 갈래가 그 산출물을 똑같이 읽는다 | D-1 · D-25 |
| C-2 | 실행 갈래는 **Electron**과 **pywebview** 둘이다. 하나를 버리거나 하나로 합치지 않는다 | INTENT 5 · BRIEF 3 · D-6 |
| C-3 | 대상 플랫폼은 **Windows**다. 이번 버전의 판정은 Windows에서 한다 | 이번 세션 확정 |
| C-4 | 반입 의존은 `dockview-core` 8.2.0 · `monaco-editor` 0.56.0 · `@vscode/codicons` 0.0.46-24 · `seti-ui` · `vscode-icons`다 | D-3 · D-17 · D-18 · D-24 · `refs/licenses.md` |
| C-5 | codicons(아이콘 CC BY 4.0 / 코드 MIT)와 vscode-icons(아이콘 CC BY-SA / 코드 MIT)는 **저작자 표시가 필수**다. vscode-icons 아이콘을 고쳐 쓰면 동일조건 공개 의무가 생기므로 고치지 않는다. 브랜드 아이콘은 각자의 라이선스를 따른다 | D-3 · D-17 · `refs/licenses.md` |
| C-6 | VS Code OSS를 fork하지 않는다. 동작과 구현을 참고하는 자료로만 본다 | D-2 |
| C-7 | 설치형 라이브러리·패키지로 배포하지 않는다. 복사해서 쓰는 템플릿이다 | INTENT 4 |
| C-8 | 프레임리스 창이라 창 끌기·크기 조절·창 버튼(최소화·최대화·닫기)을 껍데기가 직접 처리한다 | D-7 |
| C-9 | 앱에게 탭·칸을 직접 조작하는 표면을 열지 않는다. 앱은 껍데기가 여는 것(FR-I4 · FR-I5)으로만 한다 | D-19 |
| C-10 | 색 토큰은 VS Code Light Modern / Dark Modern에서 가져온다. `tab-explorer-templates`의 글꼴·간격·색은 어느 것도 참조하지 않는다 | D-29 |
| C-11 | 실행 환경(Node · Python · Electron · pywebview)의 최소 버전은 **이 SPEC에서 정하지 않는다.** 착수해서 실제로 설치할 때 정해 `PLAN`에 남긴다 | 이번 세션 확정 |

## 4. 미구현 대상

| # | 이번 버전에서 하지 않을 것 | 출처 |
| --- | --- | --- |
| X-1 | 검증용 세 앱의 도메인 기능 — 마크다운 렌더, 파일 복사·이동, 터미널 세션(xterm·PTY) | BRIEF 3 · D-4 |
| X-2 | VS Code 전체 복제 · 범용 코드 편집기 | INTENT 4 · BRIEF 3 |
| X-3 | 확장 호환성 · 소스 제어 · 검색 · 디버깅 · 통합 터미널(하단 Panel) | INTENT 4 · BRIEF 3 · D-4 |
| X-4 | 미리보기 탭 · 고정 탭 | D-5 · D-10 |
| X-5 | 멀티 루트 — 여러 폴더를 한 창에 묶기 | D-16 |
| X-6 | 탭 떼어내기 · 새 창 | D-13 |
| X-7 | 칸 닫기 · 칸 합치기 전용 명령 | D-11 · D-26 |
| X-8 | 환영 뷰 — 빈 상태의 안내 문구·버튼 | D-20 |
| X-9 | 떠오르는 알림 상자 · 알림 센터 | D-21 |
| X-10 | 껍데기가 기본으로 넣는 우클릭 항목 | D-22 |
| X-11 | 칸 배치 · 탭 · 활성 탭 · 트리 펼침 상태의 복원 | D-27 |
| X-12 | monaco의 자동완성 · 언어 서비스 · 미니맵 · 다중 커서 | D-18 |
| X-13 | 직접 그린 아이콘 세트("단순") | D-3 · D-17 |
| X-14 | 세로 띠 감추기 | D-14 |
| X-15 | 트리 키 `Alt+↑/↓/Home/End` · `Ctrl+K Ctrl+I` · `Shift+Alt+F` | D-9 |
| X-16 | 설치형 배포 · 모바일 · 클라우드 배포 | INTENT 4 |
| X-17 | 전체화면으로 전환하는 Zen | D-12 |
| X-18 | macOS · Linux 지원 | 이번 세션 확정 (C-3) |
| X-19 | 성능 수치 목표 — 시작 시간 · 탭 전환 반응 · 트리가 감당할 항목 수 | 이번 세션 확정 |
| X-20 | 화면 낭독기 대응 | 이번 세션 확정 |
| X-21 | 국제화 — 화면 문구의 언어 교체 | 이번 세션 확정 |
| X-22 | 앱 보기 **안에서** 쓸 편집기 위젯 대여 — 노트북의 셀마다 편집기 같은 형태. 탭 하나 = 편집기 하나만 둔다 | D-18 · `refs/next-version.md` N-1 |

이 표는 **영구 배제**와 **다음 버전 후보**가 섞여 있다. 후보인 것은 출처 열이
`refs/next-version.md`를 가리킨다.
> 버전: v0.1 · 작성일: 2026-09-07
>
> 이 문서는 이번 버전에 적용할 설계 결정과 그 근거를 기록한다. 변하지 않는 의도는
> `INTENT.md`에, 이번 버전에 무엇을 왜 하는지는 `BRIEF.md`에 있다. 결정은 INTENT에
> 어긋나면 안 된다.

# DECISIONS — workbench-kit

## D-1. 무빌드 원칙을 폐기하고 빌드를 전제로 한다

**선택**

이전 프로젝트(`tab-explorer-templates` · `markdown-viewer`)가 지켰던 "번들러 없이
그대로 로드하는 무빌드" 원칙을 이번 프로젝트에서는 따르지 않는다. 빌드 단계를 두고,
코드가 바뀌면 무조건 재빌드하는 것을 기본으로 한다.

**근거**

이 프로젝트의 원칙은 "무빌드"가 아니라 **"최소 구현"**이다. 필요한 기능을 제대로
구현하고 빌드해서 쓰는 편이, 무빌드 제약에 맞추느라 기능을 우회하거나 좁히는 것보다
목적에 맞다. 빌드를 전제해야 monaco-editor · codicons 같은 빌드형 라이브러리를 쓸 수
있다(D-2). 이것이 이전 프로젝트에서 이번 프로젝트로 넘어오며 바뀐 지점이다.

**배제한 대안**

- **무빌드 원칙을 유지한다(이전 프로젝트 방식).** 번들러 없이 로드되는 배포본만 써야
  해서 쓸 수 있는 라이브러리와 구현 방식이 좁아진다. 최소 구현을 오히려 방해하므로
  배제한다.

---

## D-2. VS Code OSS는 fork하지 않고 참고만 하며, bottom-up으로 필요한 기능만 구현한다

**선택**

VS Code OSS 저장소를 fork해 불필요한 것을 걷어내는 방식(top-down)을 쓰지 않는다.
VS Code OSS는 동작과 구현을 참고하는 자료로만 보고, 이 프로젝트에 필요한 기능만
밑에서부터 새로 구현한다(bottom-up). 다만 monaco-editor · codicons처럼 독립적으로
배포되어 빌드해 쓸 수 있는 라이브러리는 그대로 가져다 쓴다.

**근거**

제1원칙이 최소 구현이다. VS Code 전체를 안고 축소하면 쓰지 않는 방대한 코드와 그
상태·예외를 계속 떠안게 되어 최소·단순과 정반대가 된다. 필요한 기능만 참고해 새로
짜면 껍데기가 작게 유지되고, 왜 그 동작이어야 하는지를 VS Code라는 외부 기준과
대조할 수 있다(BRIEF의 기준 문서). 반대로 이미 검증된 라이브러리를 다시 만드는 것도
최소 구현에 어긋나므로, 그런 것은 반입한다.

**배제한 대안**

- **VS Code OSS를 fork해 축소한다(top-down).** 방대한 미사용 코드와 그 유지 부담을
  그대로 안게 되어 최소 구현 원칙과 어긋나므로 배제한다.
- **라이브러리도 쓰지 않고 전부 자체 구현한다.** monaco-editor · codicons처럼 이미 잘
  만들어진 것을 다시 만드는 비용이 크고, 이 역시 최소 구현에 어긋나므로 배제한다.

---

## D-3. 아이콘은 직접 그리지 않고 아이콘 라이브러리를 쓴다

**선택**

UI/제품 아이콘(툴바·펼침 화살표·창 버튼 등)은 **codicons**를 반입해 쓴다. VS Code가
실제로 쓰는 그 아이콘 폰트이며, `@vscode/codicons`로 배포된다. 파일 종류 아이콘은
D-17에서 정한다. 아이콘을 직접 그리거나 색 테마별로 따로 그리지 않는다. codicons는
단색 글꼴이라 세 색 테마에 맞춰 색만 바뀐다.

앞선 `tab-explorer-templates`는 UI 아이콘을 직접 그린 한 벌(`icons.svg`)로 갖고
있었다. 이번에는 그것을 쓰지 않고 codicons로 바꾼다.

**라이선스** — codicons는 이중 라이선스다. 저장소의 **코드는 MIT**지만 **아이콘
자체는 CC BY 4.0**이므로 저작자 표시가 필요하다. 반입할 때 표시 문구를 함께 넣는다.

**근거**

최소 구현 원칙(D-1 · D-2). 빌드를 전제하므로 아이콘 라이브러리를 그대로 넣을 수 있고,
이미 검증된 아이콘 세트를 다시 만들 이유가 없다.

**배제한 대안**

- **아이콘을 직접 그리고 색 테마마다 따로 그린다.** 만들고 유지하는 비용이 크고 최소
  구현에 어긋나므로 배제한다. 단색 UI 아이콘은 색만 바꿔 쓴다.

**바로잡음** — 이 항목은 처음에 "이전 프로젝트의 '세 아이콘 테마'"를 배제 대상으로
적었으나 사실이 아니었다. 앞선 `tab-explorer-templates`의 세 테마는 직접 그린 세
벌이 아니라 **사용자가 고를 수 있는 세 가지 파일 아이콘 스타일**이었고, 그중 둘은
이미 라이브러리다(D-17).

---

## D-4. 터미널 같은 살아 있는 보기는 앱이 제공하고, 껍데기는 탭과 호스트 브릿지만 준다

**선택**

폴더 탐색기의 터미널처럼 살아 있는 세션 보기는 껍데기가 만들지 않는다. 껍데기는 그런
보기를 담을 탭 · 수명 관리 · 호스트 브릿지만 제공하고, 터미널 로직(xterm.js · PTY 등)과
"폴더 경로 → 그 경로의 터미널 열기" 매핑은 멀티터미널 앱(폴더 프리셋) 안에 둔다.

**근거**

"껍데기는 리소스 종류를 모른다"(INTENT). 터미널은 대상의 한 종류일 뿐이고, 껍데기가
터미널을 알면 이 원칙이 깨져 다음 앱이 같은 껍데기를 못 쓴다. VS Code의 "통합
터미널"(하단 Panel)을 만들지 않는다는 것과도 일치한다. 껍데기가 증명할 것은 "살아
있는 비-파일 보기를 탭이 담고 수명 동안 유지한다"까지다.

**배제한 대안**

- **껍데기가 통합 터미널을 기본 기능으로 넣는다.** 리소스 종류를 아는 코드가 껍데기로
  새고 VS Code식 하단 Panel을 재현하게 되므로 배제한다.

---

## D-5. 탭은 사용자가 추가할 때만 늘어난다 — 탐색기 선택은 현재 탭에서 연다

**선택**

- 탐색기에서 항목을 고르면 **현재 탭 영역의 활성 탭에서 열린다.** 탭 개수는 늘지
  않는다. 껍데기가 스스로 탭을 만드는 경우는 없다.
- 고른 항목이 **이미 다른 탭에 열려 있으면 그 탭으로 이동한다.** 파일과 폴더에 같은
  규칙이 적용된다.
- **새 탭은 사용자가 명시적으로 추가할 때만 생긴다.**
- 사용자가 방금 추가한 탭에서 이미 열려 있는 항목을 고르면, 이동하지 않고 **그 탭에
  새로 연다.** 즉 중복 검사는 탐색기 선택에만 걸리고, 사용자가 만든 탭에는 걸리지
  않는다.

**근거**

탭이 저절로 불어나면 사용자가 탭 줄을 정리하는 데 노력을 쓴다. 제1원칙인 최소·단순에
어긋난다. 고를 때마다 현재 탭에서 열리면 탭 줄은 사용자가 의도한 만큼만 길어진다.

중복 이동 규칙이 파일과 폴더에 똑같이 적용되므로 껍데기가 리소스 종류를 알 필요가
없다(INTENT 3절 "껍데기는 리소스 종류를 모른다").

사용자가 탭을 추가하는 행위 자체가 "하나 더 원한다"는 명시적 의사다. 그래서 그
자리에서는 중복 검사를 하지 않는다. 멀티터미널이 한 폴더에 터미널을 여러 개 두는
것이 이 규칙 하나로 성립하며, 껍데기에 터미널이나 세션을 아는 코드가 들어가지
않는다(D-4).

**배제한 대안**

- **탐색기에서 고를 때마다 새 탭을 연다.** 사용자가 조작하지 않았는데 탭이 불어나고,
  그것을 정리하는 부담이 사용자에게 넘어간다. 배제한다.
- **중복이면 언제나 기존 탭으로 이동한다(사용자가 추가한 탭에서도).** 같은 폴더에
  터미널 둘을 띄울 수단이 사라진다. 앱이 그것을 우회하려면 껍데기 밖에서 탭을 직접
  조작해야 하므로 배제한다.
- **대상 종류에 따라 이동과 새로 열기를 가른다**(파일은 이동, 세션은 새로). 껍데기가
  리소스 종류를 알게 되어 INTENT 3절과 어긋나므로 배제한다.
- **VS Code의 미리보기 탭·고정 탭을 도입한다.** VS Code는 한 번 고르면 미리보기 탭,
  두 번이면 고정 탭으로 두어 탭이 불어나는 것을 막는다. 위 규칙은 "탭은 사용자가
  추가할 때만 생긴다" 하나로 같은 문제를 풀며, 탭에 두 가지 상태를 더하지 않는다.
  최소·단순에 비추어 배제한다.

---

## D-6. 이번 버전에서 두 갈래를 함께 만든다 — 검증 앱이 한쪽에만 있어도 그렇다

**선택**

v0.1에서 pywebview 갈래와 Electron 갈래를 둘 다 만든다. 먼저 착수할 세 앱(마크다운
뷰어·파일 커맨터·멀티터미널)이 전부 Electron 쪽이고 pywebview 갈래를 쓰는 앱이 이번에
없더라도, 껍데기는 두 갈래로 낸다.

**근거**

이 껍데기 위에 올릴 앱은 두 종류가 다 나온다. 수치해석·시뮬레이션·머신러닝·딥러닝처럼
계산이 파이썬에 있는 작업이 계속 있고, 그런 앱은 화면을 파이썬 쪽에서 띄워야 하므로
pywebview 갈래를 요구한다(INTENT 5절). 지금 그 앱을 안 만든다는 것과 그 갈래가
필요 없다는 것은 다르다.

공통부를 한 갈래로만 완성하면 나중에 다른 갈래를 붙일 때 공통부를 다시 헤집게 된다.
그것은 "새 앱마다 드는 노력을 최소화한다"는 목적과 정반대다. 두 갈래를 처음부터
같이 두어야 갈래 차이가 공통 코어 밖으로 밀려난다.

검증에는 도메인 앱이 필요하지 않다. 완료 조건은 껍데기 자체의 동작으로 판정되며,
리소스 종류도 시험이 등록한 것으로 확인한다(BRIEF 완료 조건). 따라서 pywebview
갈래에 도메인 앱이 없어도 동등성을 판정할 수 있다.

**배제한 대안**

- **v0.1은 Electron만 만들고 pywebview는 다음 버전으로 미룬다.** 세 앱을 빨리 쓰는
  데는 유리하지만, 두 번째 갈래를 붙일 때 이미 완성된 공통부를 뒤늦게 갈라야 한다.
  갈래 차이가 공통 코어로 스며들 위험이 가장 큰 선택이라 배제한다.
- **pywebview 갈래를 아예 버리고 Electron 하나로 간다.** 파이썬 계산이 필요한 앱을
  이 껍데기 위에 올릴 수 없게 되어 INTENT 5절과 어긋나므로 배제한다.

---

## D-7. 화면은 다섯 조각이다 — 프레임리스로 가되 세로 띠는 남기고 가로 메뉴 줄만 없앤다

**선택**

창을 프레임리스로 띄우고, 화면을 아래 다섯 조각으로 구성한다.

1. **상단 바** — 운영체제가 그려 주던 제목 줄을 우리가 대신 그린다. 왼쪽에 메뉴를
   여는 햄버거 버튼(`≡`), 가운데에 창 제목, 오른쪽에 최소화·최대화·닫기.
   창 끌기와 크기 조절도 여기서 직접 처리한다.
2. **맨 왼쪽 세로 띠(활동 표시줄)** — 자주 쓰는 명령을 아이콘으로 세워 둔다. 무엇을
   놓을지는 D-11에서 정한다.
3. **왼쪽 탐색기** — VS Code의 탐색기 형태를 그대로 따른다. 제목줄과 뷰 액션 아이콘,
   그 아래 트리(D-9).
4. **오른쪽 탭 영역** — 탭 줄과 보기(D-8 · D-10).
5. **하단 바** — 지금 대상의 경로와 앱 정보.

가로로 늘어선 **메뉴 줄은 별도 줄로 두지 않는다.** 상단 바의 햄버거 버튼을 누르면
VS Code의 컴팩트 메뉴처럼 File·View·Help의 내용이 펼쳐진다.

**근거**

프레임리스를 고른 이유는 공간 최적화다. 다만 아끼는 방식이 방향마다 다르다.

*세로 방향은 아낀다.* 창틀과 가로 메뉴 줄은 각각 한 줄씩을 상시로 차지하면서 담는
정보가 거의 없다. 메뉴는 눌러야 볼 내용이지 늘 보고 있을 내용이 아니므로, 햄버거
버튼 하나로 접으면 높이를 통째로 돌려받는다.

*가로 방향의 세로 띠는 남긴다.* 이 자리는 접기 대상이 아니라 **자주 쓰는 명령을 한
번에 누르는 자리**다. 메뉴 안에 들어가면 두 번 눌러야 하는 것을 한 번에 눌 수 있게
하는 값어치가 폭 40~50px보다 크다. 메뉴와 세로 띠는 같은 것을 두 곳에 두는 중복이
아니라 **역할이 다르다** — 메뉴는 전부를 담고, 세로 띠는 자주 쓰는 것만 담는다.

VS Code의 세로 띠는 사이드바에 어떤 뷰를 띄울지 고르는 자리지만, 이 껍데기는 뷰가
탐색기 하나뿐이라 그 용도가 없다. 그래서 **용도를 바꿔 쓴다.** 생김새는 VS Code를
따르되 담기는 것이 다르다는 점을 D-11에 적어 둔다.

탐색기를 VS Code 형태 그대로 두는 것은 이 부분이 사용자가 가장 익숙하고, 새로
발명할 이유가 없는 자리이기 때문이다(INTENT 6절).

**배제한 대안**

- **세로 띠를 없애고 그 기능을 전부 상단 바와 단축키로 옮긴다.** 폭은 아끼지만 자주
  쓰는 명령이 메뉴 안으로 들어가 조작이 한 단계 늘어난다. 상단 바는 창 버튼과 제목이
  이미 차지하고 있어 아이콘을 여러 개 놓을 자리도 아니다. 배제한다.
- **File/View/Help를 가로 메뉴 줄로 상시 노출한다.** 프레임리스로 아낀 높이를 그대로
  반납하는데, 메뉴는 늘 보고 있을 내용이 아니다. 배제한다.
- **운영체제 창틀을 그대로 쓴다(프레임리스가 아니다).** 아무 정보도 담지 않는 제목
  줄이 한 줄을 차지한다. 배제한다.
- **하단 바도 없앤다.** 지금 보고 있는 대상의 경로는 실제로 쓰이고 높이가 한 줄이라
  손해가 작다. 배제한다.

---

## D-8. 칸은 가로·세로 모두 나눌 수 있다 — 2차원 배치는 dockview가 관리한다

**선택**

오른쪽 탭 영역을 여러 칸으로 나눌 수 있게 한다. **VS Code와 같이 가로(좌우)·세로(위아래)
모두 나눌 수 있고**, 칸 개수에 제한을 두지 않는다. 칸마다 자기 탭 줄을 가진다.

배치가 2차원 격자가 되면서 따라오는 것들 — 칸을 없앨 때 남은 칸이 자리를 나눠 갖는
것, 탭을 옮길 때 목표 칸을 정하는 것, 경계를 끌어 크기를 조절하는 것, 배치를 저장하고
복원하는 것 — 은 **직접 짜지 않고 dockview에 맡긴다(D-24).**

**근거**

멀티터미널이 한 폴더에서 AI 에이전트를 여러 개 돌리는 용도이므로, 셋 이상을 나란히
보는 일이 실제로 생긴다. 칸이 하나뿐이면 탭 전환으로만 오갈 수 있어 이 용도가
성립하지 않는다.

처음에는 세로 분할을 빼서 칸 배치를 **1차원 목록**으로 유지하려 했다. 2차원이 되면
"칸을 없앨 때 남은 칸을 어디에 붙이나", "탭을 옮길 때 목표 칸이 어디인가" 같은
규칙이 급격히 늘고, 앞선 프로젝트가 무너진 자리가 정확히 그 근처(분할 해제·탭
이동)이기 때문이다(BRIEF 1절).

그 위험은 그대로다. 다만 **그 규칙을 우리가 짜지 않기로 하면서** 판단이 바뀌었다.
dockview가 2차원 격자 배치를 이미 구현하고 있고 그 코드가 VS Code에서 나왔다(D-24).
검증된 구현을 가져다 쓴다면, 1차원으로 억지로 좁혀 VS Code와 다르게 동작하게 만들
이유가 없다. 이 껍데기의 기준은 "VS Code 방식을 따른다"이고(INTENT 6절), 분할도
그 방식을 따른다. 이 껍데기가 VS Code와 의도적으로 다른 것은 Zen 모드 하나뿐이다(D-12).

**배제한 대안**

- **가로로만 나누고 세로 분할은 두지 않는다(1차원 목록).** 규칙이 짧게 유지되는
  이점이 있으나, 그 규칙을 dockview가 이미 갖고 있어 우리가 떠안는 몫이 아니다.
  1차원으로 좁히면 VS Code와 다르게 동작하고 divergence가 하나 더 생긴다. 배제한다.
- **칸을 하나로 두고 탭 전환만 지원한다.** 가장 단순하지만 나란히 비교하는 용도와
  멀티터미널이 성립하지 않으므로 배제한다.
- **가로 두 칸까지만 허용한다(앞선 프로젝트 방식).** 터미널 셋을 나란히 두지 못한다.
  개수 제한이 규칙을 단순하게 만들어 주지도 않으므로 배제한다.
- **2차원 배치 규칙을 직접 짠다.** 앞선 프로젝트가 실패한 지점이라 배제한다(D-24).

---

## D-9. 탐색기 트리는 VS Code의 외형과 키보드 조작을 그대로 따른다

**선택**

왼쪽 탐색기의 **생김새와 키보드 동작을 VS Code와 같게** 만든다.

*외형* — 폴더 앞 펼침 화살표(`▸` 접힘 / `▾` 펼침), 확장자별 파일 종류 아이콘,
깊이를 나타내는 들여쓰기 안내선, 포커스 항목과 선택 항목의 구분 표시. 뷰 제목줄
오른쪽의 액션 아이콘(새로고침·모두 접기 등 — **어느 액션을 누가 하는지는 D-30**).

*키보드* — VS Code의 목록·트리 기본 키를 그대로 채택한다.

| 키 | 동작 | VS Code 명령 |
| --- | --- | --- |
| `↑` `↓` | 위아래 이동 | `list.focusUp` · `list.focusDown` |
| `→` | 펼치기 (이미 펼쳐졌으면 첫 자식으로) | `list.expand` |
| `←` | 접기 (이미 접혔으면 부모로) | `list.collapse` |
| `Ctrl+←` | 모두 접기 | `list.collapseAll` |
| `Space` | 펼침/접힘 뒤집기 | `list.toggleExpand` |
| `Enter` | 열기 | `list.select` |
| `Home` `End` | 처음 / 끝으로 | `list.focusFirst` · `list.focusLast` |
| `PageUp` `PageDown` | 한 화면씩 | `list.focusPageUp` · `list.focusPageDown` |
| `Ctrl+↑` `Ctrl+↓` | 포커스는 두고 스크롤만 | `list.scrollUp` · `list.scrollDown` |
| `Shift+↑` `Shift+↓` | 선택 범위 넓히기 | `list.expandSelectionUp` · `...Down` |
| `Ctrl+A` | 전체 선택 | `list.selectAll` |
| `Ctrl+Shift+Enter` | 지금 항목 선택 토글 | `list.toggleSelection` |
| `Escape` | 선택 해제 | `list.clear` |
| `F3` · `Ctrl+Alt+F` | 트리 안에서 찾기 | `list.find` |
| `Ctrl+Enter` | 옆 칸에 열기 | `explorer.openToSide` |

마우스의 `Ctrl+클릭` · `Shift+클릭` 다중 선택도 함께 지원한다.

**넣지 않는 것** — `Alt+↑/↓/Home/End`(`list.focusAny*`), `Ctrl+K Ctrl+I`(항목 설명
말풍선), `Shift+Alt+F`(폴더 안 검색). 앞의 둘은 쓰임이 드물고, 마지막은 검색 기능
자체가 범위 밖이다(BRIEF 3절).

**근거**

탐색기는 사용자가 가장 익숙한 부분이고, 여기서 다르게 만들 이유가 없다. 새로
발명하지 않는다는 INTENT 6절이 가장 직접적으로 적용되는 자리다. 키를 하나라도 다르게
두면 "왜 여기만 다른가"를 매번 판단해야 하는데, 그 판단 비용이 얻는 것보다 크다.

다중 선택은 취향이 아니라 요구다. **파일 커맨터**는 여러 파일을 골라 한 번에 다루는
도구이므로 다중 선택 없이는 성립하지 않는다.

이 트리는 VS Code가 따로 배포하지 않아 가져다 쓸 수 없다. 동작만 참고해 직접 짠다.
위 표가 그 구현의 판정 기준이 된다.

**배제한 대안**

- **최소 키만 둔다(위아래·펼침·열기 정도).** 익숙한 키가 안 먹는 순간 사용자는 이
  도구가 어디까지 되는지 매번 시험해야 한다. 익숙함을 그대로 물려받는다는 목적과
  어긋나므로 배제한다.
- **다중 선택을 빼고 한 번에 하나만 고르게 한다.** 파일 커맨터가 성립하지 않으므로
  배제한다.
- **트리를 다른 라이브러리로 가져온다.** VS Code 트리는 별도 배포되지 않고, 다른
  라이브러리는 위 키 조합과 외형이 달라 "동일하게"를 만족시키지 못한다. 배제한다.

**범위에 대한 메모** — 위 목록 중 `list.find`(트리 안에서 찾기)가 가장 비싸다.
범위를 줄여야 할 상황이 오면 여기서부터 덜어낸다.

---

## D-10. 탭 영역은 VS Code 방식을 따른다 — 단 탭이 늘어나는 규칙은 D-5가 우선한다

**선택**

오른쪽 탭 영역의 생김새와 조작을 VS Code와 같게 만든다. 칸마다 탭 줄이 있고, 활성
탭이 구분되며, 탭에 닫기 버튼이 있고, 탭을 끌어서 같은 줄에서 순서를 바꾸거나 다른
칸으로 옮길 수 있다. 칸은 **둘 이상** 만들 수 있다(D-8).

다만 **탭이 언제 새로 생기는가는 D-5를 따른다.** 탐색기에서 고르면 현재 탭에서
열리고, 이미 열려 있으면 그 탭으로 가며, 새 탭은 사용자가 추가할 때만 생긴다.
VS Code의 미리보기 탭·고정 탭은 두지 않는다.

**근거**

탭 줄의 생김새와 끌어 옮기기는 익숙한 대로 두는 것이 맞다. 그러나 "무엇을 열면 탭이
몇 개가 되는가"는 이 도구의 성격이 VS Code와 다른 지점이다 — VS Code는 편집기라 파일이
계속 쌓이지만, 이 껍데기 위의 앱들은 한 번에 보는 대상이 적고 터미널처럼 오래 사는
탭이 많다. 그래서 그 규칙만 D-5로 갈라 두었고, 그 판단을 여기서 뒤집지 않는다.

**배제한 대안**

- **VS Code의 탭 생성 규칙까지 그대로 따른다.** D-5를 폐기하게 되고, 미리보기 탭·고정
  탭이라는 두 가지 탭 상태가 다시 들어온다. 최소·단순에 어긋나므로 배제한다.
- **탭을 끌어 옮길 수 없게 한다.** 칸을 나눠 놓고 탭을 옮길 수 없으면 칸을 나눈 뜻이
  절반으로 줄고, 터미널처럼 닫으면 죽는 탭은 우회할 방법도 없다. 배제한다.

---

## D-11. 세로 띠는 뷰를 고르는 자리가 아니라 자주 쓰는 명령을 담는 자리다

**선택**

맨 왼쪽 세로 띠(활동 표시줄)를 둔다. 다만 VS Code처럼 "사이드바에 어떤 뷰를 띄울지
고르는" 용도로 쓰지 않는다. 이 껍데기는 뷰가 탐색기 하나뿐이라 고를 대상이 없다.
대신 **자주 쓰는 명령을 아이콘 하나로 누르는 자리**로 쓴다.

지금 정해진 항목은 위쪽부터 탐색기 접기/펴기, 상단 바 감추기/보이기, 하단 바
감추기/보이기(D-14), 좌우 스플릿, 상하 스플릿(D-13), Zen 모드(D-12)다. 아래쪽에 테마
바꾸기를 둔다(VS Code가 설정을 띠 아래쪽에 두는 자리와 같다). **칸 삭제·칸 합치기
아이콘은 두지 않는다** — 칸은 탭을 모두 비우면 사라지고(D-26), 합치기는 VS Code에
있으나 이 껍데기는 노출하지 않는다.

메뉴와 겹치는 것은 중복이 아니다. **메뉴는 전부를 담고 세로 띠는 자주 쓰는 것만
담는다.** 어떤 명령이 자주 쓰이는지는 쓰면서 바뀔 수 있으므로 항목은 늘거나 줄 수
있다. 늘고 주는 것이 껍데기 코드를 고치는 일이 되지 않도록, 띠에 놓을 항목은
갈아끼우는 자리에서 정한다.

**근거**

메뉴 안에 들어가면 두 번 눌러야 하는 것을 한 번에 누를 수 있다. 탐색기 접기처럼
하루에 수십 번 누르는 것은 그 차이가 크다.

띠의 항목을 껍데기가 고정하면 앱마다 자주 쓰는 명령이 다른데도 같은 아이콘을 이고
가야 한다. "껍데기는 리소스 종류를 모른다"(INTENT 3절)와 같은 이유로, 무엇을 놓을지는
앱이 정하게 한다. 껍데기는 띠와 그 자리만 준다.

**배제한 대안**

- **VS Code처럼 뷰 전환 버튼을 놓는다.** 뷰가 탐색기 하나뿐이라 전환할 대상이 없다.
  배제한다.
- **띠의 항목을 껍데기가 고정한다.** 앱마다 자주 쓰는 명령이 다르므로 배제한다.
- **띠를 두지 않고 전부 메뉴로 보낸다.** D-7에서 배제했다.
- **칸 삭제·칸 합치기 아이콘을 둔다.** 칸은 탭을 모두 닫으면 사라지므로(D-26) 삭제
  아이콘은 중복이다. 합치기는 어느 칸과 합치나·탭 순서는 어떻게 되나 하는 규칙을
  새로 만든다. 둘 다 배제한다.

---

## D-12. 탐색기 접기와 Zen 모드를 둔다 — Zen은 전체화면이 아니라 탭과 보기만 남긴다

**선택**

**탐색기 접기** — 왼쪽 탐색기를 접었다 폈다 한다. 세로 띠의 첫 아이콘과 단축키로
누른다. 접으면 탐색기 자리가 사라지고 그만큼 탭 영역이 넓어진다. 세로 띠는 남는다.

**Zen 모드** — 창을 전체화면으로 만들지 않는다. 대신 **상단 바 · 세로 띠 · 탐색기 ·
그 사이 경계 · 하단 바를 모두 감추고, 탭 줄과 보기만 남긴다.** `F11`로 켜고 끄며
`Escape`로도 빠져나온다.

**근거**

앞선 `tab-explorer-templates`가 이미 이 방식이었고(`shell/shell.css`의 zen 규칙이
메뉴 줄·활동 표시줄·사이드바·스플리터·상태 표시줄을 감춘다), 실제로 쓰면서 불편이
없었다. 새로 발명할 이유가 없다.

전체화면으로 바꾸지 않는 것이 핵심이다. 전체화면은 운영체제 창 상태를 건드려서
빠져나오는 방법이 갈래마다 달라지고, 여러 창을 나란히 두고 쓰는 작업과 맞지 않는다.
이 도구의 Zen은 "창 크기는 그대로 두고 군더더기만 없앤다"이다.

탭 줄을 남기는 것도 중요하다. 멀티터미널에서 탭 줄까지 감추면 터미널 사이를 오갈
방법이 단축키밖에 없어진다.

**배제한 대안**

- **VS Code처럼 전체화면으로 전환하고 탭 줄까지 감춘다.** 창 상태를 건드리는 것이
  두 갈래에서 다르게 동작할 여지를 만들고, 탭 줄이 없으면 멀티터미널에서 오갈 수단이
  사라진다. 배제한다.
- **탐색기 접기만 두고 Zen 모드는 두지 않는다.** 접어도 세로 띠·상단 바·하단 바가
  남아 보기에 온전히 집중할 수 없다. 배제한다.
- **Zen에서 탐색기만 감춘다.** 그것은 탐색기 접기와 같아져 별도 기능일 이유가 없다.
  배제한다.

---

## D-13. 새 칸은 오른쪽 또는 아래쪽에 생긴다 — 방향마다 아이콘이 있다

**선택**

새 칸을 만드는 방향은 **오른쪽과 아래쪽 둘**이다. VS Code의 좌우·상하 분할과 같되,
이 껍데기는 이 두 방향만 노출한다. 만든 뒤에는 칸을 끌어서 자리를 바꿀 수 있다
(dockview — D-24).

부르는 자리는 넷이다. 전부 VS Code에 있는 방식이다.

1. **각 칸 탭 줄 오른쪽 끝의 두 아이콘** — 좌우 스플릿 `[|]` 정사각형 안 세로줄
   (codicon `split-horizontal`), 상하 스플릿 `[--]` 정사각형 안 가로줄
   (codicon `split-vertical`, codicons에 없으면 `split-horizontal`을 90° 돌려 쓴다).
   그 오른쪽에 새 탭 `+`(D-15).
2. **세로 띠**(D-11) — 같은 두 아이콘.
3. **View 메뉴**(D-23) — "좌우 스플릿", "상하 스플릿".
4. **탭을 편집기 가장자리로 끌기** — 오른쪽 가장자리에 떨구면 오른쪽에, 아래쪽
   가장자리에 떨구면 아래쪽에 새 칸이 생긴다.

```
┌────────────────────────────────────────────┐
│  tab   tab   tab                 [|] [-] +  │
└────────────────────────────────────────────┘
   [|] 좌우 스플릿 → 오른쪽에 새 칸
   [-] 상하 스플릿 → 아래쪽에 새 칸
   +   새 탭 (D-15)
```

**탭 제목 우클릭 메뉴에는 칸 명령을 넣지 않는다**(D-13 배제한 대안 · D-22).

탭에는 닫기 버튼이 있다(D-10). 칸을 명시적으로 닫는 길은 없다 — 칸의 탭이 모두
닫히면 그 칸이 사라지고, 마지막 한 칸은 빈 채로 남는다(D-26).

**근거**

VS Code 동작을 기본으로 삼는다(이 껍데기의 divergence는 Zen 모드 하나뿐 — D-12).
VS Code는 좌우·상하로 나누고, 각 편집기 그룹 오른쪽 위에 분할 아이콘을 두며, 탭을
가장자리로 끌어 새 그룹을 만든다. 그대로 따른다.

방향을 오른쪽·아래쪽 둘로만 좁힌 것은 왼쪽·위쪽 분할이 결국 같은 배치를 만드는
중복 조작이기 때문이다. 끌어서 자리를 바꿀 수 있으므로(dockview) 만들 때 네 방향을
다 줄 필요가 없다.

아이콘을 방향마다 하나씩 둔 것은 "지금 어느 방향으로 나뉘는가"가 누르기 전에 보여야
하기 때문이다. VS Code처럼 한 아이콘에 `Alt`를 섞어 직교 방향을 내는 방식은 숨은
조작이라 두지 않는다.

`[|]`·`[--]`와 `+`를 나눈 것은 뜻이 섞이지 않게 하기 위해서다. `+`는 브라우저에서
"새 탭"으로 굳은 기호라 칸에 쓰면 새 탭 버튼이 갈 자리가 없어진다.

**배제한 대안**

- **오른쪽 한 방향으로만, 방향 선택 없이.** 처음에 이렇게 정했다(1차원 배치 — 옛 D-8).
  D-8이 2차원 배치를 dockview에 맡기기로 바뀌면서 근거가 사라졌다. VS Code와 다르게
  동작하고, 아래쪽으로 나란히 보고 싶을 때 길이 없다. 배제한다.
- **네 방향(좌·우·상·하) 아이콘을 모두 둔다.** 왼쪽·위쪽은 끌어서 자리를 바꾸면
  같은 결과라 아이콘이 하는 일이 겹친다. 배제한다.
- **한 아이콘 + `Alt`로 직교 방향(VS Code 방식).** 누르기 전에 방향이 안 보이는 숨은
  조작이다. 배제한다.
- **탭 제목 우클릭 메뉴에도 칸 명령을 넣는다.** 같은 명령으로 가는 길이 넷을 넘고,
  우클릭 메뉴는 그 탭 자체에 대한 명령을 담는 자리로 남기는 편이 낫다. 배제한다.
- **탭을 끌어 칸을 만드는 것은 두지 않는다.** VS Code에 있고 dockview가 이미 지원한다.
  굳이 뺄 이유가 없다. 배제한다.
- **칸을 끌어 놓기로 만든다(탭을 가장자리로 끌면 칸이 생긴다).** 발견하기 어렵고
  실수로 칸이 생기기 쉽다. 끌어 놓기는 이미 있는 것을 옮기는 데만 쓴다. 배제한다.

---

## D-14. 상단 바와 하단 바를 각각 따로 감출 수 있다

**선택**

세로 띠의 기본 항목에 둘을 더한다 — **상단 바 감추기/보이기**, **하단 바
감추기/보이기**. 각각 독립이며 서로에게 영향을 주지 않는다.

Zen 모드(D-12)와 별개다. Zen은 한 번에 전부 감추고, 이 둘은 하나씩 감춘다.

**근거**

Zen은 전부 아니면 전무다. "탐색기는 그대로 두고 아래 줄만 없애고 싶다"거나 "제목
줄만 없애 세로를 한 줄 더 벌고 싶다" 같은 중간 상태가 실제로 자주 필요한데, Zen으로는
그것을 만들 수 없다.

**배제한 대안**

- **Zen 모드 하나로 충분하다고 본다.** 중간 상태를 만들 수 없어 배제한다.
- **감추기 설정을 메뉴에만 둔다.** 상황에 따라 자주 바꾸는 것이라 한 번에 눌러야
  한다. 배제한다.

**세로 띠는 감출 수 없다.** 이 둘을 포함한 감추기 아이콘이 모두 세로 띠에 있으므로,
띠까지 감출 수 있으면 되돌릴 길이 없어진다. 세로 띠가 사라지는 경우는 Zen 모드
하나뿐이고, Zen에는 `Escape`라는 탈출구가 있다(D-12).

이것이 앞서 열려 있던 문제를 닫는다 — 상단 바를 감추면 프레임리스라서 창
버튼(최소화·최대화·닫기)과 창을 끄는 자리가 함께 사라지는데, 세로 띠가 항상 보이므로
**아이콘 한 번으로 상단 바를 되돌릴 수 있다.** 창을 옮기거나 닫아야 하면 그렇게 한다.

**배제한 대안 (이어서)**

- **세로 띠도 감출 수 있게 한다.** 감추기 아이콘이 전부 거기 있어서, 감추는 순간
  되돌릴 수단이 사라진다. 배제한다.

---

## D-15. 새 탭은 항상 탭 줄 오른쪽 끝에 붙는다 — 위치는 나중에 끌어서 바꾼다

**선택**

새 탭이 생기는 자리는 **언제나 그 칸 탭 줄의 오른쪽 끝**이다. 방향을 고르는 선택지를
두지 않는다.

대신 **마우스로 끌어서** 두 가지를 한다 — 같은 탭 줄 안에서 순서 바꾸기, 다른 칸으로
옮기기(D-10).

**근거**

새 탭이 생길 자리가 하나로 고정되면 사용자가 눈으로 찾을 필요가 없다. 현재 탭 바로
오른쪽에 끼워 넣는 방식은 활성 탭이 어디냐에 따라 생기는 자리가 매번 달라져서, 방금
만든 탭을 다시 찾아야 한다.

위치를 고르는 선택지를 만들 이유도 없다. 순서가 마음에 안 들면 끌어서 옮기면 되고,
그 조작이 이미 있기 때문이다. 만들 때 방향을 정하는 것과 만든 뒤 옮기는 것 중 하나만
있으면 되는데, 옮기기는 어차피 필요하다.

칸은 다르다. 칸도 만든 뒤 끌어서 자리를 바꿀 수 있지만(dockview), 만들 때에도
방향(오른쪽·아래쪽)을 고른다 — VS Code가 그렇기 때문이다(D-13).

**배제한 대안**

- **현재 탭 바로 오른쪽에 끼워 넣는다.** 생기는 자리가 그때그때 달라진다. 배제한다.
- **새 탭도 왼쪽·오른쪽을 고르게 한다.** 매번 판단해야 하는데, 끌어서 옮길 수 있으므로
  얻는 것이 없다. 배제한다.
- **탭 순서를 고정하고 끌어서 바꾸지 못하게 한다.** 새 탭 자리를 오른쪽 끝 하나로
  정한 것이 그대로 불편으로 남는다. 배제한다.

---

## D-16. 탐색기 루트는 하나뿐이고, 바꾸는 방법은 폴더 열기 하나다

**선택**

트리 맨 위에 놓이는 최상위 폴더는 **항상 하나**다. 여러 폴더를 한 창에 나란히 묶지
않는다.

루트를 바꾸는 방법은 **폴더 열기(`Ctrl+O`)** 하나뿐이다. 다른 폴더를 열면 트리 전체가
그 폴더로 바뀐다. 최근에 연 폴더 목록은 메뉴에 둔다.

**근거**

VS Code는 여러 폴더를 한 창에 묶는 기능이 있다. 그것을 지원하면 "이 파일이 어느
루트에 속하나"가 트리·탭·경로 표시·설정 저장 등 거의 모든 규칙에 따라붙는다. 먼저
만들 세 앱 중 이것을 필요로 하는 것이 없다.

루트를 바꾸는 길을 하나로 두면 "지금 트리가 어느 폴더를 보고 있나"가 언제나 한 가지
조작의 결과다. 길이 여럿이면 그 상태가 어떻게 만들어졌는지 추적이 어려워진다.

`Ctrl+O`는 폴더·파일 열기의 관습적인 키다. 새로 정할 이유가 없다.

**배제한 대안**

- **여러 폴더를 한 창에 묶는다(VS Code의 멀티 루트).** 지금 필요 없는 복잡도를
  모든 규칙에 얹는다. 배제한다.
- **트리 안에서 상위 폴더로 거슬러 올라가 루트를 바꾼다.** 루트가 바뀌는 경로가
  둘이 되고, 실수로 루트가 바뀌기 쉽다. 배제한다.

---

## D-17. 파일 종류 아이콘은 VS Code 계열 두 세트만 쓴다

**선택**

파일 종류 아이콘은 **사용자가 고를 수 있는 두 가지 스타일**로 둔다. 둘 다 VS Code
쪽에서 온 것이며, 직접 그린 것은 쓰지 않는다.

| 이름 | 정체 | 성격 |
| --- | --- | --- |
| 기본 | **seti** (VS Code의 기본 파일 아이콘 테마) | 글꼴 글리프 + 색. 가볍고 차분하다 |
| 컬러 | **vscode-icons** | 확장자별 컬러 SVG. 종류가 한눈에 구분된다 |

앞선 `tab-explorer-templates`에는 세 번째로 "단순"(껍데기가 직접 그린 폴더·파일
아이콘 한 벌)이 있었다. **그것은 쓰지 않는다.**

**고르는 자리는 View 메뉴 하나다**(D-23). 세로 띠에는 두지 않는다 — 색 테마만큼
자주 바꾸는 것이 아니다.

껍데기는 어느 확장자가 어느 아이콘인지 알지 않는다. 트리 행의 이름과 펼침 여부만
넘기고 "무엇을 어떻게 그릴지"를 서술한 값 하나를 돌려받는 방식을 유지한다.

색 테마(흰색·회색·검정) 세 가지와는 **별개의 축**이다. 색 테마 세 개 × 아이콘 테마
두 개이지 아이콘을 색마다 따로 그리는 것이 아니다.

**근거**

D-3이 "아이콘을 직접 그리지 않는다"고 정했다. "단순"은 그 원칙에서 유일하게 벗어나
있던 자산이고, 이것까지 빼면 **손으로 그린 아이콘이 하나도 남지 않는다.** 만들고
유지하고 세 색 테마에 맞추는 일이 통째로 사라진다.

기준을 VS Code에 두는 것과도 맞다(INTENT 6절). 두 세트 모두 VS Code 사용자가 이미
보던 것이라, 새로 익힐 것이 없다.

둘을 남기는 이유는 성격이 다르기 때문이다. 확장자별 컬러 아이콘이 도움이 될 때가
있고 오히려 산만할 때가 있다. 폴더만 다루는 앱(파일 커맨터·멀티터미널)에서는 차분한
쪽이 낫다. 그 자리를 "단순" 대신 **seti**가 맡는다.

**라이선스** — 세트마다 다르다. 자세한 것은 [`refs/licenses.md`](refs/licenses.md).

- **seti** — MIT. 라이선스 본문을 함께 넣는다.
- **vscode-icons** — **코드는 MIT지만 아이콘은 CC BY-SA**이고, 브랜드 아이콘(회사
  로고 등)은 각자의 저작권 라이선스를 따른다. 저작자 표시가 필요하고, 아이콘을 고쳐
  쓰면 같은 조건으로 공개해야 한다. 고치지 않고 그대로 쓰면 표시로 충분하다.

앞선 프로젝트는 두 세트를 쓰면서 라이선스 본문을 함께 넣지 않았다. 이번에 채운다.

**바로잡음** — 이 항목은 처음에 "seti와 vscode-icons는 둘 다 MIT"라고 적었으나
사실이 아니었다. vscode-icons의 아이콘은 CC BY-SA다.

**배제한 대안**

- **직접 그린 "단순" 테마를 남긴다(앞선 프로젝트 방식).** 가장 가볍고 라이선스도
  걸리지 않는다는 이점이 있으나, 직접 그리지 않는다는 원칙(D-3)의 유일한 예외로 남고
  세 색 테마에 맞춰 유지해야 한다. 차분한 아이콘이 필요한 자리는 seti가 대신한다.
  배제한다.
- **하나로 고정한다(예: vscode-icons만).** 확장자 아이콘이 산만한 앱에서 끌 수 없다.
  배제한다.
- **VS Code 계열이 아닌 다른 아이콘 세트를 고른다.** 기준을 VS Code에 두기로 한
  INTENT 6절과 어긋나고, 사용자가 새로 익혀야 한다. 배제한다.

**남은 판단** — CC BY-SA가 부담이면 "컬러" 테마만 다른 세트로 바꿀 수 있다. 두 테마는
서로 독립이므로 "기본"에는 영향이 없다.

---

## D-18. 텍스트·코드 보기와 편집기를 껍데기가 기본으로 안고 간다 (monaco)

**선택**

VS Code의 편집 화면을 떼어낸 **monaco**를 껍데기의 기본 부품으로 반입한다. 앱이
따로 가져오지 않는다.

*켜는 기능* — 문법에 따른 색칠, 줄 번호, 찾기·바꾸기, 되돌리기/다시하기, 읽기 전용
모드. *끄는 기능* — 자동완성, 언어 서비스(오류 표시·정의로 이동), 미니맵, 다중 커서.
끄는 것들은 개발 도구의 기능이지 이 껍데기 위의 앱들이 쓰는 기능이 아니다(제1원칙).

*편집에 딸려 오는 것* — 저장, 탭에 고쳐짐 표시(●), 저장 안 한 탭을 닫거나 앱을 끌 때
경고. **탭에 표시를 찍는 것은 껍데기가, 무엇을 어떻게 저장할지는 앱이 맡는다.**
껍데기는 "이 탭은 고쳐졌다 / 저장됐다"는 상태만 받아 그린다.

**근거**

먼저 만들 세 앱 중 **파일 커맨터가 이것을 요구한다.** Total Commander처럼 두 칸을
두고 한쪽에서 고른 파일을 다른 쪽에서 보기(`F3`) · 편집(`F4`)하는 것이 그 앱의 핵심
동작이다. 마크다운 뷰어도 편집으로 갈 수 있다.

터미널과 성격이 다르다. 터미널은 멀티터미널 **한 앱만** 쓰지만, 텍스트를 보고 고치는
일은 이 껍데기 위에 올릴 앱들에서 **반복해서** 나온다. 앞으로 만들 앱이 파이썬
계산을 다루는 쪽이라도 설정 파일이나 스크립트를 열어 보는 일이 생긴다. 앱마다 같은
것을 따로 붙이면 "새 앱마다 드는 노력을 최소화한다"는 목적과 어긋난다.

`INTENT.md` 4절의 "범용 코드 편집기를 만들지 않는다"와는 어긋나지 않는다. 그 문장이
막는 것은 **편집기 제품을 만드는 것**이지, 앱들이 공유하는 부품으로 편집 화면을 두는
것이 아니다. 위에서 끄기로 한 기능들(자동완성·언어 서비스)이 그 경계다.

**배제한 대안**

- **앱이 가져온다(D-4에서 터미널을 다룬 방식).** 껍데기가 더 작게 유지되고 편집기를
  안 쓰는 앱이 그 무게를 지지 않는다는 이점이 분명하다. 그러나 여러 앱이 같은 것을
  반복해서 붙이게 되고, 그때마다 저장·고쳐짐 표시·닫을 때 경고를 각자 다시 만들어야
  한다. 터미널과 달리 쓰는 앱이 여럿이므로 배제한다.
- **껍데기에 넣되 앱이 켜고 끄게 한다.** 켜고 끄는 것 자체가 껍데기에 분기를 하나
  더한다. 배제한다.
- **monaco 대신 단순한 텍스트 영역을 직접 만든다.** 문법 색칠·찾기·되돌리기를 다시
  만드는 비용이 크고 D-2에 어긋나므로 배제한다.

**남은 판단** — 켜고 끄는 기능 목록은 위가 출발점이며, 실제로 써 보고 조정한다.
마크다운 뷰어를 편집까지 갈지는 그 앱의 결정이지 껍데기의 결정이 아니다.

---

## D-19. 앱이 "옆 칸에 열기"를 부를 수 있고, 탭 안의 키는 앱에게 간다

**선택**

파일 커맨터의 `F3` · `F4`를 성립시키려면 껍데기가 두 가지를 열어 주어야 한다.

**1. 옆 칸에 열기** — 앱이 "이 대상을 옆 칸의 탭에서 열어라"를 코드로 부를 수 있다.
사용자가 트리에서 `Ctrl+Enter`를 눌렀을 때와 같은 동작이며(D-9), 그 명령을 앱도
쓸 수 있게 여는 것이다. 옆 칸이 없으면 새로 만든다(오른쪽에 — D-13).

**2. 탭 안의 키는 앱에게 간다** — 포커스가 탭 안에 있을 때, 껍데기가 쓰지 않는 키는
그 탭의 보기에게 넘어간다. 파일 커맨터의 `F3`(보기) · `F4`(편집) · `F5`(복사) ·
`F6`(이동)과 여러 개 고르기의 `Space`가 여기 해당한다. 껍데기가 먼저 가져가는 키는
문서로 정해 두고, 그 밖은 전부 앱 몫이다.

`Space`는 트리에서 펼침/접힘이지만(D-9) 그것은 **포커스가 트리에 있을 때**다. 포커스가
탭 안에 있으면 트리 키 묶음이 적용되지 않으므로 부딪히지 않는다.

목록은 **가져가는 키**만 적지 않고, 흔히 껍데기 것이라고 오해하는 키를 **가져가지
않는다**고 함께 적는다. `Ctrl+S`(저장) · `F2`(이름 바꾸기) · `Delete`(삭제) ·
`Ctrl+Z` / `Ctrl+Y`(되돌리기·다시하기) · `Ctrl+C` / `Ctrl+V`가 그것이다. 저장은 앱이
하고(D-18), 만들기·이름 바꾸기·삭제도 앱이 한다(D-30 · D-22). 껍데기가 이 키들을
가져가면 그 순간 리소스 종류를 아는 판단이 껍데기로 들어온다. 적어 두지 않으면 앱마다
다르게 정하거나, 나중에 껍데기가 슬그머니 가져가게 된다.

**3. 앱이 칸 배치를 물을 수 있다** — 앱이 "지금 내가 어느 칸에 있나", "옆 칸에는
무엇이 열려 있나"를 물을 수 있다. 파일 커맨터가 고른 파일을 **반대편 칸의 폴더로**
복사·이동하려면 반대편이 어느 폴더를 보고 있는지 알아야 하는데, 칸 배치는 껍데기만
안다.

**근거**

파일 커맨터의 핵심 동작이 "왼쪽에서 고른 것을 오른쪽에서 본다"인데, 지금 껍데기는
**탐색기에서 고르는 경우만** 다룬다(D-5). 탭 안의 목록에서 고르는 경우는 앱의 일이고,
그 결과를 옆 칸에 띄우는 것은 껍데기만 할 수 있다. 이 자리가 열려 있지 않으면 앱이
껍데기 밖에서 탭을 직접 건드려야 하고, 그 순간 "공통 코어 수정 0줄"이 깨진다.

복사·이동도 같다. "반대편 칸으로"라는 말 자체가 칸 배치를 아는 쪽만 할 수 있는
판단이다. 다만 껍데기는 **어느 칸에 무엇이 열려 있는지만 알려주고**, 복사와 이동
자체는 하지 않는다. 파일을 옮기는 일은 리소스 종류를 아는 앱의 몫이다(INTENT 3절).

키도 같다. 껍데기가 모든 키를 가져가면 앱이 자기 단축키를 가질 수 없다. 반대로
앱이 모든 키를 가져가면 트리 조작(D-9)이 깨진다. 그래서 **껍데기가 가져가는 키를
목록으로 정하고 나머지를 넘기는** 방식으로 가른다.

**배제한 대안**

- **앱이 껍데기의 탭·칸을 직접 조작한다.** 껍데기의 내부 상태가 앱에 노출되어 규칙이
  깨질 자리가 열린다. 배제한다.
- **`F3` · `F4`를 껍데기의 키로 정한다.** 껍데기가 "보기"와 "편집"이라는 앱의 개념을
  알게 되어 "껍데기는 리소스 종류를 모른다"(INTENT 3절)에 어긋난다. 배제한다.
- **키를 앱이 먼저 받고 안 쓰면 껍데기로 넘긴다.** 앱이 실수로 트리 키를 삼키면
  탐색기가 죽는데 원인을 찾기 어렵다. 껍데기 우선이 안전하다. 배제한다.

---

## D-20. 빈 상태에는 아무것도 그리지 않는다

**선택**

폴더를 아직 열지 않았을 때의 트리 자리, 사용자가 방금 추가한 빈 탭의 안쪽, 그리고
마지막 칸의 탭이 모두 닫혔을 때 남는 그 칸(D-26)에는 **아무것도 그리지 않는다.**
안내 문구도 버튼도 두지 않고 빈 면으로 둔다.

VS Code의 환영 뷰(Welcome View)에 해당하는 것을 만들지 않는다.

**근거**

폴더를 여는 길은 이미 하나로 정해져 있다 — 메뉴의 폴더 열기와 `Ctrl+O`(D-16). 빈
트리에 [폴더 열기] 버튼을 두면 같은 일로 가는 길이 둘이 된다. D-16이 "루트를 바꾸는
길은 하나"로 정한 것과 어긋난다.

안내 문구도 만들고 유지해야 하는 것이다. 문구를 정하고, 세 색 테마에 맞추고, 두
갈래에서 같게 보이는지 확인해야 한다. 제1원칙(최소 구현)에 비추어 그만한 값어치가
없다고 본다.

**완료 조건과의 관계** — BRIEF의 "빈 상태" 시나리오는 이 결정으로 판정 가능해진다.
정해진 결과는 **"빈 면이 나오고 아무 동작도 일어나지 않는다"**이다. 무엇이 그려져야
하는지가 아니라 무엇도 그려지지 않아야 하는지가 기준이 된다.

**배제한 대안**

- **한 줄 안내와 버튼 하나를 둔다(VS Code의 환영 뷰에 해당).** 처음 쓰는 사람이
  무엇을 눌러야 할지 알 수 있다는 이점이 있으나, 폴더 여는 길이 둘이 되고 유지할
  것이 늘어난다. 배제한다.
- **빈 트리에 최근에 연 폴더 목록을 보인다.** 편하지만 빈 화면이 아닌 것을 하나 더
  만드는 셈이고, 최근 폴더는 이미 메뉴에 있다(D-16). 배제한다.

---

## D-21. 실패도 진행도 하단 바 한 줄로만 알린다

**선택**

오류·경고와 오래 걸리는 작업의 진행 표시를 **모두 하단 바(상태 표시줄)에 한 줄로**
보인다. 오른쪽 아래에 떠올랐다 사라지는 알림 상자를 만들지 않는다. 알림이 쌓이는
목록이나 지난 알림을 다시 보는 자리도 두지 않는다.

**근거**

떠오르는 알림 상자를 만들려면 그 상자를 그리고, 겹칠 때 쌓고, 시간이 지나면 지우고,
세 색 테마에 맞추고, 두 갈래에서 같게 보이는지 확인해야 한다. 하단 바는 **이미
있는 자리**라 새로 만들 것이 없다.

화면을 가리지 않는 것도 이유다. 오른쪽 아래는 탭 영역 위에 겹치는데, 터미널이나
파일 목록의 아래쪽이 자주 가려진다.

**받아들이는 단점** — 하단 바는 눈에 덜 띄어서 오류를 놓칠 수 있다. 이것을 알고
고른다. 놓치는 것이 실제로 문제가 되면 그때 알림 상자를 더한다.

**배제한 대안**

- **오류는 오른쪽 아래 짧은 메시지로, 진행은 하단 바로 나눈다.** 실패가 눈에 잘
  띈다는 이점이 분명하지만, 그리는 장치를 하나 더 만들어야 하고 탭 영역을 가린다.
  배제한다.
- **껍데기는 자리만 주고 문구와 모양은 앱이 정한다(D-4 · D-11 방식).** 알림의 생김새가
  앱마다 달라지면 같은 껍데기를 쓴다는 느낌이 깨진다. 무엇을 알릴지는 앱이 정하되
  **어떻게 보일지는 껍데기가 정한다.** 배제한다.
- **알림이 쌓이는 목록을 둔다(VS Code의 알림 센터).** 지난 알림을 다시 보는 요구가
  아직 없다. 배제한다.

---

## D-22. 우클릭 메뉴는 껍데기가 장치만 주고 항목은 앱이 채운다

**선택**

트리의 항목 위, 탭 제목 위에서 오른쪽 버튼을 눌렀을 때 뜨는 메뉴를 둔다. 껍데기는
**메뉴를 띄우고 그리는 장치만** 제공한다 — 어디에 뜨는지, 어떻게 생겼는지, 어떻게
닫히는지, 키보드로 어떻게 오르내리는지.

**무슨 항목이 들어갈지는 앱이 정한다.** 껍데기가 기본으로 넣는 항목은 없다.

**기본값은 "사용 안 함"이다.** 껍데기만 있는 상태에서는 오른쪽 버튼을 눌러도 아무
일이 일어나지 않는다. 앱이 항목을 정의하고, 사용자가 메뉴에서 켜야 뜬다. 켜고 끄는
항목은 View 묶음에 둔다(D-23).

**근거**

무슨 항목이 뜨는지는 앱마다 다르다. 파일 커맨터는 보기·편집·복사·삭제가 필요하고,
마크다운 뷰어는 옆 칸에 열기 정도면 된다. 껍데기가 이것을 알면 "껍데기는 리소스
종류를 모른다"(INTENT 3절)가 깨지고, 다음 앱이 쓰지 않는 항목을 이고 가게 된다.
터미널(D-4)과 세로 띠 항목(D-11)을 다룬 방식과 같다.

생김새는 껍데기가 정한다. 알림에서와 같은 이유다 — **무엇을 담을지는 앱이, 어떻게
보일지는 껍데기가**(D-21).

기본 항목을 하나도 넣지 않는 이유는 D-13에서 칸 명령을 뺐기 때문이다. 그러고 나면
껍데기가 넣을 만한 것으로 탭의 "닫기"가 남는데, 탭에는 이미 닫기 버튼이 있어
같은 일로 가는 길이 둘이 된다.

**배제한 대안**

- **껍데기가 탭 쪽에 "닫기 / 다른 탭 모두 닫기"를 기본으로 넣는다.** 모든 앱에서 같은
  항목이 보이는 일관성은 있으나, 껍데기가 탭 메뉴의 내용을 알게 되고 닫기로 가는 길이
  둘이 된다. 배제한다.
- **우클릭 메뉴를 아예 두지 않는다.** 파일 커맨터가 항목에 대고 무언가를 하는
  도구라, 그 명령을 전부 키나 도구 막대 버튼으로만 둬야 한다. 배제한다.
- **기본값을 "사용함"으로 둔다.** 앱이 항목을 정의하지 않았는데 오른쪽 버튼을 누르면
  빈 메뉴가 뜨거나 아무 반응이 없어, 고장인지 원래 그런지 구별되지 않는다. 꺼 두면
  "안 되는 것"이 정해진 결과가 된다. 배제한다.
- **생김새까지 앱이 정한다.** 앱마다 메뉴가 달라 보이면 같은 껍데기를 쓴다는 느낌이
  깨진다. 배제한다.

---

## D-23. 메뉴는 File · View · Help 세 묶음, 하단 바는 경로와 앱 정보

**선택**

*메뉴* — 상단 바의 햄버거 버튼(`≡`)을 누르면 세 묶음이 나온다.

| 묶음 | 항목 |
| --- | --- |
| **File** | 폴더 열기(`Ctrl+O`) · 최근 폴더 · 끝내기 — **D-31에서 탭 닫기(`Ctrl+W`) · 폴더 닫기를 더해 다섯이 되었다** |
| **View** | 탐색기 접기/펴기 · 상단 바 감추기/보이기 · 하단 바 감추기/보이기 · Zen 모드 · 테마 바꾸기 · **아이콘 테마 바꾸기**(D-17) · 좌우 스플릿 · 상하 스플릿(D-13) · 활성 칸 탭 모두 닫기(D-26) · **우클릭 메뉴 사용**(기본 꺼짐 — D-22) |
| **Help** | 정보 |

*하단 바* — 왼쪽에 지금 탭이 가리키는 대상의 경로, 오른쪽에 앱 이름·버전·실행
갈래. 그 사이가 오류·진행 메시지가 뜨는 자리다(D-21). **앱이 자기 항목을 하나 더
붙일 수 있는 자리**를 남긴다.

메뉴는 껍데기가 채운다. 앱이 항목을 더할 수 있으나, 위 세 묶음은 모든 앱에 공통으로
있다. **어느 묶음에 어떻게 더하는지는 D-31이 정한다** — File에만 더할 수 있고,
껍데기 항목 아래 가름선 다음에 붙는다.

**근거**

세 묶음은 VS Code를 비롯한 데스크톱 앱의 관습이라 어디에 무엇이 있는지 찾을 필요가
없다. 항목이 나중에 늘어도 들어갈 자리가 이미 정해져 있다.

View에 든 것은 세로 띠의 아이콘과 겹친다. 이것은 중복이 아니라 D-7에서 정한 역할
분담이다 — **메뉴는 전부를 담고, 세로 띠는 자주 쓰는 것만 담는다.** 세로 띠의 항목은
앱이 바꿀 수 있으므로(D-11), 어떤 앱이 아이콘을 빼더라도 메뉴에는 남아 있어야 한다.

메뉴를 껍데기가 채우는 것은 세로 띠·우클릭 메뉴와 반대다. 그쪽은 앱마다 다른 것을
담지만, 메뉴에 든 것은 **폴더 열기·화면 접기·테마처럼 모든 앱에 공통인 껍데기의
기능**이기 때문이다.

**배제한 대안**

- **묶음 없이 한 목록으로 늘어놓는다.** 지금은 항목이 적어 한 번 덜 누르는 이점이
  있으나, 항목이 늘면 어디에 넣을지가 매번 판단이 된다. 배제한다.
- **메뉴도 앱이 채운다(세로 띠·우클릭 메뉴 방식).** 메뉴에 든 것은 대부분 껍데기의
  기능이라 앱마다 다를 이유가 없다. 배제한다.
- **하단 바에 앱 자리를 남기지 않는다.** 앱이 상태를 보일 곳이 없어져 화면 안에
  자체 표시줄을 만들게 된다. 배제한다.

---

## D-24. 칸·탭·보기 수명은 dockview를 가져다 쓴다 — 확인을 거쳐 정했다

**선택**

칸 나누기(좌우·상하), 2차원 격자 배치, 칸 크기 조절, 탭 줄, 탭 끌어 옮기기(가장자리로
끌어 새 칸 만들기 포함), 칸 사이 이동, 마지막 탭이 닫히면 칸이 사라지는 것, 배치
저장·복원 — 이 덩어리를 **`dockview-core`(MIT)** 로 가져다 쓴다. 직접 짜지 않는다.

**안 보이는 탭의 처리** — 껍데기 기본은 dockview의 기본값(안 보이는 동안 화면 요소를
떼어 두는 쪽)으로 둔다. 떼어지면 곤란한 보기는 **앱이 그 탭만 "항상 그려 두기"로
올린다.** 터미널이 그런 경우다.

**근거**

D-2가 "이미 잘 만들어진 것을 다시 만드는 비용이 크고 최소 구현에 어긋난다"고 정했다.
codicons(D-3) · monaco(D-18)에 적용한 판단이 여기에도 적용되며, 덩어리는 이쪽이 가장
크다. 그리고 앞선 프로젝트가 무너진 자리가 정확히 여기다(BRIEF 1절 — 탭 이동, 분할
해제, 선택과 포커스). 같은 것을 손으로 다시 짜면 같은 실패를 반복할 가능성이 높다.

칸 나누기와 크기 조절 코드가 **VS Code의 해당 부분에서 나왔다고** 저장소가 밝히고
있어, "VS Code 방식을 따른다"는 기준(INTENT 6절)과 어긋나지 않는다. 의존성이 없고
MIT이며, 화면 프레임워크 없이 쓸 수 있다.

**확인한 것** — 채택 전에 BRIEF 완료 조건의 "보기 인스턴스가 다시 만들어지지 않는다"를
실제로 시험했다. `dockview-core` 8.2.0을 설치하고, 인스턴스마다 도장을 찍고 계속 도는
타이머와 DOM 안에만 있는 상태를 붙인 패널을 만들어 헤드리스 브라우저에서 두 가지를
했다 — 같은 칸 안에서 탭을 갔다가 되돌아오기, **다른 칸으로 옮기기.**

두 렌더 방식 모두에서 **만들어진 횟수 1 · 폐기 0 · 인스턴스 도장 동일 · 타이머 연속 ·
DOM 상태 유지**로 나왔다. 옮겨도 다시 만들어지지 않는다.

같은 시험에서 차이도 드러났다. 기본 방식은 탭이 활성이 아닌 동안 화면 요소를 DOM에서
**떼어 둔다.** 인스턴스는 살아 있지만 화면에서 분리되므로, 크기를 재서 동작하는
보기(xterm 같은 터미널)는 다시 붙일 때 크기를 맞춰야 한다. "항상 그려 두기"는 떼지
않는 대신 안 보이는 탭도 계속 그린다. 그래서 기본은 가벼운 쪽에 두고 필요한 보기만
앱이 올리게 한다 — 껍데기가 어느 보기가 터미널인지 알 필요가 없으므로 "껍데기는
리소스 종류를 모른다"(INTENT 3절)도 지켜진다.

**배제한 대안**

- **직접 짠다(참조 재구현).** 규칙을 온전히 장악한다는 이점이 있으나, D-8이 2차원
  격자 배치를 그대로 받으면서 난이도가 올라갔다. 크기 조절·끌어 놓기·2차원 배치·배치
  저장·보기 수명까지 전부 우리 몫이 되고, 그것이 앞선 프로젝트가 실패한 지점이다.
  배제한다.
- **모든 탭을 "항상 그려 두기"로 둔다.** 떼어짐 문제가 사라져 단순하지만, 안 보이는
  탭이 전부 계속 그려진다. 멀티터미널처럼 탭이 여럿인 앱에서 부담이 커진다. 배제한다.
- **떼어짐을 앱이 모르게 껍데기가 알아서 처리한다.** 어느 보기가 크기에 민감한지는
  껍데기가 알 수 없다. 배제한다.

**메모** — `dockview-core`는 `<script>` 하나로 읽히는 묶음본(UMD)도 함께 배포한다.
즉 **dockview 때문에 빌드가 필요한 것은 아니다.** D-1의 빌드 전제는 그대로 유효하나,
그 근거 중 "빌드형 라이브러리를 쓰려면"이라는 부분은 dockview에 해당하지 않는다.

---

## D-25. 빌드는 Vite로 하고, 산출물 한 벌을 두 갈래가 함께 읽는다

**선택**

D-1이 전제한 빌드를 **Vite**로 한다. 웹 자산을 **한 벌로** 빌드하고, 그 산출물을
Electron과 pywebview가 **똑같이** 읽는다. 갈래마다 다른 빌드를 두지 않는다.

**근거**

산출물이 한 벌이어야 "두 갈래가 같은 사용자 동작에 같은 결과를 낸다"는 완료 조건이
**구조적으로** 보장된다. 갈래별로 빌드가 갈리면 한쪽에서만 나타나는 차이가 생길
자리가 만들어지고, 그때부터 동등성을 사람이 매번 확인해야 한다.

빌드 자체는 monaco(D-18) 때문에 필요하다. dockview는 `<script>` 하나로 읽히는
묶음본이 있어 빌드 없이도 되지만(D-24), monaco는 그렇지 않다.

같은 일을 하는 도구가 여럿 있는데(webpack · rollup · esbuild · parcel) Vite를 고르는
이유는 설정이 가장 적고, 고치면 즉시 화면에 반영되는 개발 모드가 기본으로 들어 있기
때문이다. 설정이 적다는 것은 그 자체로 최소 구현에 부합한다.

**배제한 대안**

- **webpack.** 오래되어 자료가 많다는 이점이 있으나 설정이 길고, 개발 중 즉시 반영을
  따로 붙여야 한다. 배제한다.
- **rollup · esbuild를 직접 쓴다.** 더 낮은 층이라 손댈 것이 많다. Vite가 내부적으로
  이들을 쓰므로 굳이 직접 다룰 이유가 없다. 배제한다.
- **갈래마다 따로 빌드한다.** 각 갈래에 맞춘 최적화가 가능하지만, 동등성을 구조가
  아니라 확인으로 지켜야 한다. 배제한다.
- **무빌드로 돌아간다(D-1 뒤집기).** dockview는 되지만 monaco가 걸린다. D-18에서
  monaco를 껍데기에 넣기로 이미 정했으므로 배제한다.

---

## D-26. 칸은 명시적으로 닫지 않는다 — 탭이 모두 닫히면 사라지고, 마지막 한 칸은 남는다

**선택**

**칸 자체를 닫는 전용 기능을 두지 않는다.** 세로 띠·메뉴·우클릭 어디에도 "칸
닫기/삭제" 항목이 없다. 칸은 탭을 통해서만 사라진다(아래).

칸은 그 칸의 탭이 `×` 또는 `Ctrl+W`로 하나씩, 또는 한꺼번에 모두 닫혀서 **탭이 0개가
되면 사라진다.** 남은 칸들이 그 자리를 나눠 갖는다(dockview — D-24). 닫힌 탭의 보기는
그때 폐기된다(D-24). 탭을 다른 칸으로 옮기거나 살아남은 칸에 나눠 붙이지 않는다.

한꺼번에 닫는 것은 **View 메뉴의 "활성 칸 탭 모두 닫기" 하나로만** 한다. 탭 줄이나
세로 띠에는 아이콘을 두지 않는다. 활성 칸이 빈 칸이어도(탭 0개) 이 항목은 메뉴에
그대로 있으며, 눌러도 결과가 이미 빈 칸이라 바뀌는 것이 없다.

**칸이 사라져도 최소 한 칸은 남는다.** 마지막 칸의 마지막 탭까지 닫으면 그 칸은
탭 없는 빈 면으로 남는다(D-20). 편집기 영역이 통째로 없어지는 상태는 두지 않는다.
그 빈 칸에서 다시 시작하는 길은 셋이다 — 탐색기에서 파일 고르기(D-5), 세로 띠의
스플릿 아이콘, View 메뉴.

**근거**

VS Code 동작을 기본으로 삼는다(divergence는 Zen 하나뿐 — D-12). VS Code에는 편집기
그룹을 닫는 전용 버튼이 없다 — 그룹은 비면 사라지고(`workbench.editor.closeEmptyGroups`
기본값), 마지막 그룹 하나는 늘 남는다. 그대로 따른다.

전용 "칸 닫기"를 두면 조작이 하나 늘고, 그 결과(탭은 어디로 가나, 어느 칸이 활성이
되나)를 정하는 규칙이 따라붙는다. 탭을 하나씩 닫아 칸을 비우는 길이 이미 있으므로
없어도 된다.

마지막 칸을 빈 채로 남기는 것은 "편집기 영역은 항상 있다"는 VS Code의 전제와 같다.
칸이 0개가 되도록 두면 스플릿 아이콘이 기준으로 삼을 칸이 없어지고, D-5의 "현재
칸"·D-13·D-19가 전제하는 자리가 사라진다. 빈 칸 하나를 남기면 그 자리가 항상
성립한다.

**배제한 대안**

- **"칸 닫기/삭제" 아이콘을 세로 띠와 메뉴에 둔다.** 한때 이렇게 정했다(옛 D-26).
  VS Code에 없는 조작이고, 탭을 비우는 길이 이미 있어 중복이다. 배제한다.
- **칸의 탭을 옆 칸으로 옮긴다.** 닫았는데 내용이 남아 조작과 결과가 어긋난다.
  "어느 칸으로", "탭 순서 어디에", "무엇이 활성" 규칙이 새로 필요하다. 배제한다.
- **마지막 칸까지 없애 편집기 영역을 비운다(칸 0개 허용 — 옛 D-26).** 스플릿의 기준
  칸이 사라지고, 첫 칸을 다시 만드는 특수 규칙이 필요해진다. Zen 외의 divergence가
  하나 더 생긴다. 배제한다.

---

## D-27. 다시 열 때 되살리는 것은 마지막에 연 폴더뿐이다

**선택**

앱을 껐다 켜면 **마지막에 열었던 루트 폴더가 탐색기에 다시 나타난다.** 그것뿐이다.

칸 배치, 열려 있던 탭, 활성 탭, 트리의 펼침 상태는 되살리지 않는다. 시작하면 폴더
하나가 열린 채 오른쪽은 빈 칸 하나다(D-26 · D-20).

**근거**

폴더를 다시 여는 것은 켤 때마다 반복되는 일이라 아낄 값어치가 있다. 반면 탭과 칸은
그때그때 다르고, 되살리면 "그 파일이 사라졌으면", "그 폴더가 없어졌으면", "터미널
세션은 되살릴 수 없는데 탭만 되살아나면" 같은 경우를 전부 다뤄야 한다. 특히 터미널은
프로세스가 이미 죽었으므로 되살릴 수 있는 것이 아니다(D-4).

dockview가 배치 저장·복원을 제공하지만(D-24) 쓸 수 있다는 것과 써야 한다는 것은
다르다. 되살릴 것이 적을수록 켤 때의 상태가 하나로 정해진다.

**배제한 대안**

- **칸 배치와 열려 있던 탭까지 되살린다.** 편하지만 사라진 대상과 되살릴 수 없는
  세션을 다루는 규칙이 줄줄이 따라온다. 배제한다.
- **아무것도 되살리지 않는다.** 켤 때마다 폴더를 다시 고르는 것은 반복 비용이 크다.
  배제한다.

---

## D-28. 저장하지 않은 채 닫으려 하면 확인 창으로 묻는다

**선택**

고쳐 놓고 저장하지 않은 탭을 닫으려 하거나 앱을 끄려 하면 **확인 창**이 뜬다.
데스크톱 앱에서 흔히 쓰는 세 버튼을 그대로 쓴다.

| 버튼 | 결과 |
| --- | --- |
| **저장** | 저장한 뒤 닫는다 |
| **저장 안 함** | 고친 것을 버리고 닫는다 |
| **취소** | 닫지 않는다. 고친 내용도 그대로 남는다 |

새로 만든 방식이 아니라 사용자가 다른 프로그램에서 이미 아는 방식이다. 무엇을
누르면 무엇이 되는지를 배울 필요가 없다.

**D-21과의 경계** — D-21은 알림 상자를 만들지 않기로 했다. 그것과 어긋나지 않는다.
둘은 성격이 다르다.

| | 무엇 | 어디에 |
| --- | --- | --- |
| **알림** | 일방적으로 알려 주기만 한다. 답을 받지 않는다 | 하단 바 (D-21) |
| **확인** | 사용자의 답을 받아야 다음이 정해진다 | 확인 창 (이 결정) |

하단 바로는 답을 받을 수 없으므로 확인은 창이어야 한다. 반대로 답이 필요 없는 것을
창으로 띄우지 않는다.

**근거**

저장 안 한 것을 말없이 버리는 것은 되돌릴 수 없는 손실이다. 그런 것은 사용자에게
묻는다.

세 버튼인 이유는 셋 다 실제로 필요한 답이기 때문이다. 저장하고 닫고 싶을 때, 실수로
고친 것을 버리고 닫고 싶을 때, 닫는 것 자체를 그만두고 싶을 때가 모두 있다. 이것은
새로 발명하지 않는다는 원칙(INTENT 6절)이 UI 관습에도 적용되는 자리다.

**배제한 대안**

- **[취소]와 [저장하기] 둘만 둔다.** 버리고 닫는 길이 막혀 실수로 고친 파일도 반드시
  저장해야 한다. 되돌리기로 되돌린 뒤 닫는 우회로가 있지만, 흔한 방식과 달라 사용자가
  버튼을 보고 멈칫하게 된다. 배제한다.
- **묻지 않고 그냥 닫는다.** 손실이 조용히 일어난다. 배제한다.
- **하단 바에 경고만 띄우고 닫는다.** 답을 받을 수 없으므로 경고가 아니라 사후
  통보가 된다. 배제한다.

---

## D-29. 시각 체계는 VS Code를 따른다 — 세 테마(흰색·회색·검정), tab-explorer 자산은 안 쓴다

**선택**

색·글꼴·간격·모서리 같은 시각 체계는 **VS Code를 기준으로 새로 정한다.**
`tab-explorer-templates`가 정의한 값 — 글꼴(`Segoe UI` · `Cascadia Mono`), 글자
크기 12px, 간격 4/8/12/16px, 모서리 6px, 색 토큰 전부 — 은 **하나도 쓰지 않는다.**

**색 테마는 셋이다 — 흰색 · 회색 · 검정.**

- **흰색 · 검정** — VS Code의 기본 밝은 테마(Light Modern)와 어두운 테마(Dark Modern)를
  따른다. 색 토큰의 이름 체계와 값을 그 두 테마에서 가져온다.
- **회색** — VS Code에는 대응하는 기본 테마가 없다. **흰색 테마와 검정 테마 배경의
  산술 중간**(밝기 기준 — 대략 중간 회색, `#808080` 계열 L\* ≈ 54~56)에 배경을 두고,
  나머지 토큰도 두 테마의 대응 값 사이에서 같은 비율로 잡는다. 색상·채도는 VS Code
  팔레트 계열을 유지한다. tab-explorer의 값은 참조하지 않는다.

세 테마는 앱들이 공유하는 하나의 기준으로 정돈된 상태를 유지한다(BRIEF 3절). 어느
테마에서든 codicons·파일 종류 아이콘은 색만 바뀐다(D-3 · D-17). 정확한 토큰 값은
SPEC에서 정한다.

**근거**

INTENT 6절이 "VS Code 방식을 따른다"이고, BRIEF 2절이 "배치·간격·구현 방식은
물려받지 않는다", BRIEF 3절이 "겉보기는 처음부터 다시 구성한다"고 못 박았다.
`tab-explorer-templates`는 VS Code를 모르는 상태에서 시행착오로 만들어졌고, 그 시각
값을 가져오면 "VS Code를 따른다"와 "앞선 것을 안 물려받는다"가 둘 다 흔들린다.
그래서 tab-explorer의 값은 색·글꼴·간격 어느 것도 참조하지 않는다.

회색 테마는 VS Code에 대응 기본 테마가 없어 새로 정해야 한다. 흰색과 검정의 산술
중간에 두면 규칙이 하나로 단순하고("두 테마의 가운데"), 세 테마가 밝기 축에서
고르게 벌어진다. 어느 한 값을 외부에서 빌리지 않으므로 참조 대상이 남지 않는다.

**배제한 대안**

- **`tab-explorer-templates`의 세 색 토큰을 그대로 가져와 다듬는다.** 이미 세 테마가
  정돈돼 있어 빠르지만, VS Code가 아닌 값을 시각 체계의 바탕에 두게 된다. INTENT 6절·
  BRIEF 2·3절과 어긋나므로 배제한다.
- **회색 배경 밝기를 `tab-explorer-templates`의 회색(`#353b44`, L\* ≈ 25)에서 가져온다.**
  한때 이렇게 정했다. 실사용으로 검증된 값이라는 이점이 있으나, tab-explorer의 어떤
  값도 쓰지 않기로 하면서 참조 자체를 없앴다. 배제한다.
- **회색 테마를 두지 않고 흰색·검정 둘만 둔다(VS Code 기본과 동일).** 가장 단순하지만
  BRIEF 3절이 "세 색상 테마"를 명시하고 D-3·D-17이 그 위에 서 있다. 배제한다.
- **글꼴·간격까지 VS Code 값을 픽셀 단위로 복제한다.** 기준으로 삼는 것과 복제하는
  것은 다르다. VS Code를 기준으로 새로 정하되 두 갈래·세 테마에서 일관되면 되고,
  세부 수치는 SPEC에서 정한다. 배제한다.

---

## D-30. 탐색기 뷰 제목줄을 둔다 — 접기·새로 고침은 껍데기, 만들기는 앱

**선택**

VS Code처럼 탐색기 위에 **뷰 제목줄**을 두고 오른쪽에 액션 아이콘을 놓는다. D-9의
외형 항목이 이미 이것을 요구했는데 시나리오와 `SPEC`으로 내려오지 않았다. 넷을
성격에 따라 가른다.

| 액션 | 누가 하나 |
| --- | --- |
| **모두 접기** | 껍데기. 순수 트리 상태이고 키(`Ctrl+←`)가 이미 있다(D-9). 버튼만 더한다 |
| **새로 고침** | 껍데기가 버튼을 두고, 다시 읽는 일은 트리 공급자(갈아끼우는 자리)에게 "다시 물어봐"를 부른다. 무엇을 다시 읽는지는 껍데기가 모른다 |
| **새 파일 · 새 폴더** | 껍데기는 **장치만** 준다 — 트리에 인라인 이름 입력 행을 그리고 `Enter`·`Escape`를 처리한다. 이름을 받아 실제로 만드는 것은 앱이다 |

껍데기가 기본으로 넣는 액션은 **모두 접기 · 새로 고침 둘**이다. 새 파일·새 폴더는
앱이 D-31의 표면으로 꽂는다. 앱이 꽂지 않으면 그 아이콘은 없다.

**근거**

"껍데기는 리소스 종류를 모른다"(INTENT 3)를 지키면서 VS Code의 익숙한 자리를
물려받는 방법은 이 갈래뿐이다. 껍데기가 "새 파일"을 실제로 만들면 파일이 무엇인지
아는 코드가 껍데기로 새고, 폴더 축 앱(멀티터미널)이 쓰지 않는 기능을 이고 간다.

장치는 껍데기가, 내용은 앱이 — 이 갈래는 우클릭 메뉴(D-22)와 알림(D-21)에서 이미
두 번 쓴 것과 같다. 세 자리가 같은 규칙을 따르면 앱을 만드는 사람이 매번 다시
배우지 않는다.

새로 고침을 껍데기에 두는 것은, 그것이 "지금 보이는 것을 다시 물어봐"일 뿐 무엇을
물어보는지는 모르기 때문이다. 트리 공급자가 이미 갈아끼우는 자리이므로 새 표면이
늘지 않는다.

**배제한 대안**

- **넷 다 껍데기가 한다.** VS Code와 가장 같아 보이지만 파일·폴더 생성 코드가
  껍데기에 들어가 INTENT 3이 깨진다. 배제한다.
- **넷 다 앱이 한다(껍데기는 자리만).** 껍데기가 더 단순해지지만, 모두 접기는 이미
  껍데기의 키(`Ctrl+←`)이고 새로 고침도 껍데기의 트리가 하는 일이라 같은 일을 앱마다
  다시 짜게 된다. 앱마다 이름 입력 모양이 달라지는 것도 "같은 껍데기를 쓴다"는 느낌을
  깬다. 배제한다.
- **뷰 제목줄을 두지 않고 우클릭 메뉴로만 만들기를 연다.** 자리가 하나 줄지만
  우클릭 메뉴는 기본 꺼짐이라(D-22) 껍데기만 있는 상태에서 새로 고침·모두 접기로
  가는 길이 없어진다. 배제한다.
- **이름 바꾸기·삭제도 뷰 제목줄에 둔다.** VS Code도 제목줄에 두지 않는다. 이 둘은
  항목을 고른 뒤의 동작이라 우클릭 메뉴(D-22)와 앱 키(D-19)가 맡는 자리다. 배제한다.

---

## D-31. 앱이 File 묶음과 뷰 제목줄에 항목을 더한다 — View·Help는 껍데기만 채운다

**선택**

D-23은 "앱이 항목을 더할 수 있으나"라고만 적고 어디에 어떻게인지를 정하지 않았다.
여기서 정한다.

| 자리 | 앱이 더할 수 있나 |
| --- | --- |
| **File 묶음** | **그렇다.** 껍데기 항목 뒤에 가름선을 두고 그 아래에 붙는다 |
| **탐색기 뷰 제목줄** | **그렇다.** 껍데기 액션(모두 접기·새로 고침 — D-30) 왼쪽에 붙는다 |
| **View 묶음** | 아니다. 전부 껍데기 기능이다 |
| **Help 묶음** | 아니다 |

File 묶음의 껍데기 항목을 다음 다섯으로 한다. **탭 닫기**와 **폴더 닫기**를 더한 것이다.

| 항목 | 결과 |
| --- | --- |
| 폴더 열기(`Ctrl+O`) | D-16 |
| 최근 폴더 | D-16 |
| 탭 닫기(`Ctrl+W`) | 활성 탭을 닫는다. 키로 닫는 것과 같다(D-26) |
| 폴더 닫기 | 루트를 비운다. 트리가 폴더를 열지 않은 빈 상태로 돌아간다(D-20). 탭과 칸은 건드리지 않는다 |
| 끝내기 | |

앱이 더하는 것의 예 — 파일 열기 · 최근 파일 · 저장 · 다른 이름으로 저장 · 새 파일 ·
새 폴더. **껍데기는 이 이름들을 모른다.** 앱이 이름과 키와 할 일을 함께 준다.

껍데기가 넣는 항목의 목록과 순서는 앱이 바꾸거나 뺄 수 없다. 앱이 더한 항목은
가름선 아래에만 놓인다.

**근거**

File 묶음에 드는 것은 **대상을 열고 닫고 저장하는 일**이라 앱마다 다르다. 텍스트
에디터는 저장이 필요하고 마크다운 뷰어는 필요 없다. 반면 View에 든 것은 화면 접기·
테마처럼 모든 앱에 공통인 껍데기 기능이라 앱이 더할 것이 없다(D-23). 그래서 한
묶음만 연다.

이 자리가 없으면 저장·파일 열기 같은 명령이 **키에만** 존재한다. 메뉴에 없는 명령은
있는지조차 알 수 없어, 앱마다 자체 도구 막대를 화면 안에 만들게 된다 — 하단 바에
앱 자리를 남긴 것(D-23)과 같은 이유로 배제해야 할 결과다.

검증 앱 셋 중 **파일 커맨터**가 이미 이것을 요구한다. 새 폴더·삭제·이름 바꾸기를
놓을 자리가 기본 꺼짐인 우클릭 메뉴(D-22)밖에 없으면, 껍데기만 있는 상태에서 그
앱의 기본 동작에 도달할 길이 없다.

가름선 아래로만 놓는 것은 껍데기 항목의 위치가 앱마다 달라지지 않게 하기 위해서다.
"폴더 열기는 늘 맨 위"가 유지되면 앱을 옮겨 다녀도 다시 찾지 않는다.

**배제한 대안**

- **네 자리(File·View·Help·뷰 제목줄)를 모두 연다.** View에 앱 항목이 섞이면 화면
  접기·테마가 앱마다 다른 자리에 놓인다. 껍데기를 쓰는 이점이 줄어든다. 배제한다.
- **아무 자리도 열지 않고 키로만 쓴다(옛 상태).** 위 근거대로 배제한다.
- **앱이 껍데기 항목을 빼거나 순서를 바꿀 수 있게 한다.** 앱이 "폴더 열기"를 빼면
  루트를 바꿀 길이 사라지는데, 그것이 앱의 실수인지 의도인지 껍데기가 판단할 수
  없다. 배제한다.
- **이 표면을 v0.2로 미룬다.** 확장 앱(노트북·마인드맵)만의 요구라면 미룰 수
  있으나, 위 근거대로 v0.1의 검증 앱 셋이 이미 요구한다. 배제한다.

---

## D-32. 앱이 하단 바에 메시지와 진행을 올린다 — 자리와 그리기는 껍데기

**선택**

D-21은 "무엇을 알릴지는 앱이 정하되 어떻게 보일지는 껍데기가 정한다"고 했으나 앱이
부를 자리를 정하지 않았다. 여기서 정한다.

앱이 하단 바 메시지 자리에 **오류·안내 한 줄**을 올리고, **진행 표시**를 켜고 끌 수
있다. 껍데기가 자기 오류·진행(D-21)을 올릴 때 쓰는 것과 같은 자리·같은 모양이다.

- 그 자리는 **한 줄**이다. 껍데기가 올린 것이든 앱이 올린 것이든 **가장 최근 것
  하나만** 보인다. 쌓이는 목록을 두지 않는다.
- 앱이 올린 문구를 껍데기가 고치지 않는다. 모양·색·자리는 껍데기가 정한다.
- 진행은 앱이 켜고 앱이 끈다. 껍데기가 임의로 끄지 않는다.

FR-N10의 "앱이 하단 바에 자기 항목을 붙인다"와는 다른 자리다. 그쪽은 **상주하는
항목**이고 이쪽은 **지나가는 메시지**다.

**근거**

알릴 내용을 아는 쪽은 앱뿐이다 — 커널이 도는 중인지, 파일 복사가 몇 개째인지,
읽기에 실패한 것이 무엇인지는 껍데기가 모른다(INTENT 3). 그런데 그리는 자리는
껍데기에만 있다. 부를 길이 없으면 앱은 화면 안에 자체 표시줄을 만들게 되고, 그것은
D-21이 "떠오르는 알림 상자를 만들지 않는다"로 막으려던 결과와 같아진다.

한 줄에 가장 최근 것만 두는 것은 D-21의 "쌓이는 목록을 두지 않는다"를 그대로
따른 것이다. 껍데기와 앱이 자리를 다투는 규칙을 새로 만들지 않아도 된다.

**받아들이는 단점** — 껍데기 오류와 앱 메시지가 짧은 간격으로 겹치면 앞엣것이
보이지 않고 지나간다. D-21이 이미 "하단 바는 눈에 덜 띄어 놓칠 수 있다"를 알고
고른 것과 같은 단점이라, 여기서 새로 늘어나는 위험이 아니다.

**배제한 대안**

- **앱 메시지 자리를 따로 만든다.** 겹침 규칙이 없어지지만 하단 바가 길어지고,
  껍데기 오류와 앱 오류가 다른 곳에 떠서 사용자가 두 곳을 봐야 한다. 배제한다.
- **메시지를 쌓아 두고 넘긴다.** D-21이 알림 센터를 배제한 이유가 그대로 적용된다.
  배제한다.
- **앱은 FR-N10의 상주 항목만 쓴다(표면을 열지 않는다).** 상주 항목은 상태를
  보이기엔 되지만 "실패했다"·"끝났다"처럼 지나가는 것을 담지 못한다. 배제한다.
- **껍데기가 일정 시간 뒤 앱 메시지를 지운다.** 껍데기가 그 메시지가 아직 유효한지
  판단해야 하는데 알 수 없다. 앱이 끄게 둔다. 배제한다.
> 버전: v0.1 · 작성일: 2026-09-07
>
> 이 문서는 버전이 바뀌어도 변하지 않는 것만 담는다. 기술·구현·수치를 담지 않는다.
> 방법론(project-workflow) 관점에서 이 내용은 마감 시 `README.md` 1번 블럭으로
> 승격된다. 여기 적힌 것이 흔들리면 그건 major 변경이다.

# INTENT — workbench-kit

## 1. 이 프로젝트는 무엇을 하는가

여러 데스크톱 앱이 공유하는 **공통 껍데기 한 벌**을 미리 만든다. 창, 메뉴, 탐색기
트리, 좌우로 가른 패널, 탭 줄, 상태 표시줄, 설정 저장, 실행 환경 연결이 그것이다.
새 앱을 만들 때 이 껍데기를 다시 짜지 않고 **그대로 물려받아**, 달라지는 소수만
갈아끼워 완성하는 것이 목적이다.

## 2. 왜 이것을 원하는가

같은 형태의 껍데기를 앱마다 처음부터 다시 만들면, 아이콘 하나를 고칠 때에도 여러
벌을 따로 고쳐야 한다. 공통부를 한 벌로 두면 **새 앱마다 드는 구현·수정·튜닝 노력이
최소화**된다. 새 앱은 공통부를 정해진 대로 물려받고, 그 앱에서만 달라지는 소수의
자리만 갈아끼워 나오는 상태를 원한다.

그리고 앱 자체가 가벼워야 한다. VS Code처럼 기능이 끝없이 불어난 도구는 무겁고,
그중 대부분은 정작 쓰이지 않는다. 이 껍데기 위에 올리는 앱은 그 도메인에 꼭 필요한
기능만 담아 작고 단순하게 둔다. 이것이 이 프로젝트의 제1원칙이다.

## 3. 변하지 않는 원칙

- **앱은 꼭 필요한 기능만 담는다.** 기능을 더해 비대해지는 길을 택하지 않고, 그
  도메인에 없어서는 안 될 것만 남긴다. 이것이 제1원칙이며, 아래 원칙은 이를 받친다.
- **받아내는 기능은 두 축이다.** 탭 기반 파일 탐색기(탭에 고른 파일의 내용을 열거나
  그 파일을 처리한다)와 탭 기반 폴더 탐색기(탭에 폴더의 파일 리스트나 터미널을 연다).
  두 축은 같은 껍데기와 같은 탭 규칙을 공유한다.
- **껍데기는 리소스 종류를 모른다.** 파일인지 폴더인지 세션인지 아는 코드는 껍데기
  안에 있지 않고, 갈아끼우는 자리 안에만 있다.
- **달라지는 축은 갈아끼우는 자리로 드러낸다.** 트리에 무엇을 보일지, 행을 고르면
  무엇이 열릴지, 탭 안에 무엇이 그려질지 — 이 축들을 껍데기 밖에서 바꾼다.
- **탭은 파일 경로가 아니다.** 폴더나 장기 실행 세션처럼 파일이 아닌 대상도 같은
  열기·전환·이동·닫기 규칙으로 같은 탭 영역에서 다룰 수 있어야 한다.
- **보기는 탭 수명 동안 살아 있다.** 탭을 전환했다 돌아오거나 반대쪽 패널로 옮겨도
  보기가 다시 만들어지지 않고, 탭을 닫을 때만 폐기된다.

## 4. 다루지 않는 것

- 범용 코드 편집기나 VS Code 전체의 복제. 껍데기는 공통 UI의 기준으로 VS Code를
  참고할 뿐, 그 제품을 재현하지 않는다.
- 확장 호환성, 소스 제어, 검색, 디버깅, 통합 터미널 같은 편집기 제품 기능.
- 검증용 앱들의 도메인 기능. 껍데기는 이들이 꽂힐 자리이지 앱 자체가 아니다.
- 설치형 라이브러리나 패키지로의 배포. 복사해서 쓰는 템플릿으로 둔다.
- 모바일·클라우드 배포.

## 5. 두 실행 갈래

같은 공통 UI를 두 갈래가 공유한다. 어느 갈래로 낼지는 **그 앱이 파이썬을 필요로
하는가**로 갈린다.

- **필요하다** → pywebview. 화면은 웹, 계산은 파이썬이 맡는 앱.
- **필요 없다** → Electron. 화면과 로직이 모두 자바스크립트로 끝나는 앱.

두 갈래를 다 두는 이유는 **이 껍데기 위에 올릴 앱이 두 종류 다 나오기 때문**이다.
수치해석·시뮬레이션·머신러닝·딥러닝처럼 계산이 파이썬에 있는 일이 계속 있고, 그런
앱의 화면을 파이썬 쪽에서 띄우려면 pywebview 갈래가 있어야 한다. 반대로 계산이
필요 없는 도구는 Electron 쪽이 더 낫다.

어느 시점에 어느 갈래의 앱을 만드느냐와는 무관하다. **한쪽 갈래를 쓰는 앱이 지금
없더라도 그 갈래를 버리지 않는다.** 껍데기는 두 갈래를 함께 유지하며, 두 갈래는
같은 사용자 동작에 같은 결과를 낸다.

## 6. 공통 UI의 기준

공통 UI의 생김새와 동작은 **VS Code가 오랫동안 실제 제품에서 다듬어 온 구성과
동작을 기준**으로 삼는다. 탐색기 트리, 탭, 편집 영역의 분할, 선택과 포커스, 키보드
조작이 그렇다. 이 프로젝트의 목적과 범위에 맞는 동작만 골라 따르되, 기준과 다르게
둘 때는 그 차이가 의도된 것임을 판단할 수 있어야 한다.

기준을 VS Code에 두는 이유는 UI를 새로 발명하지 않기 위해서다. VS Code OSS가 이미
만들어 오래 검증한 구성과 동작을 최대한 참조해, 공통 UI를 짜고 그 동작을 확인하는
노력을 줄인다.

## 7. 이 껍데기가 받아낼 앱들

아래 앱들은 껍데기의 부속이 아니라, 껍데기가 실제로 재사용 가능한지 검증하는
사례다. 이번 버전은 이들이 꽂힐 자리를 만드는 것이지 이들 자체를 만드는 것이 아니다.

| 계열 | 앱 | 탭 영역에 무엇이 열리나 | 갈래 |
| --- | --- | --- | --- |
| 파일 | 마크다운 뷰어 | 고른 마크다운 문서를 렌더한 화면 | Electron |
| 폴더 | 파일 커맨터 | 칸 둘에 폴더를 나란히 둔다. 한쪽에서 고른 파일의 내용을 다른 쪽 칸에서 보거나 고치고, 파일을 여러 개 골라 반대편 칸의 폴더로 복사하거나 옮긴다 | (대상별 결정) |
| 폴더 | 멀티터미널 | 고른 폴더 경로에서 열린 터미널. 탭을 더하면 같은 폴더의 터미널이 하나 더 열린다 | (대상별 결정) |

멀티터미널이 같은 폴더로 탭을 여러 개 여는 이유는, 한 폴더에서 여러 AI 에이전트를
동시에 돌리기 위해서다.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rtk rg -n "''^" src/core/contextmenu.ts src/registry/kind-registry.ts src/presets/file-preset.ts src/presets/folder-preset.ts src/main.ts src/core/menu.ts src/core/layout.ts docs/reserved-keys.md' in D:\projects\workbench-kit
 succeeded in 133ms:
1156 matches in 8 files:

docs/reserved-keys.md:1:# RESERVED-KEYS — 껍데기가 먼저 가져가는 키 (FR-R1 · FR-R1a · D-19)...
docs/reserved-keys.md:2:
docs/reserved-keys.md:3:> 이 문서가 유일한 예약 키 목록이다(FR-R1). 여기 없는 키는 전부 앱(탭 안의 보기) 몫이다....
docs/reserved-keys.md:4:> 포커스가 탭 안에 있을 때 이 목록에 없는 키를 누르면 껍데기 쪽 처리 기록이 0건이고...
docs/reserved-keys.md:5:> 그 키는 탭의 보기가 받는다(FR-I6).
docs/reserved-keys.md:6:
docs/reserved-keys.md:7:## 1. 전역 — 포커스와 무관하게 껍데기가 가져간다
docs/reserved-keys.md:8:
docs/reserved-keys.md:9:| 키 | 동작 | 근거 |
docs/reserved-keys.md:10:| --- | --- | --- |
docs/reserved-keys.md:11:| `Ctrl+O` | 폴더 열기 | FR-A1, D-16 |
docs/reserved-keys.md:12:| `Ctrl+W` | 활성 탭 닫기 | FR-N6b |
docs/reserved-keys.md:13:| `Ctrl+\` | 좌우 스플릿 | FR-D1 |
docs/reserved-keys.md:14:| `F10` | 햄버거 메뉴 열기/닫기 | FR-N5, D-7 |
docs/reserved-keys.md:15:| `F11` | Zen 모드 전환 | D-12 |
docs/reserved-keys.md:16:| `Escape` | 열린 메뉴를 닫거나(메뉴가 열려 있을 때), Zen 모드를 빠져나온다(Zen일 때) | D-12 |...
docs/reserved-keys.md:17:
docs/reserved-keys.md:18:## 2. 메뉴가 열려 있을 때만
docs/reserved-keys.md:19:
docs/reserved-keys.md:20:| 키 | 동작 |
docs/reserved-keys.md:21:| --- | --- |
docs/reserved-keys.md:22:| `ArrowUp` / `ArrowDown` | 항목 사이 이동 |
docs/reserved-keys.md:23:| `ArrowLeft` / `ArrowRight` | File · View · Help 사이 이동 |
docs/reserved-keys.md:24:| `Enter` | 포커스된 항목 실행 |
docs/reserved-keys.md:25:| `Escape` | 메뉴 닫기 |
  +47 more in docs/reserved-keys.md [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_0_docs_reserved_keys_md.log]
src/core/contextmenu.ts:1:export interface ContextMenuItem {
src/core/contextmenu.ts:2:id: string;
src/core/contextmenu.ts:3:label: string;
src/core/contextmenu.ts:4:action: () => void;
src/core/contextmenu.ts:5:}
src/core/contextmenu.ts:6:
src/core/contextmenu.ts:7:/**
src/core/contextmenu.ts:8:* Generic right-click menu device (D-22). The shell only draws, positions,
src/core/contextmenu.ts:9:* and navigates the popup; it never decides what items appear. Default is
src/core/contextmenu.ts:10:* disabled, and showing zero items renders zero DOM elements (FR-G5).
src/core/contextmenu.ts:11:*/
src/core/contextmenu.ts:12:export class ContextMenuController {
src/core/contextmenu.ts:13:private enabled = false;
src/core/contextmenu.ts:14:private menuEl: HTMLElement | null = null;
src/core/contextmenu.ts:15:private currentItems: ContextMenuItem[] = [];
src/core/contextmenu.ts:16:private focusedIndex = -1;
src/core/contextmenu.ts:17:
src/core/contextmenu.ts:18:constructor(private rootContainer: HTMLElement) {
src/core/contextmenu.ts:19:document.addEventListener('mousedown', (e) => {
src/core/contextmenu.ts:20:if (this.menuEl && !this.menuEl.contains(e.target as Node)) {
src/core/contextmenu.ts:21:this.hide();
src/core/contextmenu.ts:22:}
src/core/contextmenu.ts:23:});
src/core/contextmenu.ts:24:document.addEventListener('keydown', (e) => this.handleKeydown(e));
src/core/contextmenu.ts:25:if (typeof window !== 'undefined') {
  +99 more in src/core/contextmenu.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_1_src_core_contextmenu_ts.log]
src/core/layout.ts:1:export interface WorkbenchLayoutElements {
src/core/layout.ts:2:root: HTMLElement;
src/core/layout.ts:3:titlebar: HTMLElement;
src/core/layout.ts:4:menuBtn: HTMLButtonElement;
src/core/layout.ts:5:windowTitle: HTMLElement;
src/core/layout.ts:6:windowControls: HTMLElement;
src/core/layout.ts:7:windowMinBtn: HTMLButtonElement;
src/core/layout.ts:8:windowMaxBtn: HTMLButtonElement;
src/core/layout.ts:9:windowCloseBtn: HTMLButtonElement;
src/core/layout.ts:10:activityBar: HTMLElement;
src/core/layout.ts:11:activityBarTop: HTMLElement;
src/core/layout.ts:12:activityBarBottom: HTMLElement;
src/core/layout.ts:13:sidebar: HTMLElement;
src/core/layout.ts:14:sidebarHeader: HTMLElement;
src/core/layout.ts:15:sidebarTitle: HTMLElement;
src/core/layout.ts:16:sidebarActions: HTMLElement;
src/core/layout.ts:17:sidebarAppActions: HTMLElement;
src/core/layout.ts:18:sidebarCollapseAllBtn: HTMLButtonElement;
src/core/layout.ts:19:sidebarRefreshBtn: HTMLButtonElement;
src/core/layout.ts:20:sidebarContent: HTMLElement;
src/core/layout.ts:21:mainArea: HTMLElement;
src/core/layout.ts:22:editorContainer: HTMLElement;
src/core/layout.ts:23:statusbar: HTMLElement;
src/core/layout.ts:24:statusbarPath: HTMLElement;
src/core/layout.ts:25:statusbarMessage: HTMLElement;
  +86 more in src/core/layout.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_2_src_core_layout_ts.log]
src/core/menu.ts:1:import { closeWindow } from './window';
src/core/menu.ts:2:
src/core/menu.ts:3:export interface MenuItem {
src/core/menu.ts:4:id: string;
src/core/menu.ts:5:label: string;
src/core/menu.ts:6:shortcut?: string;
src/core/menu.ts:7:type?: 'normal' | 'checkbox' | 'separator';
src/core/menu.ts:8:checked?: boolean;
src/core/menu.ts:9:action?: () => void;
src/core/menu.ts:10:}
src/core/menu.ts:11:
src/core/menu.ts:12:export interface MenuGroup {
src/core/menu.ts:13:id: 'file' | 'view' | 'help';
src/core/menu.ts:14:label: string;
src/core/menu.ts:15:items: MenuItem[];
src/core/menu.ts:16:}
src/core/menu.ts:17:
src/core/menu.ts:18:export const DEFAULT_FILE_ITEMS: readonly MenuItem[] = Object.freeze([
src/core/menu.ts:19:Object.freeze({ id: 'file:open-folder', label: '폴더 열기', shortcut: 'Ctrl+O' })...
src/core/menu.ts:20:Object.freeze({ id: 'file:open-recent', label: '최근 폴더' }),
src/core/menu.ts:21:Object.freeze({ id: 'file:close-tab', label: '탭 닫기', shortcut: 'Ctrl+W' }),...
src/core/menu.ts:22:Object.freeze({ id: 'file:close-folder', label: '폴더 닫기' }),
src/core/menu.ts:23:Object.freeze({ id: 'file:exit', label: '끝내기', action: () => closeWindow() })...
src/core/menu.ts:24:]);
src/core/menu.ts:25:
  +342 more in src/core/menu.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_3_src_core_menu_ts.log]
src/main.ts:1:import { createWorkbenchLayout, WorkbenchLayoutElements } from './core/layout';
src/main.ts:2:import { setupWindowControls } from './core/window';
src/main.ts:3:import { MenuController } from './core/menu';
src/main.ts:4:import { ActivityBarController } from './core/activitybar';
src/main.ts:5:import { ViewStateManager } from './core/viewstate';
src/main.ts:6:import { ThemeManager } from './core/theme';
src/main.ts:7:import { IconThemeManager } from './core/icontheme';
src/main.ts:8:import { SetiResolver, VscodeIconsResolver } from './icons';
src/main.ts:9:import { TreeController } from './core/tree';
src/main.ts:10:import { ExplorerTitlebarController } from './core/sidebar';
src/main.ts:11:import { FileSystemTreeProvider, promptOpenFolderDialog } from './providers/f...
src/main.ts:12:import { EditorController } from './core/editor';
src/main.ts:13:import { ContextMenuController, ContextMenuItem } from './core/contextmenu';
src/main.ts:14:import { ResourceKindRegistry } from './registry/kind-registry';
src/main.ts:15:import { registerFilePreset, FILE_KIND } from './presets/file-preset';
src/main.ts:16:import { registerFolderPreset, FOLDER_KIND } from './presets/folder-preset';
src/main.ts:17:
src/main.ts:18:export class WorkbenchApp {
src/main.ts:19:public layout: WorkbenchLayoutElements;
src/main.ts:20:public menu: MenuController;
src/main.ts:21:public activityBar: ActivityBarController;
src/main.ts:22:public viewState: ViewStateManager;
src/main.ts:23:public theme: ThemeManager;
src/main.ts:24:public iconTheme: IconThemeManager;
src/main.ts:25:public tree: TreeController;
  +309 more in src/main.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_4_src_main_ts.log]
src/presets/file-preset.ts:1:import type { ResourceKindRegistry } from '../registry/kind-registry';
src/presets/file-preset.ts:2:import type { AppEditorSurface, EditorOpenOptions } from '../core/editor';
src/presets/file-preset.ts:3:
src/presets/file-preset.ts:4:// NOT part of the common core. This preset is a minimal example of a
src/presets/file-preset.ts:5:// "file" resource kind plugged into the registration slot (WK-025); it
src/presets/file-preset.ts:6:// proves the slot works and is not the validation apps themselves
src/presets/file-preset.ts:7:// (INTENT 7). It renders a placeholder only — no real file reading.
src/presets/file-preset.ts:8:
src/presets/file-preset.ts:9:export const FILE_KIND = 'file';
src/presets/file-preset.ts:10:
src/presets/file-preset.ts:11:export function registerFilePreset(registry: ResourceKindRegistry): void {
src/presets/file-preset.ts:12:registry.register(FILE_KIND, (targetId) => {
src/presets/file-preset.ts:13:const element = document.createElement('div');
src/presets/file-preset.ts:14:element.className = 'preset-file-view';
src/presets/file-preset.ts:15:element.textContent = `File preset view: ${targetId}`;
src/presets/file-preset.ts:16:return { element };
src/presets/file-preset.ts:17:});
src/presets/file-preset.ts:18:}
src/presets/file-preset.ts:19:
src/presets/file-preset.ts:20:export function openFile(
src/presets/file-preset.ts:21:editor: AppEditorSurface,
src/presets/file-preset.ts:22:path: string,
src/presets/file-preset.ts:23:title?: string,
src/presets/file-preset.ts:24:options?: EditorOpenOptions
src/presets/file-preset.ts:25:): void {
  +2 more in src/presets/file-preset.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_5_src_presets_file_preset_ts.log]
src/presets/folder-preset.ts:1:import type { ResourceKindRegistry } from '../registry/kind-registry';
src/presets/folder-preset.ts:2:import type { AppEditorSurface, EditorOpenOptions } from '../core/editor';
src/presets/folder-preset.ts:3:
src/presets/folder-preset.ts:4:// NOT part of the common core. Minimal "folder" resource kind, proving the
src/presets/folder-preset.ts:5:// same registration slot (WK-025) accepts a second, unrelated kind with
src/presets/folder-preset.ts:6:// zero further core changes. Renders a placeholder only (INTENT 7).
src/presets/folder-preset.ts:7:
src/presets/folder-preset.ts:8:export const FOLDER_KIND = 'folder';
src/presets/folder-preset.ts:9:
src/presets/folder-preset.ts:10:export function registerFolderPreset(registry: ResourceKindRegistry): void {
src/presets/folder-preset.ts:11:registry.register(FOLDER_KIND, (targetId) => {
src/presets/folder-preset.ts:12:const element = document.createElement('div');
src/presets/folder-preset.ts:13:element.className = 'preset-folder-view';
src/presets/folder-preset.ts:14:element.textContent = `Folder preset view: ${targetId}`;
src/presets/folder-preset.ts:15:return { element };
src/presets/folder-preset.ts:16:});
src/presets/folder-preset.ts:17:}
src/presets/folder-preset.ts:18:
src/presets/folder-preset.ts:19:export function openFolder(
src/presets/folder-preset.ts:20:editor: AppEditorSurface,
src/presets/folder-preset.ts:21:path: string,
src/presets/folder-preset.ts:22:title?: string,
src/presets/folder-preset.ts:23:options?: EditorOpenOptions
src/presets/folder-preset.ts:24:): void {
src/presets/folder-preset.ts:25:editor.openItem(path, title, { ...options, meta: { ...(options?.meta || {}), ...
  +1 more in src/presets/folder-preset.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_6_src_presets_folder_preset_ts.log]
src/registry/kind-registry.ts:1:import type { IContentRenderer, GroupPanelPartInitParameters } from 'dockview...
src/registry/kind-registry.ts:2:import type { EditorComponentFactory } from '../core/editor';
src/registry/kind-registry.ts:3:
src/registry/kind-registry.ts:4:// NOT part of the common core (src/core/**). This module is free to know
src/registry/kind-registry.ts:5:// about resource kinds; the shell it plugs into never imports it (FR-I1,
src/registry/kind-registry.ts:6:// NFR-1, INTENT 3, D-4).
src/registry/kind-registry.ts:7:
src/registry/kind-registry.ts:8:export type KindRendererFactory = (targetId: string) => {
src/registry/kind-registry.ts:9:element: HTMLElement;
src/registry/kind-registry.ts:10:dispose?: () => void;
src/registry/kind-registry.ts:11:};
src/registry/kind-registry.ts:12:
src/registry/kind-registry.ts:13:/**
src/registry/kind-registry.ts:14:* Resource kind registration slot (WK-025, FR-I1). Registering a new kind
src/registry/kind-registry.ts:15:* here never touches src/core/*.ts: the shell only ever calls
src/registry/kind-registry.ts:16:* `createComponentFactory()` once, and every future kind is added by calling
src/registry/kind-registry.ts:17:* `register()` again.
src/registry/kind-registry.ts:18:*/
src/registry/kind-registry.ts:19:export class ResourceKindRegistry {
src/registry/kind-registry.ts:20:private factories = new Map<string, KindRendererFactory>();
src/registry/kind-registry.ts:21:
src/registry/kind-registry.ts:22:public register(kind: string, factory: KindRendererFactory): void {
src/registry/kind-registry.ts:23:this.factories.set(kind, factory);
src/registry/kind-registry.ts:24:}
src/registry/kind-registry.ts:25:
  +70 more in src/registry/kind-registry.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914857_grep_7_src_registry_kind_registry_ts.log]

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk cmd /c type src\\registry\\kind-registry.ts; rtk cmd /c type src\\core\\contextmenu.ts; rtk cmd /c type src\\core\\menu.ts" in D:\projects\workbench-kit
 succeeded in 321ms:
import type { IContentRenderer, GroupPanelPartInitParameters } from 'dockview-core';
import type { EditorComponentFactory } from '../core/editor';

// NOT part of the common core (src/core/**). This module is free to know
// about resource kinds; the shell it plugs into never imports it (FR-I1,
// NFR-1, INTENT 3, D-4).

export type KindRendererFactory = (targetId: string) => {
  element: HTMLElement;
  dispose?: () => void;
};

/**
 * Resource kind registration slot (WK-025, FR-I1). Registering a new kind
 * here never touches src/core/*.ts: the shell only ever calls
 * `createComponentFactory()` once, and every future kind is added by calling
 * `register()` again.
 */
export class ResourceKindRegistry {
  private factories = new Map<string, KindRendererFactory>();

  public register(kind: string, factory: KindRendererFactory): void {
    this.factories.set(kind, factory);
  }

  public has(kind: string): boolean {
    return this.factories.has(kind);
  }

  public getFactory(kind: string): KindRendererFactory | undefined {
    return this.factories.get(kind);
  }

  /**
   * Returns a single EditorComponentFactory suitable for
   * `EditorController.setComponentFactory()`. The returned renderer defers
   * kind dispatch to `init()`, since dockview only supplies panel params at
   * that point, not at component-creation time.
   */
  public createComponentFactory(): EditorComponentFactory {
    const registry = this;
    return () => new KindDispatchRenderer(registry);
  }
}

class KindDispatchRenderer implements IContentRenderer {
  public readonly element: HTMLElement;
  private inner: { element: HTMLElement; dispose?: () => void } | null = null;
  private params: { kind?: string; targetId?: string } = {};

  constructor(private registry: ResourceKindRegistry) {
    this.element = document.createElement('div');
    // Reuses the shell's editor-panel-content contract (D-20, FR-C2): an
    // unresolved/empty panel renders 0 children and 0 text, same as before
    // any kind was registered.
    this.element.className = 'editor-panel-content';
  }

  public init(parameters: GroupPanelPartInitParameters): void {
    this.params = (parameters.params || {}) as { kind?: string; targetId?: string };
    this.renderForCurrentParams();
  }

  public update(event: { params?: Record<string, unknown> }): void {
    if (event.params) {
      this.params = { ...this.params, ...event.params };
      this.renderForCurrentParams();
    }
  }

  private renderForCurrentParams(): void {
    this.inner?.dispose?.();
    this.inner = null;
    this.element.innerHTML = '';

    const { kind, targetId } = this.params;
    if (!kind || !targetId) {
      return;
    }

    const factory = this.registry.getFactory(kind);
    if (!factory) {
      return;
    }

    this.inner = factory(targetId);
    this.element.appendChild(this.inner.element);
  }

  public dispose(): void {
    this.inner?.dispose?.();
    this.inner = null;
    this.element.innerHTML = '';
  }
}
export interface ContextMenuItem {
  id: string;
  label: string;
  action: () => void;
}

/**
 * Generic right-click menu device (D-22). The shell only draws, positions,
 * and navigates the popup; it never decides what items appear. Default is
 * disabled, and showing zero items renders zero DOM elements (FR-G5).
 */
export class ContextMenuController {
  private enabled = false;
  private menuEl: HTMLElement | null = null;
  private currentItems: ContextMenuItem[] = [];
  private focusedIndex = -1;

  constructor(private rootContainer: HTMLElement) {
    document.addEventListener('mousedown', (e) => {
      if (this.menuEl && !this.menuEl.contains(e.target as Node)) {
        this.hide();
      }
    });
    document.addEventListener('keydown', (e) => this.handleKeydown(e));
    if (typeof window !== 'undefined') {
      window.addEventListener('blur', () => this.hide());
    }
  }

  public setEnabled(enabled: boolean): void {
    this.enabled = enabled;
    if (!enabled) {
      this.hide();
    }
  }

  public isEnabled(): boolean {
    return this.enabled;
  }

  public isOpen(): boolean {
    return this.menuEl !== null;
  }

  /**
   * Shows the menu at (x, y). No-op when disabled (D-22) or when the caller
   * supplies zero items, so a shell with no app-defined items draws nothing.
   */
  public show(x: number, y: number, items: ContextMenuItem[]): void {
    this.hide();
    if (!this.enabled || items.length === 0) {
      return;
    }

    this.currentItems = items;
    this.focusedIndex = 0;

    const menuEl = document.createElement('div');
    menuEl.className = 'workbench-context-menu';
    menuEl.style.left = `${x}px`;
    menuEl.style.top = `${y}px`;

    items.forEach((item, index) => {
      const row = document.createElement('div');
      row.className = 'context-menu-item-row' + (index === 0 ? ' focused' : '');
      row.dataset.itemId = item.id;
      row.textContent = item.label;
      row.addEventListener('click', (e) => {
        e.stopPropagation();
        item.action();
        this.hide();
      });
      menuEl.appendChild(row);
    });

    this.rootContainer.appendChild(menuEl);
    this.menuEl = menuEl;
  }

  public hide(): void {
    if (this.menuEl && this.menuEl.parentNode) {
      this.menuEl.parentNode.removeChild(this.menuEl);
    }
    this.menuEl = null;
    this.currentItems = [];
    this.focusedIndex = -1;
  }

  private handleKeydown(e: KeyboardEvent): void {
    if (!this.menuEl) return;

    if (e.key === 'Escape') {
      e.preventDefault();
      this.hide();
      return;
    }
    if (e.key === 'ArrowDown') {
      e.preventDefault();
      this.moveFocus(1);
      return;
    }
    if (e.key === 'ArrowUp') {
      e.preventDefault();
      this.moveFocus(-1);
      return;
    }
    if (e.key === 'Enter') {
      e.preventDefault();
      const item = this.currentItems[this.focusedIndex];
      if (item) {
        item.action();
        this.hide();
      }
    }
  }

  private moveFocus(delta: number): void {
    if (!this.menuEl || this.currentItems.length === 0) return;
    const rows = Array.from(this.menuEl.querySelectorAll('.context-menu-item-row'));
    rows[this.focusedIndex]?.classList.remove('focused');
    this.focusedIndex = (this.focusedIndex + delta + this.currentItems.length) % this.currentItems.length;
    rows[this.focusedIndex]?.classList.add('focused');
  }
}
import { closeWindow } from './window';

export interface MenuItem {
  id: string;
  label: string;
  shortcut?: string;
  type?: 'normal' | 'checkbox' | 'separator';
  checked?: boolean;
  action?: () => void;
}

export interface MenuGroup {
  id: 'file' | 'view' | 'help';
  label: string;
  items: MenuItem[];
}

export const DEFAULT_FILE_ITEMS: readonly MenuItem[] = Object.freeze([
  Object.freeze({ id: 'file:open-folder', label: '폴더 열기', shortcut: 'Ctrl+O' }),
  Object.freeze({ id: 'file:open-recent', label: '최근 폴더' }),
  Object.freeze({ id: 'file:close-tab', label: '탭 닫기', shortcut: 'Ctrl+W' }),
  Object.freeze({ id: 'file:close-folder', label: '폴더 닫기' }),
  Object.freeze({ id: 'file:exit', label: '끝내기', action: () => closeWindow() }),
]);

export const DEFAULT_VIEW_ITEMS: readonly MenuItem[] = Object.freeze([
  Object.freeze({ id: 'view:toggle-sidebar', label: '탐색기 접기/펴기' }),
  Object.freeze({ id: 'view:toggle-titlebar', label: '상단 바 감추기/보이기' }),
  Object.freeze({ id: 'view:toggle-statusbar', label: '하단 바 감추기/보이기' }),
  Object.freeze({ id: 'view:zen-mode', label: 'Zen 모드', shortcut: 'F11' }),
  Object.freeze({ id: 'view:cycle-color-theme', label: '테마 바꾸기' }),
  Object.freeze({ id: 'view:cycle-icon-theme', label: '아이콘 테마 바꾸기' }),
  Object.freeze({ id: 'view:split-horizontal', label: '좌우 스플릿' }),
  Object.freeze({ id: 'view:split-vertical', label: '상하 스플릿' }),
  Object.freeze({ id: 'view:close-active-tabs', label: '활성 칸 탭 모두 닫기' }),
  Object.freeze({ id: 'view:toggle-context-menu', label: '우클릭 메뉴 사용', type: 'checkbox', checked: false }),
]);

export const DEFAULT_HELP_ITEMS: readonly MenuItem[] = Object.freeze([
  Object.freeze({ id: 'help:about', label: '정보' }),
]);

export class MenuController {
  private menuDropdownEl: HTMLElement | null = null;
  private _isOpen = false;
  private activeCategoryId: string = 'file';
  private focusedItemIndex = 0;

  private readonly coreFileItems: readonly MenuItem[] = Object.freeze(
    DEFAULT_FILE_ITEMS.map((it) => Object.freeze({ ...it }))
  );
  private readonly coreViewItems: readonly MenuItem[] = Object.freeze(
    DEFAULT_VIEW_ITEMS.map((it) => Object.freeze({ ...it }))
  );
  private readonly coreHelpItems: readonly MenuItem[] = Object.freeze(
    DEFAULT_HELP_ITEMS.map((it) => Object.freeze({ ...it }))
  );
  private appFileItems: MenuItem[] = [];
  private itemActions: Map<string, () => void> = new Map();
  private checkboxStates: Map<string, boolean> = new Map([['view:toggle-context-menu', false]]);

  constructor(private hamburgerBtn: HTMLButtonElement, private rootContainer: HTMLElement) {
    this.setupListeners();
  }

  private setupListeners(): void {
    this.hamburgerBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      this.toggleMenu();
    });

    document.addEventListener('click', (e) => {
      if (this._isOpen && this.menuDropdownEl && !this.menuDropdownEl.contains(e.target as Node)) {
        this.closeMenu();
      }
    });

    document.addEventListener('keydown', (e) => {
      // F10 opens/closes the menu with zero mouse involvement (NFR-7, D-7).
      if (e.key === 'F10' && !e.ctrlKey && !e.altKey && !e.shiftKey) {
        e.preventDefault();
        this.toggleMenu();
        return;
      }

      if (!this._isOpen) return;

      if (e.key === 'Escape') {
        e.preventDefault();
        this.closeMenu();
        return;
      }
      if (e.key === 'ArrowDown') {
        e.preventDefault();
        this.moveItemFocus(1);
        return;
      }
      if (e.key === 'ArrowUp') {
        e.preventDefault();
        this.moveItemFocus(-1);
        return;
      }
      if (e.key === 'ArrowRight') {
        e.preventDefault();
        this.moveCategoryFocus(1);
        return;
      }
      if (e.key === 'ArrowLeft') {
        e.preventDefault();
        this.moveCategoryFocus(-1);
        return;
      }
      if (e.key === 'Enter') {
        e.preventDefault();
        this.triggerFocusedItem();
      }
    });

    const handleZenEnter = () => {
      if (this._isOpen) {
        this.closeMenu();
      }
    };
    this.rootContainer.addEventListener('workbench:zen-enter', handleZenEnter);
    if (typeof window !== 'undefined') {
      window.addEventListener('workbench:zen-enter', handleZenEnter);
    }
  }

  public get isOpen(): boolean {
    return this._isOpen;
  }

  public toggleMenu(): void {
    if (this._isOpen) {
      this.closeMenu();
    } else {
      this.openMenu();
    }
  }

  public openMenu(): void {
    if (this._isOpen) return;
    this._isOpen = true;
    this.activeCategoryId = 'file';
    this.focusedItemIndex = 0;
    this.renderMenu();
  }

  private getSelectableItems(categoryId: string): MenuItem[] {
    const group = this.getGroups().find((g) => g.id === categoryId);
    return (group?.items || []).filter((it) => it.type !== 'separator');
  }

  private moveItemFocus(delta: number): void {
    const items = this.getSelectableItems(this.activeCategoryId);
    if (items.length === 0) return;
    this.focusedItemIndex = (this.focusedItemIndex + delta + items.length) % items.length;
    this.renderMenu();
  }

  private moveCategoryFocus(delta: number): void {
    const groups = this.getGroups();
    const currentIndex = groups.findIndex((g) => g.id === this.activeCategoryId);
    const nextIndex = (currentIndex + delta + groups.length) % groups.length;
    this.activeCategoryId = groups[nextIndex].id;
    this.focusedItemIndex = 0;
    this.renderMenu();
  }

  private triggerFocusedItem(): void {
    const items = this.getSelectableItems(this.activeCategoryId);
    const item = items[this.focusedItemIndex];
    if (!item) return;
    if (item.type === 'checkbox') {
      this.setItemChecked(item.id, !this.isItemChecked(item.id));
    }
    const act = this.itemActions.get(item.id) || item.action;
    if (act) {
      act();
    }
    this.closeMenu();
  }

  public closeMenu(): void {
    if (!this._isOpen) return;
    this._isOpen = false;
    if (this.menuDropdownEl && this.menuDropdownEl.parentNode) {
      this.menuDropdownEl.parentNode.removeChild(this.menuDropdownEl);
    }
    this.menuDropdownEl = null;
  }

  public getFileItems(): readonly MenuItem[] {
    return this.coreFileItems;
  }

  public getViewItems(): readonly MenuItem[] {
    return this.coreViewItems;
  }

  public getHelpItems(): readonly MenuItem[] {
    return this.coreHelpItems;
  }

  /**
   * Apps may only append custom items to File menu below a separator (D-31, FR-N6, FR-N7).
   * Core File items cannot be removed/reordered, and View/Help cannot be modified.
   */
  public addAppFileItem(item: MenuItem): void {
    this.appFileItems.push({ ...item });
  }

  public getGroups(): MenuGroup[] {
    const combinedFileItems: MenuItem[] = [...this.coreFileItems];
    if (this.appFileItems.length > 0) {
      combinedFileItems.push({ id: 'file:separator-app', label: '', type: 'separator' });
      combinedFileItems.push(...this.appFileItems);
    }

    return [
      { id: 'file', label: 'File', items: combinedFileItems },
      { id: 'view', label: 'View', items: [...this.coreViewItems] },
      { id: 'help', label: 'Help', items: [...this.coreHelpItems] },
    ];
  }

  public findItem(id: string): MenuItem | undefined {
    for (const group of this.getGroups()) {
      const found = group.items.find((it) => it.id === id);
      if (found) return found;
    }
    return undefined;
  }

  public setAction(id: string, action: () => void): void {
    this.itemActions.set(id, action);
  }

  public triggerItem(id: string): void {
    const act = this.itemActions.get(id) || this.findItem(id)?.action;
    if (act) {
      act();
    }
  }

  public setItemChecked(id: string, checked: boolean): void {
    this.checkboxStates.set(id, checked);
    if (this.menuDropdownEl) {
      const el = this.menuDropdownEl.querySelector(`[data-item-id="${id}"] .menu-item-check`);
      if (el) {
        el.textContent = checked ? '✓' : '';
      }
    }
  }

  public isItemChecked(id: string): boolean {
    return this.checkboxStates.get(id) ?? false;
  }

  private renderMenu(): void {
    if (this.menuDropdownEl) {
      this.menuDropdownEl.remove();
    }

    const menuEl = document.createElement('div');
    menuEl.id = 'workbench-menu-dropdown';
    menuEl.className = 'workbench-menu-dropdown';

    const groups = this.getGroups();

    groups.forEach((group) => {
      const groupRow = document.createElement('div');
      groupRow.className = `menu-category-row ${group.id === this.activeCategoryId ? 'active' : ''}`;
      groupRow.dataset.categoryId = group.id;

      const labelSpan = document.createElement('span');
      labelSpan.className = 'menu-category-label';
      labelSpan.textContent = group.label;

      const arrowSpan = document.createElement('span');
      arrowSpan.className = 'menu-category-arrow';
      arrowSpan.textContent = '▶';

      groupRow.appendChild(labelSpan);
      groupRow.appendChild(arrowSpan);

      const submenuEl = document.createElement('div');
      submenuEl.className = 'menu-submenu';
      submenuEl.dataset.parentGroup = group.id;

      let selectableIndex = -1;
      group.items.forEach((item) => {
        if (item.type === 'separator') {
          const sep = document.createElement('div');
          sep.className = 'menu-separator';
          submenuEl.appendChild(sep);
          return;
        }
        selectableIndex++;
        const isKeyboardFocused =
          group.id === this.activeCategoryId && selectableIndex === this.focusedItemIndex;

        const itemRow = document.createElement('div');
        itemRow.className = 'menu-item-row' + (isKeyboardFocused ? ' kbd-focused' : '');
        itemRow.dataset.itemId = item.id;

        const checkMark = document.createElement('span');
        checkMark.className = 'menu-item-check';
        if (item.type === 'checkbox') {
          checkMark.textContent = this.isItemChecked(item.id) ? '✓' : '';
        }

        const itemLabel = document.createElement('span');
        itemLabel.className = 'menu-item-label';
        itemLabel.textContent = item.label;

        const shortcut = document.createElement('span');
        shortcut.className = 'menu-item-shortcut';
        if (item.shortcut) {
          shortcut.textContent = item.shortcut;
        }

        itemRow.appendChild(checkMark);
        itemRow.appendChild(itemLabel);
        itemRow.appendChild(shortcut);

        itemRow.addEventListener('click', (e) => {
          e.stopPropagation();
          if (item.type === 'checkbox') {
            const next = !this.isItemChecked(item.id);
            this.setItemChecked(item.id, next);
          }
          const act = this.itemActions.get(item.id) || item.action;
          if (act) {
            act();
          }
          this.closeMenu();
        });

        submenuEl.appendChild(itemRow);
      });

      groupRow.appendChild(submenuEl);

      groupRow.addEventListener('mouseenter', () => {
        this.activeCategoryId = group.id;
        this.focusedItemIndex = 0;
        menuEl.querySelectorAll('.menu-category-row').forEach((r) => r.classList.remove('active'));
        groupRow.classList.add('active');
      });

      groupRow.addEventListener('click', (e) => {
        e.stopPropagation();
        this.activeCategoryId = group.id;
        this.focusedItemIndex = 0;
        menuEl.querySelectorAll('.menu-category-row').forEach((r) => r.classList.remove('active'));
        groupRow.classList.add('active');
      });

      menuEl.appendChild(groupRow);
    });

    this.rootContainer.appendChild(menuEl);
    this.menuDropdownEl = menuEl;
  }
}

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk rg -n \"public (layout|menu|editor|contextMenu)|appEditorSurface|setAction|addAppFileItem|addApp|sidebarApp|statusbarApp|keydown|openBeside|queryPanePlacement|getApi|closePanel|addNewTab|setComponentFactory|window\\.workbench|WorkbenchApp\" src/main.ts src/core/editor.ts src/core/layout.ts src/core/sidebar.ts; rtk rg -n \"FR-I[1-9]|FR-I10|FR-I11|FR-R1a?|FR-N10|FR-N12|FR-G5|FR-G6|NFR-1|NFR-7\" docs/current/SPEC.md; rtk rg -n \"D-4\\.|D-5\\.|D-9\\.|D-11\\.|D-19\\.|D-22\\.|D-23\\.|D-30\\.|D-31\\.\" docs/current/DECISIONS.md" in D:\projects\workbench-kit
 succeeded in 211ms:
60 matches in 4 files:

src/core/editor.ts:34:* or pane (addNewTab, closePanel, splitGroup, ...) — those stay shell-only.
src/core/editor.ts:44:openBeside(targetId: string, title?: string, options?: EditorOpenOptions): ID...
src/core/editor.ts:45:queryPanePlacement(panelId?: string): { own: PanePlacement; beside: PanePlace...
src/core/editor.ts:198:this.editorController.addNewTab(this.group);
src/core/editor.ts:296:public setComponentFactory(factory: EditorComponentFactory | null): void {
src/core/editor.ts:312:public getApi(): DockviewApi {
src/core/editor.ts:364:public addNewTab(targetGroup?: DockviewGroupPanel): IDockviewPanel {
src/core/editor.ts:475:public queryPanePlacement(panelId?: string): { own: PanePlacement; beside: Pa...
src/core/editor.ts:570:public openBeside(
src/core/editor.ts:628:this.closePanel(activePanel);
src/core/editor.ts:636:public closePanel(panel: IDockviewPanel): void {
src/core/layout.ts:17:sidebarAppActions: HTMLElement;
src/core/layout.ts:26:statusbarAppInfo: HTMLElement;
src/core/layout.ts:27:statusbarAppItems: HTMLElement;
src/core/layout.ts:99:sidebarAppActions: container.querySelector('#sidebar-app-actions') as HTMLEle...
src/core/layout.ts:108:statusbarAppInfo: container.querySelector('#statusbar-app-info') as HTMLElement,
src/core/layout.ts:109:statusbarAppItems: container.querySelector('#statusbar-app-items') as HTMLEle...
src/core/sidebar.ts:53:public addAppAction(action: ViewAction): void {
src/main.ts:18:export class WorkbenchApp {
src/main.ts:19:public layout: WorkbenchLayoutElements;
src/main.ts:20:public menu: MenuController;
src/main.ts:28:public editor: EditorController;
src/main.ts:30:public contextMenu: ContextMenuController;
src/main.ts:32:public contextMenuItemsForTreeNode: (nodeId: string) => ContextMenuItem[] = (...
src/main.ts:33:public contextMenuItemsForPanel: (panelId: string) => ContextMenuItem[] = () ...
src/main.ts:62:this.editor.setComponentFactory(this.kindRegistry.createComponentFactory());
src/main.ts:71:this.layout.sidebarAppActions,
src/main.ts:82:this.menu.setAction('file:open-folder', () => this.handleOpenFolderDialog());
src/main.ts:83:this.menu.setAction('file:open-recent', () => this.handleOpenRecentFolder());
src/main.ts:84:this.menu.setAction('file:close-folder', () => this.closeFolder());
src/main.ts:109:this.menu.setAction('view:toggle-sidebar', () => this.viewState.toggleSidebar...
src/main.ts:110:this.menu.setAction('view:toggle-titlebar', () => this.viewState.toggleTitleb...
src/main.ts:111:this.menu.setAction('view:toggle-statusbar', () => this.viewState.toggleStatu...
src/main.ts:112:this.menu.setAction('view:zen-mode', () => this.viewState.toggleZenMode());
src/main.ts:114:this.activityBar.setAction('activity:toggle-sidebar', () => this.viewState.to...
src/main.ts:115:this.activityBar.setAction('activity:toggle-titlebar', () => this.viewState.t...
src/main.ts:116:this.activityBar.setAction('activity:toggle-statusbar', () => this.viewState....
src/main.ts:117:this.activityBar.setAction('activity:zen-mode', () => this.viewState.toggleZe...
src/main.ts:123:this.menu.setAction('view:cycle-color-theme', () => this.theme.cycleTheme());
src/main.ts:124:this.activityBar.setAction('activity:cycle-color-theme', () => this.theme.cyc...
src/main.ts:127:this.menu.setAction('view:cycle-icon-theme', () => this.iconTheme.toggleTheme...
src/main.ts:137:this.editor.openBeside(node.id, node.label, { meta: { kind: kindOf(node.isCon...
src/main.ts:142:this.menu.setAction('view:toggle-context-menu', () => {
  +17 more in src/main.ts [see remaining: tail -n +26 ~/AppData\Local\rtk\tee\1788914879_grep_3_src_main_ts.log]
17:그중 10건(FR-A20 ~ A24 · FR-N6b · FR-N6c · FR-I9 ~ I11)은 D-9와
19:않은 것을 D-30 · D-31로 메운 것이다. 다시 3건(FR-N10a · FR-N10b · FR-R1a)은
80:| FR-C9 | 탭 제목에서 오른쪽 버튼 | 기본 상태에서는 아무 일도 없다 (FR-G5) | 껍데기만 있는 상태에서 탭 제목 우클릭 → 메뉴 요소가 0개다 | D-22 |
125:| FR-G5 | 오른쪽 버튼을 누른다 (기본 상태) | 아무 일도 일어나지 않는다 | 트리와 탭 어디에서 우클릭해도 메뉴 요소가 0개다 | D-22 |
126:| FR-G6 | 앱이 항목을 정의하고 View에서 우클릭 메뉴를 켠다 | 그 항목들이 뜬다. 껍데기가 넣은 항목은 없다 | 켠 뒤 뜬 메뉴의 항목 목록이 앱이 정의한 목록과 정확히 같다(껍데기가 더한 항목 0개) | D-22 · D-23 |
139:| FR-I1 | 시험이 임의의 리소스 종류를 하나 등록한다 | 공통 코어 **0줄** 수정으로 그것이 탭에서 열린다 | 등록 코드를 공통 코어 밖에만 두고, 공통 코어 파일의 diff가 0줄인 상태로 그 종류가 탭에 열린다 | D-4 · D-19 |
140:| FR-I2 | 파일 프리셋을 쓴다 | 트리에 파일이 보이고, 고르면 그 파일을 대상으로 하는 보기가 탭에 열린다 | 공통 코어 diff 0줄로, 트리에 파일 행이 있고 고른 파일이 활성 탭의 대상이 된다 | D-5 · D-19 |
141:| FR-I3 | 폴더 프리셋을 쓴다 | 트리에 폴더가 보이고, 고르면 그 폴더를 대상으로 하는 보기가 탭에 열린다 | 공통 코어 diff 0줄로, 트리에 폴더 행이 있고 고른 폴더가 활성 탭의 대상이 된다 | D-5 · D-19 |
142:| FR-I4 | 앱이 "옆 칸에 열기"를 부른다 | FR-A14와 같은 결과가 난다 | 앱 코드에서 그 명령을 부른 결과가 FR-A14의 판정과 일치한다 | D-19 |
143:| FR-I5 | 앱이 칸 배치를 묻는다 | 자기가 있는 칸과 옆 칸에 무엇이 열려 있는지를 받는다 | 칸 2개에 서로 다른 대상을 열어 두고 물으면, 자기 칸 식별자와 옆 칸의 대상이 실제와 일치해 돌아온다 | D-19 |
144:| FR-I6 | 포커스가 탭 안일 때 껍데기가 안 쓰는 키를 누른다 | 그 키가 앱에 전달된다 | 껍데기 예약 키 목록에 없는 키(`F3`·`F4`·`F5`·`F6`·`Space` 등)를 누르면 탭의 보기가 그 키를 받는다 | D-19 |
145:| FR-I7 | 포커스가 트리일 때 트리 키를 누른다 | 껍데기가 처리한다. 앱에 가지 않는다 | 트리 포커스에서 트리 키를 누르면 트리 동작이 일어나고 앱 쪽 수신 기록이 0건이다 | D-9 · D-19 |
146:| FR-I8 | 앱이 탭이나 칸을 직접 건드리려 한다 | 그런 수단이 없다. 껍데기가 여는 것(FR-I4 · FR-I5)으로만 한다 | 앱에 노출된 표면에 탭·칸을 만들거나 닫거나 옮기는 수단이 0개다 | D-19 |
147:| FR-I9 † | 앱이 File 묶음에 항목을 더한다 | 껍데기 항목 다섯 아래 가름선 다음에 붙는다. 껍데기 항목은 목록·순서 그대로다 | 공통 코어 diff 0줄로 항목이 붙고, 그 뒤에도 FR-N6의 껍데기 항목 다섯이 목록·순서 그대로다. 앱이 껍데기 항목을 빼거나 순서를 바꾸는 수단이 0개다 | D-31 |
148:| FR-I10 † | 앱이 탐색기 뷰 제목줄에 액션을 더한다 | 껍데기 액션 둘(모두 접기·새로 고침) 왼쪽에 붙는다 | 공통 코어 diff 0줄로 액션이 붙고, 그 뒤에도 FR-A20의 껍데기 액션 둘이 그대로 오른쪽에 있다 | D-30 · D-31 |
149:| FR-I11 † | 앱이 View 또는 Help 묶음에 항목을 더하려 한다 | 그런 수단이 없다 | 앱에 노출된 표면에 View·Help 묶음을 건드리는 수단이 0개다 | D-31 |
210:| FR-N10 | 앱이 하단 바에 자기 항목을 하나 붙인다 | 껍데기 항목과 함께 보인다 | 공통 코어 diff 0줄로 앱 항목이 하단 바에 나타나고, 껍데기의 세 자리가 그대로 남는다 | D-23 |
211:| FR-N10a † | 앱이 하단 바에 오류·안내 한 줄을 올린다 | 껍데기 오류(FR-G2 ~ G3)와 **같은 자리에 같은 모양으로** 뜬다. 떠오르는 알림 상자는 없다 | 메시지 자리에 앱이 준 문구가 1줄 그대로(껍데기가 고친 글자 0개) 나타나고, 그때 새로 떠 있는 요소가 0개다. 껍데기가 올린 것과 앱이 올린 것을 이어서 올리면 **가장 최근 것 하나만** 보인다 | D-21 · D-32 |
212:| FR-N10b † | 앱이 진행 표시를 켜고 끈다 | 껍데기 진행(FR-G4)과 같은 자리에 같은 모양으로 뜨고, 앱이 끄면 사라진다 | 켜면 진행 표시가 나타나고, 끄기 전에는 껍데기가 임의로 끄지 않으며(일정 시간 뒤 사라지는 경우가 0건), 끄면 사라진다 | D-21 · D-32 |
214:| FR-N12 | 앱이 세로 띠 항목을 바꾼다 | 공통 코어를 고치지 않고 바뀐다. 메뉴(View)의 대응 항목은 그대로 남는다 | 공통 코어 diff 0줄로 띠의 항목이 바뀌고, 그 뒤에도 FR-N7의 View 항목 열이 그대로다 | D-11 · D-23 |
243:| FR-R1 | 껍데기가 먼저 가져가는 키가 무엇인지 찾는다 | 목록이 문서로 있다. 그 목록에 없는 키는 앱에 간다(FR-I6) | 예약 키 목록 문서가 1개 있고, 목록에 없는 키를 눌렀을 때 FR-I6이 성립한다 | D-19 |
244:| FR-R1a † | 껍데기가 **가져가지 않는** 흔한 키가 무엇인지 찾는다 | 같은 문서에 적혀 있다 — `Ctrl+S` · `F2` · `Delete` · `Ctrl+Z` / `Ctrl+Y` · `Ctrl+C` / `Ctrl+V`는 앱 몫이다 | 그 여섯이 목록에 "가져가지 않는다"로 적혀 있고, 탭 포커스에서 각각을 눌렀을 때 껍데기 쪽 처리 기록이 0건이며 탭의 보기가 받는다 | D-18 · D-19 · D-22 · D-30 |
254:| NFR-1 | 리소스 종류를 아는 코드가 공통 코어에 없다 | 공통 코어에서 파일·폴더·터미널·확장자를 아는 코드가 0건이다 | INTENT 3 · D-4 · D-17 |
260:| NFR-7 | 공통 UI의 모든 명령을 **키보드만으로** 부를 수 있다 | 메뉴(FR-N5 ~ N8) · 세로 띠(FR-N11) · 탭 줄의 명령 각각에 마우스 없이 실행하는 경로가 1개 이상 있다. 그 경로에 쓰이는 키는 FR-R1의 예약 키 목록 문서에 적혀 있다 | D-9 · D-19 · 이번 세션 확정 |
274:| C-9 | 앱에게 탭·칸을 직접 조작하는 표면을 열지 않는다. 앱은 껍데기가 여는 것(FR-I4 · FR-I5)으로만 한다 | D-19 |
90:## D-4. 터미널 같은 살아 있는 보기는 앱이 제공하고, 껍데기는 탭과 호스트 브릿지만 준다
112:## D-5. 탭은 사용자가 추가할 때만 늘어난다 — 탐색기 선택은 현재 탭에서 연다
281:## D-9. 탐색기 트리는 VS Code의 외형과 키보드 조작을 그대로 따른다
372:## D-11. 세로 띠는 뷰를 고르는 자리가 아니라 자주 쓰는 명령을 담는 자리다
726:## D-19. 앱이 "옆 칸에 열기"를 부를 수 있고, 탭 안의 키는 앱에게 간다
849:## D-22. 우클릭 메뉴는 껍데기가 장치만 주고 항목은 앱이 채운다
892:## D-23. 메뉴는 File · View · Help 세 묶음, 하단 바는 경로와 앱 정보
1195:## D-30. 탐색기 뷰 제목줄을 둔다 — 접기·새로 고침은 껍데기, 만들기는 앱
1242:## D-31. 앱이 File 묶음과 뷰 제목줄에 항목을 더한다 — View·Help는 껍데기만 채운다

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk powershell -NoProfile -Command \"Get-Content 'src\\core\\editor.ts' | Select-Object -First 90\"; rtk cmd /c type src\\main.ts; rtk cmd /c type docs\\reserved-keys.md" in D:\projects\workbench-kit
 succeeded in 632ms:
import {
  createDockview,
  DockviewApi,
  DockviewGroupPanel,
  IDockviewPanel,
  IContentRenderer,
  IHeaderActionsRenderer,
  IGroupHeaderProps,
  GroupPanelPartInitParameters,
  Direction,
  DockviewPanelRenderer,
} from 'dockview-core';

export interface EditorOpenOptions {
  renderer?: DockviewPanelRenderer;
  isUserCreatedEmptyTab?: boolean;
  /**
   * Opaque metadata the caller attaches to a panel (FR-I1, D-4, NFR-1).
   * The shell stores and forwards this bag without inspecting its keys or
   * values, so registering a new resource kind never requires a core change.
   */
  meta?: Record<string, unknown>;
}

export interface PanePlacement {
  groupId: string;
  targets: (string | null)[];
}

/**
 * The entire surface the shell exposes to app code (presets, registry
 * helpers) that opens or inspects panes (FR-I4, FR-I5, FR-I8, C-9). It
 * deliberately excludes every method that creates, closes, or moves a tab
 * or pane (addNewTab, closePanel, splitGroup, ...) ??those stay shell-only.
 * `EditorController` satisfies this structurally; no wrapper is needed.
 */
export interface AppEditorSurface {
  openItem(
    targetId: string,
    title?: string,
    options?: EditorOpenOptions,
    targetGroup?: DockviewGroupPanel
  ): IDockviewPanel;
  openBeside(targetId: string, title?: string, options?: EditorOpenOptions): IDockviewPanel;
  queryPanePlacement(panelId?: string): { own: PanePlacement; beside: PanePlacement | null } | undefined;
}

export interface PanelContentState {
  targetId: string | null;
  isUserCreatedEmptyTab: boolean;
  creationCount: number;
  disposalCount: number;
  instanceStamp: string;
  isDirty: boolean;
}

/**
 * Custom content renderer for editor panels.
 * Tracks lifecycle creation and disposal counts (FR-E1 ~ FR-E5).
 */
export class EditorContentRenderer implements IContentRenderer {
  public readonly element: HTMLElement;
  public creationCount = 0;
  public disposalCount = 0;
  public instanceStamp: string;
  public params: any = {};
  public isShown = false;
  private timer: any = null;
  private timerTicks = 0;
  public domStateValue = '';

  constructor() {
    this.creationCount++;
    this.instanceStamp = 'stamp-' + Math.random().toString(36).slice(2, 10);
    this.element = document.createElement('div');
    this.element.className = 'editor-panel-content';

    // Live continuous state tracking (FR-E1 ~ FR-E5, D-24)
    this.timer = setInterval(() => {
      this.timerTicks++;
    }, 50);
  }

  public getTimerTicks(): number {
    return this.timerTicks;
  }

  public init(parameters: GroupPanelPartInitParameters): void {
    this.params = parameters.params || {};
    this.updateContent();
import { createWorkbenchLayout, WorkbenchLayoutElements } from './core/layout';
import { setupWindowControls } from './core/window';
import { MenuController } from './core/menu';
import { ActivityBarController } from './core/activitybar';
import { ViewStateManager } from './core/viewstate';
import { ThemeManager } from './core/theme';
import { IconThemeManager } from './core/icontheme';
import { SetiResolver, VscodeIconsResolver } from './icons';
import { TreeController } from './core/tree';
import { ExplorerTitlebarController } from './core/sidebar';
import { FileSystemTreeProvider, promptOpenFolderDialog } from './providers/filesystem';
import { EditorController } from './core/editor';
import { ContextMenuController, ContextMenuItem } from './core/contextmenu';
import { ResourceKindRegistry } from './registry/kind-registry';
import { registerFilePreset, FILE_KIND } from './presets/file-preset';
import { registerFolderPreset, FOLDER_KIND } from './presets/folder-preset';

export class WorkbenchApp {
  public layout: WorkbenchLayoutElements;
  public menu: MenuController;
  public activityBar: ActivityBarController;
  public viewState: ViewStateManager;
  public theme: ThemeManager;
  public iconTheme: IconThemeManager;
  public tree: TreeController;
  public explorerTitlebar: ExplorerTitlebarController;
  public fsProvider: FileSystemTreeProvider;
  public editor: EditorController;
  public kindRegistry: ResourceKindRegistry;
  public contextMenu: ContextMenuController;
  /** App-supplied item providers for the right-click device (FR-G6). Empty by default (D-22). */
  public contextMenuItemsForTreeNode: (nodeId: string) => ContextMenuItem[] = () => [];
  public contextMenuItemsForPanel: (panelId: string) => ContextMenuItem[] = () => [];
  private recentFolders: string[] = [];

  constructor(container: HTMLElement) {
    this.layout = createWorkbenchLayout(container);
    setupWindowControls(
      this.layout.windowMinBtn,
      this.layout.windowMaxBtn,
      this.layout.windowCloseBtn
    );
    this.menu = new MenuController(this.layout.menuBtn, this.layout.root);
    this.activityBar = new ActivityBarController(
      this.layout.activityBarTop,
      this.layout.activityBarBottom
    );
    this.viewState = new ViewStateManager(this.layout);
    this.theme = new ThemeManager('dark');
    this.iconTheme = new IconThemeManager('seti', this.theme.getTheme());
    this.iconTheme.registerResolver('seti', new SetiResolver());
    this.iconTheme.registerResolver('vscode-icons', new VscodeIconsResolver());
    this.theme.onThemeChange((theme) => this.iconTheme.setColorTheme(theme));

    // Initialize EditorController layout engine (FR-C, FR-D, FR-E, FR-J, WK-019 ~ WK-024)
    this.editor = new EditorController(this.layout.editorContainer);

    // Resource kind registration slot + minimal example presets (FR-I1, FR-I2, FR-I3, WK-025, WK-026)
    this.kindRegistry = new ResourceKindRegistry();
    registerFilePreset(this.kindRegistry);
    registerFolderPreset(this.kindRegistry);
    this.editor.setComponentFactory(this.kindRegistry.createComponentFactory());

    // Right-click menu device, default off (FR-G5, FR-G6, D-22, WK-030)
    this.contextMenu = new ContextMenuController(this.layout.root);

    // Initialize TreeController and Explorer view titlebar (FR-A, D-9, D-30)
    this.tree = new TreeController(this.layout.sidebarContent, this.iconTheme);
    this.explorerTitlebar = new ExplorerTitlebarController(
      this.layout.sidebarHeader,
      this.layout.sidebarAppActions,
      this.layout.sidebarCollapseAllBtn,
      this.layout.sidebarRefreshBtn,
      this.tree
    );
    this.fsProvider = new FileSystemTreeProvider();
    this.tree.setDataProvider(this.fsProvider);

    this.loadRecentFolders();

    // Bind File menu actions (FR-A1, FR-N6a, FR-N6c)
    this.menu.setAction('file:open-folder', () => this.handleOpenFolderDialog());
    this.menu.setAction('file:open-recent', () => this.handleOpenRecentFolder());
    this.menu.setAction('file:close-folder', () => this.closeFolder());

    // Update statusbar path on node selection
    this.tree.onSelect((nodes) => {
      if (nodes.length > 0) {
        const primary = nodes[0];
        const dataObj = primary.data as { path?: string } | undefined;
        const p = dataObj?.path || primary.id;
        if (this.layout.statusbarPath) {
          this.layout.statusbarPath.textContent = String(p);
        }
      }
    });

    // Update sidebar title on root change
    this.tree.onRootChange((root) => {
      if (this.layout.sidebarTitle) {
        this.layout.sidebarTitle.textContent = root ? root.label.toUpperCase() : 'EXPLORER';
      }
      if (!root && this.layout.statusbarPath) {
        this.layout.statusbarPath.textContent = '';
      }
    });

    // Bind sidebar, titlebar, statusbar, and zen toggles
    this.menu.setAction('view:toggle-sidebar', () => this.viewState.toggleSidebar());
    this.menu.setAction('view:toggle-titlebar', () => this.viewState.toggleTitlebar());
    this.menu.setAction('view:toggle-statusbar', () => this.viewState.toggleStatusbar());
    this.menu.setAction('view:zen-mode', () => this.viewState.toggleZenMode());

    this.activityBar.setAction('activity:toggle-sidebar', () => this.viewState.toggleSidebar());
    this.activityBar.setAction('activity:toggle-titlebar', () => this.viewState.toggleTitlebar());
    this.activityBar.setAction('activity:toggle-statusbar', () => this.viewState.toggleStatusbar());
    this.activityBar.setAction('activity:zen-mode', () => this.viewState.toggleZenMode());

    // Ensure menu controller state is closed when entering Zen mode (FR-F5, D-12)
    this.viewState.onZenEnter(() => this.menu.closeMenu());

    // Bind theme cycling (FR-M1: White -> Gray -> Dark)
    this.menu.setAction('view:cycle-color-theme', () => this.theme.cycleTheme());
    this.activityBar.setAction('activity:cycle-color-theme', () => this.theme.cycleTheme());

    // Bind icon theme cycling (FR-Q1a: seti <-> vscode-icons, View menu only)
    this.menu.setAction('view:cycle-icon-theme', () => this.iconTheme.toggleTheme());

    // Bind Tree item opening rules (FR-B1 ~ FR-B4, FR-A6, FR-A14, D-5).
    // Which kind a node opens as is an app-layer decision (isContainer is a
    // generic core field); the shell itself never branches on file/folder.
    const kindOf = (isContainer: boolean | undefined) => (isContainer ? FOLDER_KIND : FILE_KIND);
    this.tree.onOpen((node) => {
      this.editor.openItem(node.id, node.label, { meta: { kind: kindOf(node.isContainer) } });
    });
    this.tree.onOpenToSide((node) => {
      this.editor.openBeside(node.id, node.label, { meta: { kind: kindOf(node.isContainer) } });
    });

    // Right-click menu wiring (FR-G5, FR-G6, D-22). The shell only draws the
    // device; whether it is enabled and what items appear are app decisions.
    this.menu.setAction('view:toggle-context-menu', () => {
      this.contextMenu.setEnabled(this.menu.isItemChecked('view:toggle-context-menu'));
    });
    this.layout.sidebarContent.addEventListener('contextmenu', (e) => {
      const row = (e.target as HTMLElement).closest('.tree-row') as HTMLElement | null;
      if (!row) return;
      e.preventDefault();
      const nodeId = row.getAttribute('data-id');
      const items = nodeId ? this.contextMenuItemsForTreeNode(nodeId) : [];
      this.contextMenu.show(e.clientX, e.clientY, items);
    });
    this.layout.editorContainer.addEventListener('contextmenu', (e) => {
      const tab = (e.target as HTMLElement).closest('.dv-tab') as HTMLElement | null;
      if (!tab) return;
      e.preventDefault();
      const panelId = tab.getAttribute('data-tab-panel-id');
      const items = panelId ? this.contextMenuItemsForPanel(panelId) : [];
      this.contextMenu.show(e.clientX, e.clientY, items);
    });

    // File menu tab actions (FR-N6b)
    this.menu.setAction('file:close-tab', () => this.editor.closeActiveTab());

    // View menu layout actions (FR-D3, FR-J2)
    this.menu.setAction('view:split-horizontal', () => this.editor.splitActiveGroup('right'));
    this.menu.setAction('view:split-vertical', () => this.editor.splitActiveGroup('below'));
    this.menu.setAction('view:close-active-tabs', () => this.editor.closeAllTabsInGroup());

    // Activity bar split actions (FR-D3)
    this.activityBar.setAction('activity:split-horizontal', () => this.editor.splitActiveGroup('right'));
    this.activityBar.setAction('activity:split-vertical', () => this.editor.splitActiveGroup('below'));

    // Minimal example wiring proving the app-facing extension slots (WK-048, WK-029):
    // File menu item below the shell's separator (FR-I9), a view-titlebar action left
    // of the shell's two actions (FR-I10), and one status bar item beside the shell's
    // three slots (FR-N10). These are wiring examples, not domain functionality (INTENT 7).
    this.menu.addAppFileItem({
      id: 'app:file:preset-info',
      label: 'Preset Info',
      action: () => {
        if (this.layout.statusbarMessage) {
          this.layout.statusbarMessage.textContent = 'Presets registered: file, folder';
        }
      },
    });
    this.explorerTitlebar.addAppAction({
      id: 'app:sidebar:preset-info',
      title: 'Preset Info',
      iconClass: 'codicon-info',
      action: () => {
        if (this.layout.statusbarMessage) {
          this.layout.statusbarMessage.textContent = 'Presets registered: file, folder';
        }
      },
    });
    if (this.layout.statusbarAppItems) {
      const presetInfoEl = document.createElement('span');
      presetInfoEl.className = 'statusbar-app-item';
      presetInfoEl.textContent = 'Presets: file, folder';
      this.layout.statusbarAppItems.appendChild(presetInfoEl);
    }

    // Global keyboard shortcuts: Ctrl+O, Ctrl+W, Ctrl+\
    if (typeof window !== 'undefined') {
      window.addEventListener('keydown', (e) => {
        if (e.ctrlKey && !e.shiftKey && !e.altKey && (e.key === 'o' || e.key === 'O')) {
          e.preventDefault();
          this.handleOpenFolderDialog();
        } else if (e.ctrlKey && !e.shiftKey && !e.altKey && (e.key === 'w' || e.key === 'W')) {
          e.preventDefault();
          this.editor.closeActiveTab();
        } else if (e.ctrlKey && !e.shiftKey && !e.altKey && e.key === '\\') {
          e.preventDefault();
          this.editor.splitActiveGroup('right');
        }
      });
    }

    // Statusbar app info: app name, version, host branch (FR-N9, D-7)
    const updateAppInfo = () => {
      const isElectron = typeof window !== 'undefined' && (Boolean(window.workbenchHost) || (navigator.userAgent && navigator.userAgent.includes('Electron')));
      const isPywebview = typeof window !== 'undefined' && (Boolean(window.pywebview) || (navigator.userAgent && navigator.userAgent.includes('pywebview')));
      const branch = isElectron ? 'Electron' : (isPywebview ? 'pywebview' : '');
      const text = branch ? `workbench-kit v0.1.0 · ${branch}` : 'workbench-kit v0.1.0';
      if (this.layout.statusbarAppInfo) {
        this.layout.statusbarAppInfo.textContent = text;
      }
    };
    updateAppInfo();
    if (typeof window !== 'undefined') {
      window.addEventListener('pywebviewready', updateAppInfo);
    }
  }

  private currentFolderRequestId = 0;

  public async openFolder(folderPath: string): Promise<void> {
    const reqId = ++this.currentFolderRequestId;
    try {
      const rootNode = await this.fsProvider.createRootNode(folderPath);
      if (this.currentFolderRequestId !== reqId) {
        return;
      }
      this.tree.setRoot(rootNode);
      if (this.layout.statusbarPath) {
        this.layout.statusbarPath.textContent = folderPath;
      }
      this.addRecentFolder(folderPath);
    } catch (err) {
      if (this.currentFolderRequestId !== reqId) {
        return;
      }
      if (this.layout.statusbarMessage) {
        this.layout.statusbarMessage.textContent = `Error opening folder: ${String(err)}`;
      }
    }
  }

  /**
   * Clears root and returns tree to empty state without altering editor tabs (FR-G1, FR-N6c).
   */
  public closeFolder(): void {
    this.currentFolderRequestId++;
    this.tree.clearRoot();
    if (this.layout.statusbarPath) {
      this.layout.statusbarPath.textContent = '';
    }
  }

  public async handleOpenFolderDialog(): Promise<void> {
    const selected = await promptOpenFolderDialog();
    if (selected) {
      await this.openFolder(selected);
    }
  }

  public async handleOpenRecentFolder(): Promise<void> {
    if (this.recentFolders.length > 0) {
      await this.openFolder(this.recentFolders[0]);
    }
  }

  public getRecentFolders(): readonly string[] {
    return this.recentFolders;
  }

  private loadRecentFolders(): void {
    try {
      if (typeof localStorage !== 'undefined') {
        const raw = localStorage.getItem('workbench:recent-folders');
        if (raw) this.recentFolders = JSON.parse(raw);
      }
    } catch {
      // Ignore localStorage errors
    }
  }

  private addRecentFolder(folderPath: string): void {
    this.recentFolders = [folderPath, ...this.recentFolders.filter((p) => p !== folderPath)].slice(0, 10);
    try {
      if (typeof localStorage !== 'undefined') {
        localStorage.setItem('workbench:recent-folders', JSON.stringify(this.recentFolders));
      }
    } catch {
      // Ignore localStorage errors
    }
  }
}

let appInstance: WorkbenchApp | null = null;

export function initWorkbench(): WorkbenchApp {
  if (appInstance) return appInstance;
  const appContainer = document.getElementById('app');
  if (!appContainer) {
    throw new Error('Root #app container not found');
  }
  appInstance = new WorkbenchApp(appContainer);
  if (typeof window !== 'undefined') {
    (window as any).__workbenchApp = appInstance;
  }
  return appInstance;
}

if (typeof window !== 'undefined') {
  if (document.readyState === 'loading') {
    window.addEventListener('DOMContentLoaded', () => {
      initWorkbench();
    });
  } else {
    initWorkbench();
  }
}
# RESERVED-KEYS — 껍데기가 먼저 가져가는 키 (FR-R1 · FR-R1a · D-19)

> 이 문서가 유일한 예약 키 목록이다(FR-R1). 여기 없는 키는 전부 앱(탭 안의 보기) 몫이다.
> 포커스가 탭 안에 있을 때 이 목록에 없는 키를 누르면 껍데기 쪽 처리 기록이 0건이고
> 그 키는 탭의 보기가 받는다(FR-I6).

## 1. 전역 — 포커스와 무관하게 껍데기가 가져간다

| 키 | 동작 | 근거 |
| --- | --- | --- |
| `Ctrl+O` | 폴더 열기 | FR-A1, D-16 |
| `Ctrl+W` | 활성 탭 닫기 | FR-N6b |
| `Ctrl+\` | 좌우 스플릿 | FR-D1 |
| `F10` | 햄버거 메뉴 열기/닫기 | FR-N5, D-7 |
| `F11` | Zen 모드 전환 | D-12 |
| `Escape` | 열린 메뉴를 닫거나(메뉴가 열려 있을 때), Zen 모드를 빠져나온다(Zen일 때) | D-12 |

## 2. 메뉴가 열려 있을 때만

| 키 | 동작 |
| --- | --- |
| `ArrowUp` / `ArrowDown` | 항목 사이 이동 |
| `ArrowLeft` / `ArrowRight` | File · View · Help 사이 이동 |
| `Enter` | 포커스된 항목 실행 |
| `Escape` | 메뉴 닫기 |

## 3. 트리에 포커스가 있을 때만 (FR-A2 ~ FR-A19, D-9)

| 키 | 동작 |
| --- | --- |
| `ArrowUp` / `ArrowDown` | 포커스 1칸 이동 |
| `ArrowRight` | 접힌 폴더 펼침 / 펼쳐진 폴더 첫 자식으로 이동 |
| `ArrowLeft` | 펼친 폴더 접기 / 접힌 폴더·리프는 부모로 이동 |
| `Ctrl+ArrowLeft` | 모두 접기 |
| `Space` | 토글(펼침/접힘) |
| `Enter` | 선택한 항목 열기 (FR-A6) |
| `Ctrl+Enter` | 옆 칸에 열기 (FR-A14) |
| `Ctrl+Shift+Enter` | 선택 토글 |
| `Home` / `End` | 처음/끝으로 이동 |
| `PageUp` / `PageDown` | 화면 단위 이동 |
| `Shift+ArrowUp` / `Shift+ArrowDown` | 연속 범위 선택 |
| `Ctrl+ArrowUp` / `Ctrl+ArrowDown` | 포커스 불변 스크롤 |
| `Ctrl+A` | 전체 선택 |
| `Escape` | 선택 해제 (또는 찾기 위젯이 열려 있으면 찾기 위젯 닫기) |
| `F3` / `Ctrl+Alt+F` | 트리 안에서 찾기 열기 |

## 4. 껍데기가 **가져가지 않는다** — 흔히 오해하는 여섯 (FR-R1a)

탭에 포커스가 있을 때 아래 여섯은 껍데기가 손대지 않고 그대로 탭의 보기가 받는다.
저장·이름 바꾸기·삭제·되돌리기·복사·붙이기는 전부 리소스 종류를 아는 앱의 일이다
(D-18 · D-22 · D-30).

| 키 | 앱 몫 |
| --- | --- |
| `Ctrl+S` | 저장 |
| `F2` | 이름 바꾸기 |
| `Delete` | 삭제 |
| `Ctrl+Z` / `Ctrl+Y` | 되돌리기 / 다시하기 |
| `Ctrl+C` / `Ctrl+V` | 복사 / 붙이기 |

## 5. 마우스 없이 부르는 경로 (NFR-7)

메뉴·세로 띠·탭 줄의 명령은 각각 마우스 없이 실행하는 경로가 최소 1개 있다.

| 자리 | 경로 |
| --- | --- |
| 메뉴(File · View · Help) | `F10`으로 열고, 화살표 키로 항목을 옮기고, `Enter`로 실행한다 |
| 세로 띠 | 네이티브 `<button>`이라 `Tab`으로 포커스하고 `Enter`/`Space`로 누른다 |
| 탭 줄(Split Right · Split Down · New Tab) | 네이티브 `<button>`이라 `Tab`으로 포커스하고 `Enter`/`Space`로 누른다 |

이 경로들에 쓰이는 키는 전부 위 1 ~ 3절에 있다. 이 목록 밖의 키가 명령 실행에 쓰이는
경우는 없다.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk rg -n -C 8 \"addEventListener\\('keydown'|e\\.key|event\\.key|preventDefault\" src/core src/main.ts; rtk rg -n \"addAppAction|getActions|appActions|setActions|innerHTML|appendChild\" src/core/sidebar.ts src/core/activitybar.ts src/core/layout.ts" in D:\projects\workbench-kit
 succeeded in 165ms:
src/main.ts-140-    // Right-click menu wiring (FR-G5, FR-G6, D-22). The shell only draws the
src/main.ts-141-    // device; whether it is enabled and what items appear are app decisions.
src/main.ts-142-    this.menu.setAction('view:toggle-context-menu', () => {
src/main.ts-143-      this.contextMenu.setEnabled(this.menu.isItemChecked('view:toggle-context-menu'));
src/main.ts-144-    });
src/main.ts-145-    this.layout.sidebarContent.addEventListener('contextmenu', (e) => {
src/main.ts-146-      const row = (e.target as HTMLElement).closest('.tree-row') as HTMLElement | null;
src/main.ts-147-      if (!row) return;
src/main.ts:148:      e.preventDefault();
src/main.ts-149-      const nodeId = row.getAttribute('data-id');
src/main.ts-150-      const items = nodeId ? this.contextMenuItemsForTreeNode(nodeId) : [];
src/main.ts-151-      this.contextMenu.show(e.clientX, e.clientY, items);
src/main.ts-152-    });
src/main.ts-153-    this.layout.editorContainer.addEventListener('contextmenu', (e) => {
src/main.ts-154-      const tab = (e.target as HTMLElement).closest('.dv-tab') as HTMLElement | null;
src/main.ts-155-      if (!tab) return;
src/main.ts:156:      e.preventDefault();
src/main.ts-157-      const panelId = tab.getAttribute('data-tab-panel-id');
src/main.ts-158-      const items = panelId ? this.contextMenuItemsForPanel(panelId) : [];
src/main.ts-159-      this.contextMenu.show(e.clientX, e.clientY, items);
src/main.ts-160-    });
src/main.ts-161-
src/main.ts-162-    // File menu tab actions (FR-N6b)
src/main.ts-163-    this.menu.setAction('file:close-tab', () => this.editor.closeActiveTab());
src/main.ts-164-
src/main.ts-198-      const presetInfoEl = document.createElement('span');
src/main.ts-199-      presetInfoEl.className = 'statusbar-app-item';
src/main.ts-200-      presetInfoEl.textContent = 'Presets: file, folder';
src/main.ts-201-      this.layout.statusbarAppItems.appendChild(presetInfoEl);
src/main.ts-202-    }
src/main.ts-203-
src/main.ts-204-    // Global keyboard shortcuts: Ctrl+O, Ctrl+W, Ctrl+\
src/main.ts-205-    if (typeof window !== 'undefined') {
src/main.ts:206:      window.addEventListener('keydown', (e) => {
src/main.ts:207:        if (e.ctrlKey && !e.shiftKey && !e.altKey && (e.key === 'o' || e.key === 'O')) {
src/main.ts:208:          e.preventDefault();
src/main.ts-209-          this.handleOpenFolderDialog();
src/main.ts:210:        } else if (e.ctrlKey && !e.shiftKey && !e.altKey && (e.key === 'w' || e.key === 'W')) {
src/main.ts:211:          e.preventDefault();
src/main.ts-212-          this.editor.closeActiveTab();
src/main.ts:213:        } else if (e.ctrlKey && !e.shiftKey && !e.altKey && e.key === '\\') {
src/main.ts:214:          e.preventDefault();
src/main.ts-215-          this.editor.splitActiveGroup('right');
src/main.ts-216-        }
src/main.ts-217-      });
src/main.ts-218-    }
src/main.ts-219-
src/main.ts-220-    // Statusbar app info: app name, version, host branch (FR-N9, D-7)
src/main.ts-221-    const updateAppInfo = () => {
src/main.ts-222-      const isElectron = typeof window !== 'undefined' && (Boolean(window.workbenchHost) || (navigator.userAgent && navigator.userAgent.includes('Electron')));
src/core\contextmenu.ts-16-  private focusedIndex = -1;
src/core\contextmenu.ts-17-
src/core\contextmenu.ts-18-  constructor(private rootContainer: HTMLElement) {
src/core\contextmenu.ts-19-    document.addEventListener('mousedown', (e) => {
src/core\contextmenu.ts-20-      if (this.menuEl && !this.menuEl.contains(e.target as Node)) {
src/core\contextmenu.ts-21-        this.hide();
src/core\contextmenu.ts-22-      }
src/core\contextmenu.ts-23-    });
src/core\contextmenu.ts:24:    document.addEventListener('keydown', (e) => this.handleKeydown(e));
src/core\contextmenu.ts-25-    if (typeof window !== 'undefined') {
src/core\contextmenu.ts-26-      window.addEventListener('blur', () => this.hide());
src/core\contextmenu.ts-27-    }
src/core\contextmenu.ts-28-  }
src/core\contextmenu.ts-29-
src/core\contextmenu.ts-30-  public setEnabled(enabled: boolean): void {
src/core\contextmenu.ts-31-    this.enabled = enabled;
src/core\contextmenu.ts-32-    if (!enabled) {
src/core\contextmenu.ts-84-    this.menuEl = null;
src/core\contextmenu.ts-85-    this.currentItems = [];
src/core\contextmenu.ts-86-    this.focusedIndex = -1;
src/core\contextmenu.ts-87-  }
src/core\contextmenu.ts-88-
src/core\contextmenu.ts-89-  private handleKeydown(e: KeyboardEvent): void {
src/core\contextmenu.ts-90-    if (!this.menuEl) return;
src/core\contextmenu.ts-91-
src/core\contextmenu.ts:92:    if (e.key === 'Escape') {
src/core\contextmenu.ts:93:      e.preventDefault();
src/core\contextmenu.ts-94-      this.hide();
src/core\contextmenu.ts-95-      return;
src/core\contextmenu.ts-96-    }
src/core\contextmenu.ts:97:    if (e.key === 'ArrowDown') {
src/core\contextmenu.ts:98:      e.preventDefault();
src/core\contextmenu.ts-99-      this.moveFocus(1);
src/core\contextmenu.ts-100-      return;
src/core\contextmenu.ts-101-    }
src/core\contextmenu.ts:102:    if (e.key === 'ArrowUp') {
src/core\contextmenu.ts:103:      e.preventDefault();
src/core\contextmenu.ts-104-      this.moveFocus(-1);
src/core\contextmenu.ts-105-      return;
src/core\contextmenu.ts-106-    }
src/core\contextmenu.ts:107:    if (e.key === 'Enter') {
src/core\contextmenu.ts:108:      e.preventDefault();
src/core\contextmenu.ts-109-      const item = this.currentItems[this.focusedIndex];
src/core\contextmenu.ts-110-      if (item) {
src/core\contextmenu.ts-111-        item.action();
src/core\contextmenu.ts-112-        this.hide();
src/core\contextmenu.ts-113-      }
src/core\contextmenu.ts-114-    }
src/core\contextmenu.ts-115-  }
src/core\contextmenu.ts-116-
src/core\menu.ts-70-    });
src/core\menu.ts-71-
src/core\menu.ts-72-    document.addEventListener('click', (e) => {
src/core\menu.ts-73-      if (this._isOpen && this.menuDropdownEl && !this.menuDropdownEl.contains(e.target as Node)) {
src/core\menu.ts-74-        this.closeMenu();
src/core\menu.ts-75-      }
src/core\menu.ts-76-    });
src/core\menu.ts-77-
src/core\menu.ts:78:    document.addEventListener('keydown', (e) => {
src/core\menu.ts-79-      // F10 opens/closes the menu with zero mouse involvement (NFR-7, D-7).
src/core\menu.ts:80:      if (e.key === 'F10' && !e.ctrlKey && !e.altKey && !e.shiftKey) {
src/core\menu.ts:81:        e.preventDefault();
src/core\menu.ts-82-        this.toggleMenu();
src/core\menu.ts-83-        return;
src/core\menu.ts-84-      }
src/core\menu.ts-85-
src/core\menu.ts-86-      if (!this._isOpen) return;
src/core\menu.ts-87-
src/core\menu.ts:88:      if (e.key === 'Escape') {
src/core\menu.ts:89:        e.preventDefault();
src/core\menu.ts-90-        this.closeMenu();
src/core\menu.ts-91-        return;
src/core\menu.ts-92-      }
src/core\menu.ts:93:      if (e.key === 'ArrowDown') {
src/core\menu.ts:94:        e.preventDefault();
src/core\menu.ts-95-        this.moveItemFocus(1);
src/core\menu.ts-96-        return;
src/core\menu.ts-97-      }
src/core\menu.ts:98:      if (e.key === 'ArrowUp') {
src/core\menu.ts:99:        e.preventDefault();
src/core\menu.ts-100-        this.moveItemFocus(-1);
src/core\menu.ts-101-        return;
src/core\menu.ts-102-      }
src/core\menu.ts:103:      if (e.key === 'ArrowRight') {
src/core\menu.ts:104:        e.preventDefault();
src/core\menu.ts-105-        this.moveCategoryFocus(1);
src/core\menu.ts-106-        return;
src/core\menu.ts-107-      }
src/core\menu.ts:108:      if (e.key === 'ArrowLeft') {
src/core\menu.ts:109:        e.preventDefault();
src/core\menu.ts-110-        this.moveCategoryFocus(-1);
src/core\menu.ts-111-        return;
src/core\menu.ts-112-      }
src/core\menu.ts:113:      if (e.key === 'Enter') {
src/core\menu.ts:114:        e.preventDefault();
src/core\menu.ts-115-        this.triggerFocusedItem();
src/core\menu.ts-116-      }
src/core\menu.ts-117-    });
src/core\menu.ts-118-
src/core\menu.ts-119-    const handleZenEnter = () => {
src/core\menu.ts-120-      if (this._isOpen) {
src/core\menu.ts-121-        this.closeMenu();
src/core\menu.ts-122-      }
src/core\tree.ts-591-  // --------------------------------------------------------------------------
src/core\tree.ts-592-  // Keyboard Navigation Handling (FR-A2 ~ FR-A5, FR-A7 ~ FR-A10, FR-A12, FR-A16 ~ FR-A18)
src/core\tree.ts-593-  // --------------------------------------------------------------------------
src/core\tree.ts-594-
src/core\tree.ts-595-  private handleKeyDown(e: KeyboardEvent): void {
src/core\tree.ts-596-    if (!this.root) return;
src/core\tree.ts-597-
src/core\tree.ts-598-    // F3 or Ctrl+Alt+F: Find in tree (FR-A13, FR-A19)
src/core\tree.ts:599:    if (e.key === 'F3' || (e.ctrlKey && e.altKey && (e.key === 'f' || e.key === 'F'))) {
src/core\tree.ts:600:      e.preventDefault();
src/core\tree.ts-601-      this.openFindWidget();
src/core\tree.ts-602-      return;
src/core\tree.ts-603-    }
src/core\tree.ts-604-
src/core\tree.ts-605-    // Ctrl+A: Select all visible items (FR-A10)
src/core\tree.ts:606:    if (e.ctrlKey && !e.shiftKey && !e.altKey && (e.key === 'a' || e.key === 'A')) {
src/core\tree.ts:607:      e.preventDefault();
src/core\tree.ts-608-      this.selectAll();
src/core\tree.ts-609-      return;
src/core\tree.ts-610-    }
src/core\tree.ts-611-
src/core\tree.ts-612-    // Escape: Clear selection (FR-A12)
src/core\tree.ts:613:    if (e.key === 'Escape') {
src/core\tree.ts:614:      e.preventDefault();
src/core\tree.ts-615-      this.clearSelection();
src/core\tree.ts-616-      return;
src/core\tree.ts-617-    }
src/core\tree.ts-618-
src/core\tree.ts-619-    // Ctrl+LeftArrow: Collapse all (FR-A7, FR-A21)
src/core\tree.ts:620:    if (e.ctrlKey && !e.shiftKey && !e.altKey && e.key === 'ArrowLeft') {
src/core\tree.ts:621:      e.preventDefault();
src/core\tree.ts-622-      this.collapseAll();
src/core\tree.ts-623-      return;
src/core\tree.ts-624-    }
src/core\tree.ts-625-
src/core\tree.ts-626-    // Ctrl+Shift+Enter: Toggle selection of current item (FR-A18)
src/core\tree.ts:627:    if (e.ctrlKey && e.shiftKey && !e.altKey && e.key === 'Enter') {
src/core\tree.ts:628:      e.preventDefault();
src/core\tree.ts-629-      if (this.focusedId) {
src/core\tree.ts-630-        if (this.selectedIds.has(this.focusedId)) {
src/core\tree.ts-631-          this.selectedIds.delete(this.focusedId);
src/core\tree.ts-632-        } else {
src/core\tree.ts-633-          this.selectedIds.add(this.focusedId);
src/core\tree.ts-634-        }
src/core\tree.ts-635-        this.render();
src/core\tree.ts-636-        this.emitSelect();
src/core\tree.ts-637-      }
src/core\tree.ts-638-      return;
src/core\tree.ts-639-    }
src/core\tree.ts-640-
src/core\tree.ts-641-    // Ctrl+Enter: Open to side (FR-A14)
src/core\tree.ts:642:    if (e.ctrlKey && !e.shiftKey && !e.altKey && e.key === 'Enter') {
src/core\tree.ts:643:      e.preventDefault();
src/core\tree.ts-644-      const node = this.getNodeById(this.focusedId);
src/core\tree.ts-645-      if (node) {
src/core\tree.ts-646-        this.emitOpenToSide(node);
src/core\tree.ts-647-      }
src/core\tree.ts-648-      return;
src/core\tree.ts-649-    }
src/core\tree.ts-650-
src/core\tree.ts-651-    // Enter: Select/open focused item (FR-A6)
src/core\tree.ts:652:    if (e.key === 'Enter' && !e.ctrlKey && !e.shiftKey && !e.altKey) {
src/core\tree.ts:653:      e.preventDefault();
src/core\tree.ts-654-      const node = this.getNodeById(this.focusedId);
src/core\tree.ts-655-      if (node) {
src/core\tree.ts-656-        this.emitOpen(node);
src/core\tree.ts-657-      }
src/core\tree.ts-658-      return;
src/core\tree.ts-659-    }
src/core\tree.ts-660-
src/core\tree.ts-661-    // Ctrl+Enter: Open item in beside pane (FR-A14)
src/core\tree.ts:662:    if (e.key === 'Enter' && e.ctrlKey && !e.shiftKey && !e.altKey) {
src/core\tree.ts:663:      e.preventDefault();
src/core\tree.ts-664-      const node = this.getNodeById(this.focusedId);
src/core\tree.ts-665-      if (node) {
src/core\tree.ts-666-        this.emitOpenToSide(node);
src/core\tree.ts-667-      }
src/core\tree.ts-668-      return;
src/core\tree.ts-669-    }
src/core\tree.ts-670-
src/core\tree.ts-671-    // Space: Toggle expand/collapse of container (FR-A5)
src/core\tree.ts:672:    if (e.key === ' ' && !e.ctrlKey && !e.shiftKey && !e.altKey) {
src/core\tree.ts:673:      e.preventDefault();
src/core\tree.ts-674-      if (this.focusedId) {
src/core\tree.ts-675-        const node = this.getNodeById(this.focusedId);
src/core\tree.ts-676-        if (node?.isContainer) {
src/core\tree.ts-677-          this.toggleExpand(node.id);
src/core\tree.ts-678-        }
src/core\tree.ts-679-      }
src/core\tree.ts-680-      return;
src/core\tree.ts-681-    }
src/core\tree.ts-682-
src/core\tree.ts-683-    // Ctrl+ArrowUp / Ctrl+ArrowDown: Scroll without moving focus (FR-A17)
src/core\tree.ts:684:    if (e.ctrlKey && !e.shiftKey && !e.altKey && (e.key === 'ArrowUp' || e.key === 'ArrowDown')) {
src/core\tree.ts:685:      e.preventDefault();
src/core\tree.ts-686-      const listEl = this.container.querySelector('.tree-list') as HTMLElement | null;
src/core\tree.ts-687-      if (listEl) {
src/core\tree.ts:688:        listEl.scrollTop += e.key === 'ArrowDown' ? 22 : -22;
src/core\tree.ts-689-      }
src/core\tree.ts-690-      return;
src/core\tree.ts-691-    }
src/core\tree.ts-692-
src/core\tree.ts-693-    const visible = this.getVisibleItems();
src/core\tree.ts-694-    if (visible.length === 0) return;
src/core\tree.ts-695-
src/core\tree.ts-696-    let curIdx = this.focusedId ? visible.findIndex((it) => it.node.id === this.focusedId) : 0;
src/core\tree.ts-697-    if (curIdx < 0) curIdx = 0;
src/core\tree.ts-698-
src/core\tree.ts-699-    // Home: Focus first visible item (FR-A8)
src/core\tree.ts:700:    if (e.key === 'Home' && !e.ctrlKey && !e.altKey) {
src/core\tree.ts:701:      e.preventDefault();
src/core\tree.ts-702-      this.focusItemByIndex(0);
src/core\tree.ts-703-      return;
src/core\tree.ts-704-    }
src/core\tree.ts-705-
src/core\tree.ts-706-    // End: Focus last visible item (FR-A8)
src/core\tree.ts:707:    if (e.key === 'End' && !e.ctrlKey && !e.altKey) {
src/core\tree.ts:708:      e.preventDefault();
src/core\tree.ts-709-      this.focusItemByIndex(visible.length - 1);
src/core\tree.ts-710-      return;
src/core\tree.ts-711-    }
src/core\tree.ts-712-
src/core\tree.ts-713-    // PageDown / PageUp: Move focus one page (FR-A16)
src/core\tree.ts:714:    if (e.key === 'PageDown' || e.key === 'PageUp') {
src/core\tree.ts:715:      e.preventDefault();
src/core\tree.ts-716-      const listEl = this.container.querySelector('.tree-list') as HTMLElement | null;
src/core\tree.ts-717-      const viewportHeight = listEl?.clientHeight || this.container.clientHeight || 0;
src/core\tree.ts-718-      const pageSize = viewportHeight > 0 ? Math.max(1, Math.floor(viewportHeight / 22)) : 10;
src/core\tree.ts:719:      const targetIdx = e.key === 'PageDown'
src/core\tree.ts-720-        ? Math.min(visible.length - 1, curIdx + pageSize)
src/core\tree.ts-721-        : Math.max(0, curIdx - pageSize);
src/core\tree.ts-722-      this.focusItemByIndex(targetIdx);
src/core\tree.ts-723-      return;
src/core\tree.ts-724-    }
src/core\tree.ts-725-
src/core\tree.ts-726-    // Shift+ArrowUp / Shift+ArrowDown: Expand selection range (FR-A9)
src/core\tree.ts:727:    if (e.shiftKey && (e.key === 'ArrowUp' || e.key === 'ArrowDown')) {
src/core\tree.ts:728:      e.preventDefault();
src/core\tree.ts-729-      if (!this.anchorId) {
src/core\tree.ts-730-        this.anchorId = this.focusedId || visible[0].node.id;
src/core\tree.ts-731-      }
src/core\tree.ts-732-      const anchorIdx = visible.findIndex((it) => it.node.id === this.anchorId);
src/core\tree.ts-733-      const safeAnchorIdx = anchorIdx >= 0 ? anchorIdx : 0;
src/core\tree.ts-734-
src/core\tree.ts:735:      const newFocusIdx = e.key === 'ArrowDown'
src/core\tree.ts-736-        ? Math.min(visible.length - 1, curIdx + 1)
src/core\tree.ts-737-        : Math.max(0, curIdx - 1);
src/core\tree.ts-738-
src/core\tree.ts-739-      const targetItem = visible[newFocusIdx];
src/core\tree.ts-740-      this.focusedId = targetItem.node.id;
src/core\tree.ts-741-
src/core\tree.ts-742-      const minIdx = Math.min(safeAnchorIdx, newFocusIdx);
src/core\tree.ts-743-      const maxIdx = Math.max(safeAnchorIdx, newFocusIdx);
src/core\tree.ts-746-      this.render();
src/core\tree.ts-747-      this.focusTree();
src/core\tree.ts-748-      this.scrollItemIntoView(targetItem.node.id);
src/core\tree.ts-749-      this.emitSelect();
src/core\tree.ts-750-      return;
src/core\tree.ts-751-    }
src/core\tree.ts-752-
src/core\tree.ts-753-    // ArrowDown: Focus next visible item (FR-A2)
src/core\tree.ts:754:    if (e.key === 'ArrowDown' && !e.ctrlKey && !e.altKey) {
src/core\tree.ts:755:      e.preventDefault();
src/core\tree.ts-756-      if (curIdx < visible.length - 1) {
src/core\tree.ts-757-        this.focusItemByIndex(curIdx + 1);
src/core\tree.ts-758-      }
src/core\tree.ts-759-      return;
src/core\tree.ts-760-    }
src/core\tree.ts-761-
src/core\tree.ts-762-    // ArrowUp: Focus previous visible item (FR-A2)
src/core\tree.ts:763:    if (e.key === 'ArrowUp' && !e.ctrlKey && !e.altKey) {
src/core\tree.ts:764:      e.preventDefault();
src/core\tree.ts-765-      if (curIdx > 0) {
src/core\tree.ts-766-        this.focusItemByIndex(curIdx - 1);
src/core\tree.ts-767-      }
src/core\tree.ts-768-      return;
src/core\tree.ts-769-    }
src/core\tree.ts-770-
src/core\tree.ts-771-    // ArrowRight: Expand container or move to first child (FR-A3)
src/core\tree.ts:772:    if (e.key === 'ArrowRight' && !e.ctrlKey && !e.altKey) {
src/core\tree.ts:773:      e.preventDefault();
src/core\tree.ts-774-      const current = visible[curIdx];
src/core\tree.ts-775-      if (current.node.isContainer) {
src/core\tree.ts-776-        if (!current.isExpanded) {
src/core\tree.ts-777-          // Expand container, load children if needed, focus stays on node (FR-A3)
src/core\tree.ts-778-          this.setExpanded(current.node.id, true);
src/core\tree.ts-779-        } else if (current.hasChildren && curIdx < visible.length - 1) {
src/core\tree.ts-780-          // Already expanded -> move focus to first child (FR-A3)
src/core\tree.ts-781-          const nextItem = visible[curIdx + 1];
src/core\tree.ts-783-            this.focusItemByIndex(curIdx + 1);
src/core\tree.ts-784-          }
src/core\tree.ts-785-        }
src/core\tree.ts-786-      }
src/core\tree.ts-787-      return;
src/core\tree.ts-788-    }
src/core\tree.ts-789-
src/core\tree.ts-790-    // ArrowLeft: Collapse container or move to parent (FR-A4)
src/core\tree.ts:791:    if (e.key === 'ArrowLeft' && !e.ctrlKey && !e.altKey) {
src/core\tree.ts:792:      e.preventDefault();
src/core\tree.ts-793-      const current = visible[curIdx];
src/core\tree.ts-794-      if (current.node.isContainer && current.isExpanded) {
src/core\tree.ts-795-        // Collapse container, focus stays on node (FR-A4)
src/core\tree.ts-796-        this.expandedIds.delete(current.node.id);
src/core\tree.ts-797-        this.reconcileFocusAndSelectionAfterCollapse(current.node.id);
src/core\tree.ts-798-        this.render();
src/core\tree.ts-799-        this.focusTree();
src/core\tree.ts-800-      } else if (current.parent) {
src/core\tree.ts-1155-    if (hadFocus) {
src/core\tree.ts-1156-      this.focusTree();
src/core\tree.ts-1157-    }
src/core\tree.ts-1158-  }
src/core\tree.ts-1159-
src/core\tree.ts-1160-  private attachDomEvents(): void {
src/core\tree.ts-1161-    const listEl = this.container.querySelector('.tree-list') as HTMLElement | null;
src/core\tree.ts-1162-    if (listEl) {
src/core\tree.ts:1163:      listEl.addEventListener('keydown', (e) => this.handleKeyDown(e));
src/core\tree.ts-1164-    }
src/core\tree.ts-1165-
src/core\tree.ts-1166-    this.attachRowEvents();
src/core\tree.ts-1167-
src/core\tree.ts-1168-    // Find widget events
src/core\tree.ts-1169-    const findInput = this.container.querySelector('.tree-find-input') as HTMLInputElement | null;
src/core\tree.ts-1170-    if (findInput) {
src/core\tree.ts-1171-      findInput.addEventListener('input', () => this.handleFindInput(findInput.value));
src/core\tree.ts:1172:      findInput.addEventListener('keydown', (e) => {
src/core\tree.ts:1173:        if (e.key === 'Escape') {
src/core\tree.ts:1174:          e.preventDefault();
src/core\tree.ts-1175-          this.closeFindWidget();
src/core\tree.ts:1176:        } else if (e.key === 'Enter') {
src/core\tree.ts:1177:          e.preventDefault();
src/core\tree.ts-1178-          if (e.shiftKey) {
src/core\tree.ts-1179-            this.goToFindMatch(this.findMatchIndex - 1);
src/core\tree.ts-1180-          } else {
src/core\tree.ts-1181-            this.goToFindMatch(this.findMatchIndex + 1);
src/core\tree.ts-1182-          }
src/core\tree.ts-1183-        }
src/core\tree.ts-1184-      });
src/core\tree.ts-1185-      const prevBtn = this.container.querySelector('.tree-find-nav-btn.prev-btn');
src/core\tree.ts-1195-        closeBtn.addEventListener('click', () => this.closeFindWidget());
src/core\tree.ts-1196-      }
src/core\tree.ts-1197-      this.updateFindCountUI();
src/core\tree.ts-1198-    }
src/core\tree.ts-1199-
src/core\tree.ts-1200-    // Inline input widget events (FR-A23, WK-047)
src/core\tree.ts-1201-    const promptInput = this.container.querySelector('.tree-input-field') as HTMLInputElement | null;
src/core\tree.ts-1202-    if (promptInput && this.promptState) {
src/core\tree.ts:1203:      promptInput.addEventListener('keydown', (e) => {
src/core\tree.ts-1204-        e.stopPropagation();
src/core\tree.ts:1205:        if (e.key === 'Enter') {
src/core\tree.ts:1206:          e.preventDefault();
src/core\tree.ts-1207-          const val = promptInput.value;
src/core\tree.ts-1208-          if (val.length > 0 && this.promptState) {
src/core\tree.ts-1209-            const commitCb = this.promptState.onCommit;
src/core\tree.ts-1210-            const res = {
src/core\tree.ts-1211-              name: val,
src/core\tree.ts-1212-              type: this.promptState.type,
src/core\tree.ts-1213-              parentId: this.promptState.parentId,
src/core\tree.ts-1214-            };
src/core\tree.ts-1215-            this.promptState = null;
src/core\tree.ts-1216-            this.render();
src/core\tree.ts-1217-            this.focusTree();
src/core\tree.ts-1218-            commitCb(res); // Emits name verbatim to app (1 record, 0 shell creation actions)
src/core\tree.ts-1219-          }
src/core\tree.ts:1220:        } else if (e.key === 'Escape') {
src/core\tree.ts:1221:          e.preventDefault();
src/core\tree.ts-1222-          this.cancelPrompt(); // Disappears, 0 app commit calls
src/core\tree.ts-1223-        }
src/core\tree.ts-1224-      });
src/core\tree.ts-1225-    }
src/core\tree.ts-1226-  }
src/core\tree.ts-1227-
src/core\tree.ts-1228-  private attachRowEvents(): void {
src/core\tree.ts-1229-    const rowEls = this.container.querySelectorAll('.tree-row:not(.tree-input-row)');
src/core\viewstate.ts-13-  private statusbarVisible = true;
src/core\viewstate.ts-14-  private _isZenMode = false;
src/core\viewstate.ts-15-
src/core\viewstate.ts-16-  constructor(private layout: WorkbenchLayoutElements) {
src/core\viewstate.ts-17-    this.setupKeyboardListeners();
src/core\viewstate.ts-18-  }
src/core\viewstate.ts-19-
src/core\viewstate.ts-20-  private setupKeyboardListeners(): void {
src/core\viewstate.ts:21:    window.addEventListener('keydown', (e: KeyboardEvent) => {
src/core\viewstate.ts:22:      if (e.key === 'F11') {
src/core\viewstate.ts:23:        e.preventDefault();
src/core\viewstate.ts-24-        this.toggleZenMode();
src/core\viewstate.ts:25:      } else if (e.key === 'Escape') {
src/core\viewstate.ts-26-        if (this._isZenMode) {
src/core\viewstate.ts:27:          e.preventDefault();
src/core\viewstate.ts-28-          this.exitZenMode();
src/core\viewstate.ts-29-        }
src/core\viewstate.ts-30-      }
src/core\viewstate.ts-31-    });
src/core\viewstate.ts-32-  }
src/core\viewstate.ts-33-
src/core\viewstate.ts-34-  public getState(): ViewState {
src/core\viewstate.ts-35-    return {
src/core/layout.ts:31:  container.innerHTML = `
src/core/sidebar.ts:12:  private appActionsEl: HTMLElement;
src/core/sidebar.ts:16:  private appActions: ViewAction[] = [];
src/core/sidebar.ts:20:    appActionsEl: HTMLElement,
src/core/sidebar.ts:26:    this.appActionsEl = appActionsEl;
src/core/sidebar.ts:53:  public addAppAction(action: ViewAction): void {
src/core/sidebar.ts:54:    this.appActions.push(action);
src/core/sidebar.ts:59:    return this.appActions;
src/core/sidebar.ts:63:    if (!this.appActionsEl) return;
src/core/sidebar.ts:64:    this.appActionsEl.innerHTML = '';
src/core/sidebar.ts:66:    for (const act of this.appActions) {
src/core/sidebar.ts:75:      btn.appendChild(icon);
src/core/sidebar.ts:78:      this.appActionsEl.appendChild(btn);
src/core/activitybar.ts:81:    container.innerHTML = '';
src/core/activitybar.ts:91:      btn.appendChild(iconEl);
src/core/activitybar.ts:100:      container.appendChild(btn);

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk powershell -NoProfile -Command \"Get-Content 'docs\\current\\DECISIONS.md' | Select-Object -Skip 725 -First 125\"; rtk powershell -NoProfile -Command \"Get-Content 'scripts\\phase5-suite.js' | Select-Object -First 380\"; rtk powershell -NoProfile -Command \"Get-Content 'scripts\\verify-phase5.mjs' | Select-Object -First 300\"" in D:\projects\workbench-kit
 succeeded in 1312ms:
- **?뚮┝???볦씠??紐⑸줉???붾떎(VS Code???뚮┝ ?쇳꽣).** 吏???뚮┝???ㅼ떆 蹂대뒗 ?붽뎄媛
  ?꾩쭅 ?녿떎. 諛곗젣?쒕떎.

---

## D-22. ?고겢由?硫붾돱??猿띾뜲湲곌? ?μ튂留?二쇨퀬 ??ぉ? ?깆씠 梨꾩슫??
**?좏깮**

?몃━????ぉ ?? ???쒕ぉ ?꾩뿉???ㅻⅨ履?踰꾪듉???뚮??????⑤뒗 硫붾돱瑜??붾떎. 猿띾뜲湲곕뒗
**硫붾돱瑜??꾩슦怨?洹몃━???μ튂留?* ?쒓났?쒕떎 ???대뵒???⑤뒗吏, ?대뼸寃??앷꼈?붿?, ?대뼸寃??ロ엳?붿?, ?ㅻ낫?쒕줈 ?대뼸寃??ㅻⅤ?대━?붿?.

**臾댁뒯 ??ぉ???ㅼ뼱媛덉????깆씠 ?뺥븳??** 猿띾뜲湲곌? 湲곕낯?쇰줈 ?ｋ뒗 ??ぉ? ?녿떎.

**湲곕낯媛믪? "?ъ슜 ?????대떎.** 猿띾뜲湲곕쭔 ?덈뒗 ?곹깭?먯꽌???ㅻⅨ履?踰꾪듉???뚮윭???꾨Т
?쇱씠 ?쇱뼱?섏? ?딅뒗?? ?깆씠 ??ぉ???뺤쓽?섍퀬, ?ъ슜?먭? 硫붾돱?먯꽌 耳쒖빞 ?щ떎. 耳쒓퀬 ?꾨뒗
??ぉ? View 臾띠쓬???붾떎(D-23).

**洹쇨굅**

臾댁뒯 ??ぉ???⑤뒗吏???깅쭏???ㅻⅤ?? ?뚯씪 而ㅻ㎤?곕뒗 蹂닿린쨌?몄쭛쨌蹂듭궗쨌??젣媛 ?꾩슂?섍퀬,
留덊겕?ㅼ슫 酉곗뼱????移몄뿉 ?닿린 ?뺣룄硫??쒕떎. 猿띾뜲湲곌? ?닿쾬???뚮㈃ "猿띾뜲湲곕뒗 由ъ냼??醫낅쪟瑜?紐⑤Ⅸ??(INTENT 3??媛 源⑥?怨? ?ㅼ쓬 ?깆씠 ?곗? ?딅뒗 ??ぉ???닿퀬 媛寃??쒕떎.
?곕???D-4)怨??몃줈 ????ぉ(D-11)???ㅻ， 諛⑹떇怨?媛숇떎.

?앷??덈뒗 猿띾뜲湲곌? ?뺥븳?? ?뚮┝?먯꽌? 媛숈? ?댁쑀????**臾댁뾿???댁쓣吏???깆씠, ?대뼸寃?蹂댁씪吏??猿띾뜲湲곌?**(D-21).

湲곕낯 ??ぉ???섎굹???ｌ? ?딅뒗 ?댁쑀??D-13?먯꽌 移?紐낅졊??類먭린 ?뚮Ц?대떎. 洹몃윭怨??섎㈃
猿띾뜲湲곌? ?ｌ쓣 留뚰븳 寃껋쑝濡???쓽 "?リ린"媛 ?⑤뒗?? ??뿉???대? ?リ린 踰꾪듉???덉뼱
媛숈? ?쇰줈 媛??湲몄씠 ?섏씠 ?쒕떎.

**諛곗젣?????*

- **猿띾뜲湲곌? ??履쎌뿉 "?リ린 / ?ㅻⅨ ??紐⑤몢 ?リ린"瑜?湲곕낯?쇰줈 ?ｋ뒗??** 紐⑤뱺 ?깆뿉??媛숈?
  ??ぉ??蹂댁씠???쇨??깆? ?덉쑝?? 猿띾뜲湲곌? ??硫붾돱???댁슜???뚭쾶 ?섍퀬 ?リ린濡?媛??湲몄씠
  ?섏씠 ?쒕떎. 諛곗젣?쒕떎.
- **?고겢由?硫붾돱瑜??꾩삁 ?먯? ?딅뒗??** ?뚯씪 而ㅻ㎤?곌? ??ぉ???怨?臾댁뼵媛瑜??섎뒗
  ?꾧뎄?? 洹?紐낅졊???꾨? ?ㅻ굹 ?꾧뎄 留됰? 踰꾪듉?쇰줈留??ъ빞 ?쒕떎. 諛곗젣?쒕떎.
- **湲곕낯媛믪쓣 "?ъ슜???쇰줈 ?붾떎.** ?깆씠 ??ぉ???뺤쓽?섏? ?딆븯?붾뜲 ?ㅻⅨ履?踰꾪듉???꾨Ⅴ硫?  鍮?硫붾돱媛 ?④굅???꾨Т 諛섏쓳???놁뼱, 怨좎옣?몄? ?먮옒 洹몃윴吏 援щ퀎?섏? ?딅뒗?? 爰??먮㈃
  "???섎뒗 寃????뺥빐吏?寃곌낵媛 ?쒕떎. 諛곗젣?쒕떎.
- **?앷??덇퉴吏 ?깆씠 ?뺥븳??** ?깅쭏??硫붾돱媛 ?щ씪 蹂댁씠硫?媛숈? 猿띾뜲湲곕? ?대떎???먮굦??  源⑥쭊?? 諛곗젣?쒕떎.

---

## D-23. 硫붾돱??File 쨌 View 쨌 Help ??臾띠쓬, ?섎떒 諛붾뒗 寃쎈줈? ???뺣낫

**?좏깮**

*硫붾돱* ???곷떒 諛붿쓽 ?꾨쾭嫄?踰꾪듉(`??)???꾨Ⅴ硫???臾띠쓬???섏삩??

| 臾띠쓬 | ??ぉ |
| --- | --- |
| **File** | ?대뜑 ?닿린(`Ctrl+O`) 쨌 理쒓렐 ?대뜑 쨌 ?앸궡湲???**D-31?먯꽌 ???リ린(`Ctrl+W`) 쨌 ?대뜑 ?リ린瑜??뷀빐 ?ㅼ꽢???섏뿀??* |
| **View** | ?먯깋湲??묎린/?닿린 쨌 ?곷떒 諛?媛먯텛湲?蹂댁씠湲?쨌 ?섎떒 諛?媛먯텛湲?蹂댁씠湲?쨌 Zen 紐⑤뱶 쨌 ?뚮쭏 諛붽씀湲?쨌 **?꾩씠肄??뚮쭏 諛붽씀湲?*(D-17) 쨌 醫뚯슦 ?ㅽ뵆由?쨌 ?곹븯 ?ㅽ뵆由?D-13) 쨌 ?쒖꽦 移???紐⑤몢 ?リ린(D-26) 쨌 **?고겢由?硫붾돱 ?ъ슜**(湲곕낯 爰쇱쭚 ??D-22) |
| **Help** | ?뺣낫 |

*?섎떒 諛? ???쇱そ??吏湲???씠 媛由ы궎????곸쓽 寃쎈줈, ?ㅻⅨ履쎌뿉 ???대쫫쨌踰꾩쟾쨌?ㅽ뻾
媛덈옒. 洹??ъ씠媛 ?ㅻ쪟쨌吏꾪뻾 硫붿떆吏媛 ?⑤뒗 ?먮━??D-21). **?깆씠 ?먭린 ??ぉ???섎굹 ??遺숈씪 ???덈뒗 ?먮━**瑜??④릿??

硫붾돱??猿띾뜲湲곌? 梨꾩슫?? ?깆씠 ??ぉ???뷀븷 ???덉쑝?? ????臾띠쓬? 紐⑤뱺 ?깆뿉 怨듯넻?쇰줈
?덈떎. **?대뒓 臾띠쓬???대뼸寃??뷀븯?붿???D-31???뺥븳??* ??File?먮쭔 ?뷀븷 ???덇퀬,
猿띾뜲湲???ぉ ?꾨옒 媛由꾩꽑 ?ㅼ쓬??遺숇뒗??

**洹쇨굅**

??臾띠쓬? VS Code瑜?鍮꾨’???곗뒪?ы넲 ?깆쓽 愿?듭씠???대뵒??臾댁뾿???덈뒗吏 李얠쓣 ?꾩슂媛
?녿떎. ??ぉ???섏쨷???섏뼱???ㅼ뼱媛??먮━媛 ?대? ?뺥빐???덈떎.

View????寃껋? ?몃줈 ?좎쓽 ?꾩씠肄섍낵 寃뱀튇?? ?닿쾬? 以묐났???꾨땲??D-7?먯꽌 ?뺥븳 ??븷
遺꾨떞?대떎 ??**硫붾돱???꾨?瑜??닿퀬, ?몃줈 ?좊뒗 ?먯＜ ?곕뒗 寃껊쭔 ?대뒗??** ?몃줈 ?좎쓽 ??ぉ?
?깆씠 諛붽? ???덉쑝誘濡?D-11), ?대뼡 ?깆씠 ?꾩씠肄섏쓣 鍮쇰뜑?쇰룄 硫붾돱?먮뒗 ?⑥븘 ?덉뼱???쒕떎.

硫붾돱瑜?猿띾뜲湲곌? 梨꾩슦??寃껋? ?몃줈 ?졖룹슦?대┃ 硫붾돱? 諛섎??? 洹몄そ? ?깅쭏???ㅻⅨ 寃껋쓣
?댁?留? 硫붾돱????寃껋? **?대뜑 ?닿린쨌?붾㈃ ?묎린쨌?뚮쭏泥섎읆 紐⑤뱺 ?깆뿉 怨듯넻??猿띾뜲湲곗쓽
湲곕뒫**?닿린 ?뚮Ц?대떎.

**諛곗젣?????*

- **臾띠쓬 ?놁씠 ??紐⑸줉?쇰줈 ?섏뼱?볥뒗??** 吏湲덉? ??ぉ???곸뼱 ??踰????꾨Ⅴ???댁젏??  ?덉쑝?? ??ぉ???섎㈃ ?대뵒???ｌ쓣吏媛 留ㅻ쾲 ?먮떒???쒕떎. 諛곗젣?쒕떎.
- **硫붾돱???깆씠 梨꾩슫???몃줈 ?졖룹슦?대┃ 硫붾돱 諛⑹떇).** 硫붾돱????寃껋? ?遺遺?猿띾뜲湲곗쓽
  湲곕뒫?대씪 ?깅쭏???ㅻ? ?댁쑀媛 ?녿떎. 諛곗젣?쒕떎.
- **?섎떒 諛붿뿉 ???먮━瑜??④린吏 ?딅뒗??** ?깆씠 ?곹깭瑜?蹂댁씪 怨녹씠 ?놁뼱???붾㈃ ?덉뿉
  ?먯껜 ?쒖떆以꾩쓣 留뚮뱾寃??쒕떎. 諛곗젣?쒕떎.

---

## D-24. 移맞룻꺆쨌蹂닿린 ?섎챸? dockview瑜?媛?몃떎 ?대떎 ???뺤씤??嫄곗퀜 ?뺥뻽??
**?좏깮**

移??섎늻湲?醫뚯슦쨌?곹븯), 2李⑥썝 寃⑹옄 諛곗튂, 移??ш린 議곗젅, ??以? ???뚯뼱 ??린湲?媛?μ옄由щ줈
?뚯뼱 ??移?留뚮뱾湲??ы븿), 移??ъ씠 ?대룞, 留덉?留???씠 ?ロ엳硫?移몄씠 ?щ씪吏??寃? 諛곗튂
??Β룸났???????⑹뼱由щ? **`dockview-core`(MIT)** 濡?媛?몃떎 ?대떎. 吏곸젒 吏쒖? ?딅뒗??

**??蹂댁씠????쓽 泥섎━** ??猿띾뜲湲?湲곕낯? dockview??湲곕낯媛???蹂댁씠???숈븞 ?붾㈃ ?붿냼瑜??쇱뼱 ?먮뒗 履??쇰줈 ?붾떎. ?쇱뼱吏硫?怨ㅻ???蹂닿린??**?깆씠 洹???쭔 "??긽 洹몃젮 ?먭린"濡??щ┛??** ?곕??먯씠 洹몃윴 寃쎌슦??

**洹쇨굅**

D-2媛 "?대? ??留뚮뱾?댁쭊 寃껋쓣 ?ㅼ떆 留뚮뱶??鍮꾩슜???ш퀬 理쒖냼 援ы쁽???닿툔?쒕떎"怨??뺥뻽??
codicons(D-3) 쨌 monaco(D-18)???곸슜???먮떒???ш린?먮룄 ?곸슜?섎ŉ, ?⑹뼱由щ뒗 ?댁そ??媛???щ떎. 洹몃━怨??욎꽑 ?꾨줈?앺듃媛 臾대꼫吏??먮━媛 ?뺥솗???ш린??BRIEF 1???????대룞, 遺꾪븷
?댁젣, ?좏깮怨??ъ빱??. 媛숈? 寃껋쓣 ?먯쑝濡??ㅼ떆 吏쒕㈃ 媛숈? ?ㅽ뙣瑜?諛섎났??媛?μ꽦???믩떎.

移??섎늻湲곗? ?ш린 議곗젅 肄붾뱶媛 **VS Code???대떦 遺遺꾩뿉???섏솕?ㅺ퀬** ??μ냼媛 諛앺엳怨??덉뼱, "VS Code 諛⑹떇???곕Ⅸ????湲곗?(INTENT 6??怨??닿툔?섏? ?딅뒗?? ?섏〈?깆씠 ?녾퀬
MIT?대ŉ, ?붾㈃ ?꾨젅?꾩썙???놁씠 ?????덈떎.

**?뺤씤??寃?* ??梨꾪깮 ?꾩뿉 BRIEF ?꾨즺 議곌굔??"蹂닿린 ?몄뒪?댁뒪媛 ?ㅼ떆 留뚮뱾?댁?吏 ?딅뒗??瑜??ㅼ젣濡??쒗뿕?덈떎. `dockview-core` 8.2.0???ㅼ튂?섍퀬, ?몄뒪?댁뒪留덈떎 ?꾩옣??李띻퀬 怨꾩냽 ?꾨뒗
??대㉧? DOM ?덉뿉留??덈뒗 ?곹깭瑜?遺숈씤 ?⑤꼸??留뚮뱾???ㅻ뱶由ъ뒪 釉뚮씪?곗??먯꽌 ??媛吏瑜??덈떎 ??媛숈? 移??덉뿉????쓣 媛붾떎媛 ?섎룎?꾩삤湲? **?ㅻⅨ 移몄쑝濡???린湲?**

???뚮뜑 諛⑹떇 紐⑤몢?먯꽌 **留뚮뱾?댁쭊 ?잛닔 1 쨌 ?먭린 0 쨌 ?몄뒪?댁뒪 ?꾩옣 ?숈씪 쨌 ??대㉧ ?곗냽 쨌
DOM ?곹깭 ?좎?**濡??섏솕?? ??꺼???ㅼ떆 留뚮뱾?댁?吏 ?딅뒗??

媛숈? ?쒗뿕?먯꽌 李⑥씠???쒕윭?щ떎. 湲곕낯 諛⑹떇? ??씠 ?쒖꽦???꾨땶 ?숈븞 ?붾㈃ ?붿냼瑜?DOM?먯꽌
**?쇱뼱 ?붾떎.** ?몄뒪?댁뒪???댁븘 ?덉?留??붾㈃?먯꽌 遺꾨━?섎?濡? ?ш린瑜??ъ꽌 ?숈옉?섎뒗
蹂닿린(xterm 媛숈? ?곕??????ㅼ떆 遺숈씪 ???ш린瑜?留욎떠???쒕떎. "??긽 洹몃젮 ?먭린"???쇱?
?딅뒗 ?????蹂댁씠????룄 怨꾩냽 洹몃┛?? 洹몃옒??湲곕낯? 媛踰쇱슫 履쎌뿉 ?먭퀬 ?꾩슂??蹂닿린留??깆씠 ?щ━寃??쒕떎 ??猿띾뜲湲곌? ?대뒓 蹂닿린媛 ?곕??먯씤吏 ???꾩슂媛 ?놁쑝誘濡?"猿띾뜲湲곕뒗
由ъ냼??醫낅쪟瑜?紐⑤Ⅸ??(INTENT 3????吏耳쒖쭊??

**諛곗젣?????*

- **吏곸젒 吏좊떎(李몄“ ?ш뎄??.** 洹쒖튃???⑥쟾???μ븙?쒕떎???댁젏???덉쑝?? D-8??2李⑥썝
  寃⑹옄 諛곗튂瑜?洹몃?濡?諛쏆쑝硫댁꽌 ?쒖씠?꾧? ?щ씪媛붾떎. ?ш린 議곗젅쨌?뚯뼱 ?볤린쨌2李⑥썝 諛곗튂쨌諛곗튂
  ??Β룸낫湲??섎챸源뚯? ?꾨? ?곕━ 紐レ씠 ?섍퀬, 洹멸쾬???욎꽑 ?꾨줈?앺듃媛 ?ㅽ뙣??吏?먯씠??
  諛곗젣?쒕떎.
- **紐⑤뱺 ??쓣 "??긽 洹몃젮 ?먭린"濡??붾떎.** ?쇱뼱吏?臾몄젣媛 ?щ씪???⑥닚?섏?留? ??蹂댁씠??  ??씠 ?꾨? 怨꾩냽 洹몃젮吏꾨떎. 硫?고꽣誘몃꼸泥섎읆 ??씠 ?щ읉???깆뿉??遺?댁씠 而ㅼ쭊?? 諛곗젣?쒕떎.
- **?쇱뼱吏먯쓣 ?깆씠 紐⑤Ⅴ寃?猿띾뜲湲곌? ?뚯븘??泥섎━?쒕떎.** ?대뒓 蹂닿린媛 ?ш린??誘쇨컧?쒖???  猿띾뜲湲곌? ?????녿떎. 諛곗젣?쒕떎.

**硫붾え** ??`dockview-core`??`<script>` ?섎굹濡??쏀엳??臾띠쓬蹂?UMD)???④퍡 諛고룷?쒕떎.
利?**dockview ?뚮Ц??鍮뚮뱶媛 ?꾩슂??寃껋? ?꾨땲??** D-1??鍮뚮뱶 ?꾩젣??洹몃?濡??좏슚?섎굹,
/**
 * Host-neutral Phase 5 Acceptance Test Suite for workbench-kit.
 *
 * Can be executed in any browser environment (Electron, pywebview/WebView2, Chrome).
 * Returns a structured array of assertion results:
 *   { success: boolean, results: [{ id: string, pass: boolean, msg: string }] }
 */
window.__runPhase5TestSuite = async function runPhase5TestSuite() {
  const results = [];

  function record(id, pass, msg) {
    results.push({ id, pass: Boolean(pass), msg });
    console.log('[TEST_ASSERT]' + (pass ? 'PASS' : 'FAIL') + '|||[' + id + '] ' + msg);
  }

  try {
    const app = window.__workbenchApp;
    if (!app) {
      record('INIT', false, 'window.__workbenchApp is defined');
      return { success: false, results };
    }
    record('INIT', true, 'window.__workbenchApp is initialized and accessible');

    const editor = app.editor;
    const tree = app.tree;
    const menu = app.menu;
    const activityBar = app.activityBar;
    const contextMenu = app.contextMenu;

    const wait = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

    function activePanelContentEl() {
      const panel = editor.getActivePanel();
      if (!panel) return null;
      return document.querySelector('.editor-panel-content');
    }

    function dispatchKey(target, key, opts) {
      target.dispatchEvent(
        new KeyboardEvent('keydown', Object.assign({ key: key, bubbles: true, cancelable: true }, opts || {}))
      );
    }

    // ------------------------------------------------------------------------
    // 1. Resource kind registration slot: a third, test-only kind proves the
    //    slot accepts arbitrary future kinds with zero further core changes
    //    (FR-I1, WK-025).
    // ------------------------------------------------------------------------
    let widgetDisposed = false;
    app.kindRegistry.register('widget', (targetId) => {
      const el = document.createElement('div');
      el.className = 'test-widget-view';
      el.textContent = 'Widget: ' + targetId;
      return { element: el, dispose: () => { widgetDisposed = true; } };
    });
    editor.openItem('widget-1', 'Widget One', { meta: { kind: 'widget' } });
    await wait(20);
    const widgetEl = activePanelContentEl();
    record(
      'P5-FR-I1-REGISTER',
      widgetEl !== null && widgetEl.textContent === 'Widget: widget-1',
      'A test-registered "widget" kind opens and renders via the registry with zero core changes (FR-I1)'
    );

    // ------------------------------------------------------------------------
    // 2. File preset & folder preset (FR-I2, FR-I3, WK-026)
    // ------------------------------------------------------------------------
    editor.openItem('/preset/file-a.txt', 'file-a.txt', { meta: { kind: 'file' } });
    await wait(20);
    const fileEl = activePanelContentEl();
    record(
      'P5-FR-I2',
      fileEl !== null && fileEl.textContent === 'File preset view: /preset/file-a.txt',
      'File preset renders its target as the active tab content (FR-I2)'
    );

    editor.openItem('/preset/folder-a', 'folder-a', { meta: { kind: 'folder' } });
    await wait(20);
    const folderEl = activePanelContentEl();
    record(
      'P5-FR-I3',
      folderEl !== null && folderEl.textContent === 'Folder preset view: /preset/folder-a',
      'Folder preset renders its target as the active tab content (FR-I3)'
    );

    // ------------------------------------------------------------------------
    // 3. Open beside + query pane placement (FR-I4, FR-I5, FR-I8, WK-027)
    // ------------------------------------------------------------------------
    editor.clear();
    editor.openItem('/q/a.txt', 'a.txt', { meta: { kind: 'file' } });
    editor.openBeside('/q/b.txt', 'b.txt', { meta: { kind: 'file' } });
    await wait(20);
    record('P5-FR-I4', editor.getGroupCount() === 2, 'openBeside creates a second pane when none exists (FR-I4, FR-A14)');

    const placement = editor.queryPanePlacement();
    record(
      'P5-FR-I5-OWN',
      Boolean(placement && placement.own.targets.includes('/q/b.txt')),
      'queryPanePlacement reports the caller\'s own pane target correctly (FR-I5)'
    );
    record(
      'P5-FR-I5-BESIDE',
      Boolean(placement && placement.beside && placement.beside.targets.includes('/q/a.txt')),
      'queryPanePlacement reports the adjacent pane\'s target correctly (FR-I5)'
    );

    // ------------------------------------------------------------------------
    // 4. Reserved key policy (FR-I6, FR-I7, FR-R1, FR-R1a)
    // ------------------------------------------------------------------------
    const panelEl = document.querySelector('.editor-panel-content');
    let appReceivedCtrlS = false;
    const ctrlSListener = (e) => {
      if (e.ctrlKey && e.key === 's') appReceivedCtrlS = true;
    };
    if (panelEl) panelEl.addEventListener('keydown', ctrlSListener);
    let evt = null;
    if (panelEl) {
      evt = new KeyboardEvent('keydown', { key: 's', ctrlKey: true, bubbles: true, cancelable: true });
      panelEl.dispatchEvent(evt);
    }
    record(
      'P5-FR-R1A',
      appReceivedCtrlS && evt !== null && evt.defaultPrevented === false,
      'Ctrl+S reaches the tab view with 0 shell interception (FR-I6, FR-R1a)'
    );
    if (panelEl) panelEl.removeEventListener('keydown', ctrlSListener);

    // FR-I7: a tree key, pressed while the tree has focus, is fully consumed
    // by the shell (defaultPrevented) and actually moves shell-owned focus,
    // rather than being left for an app-level handler to interpret.
    tree.setRoot({
      id: '/keytest',
      label: 'keytest',
      isContainer: true,
      children: [
        { id: '/keytest/a.txt', label: 'a.txt', isContainer: false },
        { id: '/keytest/b.txt', label: 'b.txt', isContainer: false },
      ],
    });
    await wait(20);
    tree.focusTree();
    const focusedBefore = document.querySelector('.tree-row.focused');
    const focusedIdBefore = focusedBefore ? focusedBefore.getAttribute('data-id') : null;
    const treeListEl = document.querySelector('.tree-list');
    let bubbledToWindowUnprevented = false;
    const windowKeyListener = (e) => {
      if (e.key === 'ArrowDown' && !e.defaultPrevented) bubbledToWindowUnprevented = true;
    };
    window.addEventListener('keydown', windowKeyListener);
    const treeKeyEvt = new KeyboardEvent('keydown', { key: 'ArrowDown', bubbles: true, cancelable: true });
    treeListEl.dispatchEvent(treeKeyEvt);
    await wait(10);
    window.removeEventListener('keydown', windowKeyListener);
    const focusedAfter = document.querySelector('.tree-row.focused');
    const focusedIdAfter = focusedAfter ? focusedAfter.getAttribute('data-id') : null;
    const knownRowIds = ['/keytest', '/keytest/a.txt', '/keytest/b.txt'];
    record(
      'P5-FR-I7',
      treeKeyEvt.defaultPrevented === true &&
        focusedIdAfter !== null &&
        focusedIdAfter !== focusedIdBefore &&
        knownRowIds.includes(focusedIdAfter) &&
        !bubbledToWindowUnprevented,
      'ArrowDown while the tree has focus moves shell focus and is consumed before it could reach an app-level listener unprevented (FR-I7)'
    );

    const wasMenuOpenBefore = menu.isOpen;
    dispatchKey(document, 'F10');
    await wait(20);
    record('P5-FR-R1-GLOBAL', !wasMenuOpenBefore && menu.isOpen, 'F10 (a reserved key) opens the hamburger menu regardless of focus (FR-R1)');
    menu.closeMenu();

    // ------------------------------------------------------------------------
    // 5. Menu keyboard navigation (NFR-7, WK-031)
    // ------------------------------------------------------------------------
    menu.openMenu();
    dispatchKey(document, 'ArrowRight'); // File -> View
    dispatchKey(document, 'ArrowDown'); // move off item 0
    await wait(10);
    const dropdown = document.getElementById('workbench-menu-dropdown');
    record(
      'P5-NFR7-MENU-NAV',
      dropdown !== null && dropdown.querySelectorAll('.kbd-focused').length === 1,
      'Arrow-key navigation moves a single keyboard-focus marker inside the open menu (NFR-7)'
    );
    menu.closeMenu();

    // ------------------------------------------------------------------------
    // 6. Right-click menu device (FR-G5, FR-G6, WK-030)
    // ------------------------------------------------------------------------
    tree.setRoot({
      id: '/ctxroot',
      label: 'ctxroot',
      isContainer: true,
      children: [{ id: '/ctxroot/item.txt', label: 'item.txt', isContainer: false }],
    });
    await wait(20);
    const row = document.querySelector('.tree-row[data-id="/ctxroot/item.txt"]');

    if (row) {
      row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: true, clientX: 10, clientY: 10 }));
    }
    record(
      'P5-FR-G5-DEFAULT-OFF',
      document.querySelectorAll('.workbench-context-menu').length === 0,
      'Right-click renders 0 menu elements by default (FR-G5, D-22)'
    );

    let ctxActionFired = false;
    app.contextMenuItemsForTreeNode = () => [
      { id: 'a', label: 'First', action: () => { ctxActionFired = 'first'; } },
      { id: 'b', label: 'Second', action: () => { ctxActionFired = 'second'; } },
    ];
    contextMenu.setEnabled(true);
    if (row) {
      row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: true, clientX: 10, clientY: 10 }));
    }
    const ctxRows1 = document.querySelectorAll('.workbench-context-menu .context-menu-item-row');
    record(
      'P5-FR-G6-ITEM-COUNT',
      ctxRows1.length === 2,
      'When enabled, the menu shows exactly the app-defined items and 0 shell-added items (FR-G6)'
    );

    dispatchKey(document, 'ArrowDown');
    dispatchKey(document, 'Enter');
    await wait(10);
    record('P5-WK030-KEYBOARD', ctxActionFired === 'second', 'Arrow-key navigation + Enter triggers the focused context menu item (WK-030, NFR-7)');
    record(
      'P5-WK030-CLOSE-AFTER-TRIGGER',
      document.querySelectorAll('.workbench-context-menu').length === 0,
      'Triggering an item closes the context menu'
    );

    contextMenu.setEnabled(false);
    if (row) {
      row.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: true, clientX: 10, clientY: 10 }));
    }
    record(
      'P5-FR-G5-DISABLE',
      document.querySelectorAll('.workbench-context-menu').length === 0,
      'Disabling the device again renders 0 menu elements (D-22)'
    );
    contextMenu.hide();

    // ------------------------------------------------------------------------
    // 7. Status bar app item (FR-N10, WK-029)
    // ------------------------------------------------------------------------
    const appItemsEl = document.getElementById('statusbar-app-items');
    record('P5-FR-N10-APPEND', appItemsEl !== null && appItemsEl.children.length >= 1, 'App status bar item is present (FR-N10)');
    record(
      'P5-FR-N10-SLOTS',
      Boolean(document.getElementById('statusbar-path')) &&
        Boolean(document.getElementById('statusbar-message')) &&
        Boolean(document.getElementById('statusbar-app-info')),
      "The shell's three status bar slots remain intact (FR-N10)"
    );

    // ------------------------------------------------------------------------
    // 8. Activity bar swap (FR-N12, WK-029)
    // ------------------------------------------------------------------------
    const originalTopItems = activityBar.getTopItems();
    const originalViewItemCount = menu.getViewItems().length;
    activityBar.setTopItems([{ id: 'swap:one', label: 'Swap One', iconClass: 'codicon-circle-filled' }]);
    await wait(10);
    const topButtons = document.querySelectorAll('#activity-bar-top .activity-bar-item');
    record(
      'P5-FR-N12-SWAP',
      activityBar.getTopItems().length === 1 && topButtons.length === 1,
      'App swaps activity bar top items with zero core changes (FR-N12)'
    );
    record(
      'P5-FR-N12-MENU-UNCHANGED',
      menu.getViewItems().length === originalViewItemCount,
      "Swapping activity bar items leaves the View menu's item list unchanged (FR-N12)"
    );
    activityBar.setTopItems(originalTopItems);

    // ------------------------------------------------------------------------
    // 9. File menu app item (FR-I9, WK-048)
    // ------------------------------------------------------------------------
    const fileGroup = menu.getGroups().find((g) => g.id === 'file');
    const coreFileItems = menu.getFileItems();
    const fileItemsMatchCore = coreFileItems.every((it, i) => fileGroup.items[i].id === it.id);
    record('P5-FR-I9-CORE-ORDER', fileItemsMatchCore, "File menu's shell items keep their exact list and order (FR-I9, FR-N6)");
    record(
      'P5-FR-I9-SEPARATOR',
      fileGroup.items[coreFileItems.length].type === 'separator',
      'A separator follows the shell items before any app-added item (FR-I9, D-31)'
    );
    record(
      'P5-FR-I9-APP-ITEM',
      fileGroup.items[coreFileItems.length + 1].id === 'app:file:preset-info',
      'The app-added File item appears below the separator (FR-I9)'
    );

    // ------------------------------------------------------------------------
    // 10. Sidebar view titlebar app action (FR-I10, WK-048)
    // ------------------------------------------------------------------------
    const sidebarActions = document.getElementById('sidebar-actions');
    const actionChildren = sidebarActions ? Array.from(sidebarActions.children) : [];
    const appActionsIndex = actionChildren.findIndex((el) => el.id === 'sidebar-app-actions');
    const collapseAllIndex = actionChildren.findIndex((el) => el.id === 'sidebar-action-collapse-all');
    const appActionsEl = document.getElementById('sidebar-app-actions');
    record(
      'P5-FR-I10-POSITION',
      appActionsIndex >= 0 && collapseAllIndex >= 0 && appActionsIndex < collapseAllIndex,
      'App-added view-titlebar action sits to the left of the two shell actions (FR-I10, D-30, D-31)'
    );
    record(
      'P5-FR-I10-COUNT',
      appActionsEl !== null && appActionsEl.children.length === 1,
      'Exactly one app action is present in the view titlebar (FR-I10)'
    );

    // ------------------------------------------------------------------------
    // 11. View/Help are closed to the app (FR-I11)
    // ------------------------------------------------------------------------
    record(
      'P5-FR-I11',
      typeof menu.addAppViewItem === 'undefined' && typeof menu.addAppHelpItem === 'undefined',
      'No surface exists for the app to add to View or Help (FR-I11)'
    );

    return { success: results.every((r) => r.pass), results };
  } catch (err) {
    record('RUNNER_ERR', false, 'Unhandled error in Phase 5 suite: ' + (err && err.stack ? err.stack : String(err)));
    return { success: false, results };
  }
};
import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';
import { execSync } from 'child_process';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const rootDir = path.resolve(__dirname, '..');

console.log('=== workbench-kit: Phase 5 (Extension Points) Quality Verification ===');
let failures = 0;

function assert(condition, msg) {
  if (!condition) {
    console.error(`[FAIL] ${msg}`);
    failures++;
  } else {
    console.log(`[PASS] ${msg}`);
  }
}

function extractComments(source) {
  const commentRegex = /\/\*[\s\S]*?\*\/|\/\/.*/g;
  return (source.match(commentRegex) || []).join('\n');
}
const koreanCharRegex = /[\uac00-\ud7a3]/;

// --------------------------------------------------------------------------
// 1. Core purity: no resource-kind vocabulary branching in src/core/*.ts
//    (NFR-1, INTENT 3). Generic passthrough plumbing (meta bags, opaque
//    kind strings) is fine; literal domain words are not.
// --------------------------------------------------------------------------
console.log('\n--- 1. Core Purity: Zero Resource-Kind Vocabulary in src/core/*.ts (NFR-1) ---');
const coreFiles = ['editor.ts', 'contextmenu.ts', 'layout.ts', 'menu.ts', 'sidebar.ts', 'activitybar.ts'];
// menu.ts legitimately uses the literal 'file' as the File MENU GROUP id
// (established in Phase 2, reviewed under A2) ??that is a UI-group label,
// not a resource-kind branch, so it is excluded from this specific check.
const kindVocabCheckFiles = ['editor.ts', 'contextmenu.ts', 'layout.ts', 'sidebar.ts', 'activitybar.ts'];
for (const file of coreFiles) {
  const p = path.join(rootDir, 'src/core', file);
  const src = fs.readFileSync(p, 'utf8');
  if (kindVocabCheckFiles.includes(file)) {
    assert(!/['"]file['"]/.test(src) && !/['"]folder['"]/.test(src), `src/core/${file} contains 0 literal 'file'/'folder' kind checks (NFR-1)`);
  }
  assert(!koreanCharRegex.test(extractComments(src)), `src/core/${file} comments are in English (0 Korean characters)`);
}

// --------------------------------------------------------------------------
// 2. App-facing surface: presets/registry never manipulate tabs/panes
//    directly (FR-I8, D-19). Only openItem/openBeside/queryPanePlacement
//    and registry.register are used.
// --------------------------------------------------------------------------
console.log('\n--- 2. App-Facing Surface: Zero Direct Tab/Pane Manipulation (FR-I8) ---');
const appLayerFiles = ['src/presets/file-preset.ts', 'src/presets/folder-preset.ts', 'src/registry/kind-registry.ts'];
const forbiddenDirectCalls = ['.closePanel(', '.closeActiveTab(', '.closeAllTabsInGroup(', '.addNewTab(', '.splitGroup(', '.splitActiveGroup('];
for (const rel of appLayerFiles) {
  const src = fs.readFileSync(path.join(rootDir, rel), 'utf8');
  for (const token of forbiddenDirectCalls) {
    assert(!src.includes(token), `${rel} contains 0 occurrences of direct manipulation call "${token}" (FR-I8)`);
  }
}
// The preset helpers must type against the narrow AppEditorSurface, not the
// full EditorController ??otherwise the exposed surface is wide even though
// nothing currently calls the extra methods (FR-I8, C-9).
for (const rel of ['src/presets/file-preset.ts', 'src/presets/folder-preset.ts']) {
  const src = fs.readFileSync(path.join(rootDir, rel), 'utf8');
  assert(src.includes('AppEditorSurface') && !src.includes('EditorController'), `${rel} types its open-helper against AppEditorSurface, not the full EditorController (FR-I8)`);
}

// --------------------------------------------------------------------------
// 3. Reserved-key documentation exists exactly once (FR-R1)
// --------------------------------------------------------------------------
console.log('\n--- 3. Reserved-Key Documentation (FR-R1, FR-R1a) ---');
const reservedKeysPath = path.join(rootDir, 'docs/reserved-keys.md');
assert(fs.existsSync(reservedKeysPath), 'docs/reserved-keys.md exists (FR-R1)');
if (fs.existsSync(reservedKeysPath)) {
  const doc = fs.readFileSync(reservedKeysPath, 'utf8');
  for (const key of ['Ctrl+S', 'F2', 'Delete', 'Ctrl+Z', 'Ctrl+Y', 'Ctrl+C', 'Ctrl+V']) {
    assert(doc.includes(key), `docs/reserved-keys.md lists "${key}" as not reserved (FR-R1a)`);
  }
}

// --------------------------------------------------------------------------
// 4. CSS invariants: no tab-explorer-templates tokens leaked by new rules
// --------------------------------------------------------------------------
console.log('\n--- 4. CSS Invariants (C-10, D-29) ---');
const css = fs.readFileSync(path.join(rootDir, 'src/style.css'), 'utf8');
const forbiddenTokens = ['12px', '4px', '8px', '16px', '6px', '#353b44', 'Segoe UI', 'Cascadia Mono'];
for (const token of forbiddenTokens) {
  const isExactWord = /^[a-zA-Z0-9]/.test(token);
  const regex = isExactWord ? new RegExp(`\\b${token}\\b`, 'g') : new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'g');
  const matches = css.match(regex);
  assert(!matches || matches.length === 0, `src/style.css contains 0 occurrences of "${token}"`);
}

// --------------------------------------------------------------------------
// 5. In-Browser Runtime Verification via Electron
// --------------------------------------------------------------------------
console.log('\n--- 5. Executing In-Browser Runtime Verification via Electron ---');
const runnerPath = path.join(__dirname, 'phase5-electron-runner.cjs');

let electronResults = null;
try {
  const runnerOutput = execSync(`npx.cmd electron "${runnerPath}"`, {
    cwd: rootDir,
    stdio: 'pipe',
    timeout: 35000,
  }).toString();

  const lines = runnerOutput.split(/\r?\n/);
  for (const line of lines) {
    if (line.startsWith('[PASS]')) {
      console.log(line);
    } else if (line.startsWith('[FAIL]')) {
      console.error(line);
      failures++;
    }
  }
  const match = runnerOutput.match(/\[electron\] Phase 5 Results:\s*(\{.*\})/);
  if (match) {
    electronResults = JSON.parse(match[1]);
  }
  assert(runnerOutput.includes('All Phase 5 Electron in-browser runtime assertions executed successfully'), 'Electron test runner completed all assertions successfully');
} catch (err) {
  console.error(`[FAIL] Electron test runner failed: ${err.message}`);
  if (err.stdout) console.log(err.stdout.toString());
  if (err.stderr) console.error(err.stderr.toString());
  failures++;
}

// --------------------------------------------------------------------------
// 6. In-Browser Runtime Verification via pywebview & Cross-Host Parity
// --------------------------------------------------------------------------
console.log('\n--- 6. Executing In-Browser Runtime Verification via pywebview & Cross-Host Parity ---');

function findPython() {
  if (process.env.WORKBENCH_PYTHON && fs.existsSync(process.env.WORKBENCH_PYTHON)) {
    return process.env.WORKBENCH_PYTHON;
  }
  const winpythonPath = 'C:\\winpython\\WPy64-31180_cpu\\python-3.11.8.amd64\\python.exe';
  if (fs.existsSync(winpythonPath)) {
    return winpythonPath;
  }
  try {
    const which = execSync('where python', { stdio: 'pipe' }).toString().trim().split(/\r?\n/)[0];
    if (which && fs.existsSync(which)) return which;
  } catch {}
  return 'python';
}

const pythonExecutable = findPython();
let pywebviewResults = null;
try {
  const pyOutput = execSync(`"${pythonExecutable}" -m src.hosts.pywebview.main --phase5-test`, {
    cwd: rootDir,
    stdio: 'pipe',
    timeout: 35000,
  }).toString();

  const pyMatch = pyOutput.match(/\[pywebview\] Phase 5 Results:\s*(\{.*\})/);
  if (pyMatch) {
    pywebviewResults = JSON.parse(pyMatch[1]);
  }
  assert(pywebviewResults && pywebviewResults.success === true, 'pywebview test runner completed all assertions successfully');
} catch (err) {
  console.error(`[FAIL] pywebview test runner failed: ${err.message}`);
  if (err.stdout) console.log(err.stdout.toString());
  if (err.stderr) console.error(err.stderr.toString());
  failures++;
}

// --------------------------------------------------------------------------
// Phase 5 Required Assertion Manifest (Immutable Contract)
// --------------------------------------------------------------------------
const REQUIRED_PHASE5_ASSERTIONS = Object.freeze([
  'INIT',
  'P5-FR-I1-REGISTER',
  'P5-FR-I2',
  'P5-FR-I3',
  'P5-FR-I4',
  'P5-FR-I5-OWN',
  'P5-FR-I5-BESIDE',
  'P5-FR-R1A',
  'P5-FR-I7',
  'P5-FR-R1-GLOBAL',
  'P5-NFR7-MENU-NAV',
  'P5-FR-G5-DEFAULT-OFF',
  'P5-FR-G6-ITEM-COUNT',
  'P5-WK030-KEYBOARD',
  'P5-WK030-CLOSE-AFTER-TRIGGER',
  'P5-FR-G5-DISABLE',
  'P5-FR-N10-APPEND',
  'P5-FR-N10-SLOTS',
  'P5-FR-N12-SWAP',
  'P5-FR-N12-MENU-UNCHANGED',
  'P5-FR-I9-CORE-ORDER',
  'P5-FR-I9-SEPARATOR',
  'P5-FR-I9-APP-ITEM',
  'P5-FR-I10-POSITION',
  'P5-FR-I10-COUNT',
  'P5-FR-I11',
]);

function validateHostContract(hostName, hostResults) {
  assert(hostResults && hostResults.success === true, `${hostName} suite completed with success: true`);
  const list = hostResults.results || [];
  assert(
    list.length === REQUIRED_PHASE5_ASSERTIONS.length,
    `${hostName} executed exact expected count (${list.length} vs expected ${REQUIRED_PHASE5_ASSERTIONS.length})`
  );

  const seen = new Set();
  for (const item of list) {
    assert(!seen.has(item.id), `No duplicate assertion IDs in ${hostName} (duplicate found: ${item.id})`);
    seen.add(item.id);
  }
  for (const reqId of REQUIRED_PHASE5_ASSERTIONS) {
    assert(seen.has(reqId), `Required assertion ${reqId} is present in ${hostName}`);
  }
  const reqSet = new Set(REQUIRED_PHASE5_ASSERTIONS);
  for (const item of list) {
    assert(reqSet.has(item.id), `Assertion ${item.id} in ${hostName} is defined in manifest`);
    assert(item.pass === true, `${hostName} assertion ${item.id} passed (${item.msg})`);
  }
}

if (electronResults) {
  validateHostContract('Electron', electronResults);
}
if (pywebviewResults) {
  validateHostContract('pywebview', pywebviewResults);
}

if (electronResults && pywebviewResults) {
  const eList = electronResults.results || [];
  const pList = pywebviewResults.results || [];
  assert(eList.length === pList.length, `Both hosts execute identical assertion count (${eList.length} vs ${pList.length})`);

  let divergences = 0;
  for (let i = 0; i < eList.length; i++) {
    const e = eList[i];
    const p = pList[i];
    if (e.id !== p.id || e.pass !== p.pass) {
      console.error(`[DIVERGENCE] Index ${i}: ID (${e.id} vs ${p.id}) | Pass (${e.pass} vs ${p.pass})`);
      divergences++;
      failures++;
    }
  }
  assert(divergences === 0, `Zero divergence between Electron and pywebview across all Phase 5 assertions (divergences: ${divergences})`);
} else {
  assert(false, 'Unable to compare host parity: one or both test runners failed to produce structured results');
}

// --------------------------------------------------------------------------
// Summary
// --------------------------------------------------------------------------
console.log('\n----------------------------------------------------');
if (failures === 0) {
  console.log('SUCCESS: All Phase 5 quality checks passed (0 failures).');
  process.exit(0);
} else {
  console.error(`FAILURE: ${failures} check(s) failed.`);
  process.exit(1);
}

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk rg -n -A 120 -B 2 \"## D-19\\.\" docs/current/DECISIONS.md; rtk rg -n \"FR-I8|addAppViewItem|addAppHelpItem|AppEditorSurface|targetGroup|IDockviewPanel|setAction\\('view|__workbenchApp|menu\\.setAction|editor\\.getApi|closePanel\" scripts/phase5-suite.js scripts/verify-phase5.mjs src/core/editor.ts src/main.ts" in D:\projects\workbench-kit
 succeeded in 159ms:
724----
725-
726:## D-19. 앱이 "옆 칸에 열기"를 부를 수 있고, 탭 안의 키는 앱에게 간다
727-
728-**선택**
729-
730-파일 커맨터의 `F3` · `F4`를 성립시키려면 껍데기가 두 가지를 열어 주어야 한다.
731-
732-**1. 옆 칸에 열기** — 앱이 "이 대상을 옆 칸의 탭에서 열어라"를 코드로 부를 수 있다.
733-사용자가 트리에서 `Ctrl+Enter`를 눌렀을 때와 같은 동작이며(D-9), 그 명령을 앱도
734-쓸 수 있게 여는 것이다. 옆 칸이 없으면 새로 만든다(오른쪽에 — D-13).
735-
736-**2. 탭 안의 키는 앱에게 간다** — 포커스가 탭 안에 있을 때, 껍데기가 쓰지 않는 키는
737-그 탭의 보기에게 넘어간다. 파일 커맨터의 `F3`(보기) · `F4`(편집) · `F5`(복사) ·
738-`F6`(이동)과 여러 개 고르기의 `Space`가 여기 해당한다. 껍데기가 먼저 가져가는 키는
739-문서로 정해 두고, 그 밖은 전부 앱 몫이다.
740-
741-`Space`는 트리에서 펼침/접힘이지만(D-9) 그것은 **포커스가 트리에 있을 때**다. 포커스가
742-탭 안에 있으면 트리 키 묶음이 적용되지 않으므로 부딪히지 않는다.
743-
744-목록은 **가져가는 키**만 적지 않고, 흔히 껍데기 것이라고 오해하는 키를 **가져가지
745-않는다**고 함께 적는다. `Ctrl+S`(저장) · `F2`(이름 바꾸기) · `Delete`(삭제) ·
746-`Ctrl+Z` / `Ctrl+Y`(되돌리기·다시하기) · `Ctrl+C` / `Ctrl+V`가 그것이다. 저장은 앱이
747-하고(D-18), 만들기·이름 바꾸기·삭제도 앱이 한다(D-30 · D-22). 껍데기가 이 키들을
748-가져가면 그 순간 리소스 종류를 아는 판단이 껍데기로 들어온다. 적어 두지 않으면 앱마다
749-다르게 정하거나, 나중에 껍데기가 슬그머니 가져가게 된다.
750-
751-**3. 앱이 칸 배치를 물을 수 있다** — 앱이 "지금 내가 어느 칸에 있나", "옆 칸에는
752-무엇이 열려 있나"를 물을 수 있다. 파일 커맨터가 고른 파일을 **반대편 칸의 폴더로**
753-복사·이동하려면 반대편이 어느 폴더를 보고 있는지 알아야 하는데, 칸 배치는 껍데기만
754-안다.
755-
756-**근거**
757-
758-파일 커맨터의 핵심 동작이 "왼쪽에서 고른 것을 오른쪽에서 본다"인데, 지금 껍데기는
759-**탐색기에서 고르는 경우만** 다룬다(D-5). 탭 안의 목록에서 고르는 경우는 앱의 일이고,
760-그 결과를 옆 칸에 띄우는 것은 껍데기만 할 수 있다. 이 자리가 열려 있지 않으면 앱이
761-껍데기 밖에서 탭을 직접 건드려야 하고, 그 순간 "공통 코어 수정 0줄"이 깨진다.
762-
763-복사·이동도 같다. "반대편 칸으로"라는 말 자체가 칸 배치를 아는 쪽만 할 수 있는
764-판단이다. 다만 껍데기는 **어느 칸에 무엇이 열려 있는지만 알려주고**, 복사와 이동
765-자체는 하지 않는다. 파일을 옮기는 일은 리소스 종류를 아는 앱의 몫이다(INTENT 3절).
766-
767-키도 같다. 껍데기가 모든 키를 가져가면 앱이 자기 단축키를 가질 수 없다. 반대로
768-앱이 모든 키를 가져가면 트리 조작(D-9)이 깨진다. 그래서 **껍데기가 가져가는 키를
769-목록으로 정하고 나머지를 넘기는** 방식으로 가른다.
770-
771-**배제한 대안**
772-
773-- **앱이 껍데기의 탭·칸을 직접 조작한다.** 껍데기의 내부 상태가 앱에 노출되어 규칙이
774-  깨질 자리가 열린다. 배제한다.
775-- **`F3` · `F4`를 껍데기의 키로 정한다.** 껍데기가 "보기"와 "편집"이라는 앱의 개념을
776-  알게 되어 "껍데기는 리소스 종류를 모른다"(INTENT 3절)에 어긋난다. 배제한다.
777-- **키를 앱이 먼저 받고 안 쓰면 껍데기로 넘긴다.** 앱이 실수로 트리 키를 삼키면
778-  탐색기가 죽는데 원인을 찾기 어렵다. 껍데기 우선이 안전하다. 배제한다.
779-
780----
781-
782-## D-20. 빈 상태에는 아무것도 그리지 않는다
783-
784-**선택**
785-
786-폴더를 아직 열지 않았을 때의 트리 자리, 사용자가 방금 추가한 빈 탭의 안쪽, 그리고
787-마지막 칸의 탭이 모두 닫혔을 때 남는 그 칸(D-26)에는 **아무것도 그리지 않는다.**
788-안내 문구도 버튼도 두지 않고 빈 면으로 둔다.
789-
790-VS Code의 환영 뷰(Welcome View)에 해당하는 것을 만들지 않는다.
791-
792-**근거**
793-
794-폴더를 여는 길은 이미 하나로 정해져 있다 — 메뉴의 폴더 열기와 `Ctrl+O`(D-16). 빈
795-트리에 [폴더 열기] 버튼을 두면 같은 일로 가는 길이 둘이 된다. D-16이 "루트를 바꾸는
796-길은 하나"로 정한 것과 어긋난다.
797-
798-안내 문구도 만들고 유지해야 하는 것이다. 문구를 정하고, 세 색 테마에 맞추고, 두
799-갈래에서 같게 보이는지 확인해야 한다. 제1원칙(최소 구현)에 비추어 그만한 값어치가
800-없다고 본다.
801-
802-**완료 조건과의 관계** — BRIEF의 "빈 상태" 시나리오는 이 결정으로 판정 가능해진다.
803-정해진 결과는 **"빈 면이 나오고 아무 동작도 일어나지 않는다"**이다. 무엇이 그려져야
804-하는지가 아니라 무엇도 그려지지 않아야 하는지가 기준이 된다.
805-
806-**배제한 대안**
807-
808-- **한 줄 안내와 버튼 하나를 둔다(VS Code의 환영 뷰에 해당).** 처음 쓰는 사람이
809-  무엇을 눌러야 할지 알 수 있다는 이점이 있으나, 폴더 여는 길이 둘이 되고 유지할
810-  것이 늘어난다. 배제한다.
811-- **빈 트리에 최근에 연 폴더 목록을 보인다.** 편하지만 빈 화면이 아닌 것을 하나 더
812-  만드는 셈이고, 최근 폴더는 이미 메뉴에 있다(D-16). 배제한다.
813-
814----
815-
816-## D-21. 실패도 진행도 하단 바 한 줄로만 알린다
817-
818-**선택**
819-
820-오류·경고와 오래 걸리는 작업의 진행 표시를 **모두 하단 바(상태 표시줄)에 한 줄로**
821-보인다. 오른쪽 아래에 떠올랐다 사라지는 알림 상자를 만들지 않는다. 알림이 쌓이는
822-목록이나 지난 알림을 다시 보는 자리도 두지 않는다.
823-
824-**근거**
825-
826-떠오르는 알림 상자를 만들려면 그 상자를 그리고, 겹칠 때 쌓고, 시간이 지나면 지우고,
827-세 색 테마에 맞추고, 두 갈래에서 같게 보이는지 확인해야 한다. 하단 바는 **이미
828-있는 자리**라 새로 만들 것이 없다.
829-
830-화면을 가리지 않는 것도 이유다. 오른쪽 아래는 탭 영역 위에 겹치는데, 터미널이나
831-파일 목록의 아래쪽이 자주 가려진다.
832-
833-**받아들이는 단점** — 하단 바는 눈에 덜 띄어서 오류를 놓칠 수 있다. 이것을 알고
834-고른다. 놓치는 것이 실제로 문제가 되면 그때 알림 상자를 더한다.
835-
836-**배제한 대안**
837-
838-- **오류는 오른쪽 아래 짧은 메시지로, 진행은 하단 바로 나눈다.** 실패가 눈에 잘
839-  띈다는 이점이 분명하지만, 그리는 장치를 하나 더 만들어야 하고 탭 영역을 가린다.
840-  배제한다.
841-- **껍데기는 자리만 주고 문구와 모양은 앱이 정한다(D-4 · D-11 방식).** 알림의 생김새가
842-  앱마다 달라지면 같은 껍데기를 쓴다는 느낌이 깨진다. 무엇을 알릴지는 앱이 정하되
843-  **어떻게 보일지는 껍데기가 정한다.** 배제한다.
844-- **알림이 쌓이는 목록을 둔다(VS Code의 알림 센터).** 지난 알림을 다시 보는 요구가
845-  아직 없다. 배제한다.
846-
scripts/verify-phase5.mjs:50://    directly (FR-I8, D-19). Only openItem/openBeside/queryPanePlacement
scripts/verify-phase5.mjs:53:console.log('\n--- 2. App-Facing Surface: Zero Direct Tab/Pane Manipulation (FR-I8) ---');
scripts/verify-phase5.mjs:55:const forbiddenDirectCalls = ['.closePanel(', '.closeActiveTab(', '.closeAllTabsInGroup(', '.addNewTab(', '.splitGroup(', '.splitActiveGroup('];
scripts/verify-phase5.mjs:59:    assert(!src.includes(token), `${rel} contains 0 occurrences of direct manipulation call "${token}" (FR-I8)`);
scripts/verify-phase5.mjs:62:// The preset helpers must type against the narrow AppEditorSurface, not the
scripts/verify-phase5.mjs:64:// nothing currently calls the extra methods (FR-I8, C-9).
scripts/verify-phase5.mjs:67:  assert(src.includes('AppEditorSurface') && !src.includes('EditorController'), `${rel} types its open-helper against AppEditorSurface, not the full EditorController (FR-I8)`);
src/main.ts:82:    this.menu.setAction('file:open-folder', () => this.handleOpenFolderDialog());
src/main.ts:83:    this.menu.setAction('file:open-recent', () => this.handleOpenRecentFolder());
src/main.ts:84:    this.menu.setAction('file:close-folder', () => this.closeFolder());
src/main.ts:109:    this.menu.setAction('view:toggle-sidebar', () => this.viewState.toggleSidebar());
src/main.ts:110:    this.menu.setAction('view:toggle-titlebar', () => this.viewState.toggleTitlebar());
src/main.ts:111:    this.menu.setAction('view:toggle-statusbar', () => this.viewState.toggleStatusbar());
src/main.ts:112:    this.menu.setAction('view:zen-mode', () => this.viewState.toggleZenMode());
src/main.ts:123:    this.menu.setAction('view:cycle-color-theme', () => this.theme.cycleTheme());
src/main.ts:127:    this.menu.setAction('view:cycle-icon-theme', () => this.iconTheme.toggleTheme());
src/main.ts:142:    this.menu.setAction('view:toggle-context-menu', () => {
src/main.ts:163:    this.menu.setAction('file:close-tab', () => this.editor.closeActiveTab());
src/main.ts:166:    this.menu.setAction('view:split-horizontal', () => this.editor.splitActiveGroup('right'));
src/main.ts:167:    this.menu.setAction('view:split-vertical', () => this.editor.splitActiveGroup('below'));
src/main.ts:168:    this.menu.setAction('view:close-active-tabs', () => this.editor.closeAllTabsInGroup());
src/main.ts:321:    (window as any).__workbenchApp = appInstance;
src/core/editor.ts:5:  IDockviewPanel,
src/core/editor.ts:32: * helpers) that opens or inspects panes (FR-I4, FR-I5, FR-I8, C-9). It
src/core/editor.ts:34: * or pane (addNewTab, closePanel, splitGroup, ...) — those stay shell-only.
src/core/editor.ts:37:export interface AppEditorSurface {
src/core/editor.ts:42:    targetGroup?: DockviewGroupPanel
src/core/editor.ts:43:  ): IDockviewPanel;
src/core/editor.ts:44:  openBeside(targetId: string, title?: string, options?: EditorOpenOptions): IDockviewPanel;
src/core/editor.ts:215:  private activePanelChangeListeners: ((panel: IDockviewPanel | undefined) => void)[] = [];
src/core/editor.ts:340:  public getPanels(): IDockviewPanel[] {
src/core/editor.ts:352:  public getActivePanel(): IDockviewPanel | undefined {
src/core/editor.ts:364:  public addNewTab(targetGroup?: DockviewGroupPanel): IDockviewPanel {
src/core/editor.ts:365:    const group = targetGroup || this.getActiveGroup() || this.api.groups[0];
src/core/editor.ts:403:    targetGroup?: DockviewGroupPanel
src/core/editor.ts:404:  ): IDockviewPanel {
src/core/editor.ts:408:    const group = targetGroup || this.getActiveGroup() || this.api.groups[0];
src/core/editor.ts:574:  ): IDockviewPanel {
src/core/editor.ts:628:      this.closePanel(activePanel);
src/core/editor.ts:636:  public closePanel(panel: IDockviewPanel): void {
src/core/editor.ts:654:  public closeAllTabsInGroup(targetGroup?: DockviewGroupPanel): void {
src/core/editor.ts:655:    const group = targetGroup || this.getActiveGroup();
src/core/editor.ts:693:  public onActivePanelChange(callback: (panel: IDockviewPanel | undefined) => void): () => void {
scripts/phase5-suite.js:17:    const app = window.__workbenchApp;
scripts/phase5-suite.js:19:      record('INIT', false, 'window.__workbenchApp is defined');
scripts/phase5-suite.js:22:    record('INIT', true, 'window.__workbenchApp is initialized and accessible');
scripts/phase5-suite.js:87:    // 3. Open beside + query pane placement (FR-I4, FR-I5, FR-I8, WK-027)
scripts/phase5-suite.js:321:      typeof menu.addAppViewItem === 'undefined' && typeof menu.addAppHelpItem === 'undefined',

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rtk rg -n -A 45 -B 2 \"## D-4\\.\" docs/current/DECISIONS.md; rtk rg -n -A 8 -B 2 \"FR-E1|FR-E2|FR-E3|FR-E4|FR-E5\" docs/current/SPEC.md; rtk rg -n -C 8 \"setTabDirty|panel\\.update|renderForCurrentParams|inner\\?\\.dispose\" src/core/editor.ts src/registry/kind-registry.ts" in D:\projects\workbench-kit
 succeeded in 192ms:
88----
89-
90:## D-4. 터미널 같은 살아 있는 보기는 앱이 제공하고, 껍데기는 탭과 호스트 브릿지만 준다
91-
92-**선택**
93-
94-폴더 탐색기의 터미널처럼 살아 있는 세션 보기는 껍데기가 만들지 않는다. 껍데기는 그런
95-보기를 담을 탭 · 수명 관리 · 호스트 브릿지만 제공하고, 터미널 로직(xterm.js · PTY 등)과
96-"폴더 경로 → 그 경로의 터미널 열기" 매핑은 멀티터미널 앱(폴더 프리셋) 안에 둔다.
97-
98-**근거**
99-
100-"껍데기는 리소스 종류를 모른다"(INTENT). 터미널은 대상의 한 종류일 뿐이고, 껍데기가
101-터미널을 알면 이 원칙이 깨져 다음 앱이 같은 껍데기를 못 쓴다. VS Code의 "통합
102-터미널"(하단 Panel)을 만들지 않는다는 것과도 일치한다. 껍데기가 증명할 것은 "살아
103-있는 비-파일 보기를 탭이 담고 수명 동안 유지한다"까지다.
104-
105-**배제한 대안**
106-
107-- **껍데기가 통합 터미널을 기본 기능으로 넣는다.** 리소스 종류를 아는 코드가 껍데기로
108-  새고 VS Code식 하단 Panel을 재현하게 되므로 배제한다.
109-
110----
111-
112-## D-5. 탭은 사용자가 추가할 때만 늘어난다 — 탐색기 선택은 현재 탭에서 연다
113-
114-**선택**
115-
116-- 탐색기에서 항목을 고르면 **현재 탭 영역의 활성 탭에서 열린다.** 탭 개수는 늘지
117-  않는다. 껍데기가 스스로 탭을 만드는 경우는 없다.
118-- 고른 항목이 **이미 다른 탭에 열려 있으면 그 탭으로 이동한다.** 파일과 폴더에 같은
119-  규칙이 적용된다.
120-- **새 탭은 사용자가 명시적으로 추가할 때만 생긴다.**
121-- 사용자가 방금 추가한 탭에서 이미 열려 있는 항목을 고르면, 이동하지 않고 **그 탭에
122-  새로 연다.** 즉 중복 검사는 탐색기 선택에만 걸리고, 사용자가 만든 탭에는 걸리지
123-  않는다.
124-
125-**근거**
126-
127-탭이 저절로 불어나면 사용자가 탭 줄을 정리하는 데 노력을 쓴다. 제1원칙인 최소·단순에
128-어긋난다. 고를 때마다 현재 탭에서 열리면 탭 줄은 사용자가 의도한 만큼만 길어진다.
129-
130-중복 이동 규칙이 파일과 폴더에 똑같이 적용되므로 껍데기가 리소스 종류를 알 필요가
131-없다(INTENT 3절 "껍데기는 리소스 종류를 모른다").
132-
133-사용자가 탭을 추가하는 행위 자체가 "하나 더 원한다"는 명시적 의사다. 그래서 그
134-자리에서는 중복 검사를 하지 않는다. 멀티터미널이 한 폴더에 터미널을 여러 개 두는
135-것이 이 규칙 하나로 성립하며, 껍데기에 터미널이나 세션을 아는 코드가 들어가지
75-| FR-C4 | 한 칸의 **마지막** 탭을 닫는다 | 그 칸이 사라지고, 남은 칸들이 그 자리를 나눠 갖는다. **단 마지막 한 칸이면 빈 면으로 남는다** | 칸 2개에서 한쪽의 마지막 탭을 닫으면 칸 1개가 되고 남은 칸이 전체를 차지한다. 칸 1개에서 마지막 탭을 닫으면 칸 수 1 · 탭 수 0인 빈 면이다 | D-26 |
76-| FR-C5 | 탭을 같은 줄에서 끈다 | 순서가 바뀐다. 각 탭의 내용은 그대로다 | 탭 순서가 바뀌고, 각 탭의 대상과 보기 인스턴스 도장이 이동 전과 같다 | D-10 |
77:| FR-C6 | 탭을 다른 칸으로 끈다 | 그 칸으로 옮겨진다. 보기는 다시 만들어지지 않는다 (FR-E2) | 탭이 목표 칸에 있고, 인스턴스 도장이 같으며 생성 횟수가 늘지 않았다 | D-10 · D-24 |
78-| FR-C7 | 탭을 편집기 **가장자리로** 끈다 | 오른쪽 가장자리면 오른쪽에, 아래쪽 가장자리면 아래쪽에 새 칸이 생기고 그 탭이 거기로 간다 | 오른쪽 가장자리에 떨구면 칸이 1 늘고 새 칸이 오른쪽에 있다. 아래쪽도 같은 방식으로 아래에 생긴다 | D-13 |
79-| FR-C8 | 탭을 창 밖으로 끈다 | 아무 일도 일어나지 않는다. 떼어내기·새 창은 없다 | 창 밖에 떨궈도 창 개수가 1 그대로이고 탭의 소속 칸과 순서가 변하지 않는다 | D-13 |
80-| FR-C9 | 탭 제목에서 오른쪽 버튼 | 기본 상태에서는 아무 일도 없다 (FR-G5) | 껍데기만 있는 상태에서 탭 제목 우클릭 → 메뉴 요소가 0개다 | D-22 |
81-
82-### 1.4 칸 (FR-D)
83-
84-| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
85-| --- | --- | --- | --- | --- |
97-| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
98-| --- | --- | --- | --- | --- |
99:| FR-E1 | 탭 A에서 B로 갔다가 A로 돌아온다 | A의 보기 인스턴스가 **그대로**다. 다시 만들어지지 않는다 | A의 인스턴스 도장이 같고, 생성 횟수 1 · 폐기 0이다 | D-24 |
100:| FR-E2 | 탭을 다른 칸으로 옮긴다 | 인스턴스가 그대로다 | 옮긴 뒤 인스턴스 도장이 같고 생성 횟수 1 · 폐기 0이다 | D-24 |
101:| FR-E3 | 탭을 닫는다 | **그때** 폐기된다. 그 전에는 폐기되지 않는다 | FR-E1·E2를 거치는 동안 폐기 0이고, 닫는 순간 폐기 1이 된다 | D-24 |
102:| FR-E4 | "항상 그려 두기"로 올린 탭을 안 보이게 둔다 | 화면 요소가 떼어지지 않는다 | 그 탭을 비활성으로 둔 상태에서 보기의 DOM 요소가 문서에 여전히 붙어 있다 | D-24 |
103:| FR-E5 | 기본 설정인 탭을 안 보이게 둔다 | 화면 요소는 떼어지되 인스턴스는 살아 있다 | 비활성 상태에서 DOM 요소가 떨어져 있고, 인스턴스 도장과 타이머는 계속 살아 있다 | D-24 |
104-
105-### 1.6 화면 접기와 Zen (FR-F)
106-
107-| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
108-| --- | --- | --- | --- | --- |
109-| FR-F1 | 탐색기 접기 | 탐색기가 사라지고 탭 영역이 그만큼 넓어진다. **세로 띠는 남는다** | 접은 뒤 탐색기가 보이지 않고, 탭 영역 폭이 탐색기 폭만큼 늘며, 세로 띠는 보인다 | D-12 |
110-| FR-F2 | 상단 바 감추기 | 상단 바가 사라진다. 창 버튼도 함께 사라지지만, 세로 띠 아이콘으로 되돌릴 수 있다 | 감춘 뒤 상단 바와 창 버튼이 보이지 않고, 세로 띠의 해당 아이콘을 누르면 되돌아온다 | D-14 |
111-| FR-F3 | 하단 바 감추기 | 하단 바가 사라진다 | 감춘 뒤 하단 바가 보이지 않고, 같은 아이콘으로 되돌아온다 | D-14 |
157-| FR-J3 | "활성 칸 탭 모두 닫기"를 탭 줄·세로 띠에서 찾는다 | 없다. **메뉴에만 있다** | 탭 줄과 세로 띠에 해당 항목이 0개이고, View 메뉴에 1개 있다 | D-26 |
158-| FR-J4 | 빈 칸(탭 0개)에서 "활성 칸 탭 모두 닫기"를 누른다 | 이미 빈 칸이라 바뀌는 것이 없다. 항목은 메뉴에 그대로 있다 | 실행 전후 칸 수와 탭 수가 같고, 메뉴에 그 항목이 여전히 있다 | D-26 |
159:| FR-J5 | 사라지는 칸의 탭 | 전부 함께 닫힌다. 다른 칸으로 옮겨지거나 나눠 붙지 않는다. 보기는 그때 폐기된다 (FR-E3) | 칸이 사라진 뒤 남은 칸들의 탭 수 합이 사라진 칸의 탭 수만큼 줄고, 그 탭들의 보기 폐기 횟수가 각각 1이다 | D-24 · D-26 |
160-| FR-J6 | "칸 닫기/삭제" 전용 명령을 찾는다 | 없다. 세로 띠·메뉴·우클릭 어디에도 항목이 없다 | 세 자리 전부에서 해당 항목이 0개다 | D-26 |
161-| FR-J7 | 마지막 칸의 마지막 탭까지 닫는다 | 그 칸이 탭 없는 빈 면으로 남는다. 편집기 영역이 통째로 없어지지 않는다 | 닫은 뒤 칸 수 1 · 탭 수 0이고, 칸 영역이 화면에 자리를 차지하고 있다 | D-20 · D-26 |
162-| FR-J8 | 빈 칸에서 다시 시작한다 | 탐색기에서 파일 고르기(FR-B1) · 세로 띠 스플릿 아이콘 · View 메뉴 중 하나로 | 빈 칸 상태에서 세 경로 각각을 실행해 모두 탭 또는 칸이 생긴다 | D-5 · D-26 |
163-
164-### 1.11 다시 열 때 (FR-K)
165-
166-| FR | 사용자가 하는 것 | 정해진 결과 | 판정 방법 | 출처 |
167-| --- | --- | --- | --- | --- |
src/registry/kind-registry.ts-53-    // Reuses the shell's editor-panel-content contract (D-20, FR-C2): an
src/registry/kind-registry.ts-54-    // unresolved/empty panel renders 0 children and 0 text, same as before
src/registry/kind-registry.ts-55-    // any kind was registered.
src/registry/kind-registry.ts-56-    this.element.className = 'editor-panel-content';
src/registry/kind-registry.ts-57-  }
src/registry/kind-registry.ts-58-
src/registry/kind-registry.ts-59-  public init(parameters: GroupPanelPartInitParameters): void {
src/registry/kind-registry.ts-60-    this.params = (parameters.params || {}) as { kind?: string; targetId?: string };
src/registry/kind-registry.ts:61:    this.renderForCurrentParams();
src/registry/kind-registry.ts-62-  }
src/registry/kind-registry.ts-63-
src/registry/kind-registry.ts-64-  public update(event: { params?: Record<string, unknown> }): void {
src/registry/kind-registry.ts-65-    if (event.params) {
src/registry/kind-registry.ts-66-      this.params = { ...this.params, ...event.params };
src/registry/kind-registry.ts:67:      this.renderForCurrentParams();
src/registry/kind-registry.ts-68-    }
src/registry/kind-registry.ts-69-  }
src/registry/kind-registry.ts-70-
src/registry/kind-registry.ts:71:  private renderForCurrentParams(): void {
src/registry/kind-registry.ts:72:    this.inner?.dispose?.();
src/registry/kind-registry.ts-73-    this.inner = null;
src/registry/kind-registry.ts-74-    this.element.innerHTML = '';
src/registry/kind-registry.ts-75-
src/registry/kind-registry.ts-76-    const { kind, targetId } = this.params;
src/registry/kind-registry.ts-77-    if (!kind || !targetId) {
src/registry/kind-registry.ts-78-      return;
src/registry/kind-registry.ts-79-    }
src/registry/kind-registry.ts-80-
src/registry/kind-registry.ts-83-      return;
src/registry/kind-registry.ts-84-    }
src/registry/kind-registry.ts-85-
src/registry/kind-registry.ts-86-    this.inner = factory(targetId);
src/registry/kind-registry.ts-87-    this.element.appendChild(this.inner.element);
src/registry/kind-registry.ts-88-  }
src/registry/kind-registry.ts-89-
src/registry/kind-registry.ts-90-  public dispose(): void {
src/registry/kind-registry.ts:91:    this.inner?.dispose?.();
src/registry/kind-registry.ts-92-    this.inner = null;
src/registry/kind-registry.ts-93-    this.element.innerHTML = '';
src/registry/kind-registry.ts-94-  }
src/registry/kind-registry.ts-95-}
src/core/editor.ts-672-    if (this.api.groups.length === 0) {
src/core/editor.ts-673-      this.api.addGroup();
src/core/editor.ts-674-    }
src/core/editor.ts-675-  }
src/core/editor.ts-676-
src/core/editor.ts-677-  /**
src/core/editor.ts-678-   * Sets tab dirty indicator (●) (FR-L1).
src/core/editor.ts-679-   */
src/core/editor.ts:680:  public setTabDirty(panelId: string, dirty: boolean): void {
src/core/editor.ts-681-    const panel = this.api.getPanel(panelId);
src/core/editor.ts-682-    if (!panel) return;
src/core/editor.ts-683-
src/core/editor.ts-684-    const baseTitle = (panel.title || '').replace(/^●\s*/, '');
src/core/editor.ts-685-    const newTitle = dirty ? `● ${baseTitle}` : baseTitle;
src/core/editor.ts-686-    panel.setTitle(newTitle);
src/core/editor.ts:687:    panel.update({ params: { isDirty: dirty } });
src/core/editor.ts-688-  }
src/core/editor.ts-689-
src/core/editor.ts-690-  /**
src/core/editor.ts-691-   * Subscribes to active panel changes.
src/core/editor.ts-692-   */
src/core/editor.ts-693-  public onActivePanelChange(callback: (panel: IDockviewPanel | undefined) => void): () => void {
src/core/editor.ts-694-    this.activePanelChangeListeners.push(callback);
src/core/editor.ts-695-    return () => {

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rtk rg -n -C 5 "phase5|__runPhase5|evaluate_js|success|testResult|loadURL|dist|http" src/hosts/pywebview/main.py scripts/phase5-electron-runner.cjs package.json; rtk rg -n -C 4 "Phase 5|verify:phase5|build" package.json' in D:\projects\workbench-kit
 succeeded in 151ms:
package.json-9-    "build": "vite build",
package.json-10-    "preview": "vite preview",
package.json-11-    "typecheck": "tsc --noEmit",
package.json-12-    "start:electron": "electron src/hosts/electron/main.cjs",
package.json-13-    "start:pywebview": "python -m src.hosts.pywebview.main",
package.json:14:    "verify:dist": "node scripts/verify-dist.mjs",
package.json-15-    "verify:phase2": "node scripts/verify-phase2.mjs",
package.json-16-    "verify:phase3": "node scripts/verify-phase3.mjs",
package.json-17-    "verify:phase4": "node scripts/verify-phase4.mjs",
package.json:18:    "verify:phase5": "node scripts/verify-phase5.mjs",
package.json:19:    "test": "npm run typecheck && npm run verify:dist && npm run verify:phase2 && npm run verify:phase3 && npm run verify:phase4 && npm run verify:phase5"
package.json-20-  },
package.json-21-  "engines": {
package.json-22-    "node": ">=22.0.0"
package.json-23-  },
package.json-24-  "repository": {
package.json-25-    "type": "git",
package.json:26:    "url": "git+https://github.com/nampluskr/workbench-kit.git"
package.json-27-  },
package.json-28-  "keywords": [],
package.json-29-  "author": "workbench-kit contributors",
package.json-30-  "license": "MIT",
package.json-31-  "devDependencies": {
scripts/phase5-electron-runner.cjs-5-
scripts/phase5-electron-runner.cjs-6-// Robust switches for headless / CI / restricted environments
scripts/phase5-electron-runner.cjs-7-app.commandLine.appendSwitch('disable-gpu');
scripts/phase5-electron-runner.cjs-8-app.commandLine.appendSwitch('no-sandbox');
scripts/phase5-electron-runner.cjs-9-app.commandLine.appendSwitch('disable-dev-shm-usage');
scripts/phase5-electron-runner.cjs:10:app.setPath('userData', path.join(os.tmpdir(), 'wb-phase5-userdata-' + Math.random().toString(36).slice(2)));
scripts/phase5-electron-runner.cjs-11-
scripts/phase5-electron-runner.cjs-12-const rootDir = path.resolve(__dirname, '..');
scripts/phase5-electron-runner.cjs:13:const distIndexPath = path.join(rootDir, 'dist', 'index.html');
scripts/phase5-electron-runner.cjs:14:const suitePath = path.join(__dirname, 'phase5-suite.js');
scripts/phase5-electron-runner.cjs-15-
scripts/phase5-electron-runner.cjs-16-let failureCount = 0;
scripts/phase5-electron-runner.cjs-17-
scripts/phase5-electron-runner.cjs-18-function assert(condition, message) {
scripts/phase5-electron-runner.cjs-19-  if (!condition) {
scripts/phase5-electron-runner.cjs-45-    } else {
scripts/phase5-electron-runner.cjs-46-      console.log('BROWSER_LOG:', message);
scripts/phase5-electron-runner.cjs-47-    }
scripts/phase5-electron-runner.cjs-48-  });
scripts/phase5-electron-runner.cjs-49-
scripts/phase5-electron-runner.cjs:50:  await win.loadFile(distIndexPath);
scripts/phase5-electron-runner.cjs-51-
scripts/phase5-electron-runner.cjs-52-  try {
scripts/phase5-electron-runner.cjs-53-    const suiteCode = fs.readFileSync(suitePath, 'utf8');
scripts/phase5-electron-runner.cjs:54:    const testResult = await win.webContents.executeJavaScript(`
scripts/phase5-electron-runner.cjs-55-      ${suiteCode}
scripts/phase5-electron-runner.cjs:56:      window.__runPhase5TestSuite();
scripts/phase5-electron-runner.cjs-57-    `);
scripts/phase5-electron-runner.cjs-58-
scripts/phase5-electron-runner.cjs:59:    console.log('[electron] Phase 5 Results: ' + JSON.stringify(testResult));
scripts/phase5-electron-runner.cjs:60:    assert(testResult && testResult.success === true, 'All Phase 5 Electron in-browser runtime assertions executed successfully');
scripts/phase5-electron-runner.cjs-61-  } catch (err) {
scripts/phase5-electron-runner.cjs-62-    console.error(`[FAIL] Error executing in-browser tests: ${err.message}`);
scripts/phase5-electron-runner.cjs-63-    failureCount++;
scripts/phase5-electron-runner.cjs-64-  } finally {
scripts/phase5-electron-runner.cjs-65-    win.destroy();
src/hosts/pywebview/main.py-6-import time
src/hosts/pywebview/main.py-7-import threading
src/hosts/pywebview/main.py-8-
src/hosts/pywebview/main.py-9-is_smoke_test = "--smoke-test" in sys.argv
src/hosts/pywebview/main.py-10-is_phase4_test = "--phase4-test" in sys.argv
src/hosts/pywebview/main.py:11:is_phase5_test = "--phase5-test" in sys.argv
src/hosts/pywebview/main.py-12-loaded_called = False
src/hosts/pywebview/main.py-13-
src/hosts/pywebview/main.py-14-INSPECTION_EXPRESSION = """
src/hosts/pywebview/main.py-15-JSON.stringify({
src/hosts/pywebview/main.py-16-  href: window.location.href,
src/hosts/pywebview/main.py-27-def hash_file(file_path: str) -> str:
src/hosts/pywebview/main.py-28-    with open(file_path, "rb") as f:
src/hosts/pywebview/main.py-29-        return hashlib.sha256(f.read()).hexdigest()
src/hosts/pywebview/main.py-30-
src/hosts/pywebview/main.py-31-
src/hosts/pywebview/main.py:32:def compute_digest_map(dist_dir: str, dom_assets: list) -> dict:
src/hosts/pywebview/main.py-33-    digest_map = {}
src/hosts/pywebview/main.py:34:    index_path = os.path.join(dist_dir, "index.html")
src/hosts/pywebview/main.py-35-    digest_map["index.html"] = hash_file(index_path)
src/hosts/pywebview/main.py-36-
src/hosts/pywebview/main.py-37-    for raw in dom_assets:
src/hosts/pywebview/main.py-38-        if not raw:
src/hosts/pywebview/main.py-39-            continue
src/hosts/pywebview/main.py-40-        clean = raw.lstrip("./").lstrip("/")
src/hosts/pywebview/main.py:41:        asset_path = os.path.join(dist_dir, clean)
src/hosts/pywebview/main.py-42-        if os.path.exists(asset_path):
src/hosts/pywebview/main.py:43:            rel_key = os.path.relpath(asset_path, dist_dir).replace("\\", "/")
src/hosts/pywebview/main.py-44-            digest_map[rel_key] = hash_file(asset_path)
src/hosts/pywebview/main.py-45-
src/hosts/pywebview/main.py-46-    return digest_map
src/hosts/pywebview/main.py-47-
src/hosts/pywebview/main.py-48-
src/hosts/pywebview/main.py-106-
src/hosts/pywebview/main.py-107-def main():
src/hosts/pywebview/main.py-108-    global loaded_called
src/hosts/pywebview/main.py-109-
src/hosts/pywebview/main.py-110-    root_dir = os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))))
src/hosts/pywebview/main.py:111:    dist_dir = os.path.abspath(os.path.join(root_dir, "dist"))
src/hosts/pywebview/main.py:112:    dist_index = os.path.abspath(os.path.join(dist_dir, "index.html"))
src/hosts/pywebview/main.py-113-
src/hosts/pywebview/main.py:114:    if not os.path.exists(dist_index):
src/hosts/pywebview/main.py:115:        sys.stderr.write(f"[pywebview] Error: Dist index not found at {dist_index}\n")
src/hosts/pywebview/main.py-116-        sys.stderr.flush()
src/hosts/pywebview/main.py-117-        os._exit(1)
src/hosts/pywebview/main.py-118-
src/hosts/pywebview/main.py-119-    api = WindowApi()
src/hosts/pywebview/main.py-120-    window = webview.create_window(
src/hosts/pywebview/main.py-121-        title="workbench-kit",
src/hosts/pywebview/main.py:122:        url=dist_index,
src/hosts/pywebview/main.py-123-        frameless=True,
src/hosts/pywebview/main.py-124-        easy_drag=False,
src/hosts/pywebview/main.py-125-        width=WINDOW_WIDTH,
src/hosts/pywebview/main.py-126-        height=WINDOW_HEIGHT,
src/hosts/pywebview/main.py-127-        js_api=api,
src/hosts/pywebview/main.py-149-        loaded_called = True
src/hosts/pywebview/main.py-150-
src/hosts/pywebview/main.py-151-        if is_smoke_test:
src/hosts/pywebview/main.py-152-            try:
src/hosts/pywebview/main.py-153-                current_url = window.get_current_url()
src/hosts/pywebview/main.py:154:                inspection_json = window.evaluate_js(INSPECTION_EXPRESSION)
src/hosts/pywebview/main.py-155-                inspection = json.loads(inspection_json)
src/hosts/pywebview/main.py:156:                digest_map = compute_digest_map(dist_dir, inspection.get("domAssets") or [])
src/hosts/pywebview/main.py-157-
src/hosts/pywebview/main.py:158:                sys.stdout.write(f"[pywebview] Local Path: {dist_index}\n")
src/hosts/pywebview/main.py-159-                sys.stdout.write(f"[pywebview] Loaded URL: {current_url}\n")
src/hosts/pywebview/main.py-160-                sys.stdout.write(f"[pywebview] Digest Map: {json.dumps(digest_map, sort_keys=True)}\n")
src/hosts/pywebview/main.py-161-                sys.stdout.write(f"[pywebview] DOM Inspection: {inspection_json}\n")
src/hosts/pywebview/main.py-162-                sys.stdout.flush()
src/hosts/pywebview/main.py-163-                window.destroy()
src/hosts/pywebview/main.py-172-                try:
src/hosts/pywebview/main.py-173-                    time.sleep(0.3)
src/hosts/pywebview/main.py-174-                    suite_path = os.path.join(root_dir, "scripts", "phase4-suite.js")
src/hosts/pywebview/main.py-175-                    with open(suite_path, "r", encoding="utf-8") as f:
src/hosts/pywebview/main.py-176-                        suite_code = f.read()
src/hosts/pywebview/main.py:177:                    window.evaluate_js(suite_code)
src/hosts/pywebview/main.py:178:                    window.evaluate_js("""
src/hosts/pywebview/main.py-179-                        (async () => {
src/hosts/pywebview/main.py-180-                            try {
src/hosts/pywebview/main.py-181-                                const res = await window.__runPhase4TestSuite();
src/hosts/pywebview/main.py-182-                                window.__phase4Results = JSON.stringify(res);
src/hosts/pywebview/main.py-183-                            } catch (e) {
src/hosts/pywebview/main.py:184:                                window.__phase4Results = JSON.stringify({ success: false, results: [{ id: 'RUNNER_ERR', pass: false, msg: String(e) }] });
src/hosts/pywebview/main.py-185-                            }
src/hosts/pywebview/main.py-186-                        })();
src/hosts/pywebview/main.py-187-                    """)
src/hosts/pywebview/main.py-188-                    for _ in range(100):
src/hosts/pywebview/main.py-189-                        time.sleep(0.1)
src/hosts/pywebview/main.py:190:                        res = window.evaluate_js("window.__phase4Results")
src/hosts/pywebview/main.py-191-                        if res:
src/hosts/pywebview/main.py-192-                            sys.stdout.write(f"[pywebview] Phase 4 Results: {res}\n")
src/hosts/pywebview/main.py-193-                            sys.stdout.flush()
src/hosts/pywebview/main.py-194-                            window.destroy()
src/hosts/pywebview/main.py-195-                            os._exit(0)
src/hosts/pywebview/main.py-206-
src/hosts/pywebview/main.py-207-            t = threading.Thread(target=run_phase4, daemon=True)
src/hosts/pywebview/main.py-208-            t.start()
src/hosts/pywebview/main.py-209-            return
src/hosts/pywebview/main.py-210-
src/hosts/pywebview/main.py:211:        if is_phase5_test:
src/hosts/pywebview/main.py:212:            def run_phase5():
src/hosts/pywebview/main.py-213-                try:
src/hosts/pywebview/main.py-214-                    time.sleep(0.3)
src/hosts/pywebview/main.py:215:                    suite_path = os.path.join(root_dir, "scripts", "phase5-suite.js")
src/hosts/pywebview/main.py-216-                    with open(suite_path, "r", encoding="utf-8") as f:
src/hosts/pywebview/main.py-217-                        suite_code = f.read()
src/hosts/pywebview/main.py:218:                    window.evaluate_js(suite_code)
src/hosts/pywebview/main.py:219:                    window.evaluate_js("""
src/hosts/pywebview/main.py-220-                        (async () => {
src/hosts/pywebview/main.py-221-                            try {
src/hosts/pywebview/main.py:222:                                const res = await window.__runPhase5TestSuite();
src/hosts/pywebview/main.py:223:                                window.__phase5Results = JSON.stringify(res);
src/hosts/pywebview/main.py-224-                            } catch (e) {
src/hosts/pywebview/main.py:225:                                window.__phase5Results = JSON.stringify({ success: false, results: [{ id: 'RUNNER_ERR', pass: false, msg: String(e) }] });
src/hosts/pywebview/main.py-226-                            }
src/hosts/pywebview/main.py-227-                        })();
src/hosts/pywebview/main.py-228-                    """)
src/hosts/pywebview/main.py-229-                    for _ in range(100):
src/hosts/pywebview/main.py-230-                        time.sleep(0.1)
src/hosts/pywebview/main.py:231:                        res = window.evaluate_js("window.__phase5Results")
src/hosts/pywebview/main.py-232-                        if res:
src/hosts/pywebview/main.py-233-                            sys.stdout.write(f"[pywebview] Phase 5 Results: {res}\n")
src/hosts/pywebview/main.py-234-                            sys.stdout.flush()
src/hosts/pywebview/main.py-235-                            window.destroy()
src/hosts/pywebview/main.py-236-                            os._exit(0)
src/hosts/pywebview/main.py-243-                    sys.stderr.write(f"[pywebview] Error executing Phase 5 test: {e}\n")
src/hosts/pywebview/main.py-244-                    sys.stderr.flush()
src/hosts/pywebview/main.py-245-                    window.destroy()
src/hosts/pywebview/main.py-246-                    os._exit(1)
src/hosts/pywebview/main.py-247-
src/hosts/pywebview/main.py:248:            t = threading.Thread(target=run_phase5, daemon=True)
src/hosts/pywebview/main.py-249-            t.start()
src/hosts/pywebview/main.py-250-            return
src/hosts/pywebview/main.py-251-
src/hosts/pywebview/main.py-252-    window.events.loaded += on_loaded
src/hosts/pywebview/main.py-253-
5-  "description": "여러 데스크톱 앱이 공유하는 공통 껍데기 한 벌",
6-  "type": "module",
7-  "scripts": {
8-    "dev": "vite",
9:    "build": "vite build",
10-    "preview": "vite preview",
11-    "typecheck": "tsc --noEmit",
12-    "start:electron": "electron src/hosts/electron/main.cjs",
13-    "start:pywebview": "python -m src.hosts.pywebview.main",
14-    "verify:dist": "node scripts/verify-dist.mjs",
15-    "verify:phase2": "node scripts/verify-phase2.mjs",
16-    "verify:phase3": "node scripts/verify-phase3.mjs",
17-    "verify:phase4": "node scripts/verify-phase4.mjs",
18:    "verify:phase5": "node scripts/verify-phase5.mjs",
19:    "test": "npm run typecheck && npm run verify:dist && npm run verify:phase2 && npm run verify:phase3 && npm run verify:phase4 && npm run verify:phase5"
20-  },
21-  "engines": {
22-    "node": ">=22.0.0"
23-  },

codex
## Findings

### Critical — The app-facing editor boundary is completely bypassable

[main.ts](/D:/projects/workbench-kit/src/main.ts:28) exposes the full `EditorController`, then [main.ts](/D:/projects/workbench-kit/src/main.ts:321) publishes the entire `WorkbenchApp` as `window.__workbenchApp`.

Reproduction:

```js
const app = window.__workbenchApp;
app.editor.addNewTab();
app.editor.splitActiveGroup('right');
app.editor.closeActiveTab();
app.editor.getApi().clear();
```

Even the nominally narrow `AppEditorSurface` leaks `DockviewGroupPanel` as an argument and returns a full `IDockviewPanel` at [editor.ts](/D:/projects/workbench-kit/src/core/editor.ts:37), allowing callers to recover Dockview’s direct panel/group APIs.

This directly violates FR-I8, C-9, and D-19: app code can create, close, split, move, and otherwise manipulate tabs and panes.

The tests miss it because [verify-phase5.mjs](/D:/projects/workbench-kit/scripts/verify-phase5.mjs:54) only searches three source files for six literal method-call strings. It never inspects the actual exported runtime object or the capabilities returned by `AppEditorSurface`.

### Major — Apps can replace shell File/View/Help command behavior

`MenuController.setAction()` accepts any string at [menu.ts](/D:/projects/workbench-kit/src/core/menu.ts:222), and the complete controller is publicly exposed through [main.ts](/D:/projects/workbench-kit/src/main.ts:20).

Reproduction:

```js
const app = window.__workbenchApp;

app.menu.setAction('file:open-folder', () => console.log('hijacked'));
app.menu.setAction('view:zen-mode', () => console.log('hijacked'));
app.menu.setAction('help:about', () => console.log('hijacked'));

app.menu.triggerItem('view:zen-mode');
```

An extension can therefore disable or replace shell-owned behavior and reach View and Help despite there being no `addAppViewItem` method. Duplicate IDs supplied through `addAppFileItem()` also share the same global `itemActions` namespace.

This violates FR-I9, FR-I11, and D-31.

The FR-I11 test at [phase5-suite.js](/D:/projects/workbench-kit/scripts/phase5-suite.js:319) merely checks for two specifically named missing methods, so this bypass passes.

### Major — Ordinary parameter updates destroy and recreate registered views

Every `KindDispatchRenderer.update()` calls `renderForCurrentParams()` at [kind-registry.ts](/D:/projects/workbench-kit/src/registry/kind-registry.ts:64). That method immediately disposes the current view and invokes its factory again at [kind-registry.ts](/D:/projects/workbench-kit/src/registry/kind-registry.ts:71).

Reproduction:

1. Register a renderer whose factory and `dispose` increment counters.
2. Open one target.
3. Call `editor.setTabDirty(panel.id, true)`.
4. `setTabDirty()` issues `panel.update({params: {isDirty: true}})` at [editor.ts](/D:/projects/workbench-kit/src/core/editor.ts:680).
5. The target and kind are unchanged, but the original app view is disposed and a new instance is created.

A live terminal/session view would be destroyed merely by changing unrelated metadata. This violates D-4’s requirement that the shell maintain a live non-file view for the tab lifetime and regresses the view-lifetime contract underpinning the extension slot.

No Phase 5 test updates metadata after registering a custom kind.

### Major — File and folder preset acceptance is not tested as specified

FR-I2 and FR-I3 require a preset-provided row to appear in the tree and selecting it to make that target active. Instead, [phase5-suite.js](/D:/projects/workbench-kit/scripts/phase5-suite.js:68) and [phase5-suite.js](/D:/projects/workbench-kit/scripts/phase5-suite.js:77) call `editor.openItem()` directly.

The preset modules only register placeholder renderers and open helpers; for example [file-preset.ts](/D:/projects/workbench-kit/src/presets/file-preset.ts:11). The suite never proves that either preset supplies tree content or that tree selection is wired to it.

Consequently the current suite can pass when the user-visible FR-I2/FR-I3 flow is absent. This violates the explicit SPEC acceptance wording for FR-I2 and FR-I3 and weakens the Phase 5 completion condition.

### Major — Context-menu keys are missing from the reserved-key contract

When a context menu is open, [contextmenu.ts](/D:/projects/workbench-kit/src/core/contextmenu.ts:89) globally intercepts:

- `Escape`
- `ArrowUp`
- `ArrowDown`
- `Enter`

But [reserved-keys.md](/D:/projects/workbench-kit/docs/reserved-keys.md:18) documents those keys only for the File/View/Help menu, not for the right-click menu. There is no context-menu state section.

Reproduction:

1. Enable the context menu.
2. Open it over a tree row or tab.
3. Dispatch `ArrowDown` or `Enter` on `document`.
4. Observe `defaultPrevented === true` and shell-side navigation/action execution despite this interception context not appearing in the reserved-key list.

This violates FR-R1 and D-19’s requirement that the documented list completely define what the shell takes.

### Major — The “zero resource-kind vocabulary in src/core/*.ts” verifier is easily bypassed

Despite claiming to check `src/core/*.ts`, [verify-phase5.mjs](/D:/projects/workbench-kit/scripts/verify-phase5.mjs:34) checks only six hand-picked files. It also searches only exact quoted lowercase literals `"file"` and `"folder"`.

It does not reject:

- `terminal` or extension-related knowledge;
- branching on `"File"`, template literals, constants, enums, or regexes;
- resource-kind logic in any omitted `src/core/*.ts` file.

For example, a core branch on `kind === "terminal"` would pass the verifier unchanged. This fails to enforce NFR-1, INTENT section 3, D-4, and the Phase 5 “common core” completion condition.

### Minor — Opening both menus makes one keystroke execute two commands

The hamburger menu and context menu each install independent document-level keydown handlers at [menu.ts](/D:/projects/workbench-kit/src/core/menu.ts:78) and [contextmenu.ts](/D:/projects/workbench-kit/src/core/contextmenu.ts:24).

Reproduction:

1. Open an app-defined context menu.
2. Press `F10`, which opens the hamburger menu without closing the context menu.
3. Press `Enter`.
4. Both listeners process the event: a shell menu command and a context-menu command can execute from one keystroke.

Neither handler uses an exclusive menu coordinator or stops immediate propagation. This breaks deterministic keyboard command execution under NFR-7.

Review result: **1 Critical, 5 Major, 1 Minor**. No files were modified.
tokens used
92,532
## Findings

### Critical — The app-facing editor boundary is completely bypassable

[main.ts](/D:/projects/workbench-kit/src/main.ts:28) exposes the full `EditorController`, then [main.ts](/D:/projects/workbench-kit/src/main.ts:321) publishes the entire `WorkbenchApp` as `window.__workbenchApp`.

Reproduction:

```js
const app = window.__workbenchApp;
app.editor.addNewTab();
app.editor.splitActiveGroup('right');
app.editor.closeActiveTab();
app.editor.getApi().clear();
```

Even the nominally narrow `AppEditorSurface` leaks `DockviewGroupPanel` as an argument and returns a full `IDockviewPanel` at [editor.ts](/D:/projects/workbench-kit/src/core/editor.ts:37), allowing callers to recover Dockview’s direct panel/group APIs.

This directly violates FR-I8, C-9, and D-19: app code can create, close, split, move, and otherwise manipulate tabs and panes.

The tests miss it because [verify-phase5.mjs](/D:/projects/workbench-kit/scripts/verify-phase5.mjs:54) only searches three source files for six literal method-call strings. It never inspects the actual exported runtime object or the capabilities returned by `AppEditorSurface`.

### Major — Apps can replace shell File/View/Help command behavior

`MenuController.setAction()` accepts any string at [menu.ts](/D:/projects/workbench-kit/src/core/menu.ts:222), and the complete controller is publicly exposed through [main.ts](/D:/projects/workbench-kit/src/main.ts:20).

Reproduction:

```js
const app = window.__workbenchApp;

app.menu.setAction('file:open-folder', () => console.log('hijacked'));
app.menu.setAction('view:zen-mode', () => console.log('hijacked'));
app.menu.setAction('help:about', () => console.log('hijacked'));

app.menu.triggerItem('view:zen-mode');
```

An extension can therefore disable or replace shell-owned behavior and reach View and Help despite there being no `addAppViewItem` method. Duplicate IDs supplied through `addAppFileItem()` also share the same global `itemActions` namespace.

This violates FR-I9, FR-I11, and D-31.

The FR-I11 test at [phase5-suite.js](/D:/projects/workbench-kit/scripts/phase5-suite.js:319) merely checks for two specifically named missing methods, so this bypass passes.

### Major — Ordinary parameter updates destroy and recreate registered views

Every `KindDispatchRenderer.update()` calls `renderForCurrentParams()` at [kind-registry.ts](/D:/projects/workbench-kit/src/registry/kind-registry.ts:64). That method immediately disposes the current view and invokes its factory again at [kind-registry.ts](/D:/projects/workbench-kit/src/registry/kind-registry.ts:71).

Reproduction:

1. Register a renderer whose factory and `dispose` increment counters.
2. Open one target.
3. Call `editor.setTabDirty(panel.id, true)`.
4. `setTabDirty()` issues `panel.update({params: {isDirty: true}})` at [editor.ts](/D:/projects/workbench-kit/src/core/editor.ts:680).
5. The target and kind are unchanged, but the original app view is disposed and a new instance is created.

A live terminal/session view would be destroyed merely by changing unrelated metadata. This violates D-4’s requirement that the shell maintain a live non-file view for the tab lifetime and regresses the view-lifetime contract underpinning the extension slot.

No Phase 5 test updates metadata after registering a custom kind.

### Major — File and folder preset acceptance is not tested as specified

FR-I2 and FR-I3 require a preset-provided row to appear in the tree and selecting it to make that target active. Instead, [phase5-suite.js](/D:/projects/workbench-kit/scripts/phase5-suite.js:68) and [phase5-suite.js](/D:/projects/workbench-kit/scripts/phase5-suite.js:77) call `editor.openItem()` directly.

The preset modules only register placeholder renderers and open helpers; for example [file-preset.ts](/D:/projects/workbench-kit/src/presets/file-preset.ts:11). The suite never proves that either preset supplies tree content or that tree selection is wired to it.

Consequently the current suite can pass when the user-visible FR-I2/FR-I3 flow is absent. This violates the explicit SPEC acceptance wording for FR-I2 and FR-I3 and weakens the Phase 5 completion condition.

### Major — Context-menu keys are missing from the reserved-key contract

When a context menu is open, [contextmenu.ts](/D:/projects/workbench-kit/src/core/contextmenu.ts:89) globally intercepts:

- `Escape`
- `ArrowUp`
- `ArrowDown`
- `Enter`

But [reserved-keys.md](/D:/projects/workbench-kit/docs/reserved-keys.md:18) documents those keys only for the File/View/Help menu, not for the right-click menu. There is no context-menu state section.

Reproduction:

1. Enable the context menu.
2. Open it over a tree row or tab.
3. Dispatch `ArrowDown` or `Enter` on `document`.
4. Observe `defaultPrevented === true` and shell-side navigation/action execution despite this interception context not appearing in the reserved-key list.

This violates FR-R1 and D-19’s requirement that the documented list completely define what the shell takes.

### Major — The “zero resource-kind vocabulary in src/core/*.ts” verifier is easily bypassed

Despite claiming to check `src/core/*.ts`, [verify-phase5.mjs](/D:/projects/workbench-kit/scripts/verify-phase5.mjs:34) checks only six hand-picked files. It also searches only exact quoted lowercase literals `"file"` and `"folder"`.

It does not reject:

- `terminal` or extension-related knowledge;
- branching on `"File"`, template literals, constants, enums, or regexes;
- resource-kind logic in any omitted `src/core/*.ts` file.

For example, a core branch on `kind === "terminal"` would pass the verifier unchanged. This fails to enforce NFR-1, INTENT section 3, D-4, and the Phase 5 “common core” completion condition.

### Minor — Opening both menus makes one keystroke execute two commands

The hamburger menu and context menu each install independent document-level keydown handlers at [menu.ts](/D:/projects/workbench-kit/src/core/menu.ts:78) and [contextmenu.ts](/D:/projects/workbench-kit/src/core/contextmenu.ts:24).

Reproduction:

1. Open an app-defined context menu.
2. Press `F10`, which opens the hamburger menu without closing the context menu.
3. Press `Enter`.
4. Both listeners process the event: a shell menu command and a context-menu command can execute from one keystroke.

Neither handler uses an exclusive menu coordinator or stops immediate propagation. This breaks deterministic keyboard command execution under NFR-7.

Review result: **1 Critical, 5 Major, 1 Minor**. No files were modified.
